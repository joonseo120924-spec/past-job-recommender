# 승인 · 반려 기록

> 노션 「📓 작업 일지」 기준으로 복원. **로컬 원본 `approvals.md` 의 개별 승인 줄 단위 기록은 노션에 없어 복원되지 않았습니다.**

## 사이클 1 · FocusNoise (앱 폴더 `2026-08-07`)

| 단계 | 판정 | 근거 | 비고 |
|---|---|---|---|
| ① 전략 — 아이디어 승인 | ✅ 승인 | 아이디어 5개 조사, 출처 URL 16건, FocusNoise 85점 선정 | 검토: `product-planner` |
| ② 프로덕트 — 설계 승인 | ✅ 승인 | 기획서 1,972행. 개발 착수 7항목 충족, 모호 표현 0건 | 검토: `tech-lead` |
| ③ 디자인 — 디자인 승인 | 🟡 **면제** | **D-013 — 사이클 1 한정 면제** | 디자인본부 신설 이전 진행분. 면제 항목은 **미검증 상태**이며 ⑥에서 검증된 것처럼 기술 금지 |
| ④ 개발 — 구현 완료 승인 | ✅ 승인 | `src/` 14파일 약 5,300줄, 콘솔 에러 0건 실행 확인 | 되돌림 발생 시 재승인 필요 |
| ⑤ 품질 — 품질 승인 | ❌ **승인 불가** | **남은 오류 2개** | 「남은 오류 0개」는 면제 대상 아님 |
| ⑥ 출시운영 — 출시 준비/운영 승인 | ⬜ 미착수 | ⑤ 품질 승인 부재 | |

## 감사관 판정 이력 (조직 개편 v2)
| 차수 | 판정 | 핵심 지적 |
|---|---|---|
| 1차 | ❌ 반려 (32건 / 치명 5) | **"조직 개편이 문서로만 존재하고 실행 경로에 연결되지 않았다"** |
| 2차 | ❌ 재반려 | v0/v1/v2 3개 판본이 동시 존재 |
| 3차 | ❌ 재반려 | handoff 실물/명세 불일치 — "문구만 고치고 파일은 안 바꿈" |
| 4차 | ❌ 재반려 | **"마스터가 3회 연속 고쳤다고 보고한 항목이 실측 미수정"** |
| **5차** | **⚠️ 조건부 승인** | 치명 0건. 판본 드리프트 검사 **6/6 합격** (추이: 0/6 → 1/6 → 2/6 → 6/6) |

## 이번 복원 (2026-08-10 KST, 원격 컨테이너)
| 항목 | 판정 |
|---|---|
| 노션 → 저장소 팀 복원 (D-016) | ⚠️ **조건부 승인** (2026-08-12 3차 재감사). 1차·2차는 ❌ 반려 — 아래 3개 절 참조 |

### D-016 감사 판정 (2026-08-11 07:07 정기 실행)
**❌ 반려** — 치명 2 · 중대 7 · 경미 5 · 판본 드리프트 **4/6**

반려 대상은 **D-016 승인(D-014 요건 ③)** 이며, 복원 산출물을 되돌리라는 뜻이 아닙니다. 복원물 자체(에이전트 20개·정본 3종·규정 정합성)는 실측 전 항목 합격했습니다.

| # | 지적 | 조치 |
|---|---|---|
| 치명-1 | 정본이 07:07 자동 실행을 "동작하지 않음"으로 단정 — 실측으로 반증됨 | `README.md`·`RESTORE.md` 정정 ✅ / **`.claude/team-org.md:70` 은 차단됨** |
| 치명-2 | 감사 판정 전 사이클 2 착수 + D-017 미기록 | D-017 기록 ✅ · 상태 파일 동기화 ✅ · **단계 승인 정지** ✅ |
| 중대-1 | `.claude/` 수정 2건이 결정 원장에 없음 | D-018 기록 ✅ |
| 중대-2 | Routine 프롬프트가 정본 복붙이며 낡음 (STATE.md 4-1 누락) | `routine-prompt.md` 사본 + 차이 기록 ✅ / 프롬프트 변경은 사용자 지시 필요 |
| 중대-3 | 에이전트 20개 산출 경로에 앱 폴더 접두사 없음 | **차단됨** — `board.md` 에 경로 규칙 명시 ✅ |
| 중대-4 | `RESTORE.md` 가 Windows 스케줄 재등록을 지시 (설계와 충돌) | 정정 ✅ |
| 중대-5 | `STATE.md` 가 Windows 중지를 확정으로 서술 | "중지 여부 미확인" 으로 정정 ✅ |
| 중대-6 | 노션에만 "실제 작동 확인됨" — 저장소·로그에 근거 없음 | 노션 정정 ✅ |
| 중대-7 | 4일 주기 일차 산정 기준 부재 | D-019 결정 ✅ |
| 경미-1~5 | 스킬 복제 · BOM 예외 미기록 · clone 위치 · 검사 범위 · `.ps1` 오류 처리 | D-020 기록 ✅ · 나머지 정정 ✅ |

**마스터 자기 감사 결과 (인정)**
- GREEN 결정을 `decisions.md` 에 기록하지 않고 진행 — `master-doctrine.md:8` "기록 없는 결정은 독단이다" 위반
- 감사관 판정 전 다음 단계 착수 — 2026-08-08 에 4차 반려당한 것과 같은 유형
- 노션에 "실제 작동 확인됨" 1건 과대 진술. 저장소 기록(`SESSION-LOG.md` 「2026-08-10 (2)」 절 → **"검증하지 못한 것"**)은 정직했으므로 조직적 은폐는 아니나, **노션이 사람이 읽는 곳이라 더 나쁩니다**

### D-016 재감사 판정 (2026-08-12 07:07 정기 실행)
**❌ 반려** — 치명 2 · 중대 3 · 경미 6 · 판본 드리프트 **4/6** (개선 없음)

직전 요건(치명-2 + 중대-1·4·5·6)은 **실질적으로 이행됐다고 인정**받았습니다. 반려 사유는 **시정 과정에서 새로 생긴 치명 2건**이며, 둘 다 "못 한 것을 했다고 쓰지 않는다"(`master-doctrine.md:45`) 축에 있습니다.

| # | 지적 | 조치 |
|---|---|---|
| 치명-1 | 노션 상단 「현재 상태」 표를 갱신하지 않고 큐를 "반영됨", `STATE.md` 를 "미동기화 0건" 으로 보고. **감사관이 반려했다는 사실이 최상단 표에서 지워져 있었음** | 표 6개 칸 갱신 ✅ · 큐 상태 줄에 사실 명기 ✅ · `notion-sync.md` 에 「append 완료 ≠ 동기화 완료」 신설 ✅ |
| 치명-2 | D-018 이 「③ 감사관 검증 ✅ — 2026-08-11 감사에서 확인됨」 을 자기 기록. **그 감사는 반려였음** | 근거란 정정 ✅ · **③ 은 본 재감사에서 부여됨** (2026-08-12) · ② 요건이 약하다는 지적도 함께 기록 ✅ |
| 중대-1 | `decisions.md` D-016 상세가 여전히 "③ 미수행 / 다음 세션의 첫 작업은 감사" | 감사 2회 수행·반려 경과표 추가 ✅ |
| 중대-2 | 산출 경로 4가지 병존 (드리프트 6) | **차단됨** — 사용자 지시 요청 최우선 |
| 중대-3 | **`.claude/` 내부가 서로 모순** — `team-org.md:70` ↔ `daily-app/SKILL.md:68` | 차단 해제 요청에 함께 제시 ✅ (새 문안 불필요 — 정확한 문장이 이미 `.claude/` 안에 있음) |
| 경미-1 | `Invoke-Git` 회귀 — `rev-parse` 가 stderr 를 SHA 로 오인할 수 있음 | `Get-Sha` 헬퍼로 SHA 형식만 통과 ✅ |
| 경미-2·3·6 | 죽은 값 `16` · 큐 판정 취약 · 근거 인용 위치 | 전부 정정 ✅ |
| 경미-4 | 감사 대상 HEAD 고지 불일치 | 재감사 요청 시 개시 시점 HEAD 정확히 고지 |
| 경미-5 | `handoff/03·04` 를 "반드시 읽습니다" 로 명시한 에이전트 없음 — 인수인계 사슬 구멍 | `.claude/agents/` 수정 필요 → **차단됨** |

**감사관이 인정한 것**: 차단됨 처리는 타당 (`.claude/` 아래 한 건도 수정 안 됨, 실측) · 인용 줄번호 정확 · `routine-prompt.md` 사본이 실물과 **한 글자까지 일치** · D-017 에 절차 위반 사실을 스스로 명기한 점

### D-016 3차 재감사 판정 (2026-08-12)
**⚠️ 조건부 승인** — 치명 **0** · 중대 3 · 경미 6 · 판본 드리프트 4/6. **① 전략 종합·승인 재개 허용.**

| # | 조건 | 이행 |
|---|---|---|
| 1 | `cycle.md` 갱신 — 규정상 생략 불가인데 08-12 에 통째로 빠짐 | ✅ |
| 2 | 인용 정정 + 줄번호 인용 전수 재검증, append 형 문서는 절 제목으로 인용 | ✅ (`README.md` 「인용 규약」 신설) |
| 3 | `STATE.md` 노션 건수를 5단계 완료 후 확정 · `daily-app` 4-1↔5 순서 결함을 차단 요청에 추가 · 08-12 흔적 명기 | ✅ |
| 4 | 자기모순 해소 → 드리프트 5 복구 | ✅ (`SESSION-LOG` 16초 서술 · `approvals` 반려 횟수) |
| 5 | D-016 ③ 란을 "조건부 승인 — 조건 미해소" 로 | ✅ |
| 6 | 경미-3·4·5·6 처리 | ✅ (`.ps1` 2건 · D-016 날짜 KST 통일 · 지시 원문 인용 복원) |

**감사관이 인정한 것**: 노션 상단 표 6칸 실측 일치 · `.claude/` 무수정 · 인용 정확(1건 제외) · `routine-prompt.md` 가 Routine 실물과 한 글자까지 일치

**3회 연속 지적된 패턴**: "시정하면서 새 구멍을 낸다" · "지목된 한 건만 고치고 같은 계열은 남긴다". 이번 시정부터 **같은 계열 전수 검색**을 적용했습니다 — `.ps1` 의 `.Output` 사용처 4곳을 전수 점검해 지목되지 않은 2곳을 함께 고쳤습니다.

## 사이클 2 (앱 폴더 `2026-08-11`)
| 단계 | 판정 | 근거 |
|---|---|---|
| ① 전략 — 아이디어 승인 | ✅ **승인** | D-016 3차 재감사(2026-08-12) 조건부 승인으로 종합·승인 재개. **B(가계부/지출 계산기) 89점 선정** — 채점표·근거 `apps/2026-08-11/docs/시장분석.md`. 검토: `strategy-lead`(본인). `product-planner` 교차검토는 아래 줄에서 **수행 완료** |
| ① 전략 — `product-planner` 교차검토 | ⚠️ **조건부 통과** (되돌림 없음) | 2026-08-11 수행. 지적 4건 — `questions.md` Q-002~Q-005. **중대 2건**(B 차별점 24/25 의 표본 n=2·반례 누락·출처 교차 상충 / B 사용자 가치 근거 3건 중 2건이 은행연동형 세그먼트 문제이며 후보 D 에 적용한 논리를 B 에 미적용), **치명 1건**(전제 오류 — "순수 정적 PWA" 가 `file://` 에서 성립 불가: SW 등록 불가·IndexedDB 차단. A~E 5개 후보 공통이라 순위 불변 → ② 가 정정), **경미 1건**(문서 내 시각화 형태 불일치·일정 산정 미실측). **재채점 시 B 82 · A 84 로 역전 가능성 실재함을 명기**하되, ⓐ 지적이 점수 강도 문제이지 후보 적합성 문제가 아니고 ⓑ 대안 A 는 상위호환 경쟁자(Loop Habit Tracker 760만 DL·완전 무료·오프라인)가 조사로 **확인**된 반면 B 의 약점은 **미확인**이며 ⓒ 되돌림 비용이 이득보다 큼(`master-doctrine.md:6`)을 근거로 B 선정 유지. 이행 조건 5건(C-1~C-5)은 `apps/2026-08-11/docs/기획-분배안.md` §1 |
| ② 프로덕트 — 설계 승인 | ⚠️ **조건부 승인** (2026-08-15) · **조건 1 해소 (2026-08-16)** · 조건 2 미해소 · **Q-026 판정 반영 (2026-08-18)** | **2026-08-18 갱신**: ③ 제기 **Q-026 닫힘** — `E-413` 슬롯 **T → B(사건형)**, T 슬롯 적격 기준·길이 상한(전체 65자·첫 문장 32자) 확정, 문구 3건 단축(`E-403`·`E-121`·`E-306`). **E-코드 총계 64 불변 · 되돌림 0건 · ③ 시안 재작업 0건 · ④ 호출부 0줄.** 승인 상태와 조건 2건은 **변동 없음**(`기획서.md` §7-10 INT-32·INT-33 · 아래 「③ 제기 Q-026 에 대한 ② 판정」 절). — **2026-08-16 갱신**: `tech-lead` 검토가 건 ④ 착수 전 조건 **5/5 해소**(아래 「위 5건에 대한 ② 판정」 절 · `기획서.md` §7-9 INT-23~INT-31). **남은 조건은 ③ 디자인 승인 1건뿐**이며, ④ 개발 착수 불허 사유는 이제 ② 산출물의 결함이 아니라 ③ 단계 미완료입니다. — 이하 2026-08-15 기록: 분배안 ✅ · 화면설계 ✅(256행) · 구조설계 ✅(1448행, 완성) · **통합본 `docs/기획서.md` ✅**. ④ 개발 착수 게이트 8항목 중 **② 책임분 5항목(기능정의·화면설계·데이터구조·API정의·예외정의) 전부 충족**. 통합 검수에서 구멍 5건·미정의 8건을 발견해 기획서 §7 에 INT-01~INT-21 로 전부 확정. 판정자 `product-planner`. **조건 2건은 아래 절 참조** |
| ② 프로덕트 — `tech-lead` 교차검토 | ⚠️ **조건부 통과** (되돌림 없음, 2026-08-15) | **② 설계 승인 조건 1(`tech-lead` 검토 미수행)을 해소하는 검토입니다**(`team-rules.md:28`). 지적 9건 — `questions.md` **Q-018~Q-025**. **중대 6건**: ⓐ E-코드 실제 **63개**인데 4개 문서에 **60개**로 전파 + `기획서.md` §6 대역표는 자기 합이 **64**(E-6xx 7 → 실제 6) — §7-7 “역방향 검사 0건·60개 전부 대조” 진술이 실측과 불일치(Q-018) ⓑ `applyImport` 를 §4-4 는 `JR.model` 로, §5-6-2 는 `JR.io` 로 귀속 + §5-1 이 `JR.io`→`JR.stats` 호출을 허용·금지 어디에도 안 적음 → **병렬 구현 시 가져오기 후 통계 캐시 미무효화가 설계상 예정됨**(Q-019) ⓒ `monthIndex` 가 §5 에 없어 **소유 모듈 미정**, 소비자는 `JR.model` 인데 서술은 `JR.stats` 절에 있음(Q-020) ⓓ 실측 **#1**(`file://` localStorage) 실패 시 F-01~F-09 전부가 세션 한정이 되어 §1 제품 정의가 성립 불가 — **#9 와 동일한 ② 즉시 보고 대상으로 승격 + 지원 브라우저 범위 명시 요청**(Q-021) ⓔ 가져오기 롤백 스냅샷으로 저장소 피크 약 3배인데 §3-5 산정에 없음 + `MAX_IMPORT_CHARS(4M) > LIMIT_CHARS(2.5M)` + §6-2 복구 사다리가 `jr.v1.rollback` 을 지울 수 없음(Q-022) ⓕ INT-12 4항이 다른 대상의 유효 초안을 `clearDraft()` 로 **소리 없이 파괴** — §6-6 “사고로 사라지면 되살린다” 위반(Q-023). **경미 3건**: T 슬롯 다중 오류 상호 덮어쓰기(Q-024) · E-307 읽기 함수 반환 형태·`E-502` 반환 주체·4개 화면 DOM 정적/동적 계약 미정(Q-025). **③ 의 Q-014(`E-118` 도달 불가)를 ④ 관점에서 지지** — 미판정 시 ⑤ QA 가 결함 등록. **환경 전제 P-1~P-7 재검증: 오류 0건** — “순수 정적 PWA” 급 전제 오류 추가 발견 없음(ES모듈·IndexedDB·SW·`toISOString` UTC 함정·`Intl`·`localeCompare`·`maxlength` 전부 정확히 대응됨). **② 자체 검수 주장 4건 중 3건은 재측정 결과 사실**(v2 침투 0건 · 화면설계 색상/px 0건 · 모호 표현 확장 grep 추가 0건), 틀린 것은 E-코드 총계 1건. **게이트 ② 책임분 5항목 전부 실질 충족**(API정의·예외정의는 ⚠️ — 판정 한 줄로 닫히는 구멍이며 구현 불가 수준 아님). **되돌림 없음 사유**: 설계를 다시 해야 하는 지적 0건, 화면설계 256행·구조설계 1448행 본체 무수정. **③ 디자인 진행에 영향 없음.** **④ 개발 착수 전 해소 조건 5건**은 아래 절 참조 |
| ④ 개발 — **구현 완료 승인** | ✅ **승인** (2026-08-22 · `tech-lead` 2차 호출) | `src/` **9파일 4,435줄** 통합 심사 완료. **되돌림 0건.** 자동 반려 검사 **파트장 직접 재실행 + 범위 확대**(`innerHTML`·`TODO`/`FIXME`·`eval`·`fetch`/XHR/WebSocket·`document.write`·`localeCompare`·다크모드 분기 **전부 0건**). **콘솔 에러 0건** — Chromium 141 + Firefox 실제 `file://` 종합 스윕 + 개별 시나리오 20여 건, **외부 네트워크 요청 0건 실측 확증**. **화면 S-01~S-04 · 기능 F-01~F-09 · 예외 8종 전부 실행 확인.** 이음매 무결: **INT-24**(`JR.io`→`JR.stats` 호출 0건 — 등장 3건 전부 주석 / `applyImport`→`replaceAll`→`notifyChange('bulk')`→`invalidate` 실동작, 실패 2경로도 무효화) · **INT-25**(`monthIndex` 는 `model.js` 사설 `var`, 공개·외부참조 0) · **INT-30**(읽기전용 시 `{ok:true}`+빈 배열) · **INT-32**(배너 우선순위 15단 계약과 완전 일치 · `scrollTo` 135→0 실동작) · **INT-33**(문구 3건 **글자 그대로** 일치 64/61/29자). **E-코드 64개**(중복 0 · 슬롯 T33/I15/B15/S1 · 대역 분포 착수 계수와 한 칸도 안 어긋남). 전역은 `JR` 하나 · 로드 순서 7줄 계약대로 · 함정 8개 전부 회피. 상세 `apps/2026-08-11/handoff/04-개발.md`, 재현 스크립트 19개 `apps/2026-08-11/verify/`. **⚠️ YELLOW 4건 — `product-planner` 교차검토 미수행(이 승인의 전제 아님) · Safari/WebKit 확인 불가(기동 실패, 4종 중 2종만 측정) · 비-`{ok}` 함수 실제 17개(계약 12개는 파트장 문서의 자기모순 — ⑤ 기준선 정정 필요) · 5,000건 쓰기 경로 16ms 초과(설계 목표 1,000건에서는 5~8배 여유, 「미리 최적화하지 않습니다」 준수로 미수정 · 알려진 제한사항)** |
| ④ 개발 — `product-planner` 교차검토 | ⚠️ **조건부 통과** (되돌림 없음, 2026-08-23) | **④ 가 Y-1 로 「미수행」이라 적어 둔 그 검토입니다 — 지적이 나왔습니다.** 판정 전문 `apps/2026-08-11/docs/기획서.md` **§7-11(INT-34~INT-40)** · `questions.md` **Q-063·Q-064**. **되돌림 아님** — F-01~F-09 · 화면 4개 · 필수 예외 8종 · INT-24·25·27·32·33 은 ② 가 코드 경로로 역추적해 **확정대로 구현된 것을 확인**했고, **E-코드 64개 문구를 ② 가 전건 대조해 불일치 0건**(④ 는 3건만 대조). **시정 필요 4건**: ⓐ **`E-123` 발생 지점 0건** — 메모 100자 초과분을 **자르기만 하고 침묵**(구조설계 §7-9 「알리지 않는 것」 목록에 없는 항목) + `<textarea maxlength="100">` 이 `String.length` 기준이라 **Q-008 확정(`countChars` 단일 기준) 위반** ⓑ **`E-604` 발생 지점 0건** — 백그라운드 초안 저장 실패를 `E-606`(T)로 대체했으나 **화면이 가려진 순간의 토스트는 전달되지 않음**(INT-32 에서 `E-413` 을 B 로 옮긴 것과 같은 논리) ⓒ **`E-605` 가 사실과 다름** — `boot.js` `onVisible()` 이 `model.init()` 만 부르고 `notifyChange` 를 부르지 않아 **재렌더가 없는데 「화면을 새로 불러왔습니다」** 를 표시(가계부에서는 문구가 아니라 숫자 문제) ⓓ **`E-203` 의 `{percent}` 가 빈칸**으로 나가는 경로 1건(저장 성공 직후 `warnings` 경로 — Q-055 의 `noticeParams` 는 부팅 경로만 닫음). **권고 1건**: INT-10 미이행(내보내기 대체 영역이 S-04 이탈 후에도 남아 **기록 JSON 전문이 상시 노출**). **④ Y 고지 판정**: **Y-3 = ④ 가 옳음(17 승인)** — 단 원인은 ② 문서가 규칙의 **적용 범위**를 안 적은 것이고 `stats.invalidate` 누락은 **② 귀책 인정**(INT-34) · **Y-4 = ④ 의 미수정 판단 승인** + **v1 지원 규모 1,000건을 제품 정의에 명시**(INT-35) · **Y-2 = 정직한 기술, 유지**. **⑤ 품질 승인은 위 ⓐ~ⓓ 해소 전까지 불가.** ② 가 **실행 재현은 하지 못했습니다(브라우저·Node 불가) — 확인 불가**이며, 지적 4건은 전부 **발생 지점 부재라는 정적 사실**입니다 |
| ⑤~⑥ | ⬜ 미착수 | ⑤ 품질본부 착수 가능 — `handoff/04-개발.md` (⑤ 착수 5자료 전부 포함). **단 ② 교차검토 시정 4건이 ⑤ 합격의 전제입니다**(위 줄) |

**사이클 2 는 ③ 디자인 승인을 정상 수행합니다** — D-013 면제는 사이클 1 한정입니다.

### ~~구조설계에 빠진 것~~ → **해소됨 (2026-08-15)**
2026-08-14 실행에서 `system-architect` 가 §5(모듈 인터페이스) · §6(예외 8종) · §7(E-코드 **63개** · 문구 · 슬롯) · §8 · §9 를 이어 써 **1448행으로 완성**했습니다. 슬롯 이음매 대조 결과 **구멍 0건**(기획서 §7-7 대조표).

> **2026-08-16 정정 (INT-23)**: 이 줄의 「E-코드 60개」는 ② 의 오기였습니다. 산출물의 실제 정의 수는 처음부터 **63개**였고, 2026-08-16 ② 재대조에서도 **슬롯 누락 0건**이 재확인되었습니다(다만 기획서 §7-7 대조표 자체의 표기 오류 2건을 새로 찾아 정정). INT-27 이 `E-413` 1개를 신설해 **④ 가 구현할 확정 총계는 64개**입니다.

### ② 설계 승인의 조건 2건 (2026-08-15 · `product-planner`)

| # | 조건 | 해소 전까지 |
|---|---|---|
| 1 | ~~**`tech-lead` 검토 1회**~~ → **✅ 해소 (2026-08-16)** — 검토 수행(2026-08-15) + 검토가 건 조건 5건을 ② 가 전부 판정 | (해소됨) 화면·문구 영향은 Q-014 **1건뿐이며 ③ 시안 재작업 없음** |
| 2 | **③ 디자인 승인** (게이트 8번) | ✅ **해소 (2026-08-20)** — ③ 조건부 승인(08-18) → **확정 승인**(개정 1차). 아래 「③ 디자인본부」 절 참조. **사이클 2 는 D-013 면제 대상이 아니며, 면제가 아니라 실제로 채워졌습니다** |

**무조건 승인이 아닌 이유**: 두 실무자 산출물은 파트장이 검토했으나 **통합본을 검토한 사람이 아직 없습니다.** 이 상태를 "승인 완료"로 적는 것은 `team-rules.md:18` "주장을 믿지 않는다"에 걸립니다. **반려도 아닌 이유**: 미충족은 검토 **절차**이지 산출물 내용이 아니며, 게이트 5항목은 실측으로 충족되어 ③ 착수에 필요한 것은 전부 확정되었습니다.

### 위 조건 1의 이행 — `tech-lead` 교차검토 수행됨 (2026-08-15)

**조건 1(`tech-lead` 검토 1회)은 수행되었습니다.** 결과는 위 표의 「② 프로덕트 — `tech-lead` 교차검토」 줄, 상세는 `questions.md` Q-018~Q-025.

**"검토받았다"는 사실만으로 조건이 닫히지는 않습니다.** 검토에서 지적 9건이 나왔고, 그중 5건은 ④ 개발 착수 전에 ② 판정이 필요합니다.

| # | ④ 착수 전 해소 조건 | 근거 | ③ 디자인 영향 |
|---|---|---|---|
| 1 | E-코드 총계 **63** · `E-6xx` **6** 으로 정정 + 전파처 4개 문서(`기획서.md` · `handoff/02-기획.md` · `docs/디자인-분배안.md` · `approvals.md`) 동기화 + §7-7 재대조 1회 실제 수행 | Q-018 | 있음 — ③ 이 이미 60 을 인용 중(`디자인-분배안.md:28`). **문구·슬롯 변경은 없으므로 시안 재작업은 아님** |
| 2 | `applyImport`/`replaceAll` 귀속과 `JR.stats.invalidate` 호출 지점 확정 | Q-019 | 없음 |
| 3 | `monthIndex` 소유 모듈 확정 | Q-020 | 없음 |
| 4 | 실측 **#1** 을 #9 와 동일한 「② 즉시 보고 · ④ 단독 결정 금지」로 승격 + **지원 브라우저 범위 명시** | Q-021 | 없음 |
| 5 | Q-022 · Q-023 · Q-024 · Q-025 및 **③ 제기분 Q-014** 판정 | 해당 Q | Q-014 만 있음(카테고리 1개 시 안내문 자리 — ③ 이 이미 잠정 확보) |

**조건 2(③ 디자인 승인)는 그대로 유효합니다.** 게이트 8번은 아직 열리지 않았고, ④ 는 오늘 검토만 수행하고 **착수하지 않았습니다.** 위 5건은 ③ 진행과 병행해 닫을 수 있으므로 일정 손실이 없습니다.

### 위 5건에 대한 ② 판정 — **5/5 해소** (2026-08-16 · `product-planner` 3차 호출)

**판정 위치: `apps/2026-08-11/docs/기획서.md` §7-9 (INT-23 ~ INT-31).** 기존 INT-01~INT-21 은 한 개도 바꾸지 않았습니다(INT-08·18·22 는 원래부터 결번 — 전수 검색 확인). 답변은 `questions.md` Q-014 · Q-018~Q-025 의 답변란에 있습니다.

| # | 조건 | 판정 | 확정 |
|---|---|---|---|
| 1 | Q-018 E-코드 총계 정정 + 전파 + §7-7 재대조 **수행** | ✅ **해소** | **인정.** 구조설계 §7 표 실제 정의 수 **63**(E-6xx 는 7이 아니라 6), INT-27 의 `E-413` 을 더해 **④ 구현·⑤ QA 기준 확정 총계 64**. §6 대역표에 **합계 검산 행** 신설. **§7-7 재대조를 ② 가 직접 1회 수행**(④ 결과 전재 아님 — `master-doctrine.md:45`) → 슬롯 누락 0건 재확인 + **대조표 표기 오류 2건 추가 발견·정정**(`E-604` 를 범위 표기 `E-601~E-606` 이 T 로 삼킴 / S-02 B 행에서 `E-203` 누락). **범위 표기가 오류를 숨겼다**는 것이 교훈이며 슬롯이 갈리는 대역은 개별 나열로 전환 | INT-23 |
| 2 | Q-019 `applyImport` 귀속 | ✅ **해소** | ④ 권고 **1번 채택** — §4-4 목록의 `applyImport` → **`replaceAll`**, §6-4-2 5단계의 `invalidate()` 삭제, **§5-1 `JR.io` 행에 `JR.stats` 를 「절대 호출 금지」로 명시**(허용·금지 어디에도 없던 공백을 닫음). 실패 경로도 함께 닫음 — `replaceAll` 실패 시에도 메모리 롤백 후 `notifyChange('bulk')` 호출 | INT-24 |
| 3 | Q-020 `monthIndex` 소유 | ✅ **해소** | **`JR.model` 내부 상태**로 확정. 공개 API 추가 없음, 시그니처 불변. 재구축은 `notifyChange` 안에서 `invalidate()` **직전** | INT-25 |
| 4 | Q-021 실측 #1 승격 + 지원 브라우저 범위 | ✅ **해소** | **승격 완료**(§8-2 가 「예외 1건」 → 「② 즉시 보고 **2건**」). **지원 브라우저 4등급 확정** — 기준(데스크톱 크롬 최신, 여기서 합격 판정) / 지원(엣지·파이어폭스) / **조건부(사파리 — 저장 차단 가능성, ② 재판정 대상)** / **v1 범위 밖(모바일 — 단 화면설계 §0-3 터치 요구는 유지)**. **⑤ 합격 기준 문장까지 확정**해 ④ 가 그대로 쓸 수 있음. `E-001` 문구는 실측 전까지 현행 유지, 수정 권한은 ② | INT-26 · §8-2 |
| 5 | Q-022·023·024·025 + **Q-014(③ 제기분)** | ✅ **해소** | **Q-022**: `MAX_IMPORT_CHARS` 를 `LIMIT_CHARS/4` **파생 상수**로(독립 상수 4,000,000 폐기 — 저장 불가능한 파일이 V-3 을 통과했음) + **`V-3-1` 사전 용량 검사 신설**(대화상자 **전에** 차단) + **`E-413` 신규 E-코드**(T, 문구 확정) + §3-5 **롤백 피크 행 3개** 추가 + **사다리는 `jr.v1.rollback` 을 어느 단계에서도 지우지 않음**을 명문화 + §6-4-2 4단계 **되돌리기 성공 시 rollback 삭제** 확정(누락되어 있던 것) · **Q-023**: INT-12 4항을 **"채우지 않되 지우지도 않는다"로 축소**(만료 `E-603` 만 예외). 단수 초안 키는 v1 의도임을 확인하고 덮어쓰기를 §7-9 무고지 표에 명기, 다중 초안은 **v2** · **Q-024**: T 슬롯은 **우선순위 1건만**(`E-113` 최우선, 꼬리는 코드 오름차순으로 결정적) · **Q-025**: `E-307` 읽기 함수 = **성공+빈 값**, `isReady()`=**false**, 새 공개 함수 없음 / `E-502` 는 **경계 함수만** / 화면 마크업 **정적 HTML + `#jr-app` 부팅 전 `hidden`**, `innerHTML` 금지 유지 · **Q-014**: ③ 잠정안 **2번+3번 결합** — `삭제` 비활성 유지 + **상시 안내문 `카테고리는 최소 1개가 있어야 합니다.`**(② 신규 확정, `JR.ui.MSG.CAT_MIN_HINT`) + `E-118` 은 `E-117` 과 같은 **두 번째 방어선**으로 명기(⑤ QA 검증 경로 지정) | INT-27~INT-31 |

**되돌림 0건. ③ 시안 재작업 0건.** Q-014 는 ③ 이 확보해 둔 안내문 자리를 그대로 쓰며, 나머지는 문구·슬롯을 바꾸지 않습니다.

**④ 의 잠정 채택안 판정**: Q-019·Q-020·Q-023·Q-024·Q-025 **전부 승인**(분할안 재작성 불필요). **Q-022 만 잠정안과 다릅니다** — 상수 파생화 + `V-3-1` + `E-413` 을 반영해 구현해야 합니다.

**남은 것 (닫혔다고 적지 않습니다)**: ③ 제기분 **Q-015 · Q-016 · Q-017** 은 **미판정**입니다. 셋 다 `막힌 정도 = 진행 가능`이고 ③ 이 잠정안으로 진행 중이며 **`tech-lead` 조건 5건에 포함되지 않습니다.** Q-015 는 `tech-lead` 실측 결과 없이 확정하면 근거 없는 승인이 되므로 실측을 기다립니다. 사유는 `기획서.md` §9-4.

**② 가 남의 파일에 손대지 않은 것**: `docs/디자인-분배안.md`:28 의 「E-코드 60개」는 **③ 소유**라 ② 가 고치지 않았습니다. `handoff/02-기획.md` §11-1 로 **정정 요청(60 → 64)만 통지**했습니다. 문구·슬롯 변경이 없어 시안 영향은 없습니다.

**게이트 재판정 — ② 책임분 5항목 전부 ✅**: `tech-lead` 가 ⚠️ 로 둔 **API정의**(Q-019·Q-020 구멍 → INT-24·INT-25 로 폐쇄)와 **예외정의**(총계 요약 결함 → INT-23·INT-27 로 표와 요약 일치)가 해소되었습니다.

**`tech-lead` 가 되돌리지 않은 이유**: 지적 9건 중 설계를 다시 해야 하는 것이 0건입니다. 화면설계 256행·구조설계 1448행 본체는 손댈 곳이 없고, §6 예외 8종과 §7 E-코드 63개 표는 `js/err.js` 로 그대로 옮길 수 있는 완성도입니다(63개 기준 슬롯 재대조 결과 **누락 0건** — 결론은 맞고 숫자만 틀렸습니다). **`tech-lead` 가 무조건 통과시키지 않은 이유**: Q-018 은 오타가 아니라 "전수 대조했다"는 **절차 진술**이 실측과 어긋난 건이며, ② 스스로 "제 통합본을 검토한 사람이 없다"고 적은 위험이 실제로 한 건 현실화된 것입니다. 검토 절차가 제대로 작동했다고 봅니다.

### ③ 제기 **Q-026** 에 대한 ② 판정 — **닫힘** (2026-08-18 · `product-planner` 4차 호출)

**판정 위치: `apps/2026-08-11/docs/기획서.md` §7-10 (INT-32 · INT-33).** INT-01~INT-31 은 **한 개도 바꾸지 않았습니다**(INT-08·18·22 는 그대로 결번). 답변 전문은 `questions.md` Q-026 답변란.

| 질문 | 판정 | 확정 |
|---|---|---|
| **Q-026** — `E-413`(87자)을 3초 자동 소멸 토스트로 전달하면 사용자가 다 읽지 못함 | ✅ **닫힘 — ③ 대안 2번 채택** | **`E-413` 슬롯 `T` → `B`(배너·사건형).** 문구는 87자 **그대로** 유지 · 발생 조건(`V-3-1`) 그대로 · **E-코드 총계 64 불변**(슬롯 열 한 칸만 바뀜) · **④ 호출부 코드 0줄 변경**(INT-15 로 표시가 `JR.ui.show()` 단일 경로이므로 표의 슬롯 칸만 고치면 자동으로 배너로 나감) | INT-32 |

**채택 근거 3가지 (③ 이 든 "87자/3초"보다 한 단계 앞)**: ⓐ **계열 정합** — 가져오기 실패 계열은 이미 전부 B(`E-410` 사건형 · `E-411` 차단형)이고 `E-413` 은 `E-410` 과 문장 구조까지 같습니다. INT-27 이 급히 신설하며 T 로 둔 것이 유일한 예외였고 **87자는 그 부정합의 증상**입니다. ⓑ **복구 절차가 화면을 넘어감** — 「설정 › 내보내기 → 기록 정리(S-01) → S-04 복귀 → 재시도」로 화면 이동 2회 이상이라, 지시를 수행하는 동안 토스트가 이미 없습니다. ⓒ **동반 슬롯 없음** — INT-29 저장 실패 경로는 T 1건 + I n건이 함께 떠서 T 를 놓쳐도 인라인 오류가 남지만, `E-413` 은 **T 단독**이라 3초 뒤 전달 수단이 0개가 됩니다.

**Q-024 와 함께 판정했습니다 — 겹치는 지점이 실제로 있었습니다.** 두 질문은 같은 T 슬롯의 다른 병목입니다(Q-024 = **자리**, Q-026 = **시간**). **INT-29 T 우선순위 2위가 `E-121`(74자)** 이어서, 우선순위가 골라낸 단 1건이 하필 최장급 문구였습니다 — **INT-29 를 적용해도 그 1건이 3초에 안 읽히면 아무것도 전달되지 않습니다.** `tech-lead` 가 Q-024 를 경미로 둔 판단 자체는 옳았으나(자리 규칙만 보면 한 줄로 닫힘), **Q-026 과 겹치면 경미 2건이 「우선순위 1위 코드가 전달되지 않는다」는 하나의 경로**가 됩니다. INT-33 의 `E-121` 단축이 그 지점을 닫습니다.

**INT-33 — ③ 이 요청한 T 슬롯 기준을 확정했습니다** (한 건만 옮기면 같은 계열이 남는다는 ③ 의 지적이 정확합니다):
- **적격 기준(2조건 전부 충족해야 T)**: (가) 놓쳐도 **같은 화면 조작 1회**로 같은 문구가 다시 나올 것 · (나) 실패한 조작을 성공시키는 **복구 절차가 2개 이상 화면을 거치지 않을 것**((나)는 조작이 이미 성공한 뒤의 권고에는 미적용 — 예 `E-204`). **T 34개 전수 적용 결과 위반은 `E-413` 1건뿐** — 독립적으로 세운 기준이 ③ 이 지목한 코드 하나를 정확히 집어냈습니다.
- **길이 상한**: **전체 65자 · 첫 문장 32자.** 근거는 독해 속도가 아니라 **③ 의 레이아웃 실측**(360px 에서 87자 = 5줄 → 17.4자/줄 → 4줄 = 69.6자 / 2줄 = 34.8자). 3초 × 6자/초 = 18자는 **완독** 기준인데 (가)(나)를 통과한 T 코드는 완독을 요구하지 않으므로 상한으로 쓰지 않습니다.
- **문구 단축 3건 (문구는 ② 소관 · ④ 미착수라 비용 0)**: `E-403` 77→**64자** · `E-121` 74→**61자** · `E-306` 35→**29자**.
- **`E-121` 은 문장 순서를 뒤집었습니다** — 원문은 첫 문장이 이모지 계산 방식 설명이고 실제 결과(저장 실패)가 두 번째 문장이어서 **구조설계 §7-0 문구 원칙 1(「무엇이 어떻게 됐는지 먼저」) 위반**이었습니다. 이번 검사에서 새로 찾은 것입니다.

**「표시 시간을 문구 길이에 연동」은 검토 후 기각**: ⓐ **연동식을 끝까지 계산하면 배너가 됩니다** — 87자 ÷ 6자/초 = 14.5초이고, 자동으로 사라지는 14.5초짜리 배너는 배너보다 나쁩니다(닫을 수 없고 읽는 도중 사라짐). 이 산술 자체가 "T 는 이 문구를 담을 그릇이 아니다"를 말합니다. ⓑ ⑤ QA 검증 항목이 코드 수만큼 늘어납니다. ⓒ 문구를 고칠 때마다 표시 시간이 조용히 달라져 ⑤ 가 기대값을 고정할 수 없습니다. → **토스트 3초는 단일 상수 유지.**

**③ 이 묻지 않았지만 이 이동이 만드는 구멍을 함께 닫았습니다**: 배너는 `sticky; top:0` 인데 S-04 `가져오기` 버튼은 화면 아래쪽이라 스크롤이 내려간 상태에서는 배너가 시야 밖에서 뜹니다. → **가져오기 결과 배너 표시 직후 최상단 스크롤**(`window.scrollTo({top:0})`), 대상은 **`E-410`·`E-411`·`E-413` 3개 전부**. `E-413` 하나만 고치면 같은 구멍이 두 개 남습니다. **배너 우선순위**는 `E-410` 다음·`E-302` 앞(특히 `E-203` 80% 경고보다 반드시 앞 — `E-413` 이 뜨는 시점엔 `E-203` 배너가 이미 떠 있을 확률이 높습니다).

**③ 의 전제 1건이 틀렸음을 실측으로 확인**: ③ 은 「종전 최장은 `E-121` 74자」로 적었으나, ② 가 T 슬롯 34개 문구를 전수 계수한 결과 **`E-403` 이 77자로 더 깁니다.** `E-413` 신설 이전에도 최장은 `E-121` 이 아니었습니다.

**되돌림 0건 · ③ 시안 재작업 0건 · ④ 호출부 0줄.** 배너 시안은 §4-11 에 이미 있고, `E-413` 87자는 배너 최장 `E-201` 95자보다 짧아 ③ 의 §9-4 잘림 실측이 그대로 적용됩니다(**배너·토스트 둘 다 재실측 불필요**).

**② 가 남의 파일에 손대지 않은 것**: `docs/디자인가이드.md` 6곳(§4-11 사건형 목록 · §4-12 두 곳 · §9-4 각주 · §12 미결표 · 변경표 9행)은 **③ 소유**라 ② 가 열어보기만 하고 고치지 않았습니다. `handoff/02-기획.md` **§12-2** 로 정정 요청만 통지했습니다. **전부 문서 표기 정정이며 시안 변경이 아닙니다.**

**게이트·승인 상태 — 변동 없음**: E-코드 총계 **64 불변**(슬롯 분포만 T 34·B 14 → **T 33 · I 15 · B 15 · 정적 1 = 64**), **⑤ QA 커버리지 분모 64 그대로.** ② 책임분 5항목 전부 ✅ 유지, ② 조건부 승인의 조건 2건도 그대로입니다(조건 2 = ③ 디자인 승인, 미해소). **④ 개발 착수는 여전히 불허.**

**여전히 미판정 (닫혔다고 적지 않습니다)**: ③ 제기분 **Q-015 · Q-016 · Q-017**. Q-015 는 ③ 의 2차 대조 실험으로 **잠정안의 근거 한 줄이 틀렸음**이 밝혀졌으나(`animation-duration` 강제는 `animation-delay` 를 죽이지 않음) **결론은 그대로**이며, ② 는 여전히 ④ 실측 없이 확정하지 않습니다. 세 건 모두 **④ 착수 전 해소 조건이 아닙니다.**

### ② 통합 검수에서 발견한 것 (전부 기획서 §7 에서 닫음 · 되돌림 0건)

| 유형 | 건수 | 내용 |
|---|---|---|
| 두 문서가 어긋남 | 3 | S-02 B 슬롯 존재 여부(INT-01, 구조설계 Q-010 **기각**) · 통계에서 `__deleted__` 정렬 위치(INT-07) · dangling 라벨 2종 병존(INT-06) |
| 문서 내부 자기모순 | 2 | S-02 `저장` 비활성 조건 ↔ "저장 시 I 슬롯 오류" 서술(INT-02, **E-코드 8개가 죽은 코드가 될 뻔함**) · S-04 카테고리 버튼 동일 문제(INT-03) |
| 문구 중복·자리표시자 | 2 | E-112 와 화면설계 안내문이 글자까지 동일(INT-04) · `(오늘로부터 1년 뒤)` 를 그대로 출력할지 미정(INT-05) |
| 어느 문서에도 없던 것 | 8 | E-307 화면 규칙 · 내보내기 대체 영역 상시 노출 · 초안 복원 진입 규칙 · 가져오기 버튼 비활성 조건 · 정적 HTML 요소 3종 · `JR.ui.show` 필수화 · 성공 문구 8종 보관 위치 · 금액/날짜 입력 요소 계약 |
| 모호 표현 | 1 | 구조설계 §5-8 "알아서 뿌리는 헬퍼를 두는 것을 권합니다" → INT-15 로 **필수** 확정. 옆에 구현 코드가 있어 되돌림 사유 아님 |

**실무자 판정**: `ux-designer` ✅ 통과(되돌림 없음) · `system-architect` ✅ 통과(되돌림 없음). 필수 예외 8종 누락 0건, v2 항목(회원·결제·광고) 침투 0건(전수 검색 확인).

## 사이클 1 종료 처리
| 항목 | 판정 |
|---|---|
| 사이클 1 (FocusNoise) | ❄️ **미완 종료 (동결)** — D-017. 산출물 부재로 ⑤ 재개 불가. 품질 게이트를 낮춘 것이 아님 |

---

## 사이클 2 · ③ 디자인본부 — 디자인 승인 (2026-08-18 조건부 → **2026-08-20 확정** · `design-lead`)

| 단계 | 판정 | 근거 | 검토 |
|---|---|---|---|
| ③ 디자인 — 디자인 승인 | ✅ **승인 (확정)** · **2026-08-20 갱신** | **`docs/디자인가이드.md` 개정 1차 완료** — `frontend-dev` 지적 **10건(Q-027~Q-036) 전건** + ② 의 **Q-026 판정(INT-32·INT-33)** + ② 요청 정정 **6/6** 반영. **시안 되돌림 0건.** 접근성은 초판 실측(Chromium 141 · 대비 44조합 + 렌더 픽셀 RGB 대조 · 터치영역 32/32 · 간격 · 글자크기 · 포커스링 · 감속 대조 실험)에 **개정 1차 34조합 재계산(불일치 0건)** 을 더했습니다 | `frontend-dev` ✅ **수행됨 (2026-08-18)** |

**조건 2건 — 둘 다 해소되었습니다 (2026-08-20)**

| # | 조건 | 성격 | 상태 |
|---|---|---|---|
| 1 | `frontend-dev` 교차검토 1회 (`team-rules.md:29` · 인수인계서 02 §9-5) | 절차. ③ 이 스스로 채울 수 없음 | ✅ **해소** — 2026-08-18 수행(⚠️ 조건부 통과 · 되돌림 0건). 지적 10건은 **개정 1차로 전건 닫힘** |
| 2 | `questions.md` **Q-026** ② 판정 | 슬롯·표시시간 판정 권한이 ② | ✅ **해소** — 2026-08-18 판정(`기획서.md` §7-10 INT-32·INT-33). `E-413` **T → B(사건형)**. 개정 1차에 반영 |

### 재승인 — **「불필요」로 넘기지 않고 실제로 심사했습니다** (2026-08-20)

`frontend-dev` 는 「§15 재승인 조건은 **시안 되돌림**인데 되돌림 0건이므로 ③ 재승인 불필요」라고 적었습니다. **그 판단은 세 조건 중 하나만 본 것입니다.** 초판 §15 가 적은 재승인 조건은 셋이었고, 그중 **「Q-026 판정이 토스트 슬롯을 B(배너)로 옮길 때」가 실제로 발동했습니다**(INT-32). 자기 문서가 「이때는 재승인한다」고 적어 둔 사건이 일어났는데 남이 불필요하다고 했다고 넘기면, 그 조건문은 처음부터 없는 것과 같습니다.

| 초판이 정한 재승인 조건 | 발동 | 처리 |
|---|---|---|
| `frontend-dev` 검토에서 **시안 되돌림** | 미발동 (0건) | — |
| **Q-026 판정이 토스트 슬롯을 B 로 옮길 때** | ✅ **발동** | **심사 수행** — 아래 5항목 |
| ④ 실측에서 레이아웃이 깨질 때 | 미발동 (④ 착수 중) | — |

**심사 5항목 전건 통과**: 배너 시안 존재(§4-11, 새로 그린 것 0건) · 87자 잘림 없음(배너 최장 `E-201` **95자** > 87) · 슬롯 이동으로 자리를 잃은 코드 0건(**T 33 · I 15 · B 15 · 정적 1 = 64** 불변) · 이동이 만든 구멍은 ② 가 함께 닫음(최상단 스크롤 → §6-4 상태 7) · **접근성은 오히려 좋아짐**(3초 소멸·되짚기 불가인 T → 닫기 버튼 있고 잔류하는 B). **결론: 재작업 0건 · 확정 승인.**

### 개정 1차에서 닫은 것 — `frontend-dev` 지적 10건

| Q | 지적 | 개정 처리 |
|---|---|---|
| **Q-027** | 고정 탭바가 마지막 조작 요소를 **100% 덮음**(겹침 56.3px · 히트 픽셀 **0개**). 하단 여백 값이 전 문서에 없음 | §2 에 `body{padding-bottom:calc(--jr-tabbar-h + --jr-sp-4 + env(...,0px))}`. **산술 검증: `env`=0/34 양쪽에서 여유 16px 고정** |
| **Q-028** | `--jr-tabbar-h` 를 §4-9(전체 높이)와 §4-12(안전영역 제외)가 **다른 의미로 사용** — CSS 내부 모순 | **「안전영역 제외 높이」로 의미 통일** + 탭바 높이식 시정. `env`=34px 대입 시 콘텐츠 박스 21→**55px**, 토스트 간격 42→**8px**. **`env()` 폴백 `0px` 도 전면 부착**(④ 미지적분) |
| **Q-029** | `#jr-unsupported` 가 **정상 부팅 중 화면 전체 클릭을 가로챔**(핸들러 호출 0회) + §9 측정 대상 서술이 실제와 다름 | `pointer-events:none` + 키프레임에서 `auto`. **§9 머리말·§9-2 서술 정정**. `aria-hidden` 은 **JS 가 죽었을 때 걷어낼 수 없어** 넣지 않음 |
| **Q-030** | 배너의 본문 열 안/밖 미지정, §9-4 값은 한쪽에서만 재현 | **본문 열 밖(`<body>` 직계) 확정** — 결정 근거는 `sticky` 부모(INT-01 「스크롤 무관 고정」). §9-4 배너 값을 **5줄·143.95px** 로 교체 |
| **Q-031** | §9-4 **토스트 줄 수 2건이 각각 +1** (패딩 24px 미차감) | **4줄 / 7줄** 로 정정. **산출식 `(박스높이−패딩)÷22.4` 를 §9-4 에 명시.** ② 에 INT-33 근거 수치 정정 통지(상한값은 유효) |
| **Q-032** | `E-114`·`E-115`·`E-116` 이 추가 경로에서 뜰 **I 슬롯 없음** — 게이트 「슬롯 누락 0건」 **반례 1건** | **I 슬롯 4자리 → 5자리.** INT-03 의 이행. 요소 추가에 대한 ② 확인은 **Q-044** |
| **Q-033** | 정본 통합에서 **토스트 `role`·`aria-live` 탈락** — T 슬롯 코드가 보조기술에 하나도 전달되지 않음 | **`role="status" aria-live="polite" aria-atomic="true"` + 상시 DOM 복원.** 대체 영역 `aria-live` 복원, **차단형 배너 `role="alert"`**. ③ 이 한 겹 더: 상시 DOM 토스트의 **클릭 가로채기**를 `pointer-events:none` 으로 차단 |
| **Q-034** | 탭바 **아이콘 3개 SVG 가 정본에 없음** · 탭 요소 종류 미지정 · 중립 아이콘 자리 미지정 | **SVG 3개 §4-9 수록** · **`<button type="button">`** 확정 · 중립 아이콘은 **카테고리 슬롯 한 곳**. **§10-4 가 남을 지적한 것과 같은 누락이 정본에서 재발 — 파트장 책임으로 명기** |
| **Q-035** | `<meta viewport>`·`<html lang>`·`<title>` 이 **전 문서 0건** | **§2-0 `<head>` 계약 신설** — viewport(+`viewport-fit=cover`, 확대차단 금지) · `lang="ko"` · **`charset` 추가**(④ 미지적분). **`<title>` 문구는 ② 소유라 확정하지 않고 Q-043 으로 상신** |
| **Q-036** | 칩 「20개 = 7줄」은 **상한이 아니라 표본 1개**, 이름 미기록 | 표본 3개 + **최악 12자×20 = 20줄·1032px** 명기. `그룹높이 = 44n+8(n−1)` 검산식. **초판 표본은 남아 있지 않아 「미기록」으로 표시 — 복원했다고 적지 않음** |

**`tech-lead` 제기 2건도 답변 완료**: **Q-040**(개정 미반영 → 개정본 도착 · **잠정값과 갈리는 3건** 통지) · **Q-041**(Q-029 의 2엔진 독립 재현 → §9 서술 정정 반영).

### ③ 이 새로 ② 앞에 올린 것 — 2건 (④ 착수 차단 아님)

| Q | 내용 | 왜 ③ 이 정하지 않는가 | ④ 처리 |
|---|---|---|---|
| **Q-043** | **`<title>` 문구** | **문구는 ② 소유**(인수인계서 02 §3). ③ 이 정하면 ② 확정 밖 문구가 하나 생깁니다 | 잠정 `한달정리` 로 진행. 판정 후 **1줄 교체** |
| **Q-044** | S-04 에 **I 슬롯 요소 1개 추가** 확인 | 화면 요소 증감은 화면설계(② 소유)에 닿음. **③ 은 INT-03 의 이행으로 보고 확정했고 T 슬롯 대안은 택하지 않았습니다** | **③ 확정대로 구현.** 뒤집혀도 요소 하나 삭제 |

### 승인에 **포함되지 않는 것** (정직하게)

| 항목 | 소관 | ④ 착수 차단 |
|---|---|---|
| Q-043 `<title>` 문구 · Q-044 요소 확인 | ② | ❌ |
| Q-015 · Q-016 · Q-017 (초판과 동일) | ② 미판정 | ❌ |
| **개정 1차 CSS 의 브라우저 실행 검증 7건** — **파트장이 이번 호출에서 브라우저를 실행하지 못했습니다.** 대비는 재계산, 기하는 산술로만 검증했고 원시 렌더 수치는 `frontend-dev`·`tech-lead` 재측정을 출처와 함께 인용했습니다 | ④ (가이드 §11-3) | ❌ |
| `#jr-unsupported` **접근성 트리 잔존** — `pointer-events` 는 클릭만 막습니다. 순수 CSS 후보(`visibility` 키프레임)는 실패 모드가 「문구가 영영 안 뜸」이라 **미검증 상태로 정본에 넣지 않았습니다. 통과로 쓰지 않습니다** | ④ 실측 | ❌ |
| `file://` 파비콘 · `<input type="date">` 아이콘 · 안전영역 실기 · 한글 시스템 폰트 | ④ | ❌ |

### 초판이 틀렸던 것 — 감추지 않고 기록합니다

1. 「이 문서 하나만 읽고 착수할 수 있게 썼습니다」는 **사실이 아니었습니다**(`<head>`·탭바·S-04 추가 경로에서 막힘).
2. 게이트 「E-코드 64개 전부 갈 곳 있음」에 **반례 1건**이 있었습니다 — 자기 점검표에 ✅ 로 적혀 있던 항목입니다.
3. 터치영역 **32/32 통과가 더 큰 결함을 감쌌습니다** — 탐침 범위(최하단 스크롤 미포함)를 기록하지 않은 것이 원인입니다.
4. **§9 머리말의 측정 대상 서술이 실제와 달랐습니다**(`team-rules.md:52` 해당) — 측정 페이지에 `#jr-unsupported` 가 없었습니다.
5. **§9-4 줄 수 2건이 틀렸고 그 숫자가 ② 판정 자료로 올라갔습니다** — 산출식을 적지 않아 오산이 숨었습니다.
6. **남을 지적한 항목(§10-4)에서 같은 잘못**을 했습니다 — 탭 아이콘 3개를 세기만 하고 싣지 않았습니다.
7. 「T 최장은 `E-121` 74자」라는 **전제가 틀렸습니다**(실제 `E-403` 77자) — ② 가 전수 계수로 확인했습니다.

---

### 초판 기록 (2026-08-18) — 아래는 그대로 보존합니다

**④ 개발 착수 게이트**: 이 조건부 승인으로 **8/8 을 채우는 것으로 인정**합니다. 두 조건 모두 **시안 되돌림을 요구하지 않기** 때문입니다 — 조건 1 은 결과가 나와야 알 수 있고, 조건 2 는 시안이 이미 5줄을 수용하도록 완성되어 판정이 어느 쪽으로 나도 그리는 것이 바뀌지 않습니다(값만 바뀜). **재승인 필요 조건**: `frontend-dev` 검토에서 시안 되돌림이 나올 때 · Q-026 이 토스트 슬롯을 B 로 옮길 때 · ④ 실측에서 레이아웃이 깨질 때.

**왜 무조건 승인이 아닌가**: 게이트 6항목 중 5항목은 충족했으나 **§9-5(`frontend-dev` 검토)가 미충족**입니다. 검토를 받지 않고 "6/6" 이라고 적으면 `team-rules.md:52`("검증하지 않은 것을 검증한 것처럼 서술")에 정확히 걸립니다.
**왜 반려가 아닌가**: 실측으로 찾은 결함 4건을 파트장이 이미 시정하고 **같은 브라우저에서 시정 후 재측정까지 마쳤습니다.** 지금 반려하면 이미 고쳐진 시안을 다시 그리게 하는 것이라 얻는 것이 없습니다.

**실측으로 찾아 시정한 결함 4건** (제출본 그대로 구현했다면 화면에서 깨졌을 것들)

| # | 결함 | 실측값 | 시정 후 재측정 |
|---|---|---|---|
| 1 | 토스트가 `left:50%`+`translateX(-50%)` 라 폭이 **뷰포트의 50%** 로 묶임. `max-width:488px` 도달 불가 | 360px 화면에서 폭 180px · **8줄** | 폭 328px · **5줄** |
| 2 | `.jr-btn--sm` 36px+`::before` 확장의 실제 히트 **43px**, `이름변경`↔`삭제` 히트 간격 **5px**(파괴적 12px 기준 미달) | 96×43 · 간격 5px | 90×45 · **간격 12px** |
| 3 | 선택 칩의 "1.5px 굵은 테두리"가 **흰 배경 위 흰 테두리 = 1.00:1** — §2-5 가 요구한 색 외 신호가 존재하지 않음 | 1.00:1 | 채움 명도 반전 **5.93:1** + 굵기 + `aria-checked` |
| 4 | 목록 행·탭의 바깥 포커스링이 이웃 행 침범 / 탭에서는 **5px 화면 밖** | 링 하단 905px > 뷰포트 900px | `outline-offset:-3px`, 대비 6.70:1 유지 |

**감추지 않고 기록한 것**: 인접 간격 **경계 2건**(칩 세로·`추가` 버튼 = 실측 7px, 기준 8px) · **예외 2건**(탭바 3탭·목록 행 = 0px, 파괴적 동작 아님·타깃 매우 큼·표준 관용). 비텍스트 대비 **미달 3건**(장식 구분선 1.64:1 · 보조버튼 면 1.08:1 · 막대 트랙 안쪽 2.96:1)은 각각 장식 전용이거나 반대편이 3:1 이상이라 1.4.11 을 충족하며, **값과 사유를 함께** `디자인가이드.md` §9-1 에 남겼습니다.

**타 산출물의 오류를 정정한 것**: `ui-designer` §3 의 `--jr-border`/`--jr-surface` = 3.10:1 → **실제 3.37:1**(3.10 은 `--jr-surface-sunken` 값을 옮겨 적은 것) · `ui-designer` §5-4 와 **파트장 자신의 분배안 §2-7** 의 "`prefers-reduced-motion` 리셋이 3초 지연을 죽인다" → **대조 실험 결과 틀림**(`animation-duration` 은 `animation-delay` 에 영향 없음. 리셋 적용 요소도 3.4초 뒤 정상 노출) · `brand-designer` §6 장1 의 "`미분류` 가 목록 가장 아래" → S-01 은 날짜 내림차순이라 **가장 위**.

**파트장이 직접 고친 자기 파일**: `docs/디자인-분배안.md`:28 「E-코드 60개」 → **64개** (`handoff/02-기획.md` §11-1 정정 요청 이행). 다만 `E-413`(87자)이 **토스트 최장 문구를 갱신**하므로 "시안 영향 없음"은 부분적으로만 맞습니다 — 잘림 검증값이 바뀌었고 그 결과 Q-026 이 나왔습니다.

**두 실무자 판정**: `ui-designer` ⚠️ **조건부 통과** — 24종 상태·13종 컴포넌트 누락 0건, 문구 창작 0건, 토큰 임의 변경 0건. 다만 자신이 새로 만든 조합 4개만 계산하고 **알파 합성 결과 대비는 계산하지 않았으며**(Q-018), 44px·12px 주장이 **명목값 기준**이라 실측에서 3건이 무너졌습니다. `brand-designer` ✅ **통과** — 16px 픽셀 RGB 를 실제 추출해 검증한 수준이 파트장이 요구한 기준선이었고, 확인 못 한 것(`file://` 파비콘)을 정확히 "확인 불가"로 남겼습니다. 되돌림 0건.

### 사이클 2 · ③ 디자인 산출물 교차검토 (2026-08-18 · `frontend-dev`) — 조건 1 이행

| 검토자 | 대상 | 판정 | 되돌림 | 근거 |
|---|---|---|---|---|
| `frontend-dev` (④ 개발본부 · 규정상 디자인 산출물 검토자, `team-rules.md:29`) | `docs/디자인가이드.md` 정본 + `handoff/03-디자인.md` | ⚠️ **조건부 통과** | **0건** | `questions.md` **Q-027~Q-036** (10건) |

**이 줄로 `디자인가이드.md` §13 게이트 5번 · §15 조건 1 · 위 표의 「`frontend-dev` 미수행」이 해소됩니다.** 남은 조건은 **Q-026(② 판정 대기)** 1건입니다.

**검토 방법**: 가이드 §2~§6 의 CSS 를 한 글자도 바꾸지 않고 옮겨 붙인 페이지를 만들어 **파트장과 동일 환경(Chromium 141.0.7390.37 · Playwright 1.56.1 · `deviceScaleFactor:1` · 360/520px · `file://`)에서 재측정**했습니다. 대비는 WCAG 2.1 공식을 **독립 구현**해 다시 계산했습니다.

**재현된 것 — §9 실측은 신뢰할 수 있습니다**: 대비 **34조합 전부 소수 둘째 자리까지 일치(불일치 0건)** · `.jr-btn--sm` 시정 후 히트 **90×45 / 56×45** 정확 일치 · 인접 간격 **12 / 8 / 7 / 7** 정확 일치(경계 2건 포함) · 44×44 미달 **0건** · `미분류(삭제된 카테고리)` **175.1px** · `999,999,999` **89.0px** · 0% 막대 **6.00px** · 칩 폭 **66.00/66.00** · I 슬롯 **22.4px** · 최소 글자 **14px** · 가로 넘침 **없음** · 토스트 폭 **328px** / 제출본 **180px·180.73px**(가이드 180.7 과 소수점까지 동일) · 탭 포커스링 화면 밖 **0px** · `prefers-reduced-motion` 대조 실험 **재현**.

**되돌림이 아닌 이유**: 10건 중 시안을 다시 그려야 하는 것이 **0건**입니다 — 없는 값 채우기(4) · CSS 한두 줄 시정(3) · 문서 수치 정정(3). 색·간격·타이포·컴포넌트 13종·화면 24종은 그대로 씁니다.
**무조건 통과가 아닌 이유**: 이 문서만으로 착수하면 `index.html` 의 `<head>` 부터 막힙니다.

**④ 착수 전 닫아야 하는 5건 (③ 정본 개정 1회로 전부 닫힘)**

| # | Q | 내용 | 실측 근거 |
|---|---|---|---|
| 1 | **Q-035** | `<meta name="viewport">`·`<html lang>`·`<title>` 이 **전 문서에 0건**. 360px 실측과 `env(safe-area-inset-*)` 의 전제 | 전수 검색 0건 |
| 2 | **Q-034** | 탭바 아이콘 **홈·통계·설정 SVG 3개가 정본에 없음**(§8-3 은 11개로 셈, 정본 제공은 8개). 탭 요소 종류(`a`/`button`)도 미지정 | §10-4 가 실무자에게 지적한 것과 같은 형태 |
| 3 | **Q-032** | `E-114`·`E-115`·`E-116` 이 **「새 카테고리 추가」 경로에서 뜰 I 슬롯이 §6-4 레이아웃에 없음**. 게이트 「슬롯 누락 0건」의 반례 1건 | INT-03 은 이 경로에서 버튼 활성 + I 슬롯 오류로 확정 |
| 4 | **Q-027** | **고정 탭바가 본문 마지막 조작 요소를 100% 덮음.** 본문 하단 여백 값이 가이드 어디에도 없음 | 겹침 **56.3px**, `전체 삭제` 히트 가능 픽셀 **0개** |
| 5 | **Q-033** | 정본 통합에서 **토스트의 `role="status" aria-live="polite"` 가 탈락**(실무 원본에는 있음). T 슬롯은 3초 자동 소멸·닫기 없음이라 되짚을 수 없음 | `디자인-UI.md`:585 대 정본 §4-12 |

**함께 닫기를 권하는 5건**: **Q-029** `#jr-unsupported`(`opacity:0`)가 **정상 부팅 중 화면 전체의 클릭을 가로챔** — 실제 클릭 호출 **0회**, 동시에 이 요소를 포함하면 §9-2 의 `elementFromPoint` 탐침이 전 지점에서 실패하므로 **"§2~§6 을 그대로 구현한 페이지로 32개를 탐침했다"는 서술이 실제 측정 대상과 다름** · **Q-028** `--jr-tabbar-h` 를 §4-9(전체 높이)와 §4-12(안전영역 제외 높이)가 서로 다른 의미로 사용 — `env`=34px 시뮬레이션에서 **탭바 콘텐츠 21px·탭이 안전영역 23px 침범·토스트 간격 8px → 42px** · **Q-030** 배너의 본문 열 안/밖 미지정 — **열 안 6줄·166.34px(=§9-4 값) / 열 밖 5줄·143.95px**, §4-11 CSS 는 열 밖을 시사 · **Q-031** §9-4 **토스트 줄 수 2건이 각각 +1**(시정 후 5→**4줄**, 제출본 8→**7줄**; 박스 높이를 `line-height` 로 나누며 패딩 24px 미차감. 박스 높이는 소수점까지 일치) — **Q-026 이 ② 에 올린 근거 수치이므로 함께 정정 필요** · **Q-036** 칩 「20개 = 7줄」은 상한이 아니라 표본 1개(규격상 최악 12자×20 = 360px **20줄·1032px**, 가로 넘침은 없음).

**③ 재승인 불필요**: §15 의 재승인 조건은 「시안 되돌림이 나올 때」인데 되돌림 0건입니다. 정본 §2·§4-9·§4-11·§4-12·§5-1·§5-2·§5-4·§6-4·§8-3·§9 개정 1회로 닫힙니다. 단 Q-035 의 `<title>` 문구와 Q-032 를 T 슬롯으로 처리할 경우에는 **② 판정**이 필요합니다.

**④ 는 오늘 개발에 착수하지 않았습니다.** 재측정은 `scratchpad` 임시 페이지로 수행했고 `apps/2026-08-11/` 아래에 파일을 만들지 않았으며, 남의 산출 파일을 고치지 않았습니다.

---

## 사이클 2 · ④ 개발본부 — 착수 게이트 심사 (2026-08-20 · `tech-lead` 1차 호출)

| 단계 | 판정 | 근거 |
|---|---|---|
| ④ 개발 — **착수 게이트** | ✅ **8/8 개방 · 착수** | 파트장 직접 심사(문서 실재 확인 + E-코드 전수 계수). 면제로 메운 칸 **0건** |
| ④ 개발 — 구현 완료 승인 | ⬜ **미착수** | 오늘은 게이트·실측·분할안까지. **2차 호출에서 판정** |

**게이트 8항목 — 남이 적은 "8/8" 을 그대로 받지 않고 재판정했습니다.**

| # | 항목 | 판정 | 파트장이 직접 확인한 것 |
|---|---|---|---|
| 1 | 시장분석 | ✅ | `docs/시장분석.md` 98행 + 실무 3종 실재 |
| 2 | 아이디어 승인 | ✅ | `approvals.md`:91 — B안 89점, ① 승인 + ② 교차검토 완료 |
| 3 | 기능정의 | ✅ | `기획서.md` §2 — **F-01~F-09 전 9개** 전수 검색 확인 |
| 4 | 화면설계 | ✅ | `기획-화면설계.md` — **S-01~S-04 전 4개** 확인 |
| 5 | 데이터구조 | ✅ | `구조설계.md` §2-1~2-7 · §3-1~3-5 · §4 전 절 실재 |
| 6 | API정의 | ✅ | `구조설계.md` §5-1~5-9 모듈 6개 시그니처 확정 |
| 7 | 예외정의 | ✅ | **전수 계수 재검증 — 아래** |
| 8 | 디자인 승인 | ✅ | ⚠️ 조건부 승인의 **조건 2건 모두 해소** (조건1 `frontend-dev` 08-18 수행·되돌림 0 / 조건2 Q-026 → INT-32·33) |

**게이트 7번을 그대로 받지 않은 이유**: 이 항목은 **이미 한 번 틀린 전력**이 있습니다(Q-018 — ② 가 "60개 전수 대조"라 적었으나 실제 63). 그래서 직접 셌습니다.
**결과: ② 의 64 는 정확합니다.** 구조설계 §7 의 서로 다른 E-코드 **63개**(전수 추출·중복 제거) + INT-27 신설 `E-413` = **64**. 대역 분포도 `기획서.md` §6 주장과 **한 칸도 어긋나지 않았습니다** — E-0xx 2 · E-1xx 25 · E-2xx 5 · E-3xx 9 · E-4xx 13 · E-5xx 4 · E-6xx 6.
**④ 가 추가로 확인한 함정**: **`E-413` 은 `구조설계.md` §7 표에 물리적으로 없습니다**(`기획서.md` §7-10 에만 존재). `js/err.js` 를 구조설계만 보고 옮기면 **63개가 되어 게이트 7번이 깨집니다.** 분할안 §1-1 에 경고로 못박았습니다.

### ⚠️ YELLOW 2건 — 보고서 최상단 계속 표기

| # | 항목 | 내용 |
|---|---|---|
| **Y-1** | **③ 디자인 정본이 개정 전 판본** | `docs/디자인가이드.md` 최종 수정 **2026-08-17 17:42** — `frontend-dev` 교차검토(08-18)·INT-32·33(08-18)보다 **앞섭니다.** Q-027~036 반영 흔적 **0건**(전수 검색). 「④ 착수 전 닫아야 하는 5건」이 **정본에 아직 없습니다** → ④ 는 **잠정값 8건**(분할안 §2-3)으로 착수 |
| **Y-2** | **면제 기록 없음** | `approvals.md` 에 이 앱(2026-08-11)에 대한 게이트 **명시적 면제 0건**(D-013 면제는 사이클 1 한정으로 명시). 8항목 전부 **실제 충족**으로 통과 |

**Y-1 이 착수를 막지 않는 이유**: `fullstack-dev`(6파일) 는 디자인 정본에 **의존하지 않아 100% 착수** 가능하고, `frontend-dev` 가 막히는 5건 중 **④ 가 기술값으로 정할 수 있는 것이 7건, ③ 판단이 필요한 것은 Q-034(탭바 아이콘) 1건뿐**입니다. 그 1건은 **아이콘을 그리지 않고 텍스트 라벨로 우회**해 조형 유입을 0으로 막았습니다.

### §9 실측 14항목 — **착수 첫날 전량 수행** (Chromium 141 + Firefox · 실제 `file://`)

| 결과 | 항목 |
|---|---|
| ✅ **PASS 12** | #1 localStorage · #3 storage이벤트 · #4 secure context · #5 anchor다운로드 · #6 상한 · #7 data URL · #8 Blob · #9 **FileReader** · #11 성능 · #12 이벤트존재 · #13 date입력 · #14 3초지연 |
| ⚠️ **설계 예상과 다름 1** | #2 origin **격리 안 됨**(폴더 무관 공유) → Q-037 |
| ❌ **방법 없음 1** | #10 조용한 다운로드 차단 감지 → Q-012 유지 |

**「② 즉시 보고」 2건 — 둘 다 PASS 입니다.**
- **#1 (`file://` localStorage)**: `probeStorage()` **true**, 새로고침 후 값 **유지**. Chromium·Firefox 양쪽. **§1 제품 정의 성립.** 메모리 모드가 기본 경로가 되는 브라우저는 측정 2종 중 **0종**. **E-001 문구 ④ 임의 수정 0건.**
- **#9 (`FileReader`)**: **동작.** **F-08 축소 불요** — ② 의 축소 판단 자체가 불필요해졌습니다.

**#6 실측 5,242,880자(정확히 5 MiB)** — 추정 2.5M 의 **2.1배**. §9 규칙상 「올려도 된다」이지 「올려야 한다」가 아니므로 **④ 는 `LIMIT_CHARS = 2500000` 유지**로 판단했습니다(근거: #2 로 origin 이 공유되어 5MiB 가 이 앱 전용이 아님). 상향은 ② 재량 → Q-038.
**#11 성능** — 1,000건 **0.4ms** / 5,000건 **2.0ms** / 5,000건 전부 한 달(적대적) **2.7ms**(Firefox 워스트 11ms). **목표 16ms 를 어느 경우에도 넘지 않음** → **§3-1 월별 키 분할 하지 않음**(「미리 최적화하지 않습니다」 준수). 부수 검증으로 `allocatePercents` 합이 **전 시나리오 정확히 100**, 1,000건 직렬화 **129,972자**로 설계 추정 130,000자와 오차 **0.02%**.
**#14 (Q-015)** — 정상 부팅 `boot.js` 제거 **12.8ms(Chromium)/4ms(Firefox)**, JS 죽은 페이지 **2.5초 opacity 0 → 3.4초 opacity 1**, `prefers-reduced-motion` 에서도 동일. **③ 의 §9-9 정정이 옳았음을 2엔진에서 독립 재현.**

**확인 불가 — 통과로 쓰지 않았습니다**: **Safari/WebKit 전 항목**(시스템 라이브러리 부재로 기동 실패 — INT-26 요구 4종 중 **2종만** 측정) · Chrome·Edge **정품 빌드**(Chromium 141 로 대신, 동일하다고 단정 안 함) · #12 **모바일 실제 호출** · #5·#7 **실기 다운로드 폴더** · #9 **OS 파일 선택창 경로**(`setInputFiles` 는 합성 입력) · `env(safe-area-inset-*)` **실제 값** · **실제 한글 시스템 폰트 렌더**.

**부수 확인 — Q-029 를 2엔진에서 독립 재현**: `#jr-unsupported` 가 전면 클릭을 **가로챕니다**(핸들러 호출 0회). §9-2 `elementFromPoint` 탐침이 이 요소를 포함한 페이지에서는 **전 지점에서 이 요소를 집는다**는 `frontend-dev` 의 두 번째 주장도 **성립**합니다. → Q-041, CSS 1줄 시정.

### 분할안 확정 — `docs/개발-분할안.md`

**같은 파일을 두 명에게 배정하지 않았습니다. 공유 파일 0건.**

| 실무자 | 파일 | 내용 |
|---|---|---|
| `fullstack-dev` | `src/js/` **err · store · model · stats · io · boot** (6) | 코어 로직·데이터·상태·저장소·비즈니스 규칙 |
| `frontend-dev` | `src/index.html` · `src/css/app.css` · `src/js/ui.js` (3) | HTML 구조·CSS·UI 렌더·이벤트 바인딩 |

**경계가 헷갈릴 자리를 미리 못박은 것**: `boot.js` = `fullstack-dev`(§5-9 오케스트레이션, DOM 렌더 0줄) · INT-14 두 요소의 **마크업은 `frontend-dev` / 제거는 `boot.js`** · **DOM id·클래스는 `frontend-dev` 단독**(`index.html` 과 `ui.js` 가 둘 다 같은 사람 소유라 교차 계약 불필요) · **금액 파싱은 `fullstack-dev`**(UI 가 미리 파싱하면 규칙이 두 곳에 생김) · 금액 포맷은 `stats.formatAmount` 이나 **`원` 접미사는 `frontend-dev`**.

**파트장이 확정한 공유 계약**: 전역 `JR` 6모듈 분할 · `{ok}` 규약 R-1~R-7 · **비-`{ok}` 함수 정확히 12개** · 의존 방향과 **유일한 역방향 1건**(`model` → `stats.invalidate`) · **`JR.ui` 표면 10개**(`init`·`show`·`toast`·`inline`·`banner`·`dismissBanner`·`lock`·`unlock`·`MSG`·`TOAST_MS`) · `JR.ui.MSG` **10종 문구** · `<script>` **7개 고정 순서**(`defer`/`async`/`module` 금지) · INT-14 **id 2개** · 부팅 9단계 · **INT-32 배너 우선순위 15단**.

**자동 반려 대상을 착수 전에 통지**: `innerHTML` · `TODO` · `FIXME` · 외부 URL — 2차 호출 통합 시 Grep 검사에서 **하나라도 나오면 반려**.

**신규 제기 5건**: `questions.md` **Q-037**(origin 공유 → ② 사용자 문서 문구) · **Q-038**(`LIMIT_CHARS` 상향 여부 → ②) · **Q-039**(`JR.ui.lock` 이 13번째 비-`{ok}` 함수 → ②·SA) · **Q-040**(정본 개정 미반영 → ③) · **Q-041**(Q-029 2엔진 재현 → ③). **전부 「진행 가능」 — 착수를 막는 것 0건.**

**④ 는 오늘 구현 코드를 작성하지 않았습니다.** 실측은 `scratchpad` 임시 페이지로 수행했고, `apps/2026-08-11/src/` 에는 **빈 디렉터리만** 만들었습니다. **남의 산출 파일을 고치지 않았습니다** — `디자인가이드.md`·`기획서.md`·`구조설계.md` 전부 열어보기만 했습니다.

### 정정 — Y-1 은 작성 도중 해소되었습니다 (2026-08-20 · 같은 호출 내)

위 Y-1 은 **파트장이 게이트 심사를 시작한 시점**의 사실입니다(`디자인가이드.md` = 2026-08-17 17:42 판본). **작업 중 `design-lead` 의 개정 1차(2026-08-19 22:25 · +407행)가 도착**해 Q-027~036 이 전부 반영되었습니다. 파트장이 **잠정값 8건을 정본과 1:1 대조**했습니다.

| 결과 | 건수 | 내용 |
|---|---|---|
| ✅ 일치 | 4 | viewport 문자열 · `<html lang="ko">` · Q-027 하단여백(값 동일) · Q-029 `pointer-events` |
| ❌ **잠정값 폐기 · 정본 채택** | 3 | **Q-028** · **Q-034** · **Q-033** |
| ⚠️ 미확정 | 1 | `<title>` — ③ 이 **② 판정 대기**로 남김 (④ 잠정 `한달정리`) |

**Q-028 이 가장 위험했습니다.** ④ 는 `--jr-tabbar-h` 를 **변수 2개로 분리**하려 했으나, 정본은 **변수 1개를 「안전영역 제외 높이」(56px)로 통일**하고 쓰는 곳마다 `+ env(safe-area-inset-bottom, 0px)` 를 더하는 방식입니다. ④ 안대로 갔다면 정본의 **세 계산식**(탭바 높이 · 본문 하단여백 · 토스트 기준선)이 **전부 어긋났을 것**입니다. **정본 도착 전에 코드를 쓰지 않은 것이 맞았습니다** — 오늘 착수를 게이트·실측·분할안까지로 끊은 판단의 실질 이득이 이 한 건입니다.
**Q-034 우회안(텍스트 라벨)도 폐기**했습니다. 정본 §4-9 가 `<nav>` + `<button type="button">` × 3 + **홈·통계·설정 SVG 3개**를 전량 수록했습니다. ④ 가 권고한 `<button type="button">` 은 정본이 채택했습니다.
**Q-033 은 ④ 잠정값이 불충분**했습니다 — 정본은 `aria-atomic="true"` 추가에 더해 **토스트를 상시 DOM 에 두고 `textContent` 만 교체**(라이브 리전은 생성·삭제하면 낭독되지 않음), 숨김을 **`opacity` 로만**(`display:none`·`visibility:hidden` 은 접근성 트리에서 제거), 숨은 동안 `pointer-events:none` 까지 요구합니다.

**분할안 §2-3 은 「잠정값 표」가 아니라 「정본 대조 완료표」로 교체했습니다.** 두 실무자는 **정본 확정값**을 받습니다.

### 추가 제기 — `getCategories()` 키 충돌 (Q-042 · 중대)

파트장이 대조 중 찾았습니다. 구조설계 §5-4-4 는 `{data:{`**`items`**`}}`, **INT-30** 은 `{data:{`**`categories`**`}}` 로 **같은 함수의 키가 다릅니다.**
**두 실무자가 서로 다른 문서를 정본으로 읽으므로**, 한쪽이 `categories` 로 쓰고 다른 쪽이 `items` 로 읽으면 **예외 없이 `undefined` 가 되어 읽기 전용 모드에서 카테고리 목록이 조용히 빈 채로 그려집니다.** `E-501` 도 뜨지 않습니다.
**④ 확정: 두 경우 모두 `items`** (INT-30 표의 다른 행은 전부 §5-4-4 키를 그대로 쓰므로 오기로 판단). ② 판정이 뒤집히면 `model.js` 한 줄. → **Q-042**.

**신규 제기 최종 6건**: Q-037 ~ Q-042. **전부 「진행 가능」 — 착수를 막는 것 0건.** Q-040 은 위 대조로 **자체 해소**되었습니다.

---

## 사이클 2 · ④ 개발본부 — **구현 완료 승인** (2026-08-22 · `tech-lead` 2차 호출)

**판정: ✅ 구현 완료 승인 (통과) — 되돌림 0건.** 인계: `apps/2026-08-11/handoff/04-개발.md`

### ⚠️ 보고서 최상단 계속 표기 — YELLOW 4건

| # | 항목 | 내용 |
|---|---|---|
| **Y-1** | **`product-planner` 교차검토 미수행** | 마스터가 별도로 붙인다고 통지했고 **이 승인 시점까지 수행되지 않았습니다.** **이 승인의 전제가 아니며**, ④ 는 교차검토 없이 자체 심사만으로 승인했습니다. 교차검토가 뒤에 붙어 지적이 나오면 이 승인은 그만큼 약해집니다 |
| **Y-2** | **Safari / WebKit 확인 불가** | 1차 호출과 **같은 사유로 이번에도 기동 실패**(`libgtk-4.so.1`·`libgraphene`·`libwoff2dec`·`libvpx`·`libevent`·`libopus`·`libgst*` 부재). INT-26 요구 4종 중 **2종만 측정**. **「통과」로 쓰지 않았습니다** |
| **Y-3** | **비-`{ok}` 함수가 계약 12개가 아니라 실제 17개** | **실무자 잘못이 아니라 파트장 계약 문서의 자기모순.** 분할안 §5-4 `show()` 예시가 `JR.err.slot`/`get` 을, §5-3 `notifyChange` 예시가 `JR.stats.invalidate()` 를 **비-`{ok}` 로 쓰라고 지시해 놓고 12개 목록에서 빠뜨렸습니다. 12(계약) + `ui.lock`(Q-039) + `err.get`·`err.slot`·`err.format`·`stats.invalidate` = **17**. **⑤ 는 17개를 기준선으로** 검사해야 하며, 12개로 세면 **오탐 5건** |
| **Y-4** | **쓰기 경로 5,000건에서 16ms 초과** | 1차 호출의 「어느 경우에도 16ms 미만」은 **읽기 경로에만** 해당했습니다 — **기록을 정정합니다.** 읽기(목록·통계)는 5,000건에서도 0.1~1.5ms. 쓰기는 월이동 12.3/16.5ms · 저장 16/24.9ms. **설계 목표 1,000건에서는 2.1/3ms · 1.8/2.4ms 로 5~8배 여유.** 반려하지 않은 근거는 아래 |

### 게이트 — 8항목 유지 확인 · 면제 0건

착수 게이트 8항목은 1차 호출에서 파트장이 직접 심사해 **8/8 실제 충족**으로 통과했고, **`approvals.md` 에 이 앱에 대한 명시적 면제는 여전히 0건**입니다(D-013 면제는 사이클 1 한정). **면제로 메운 칸 없음.**

### 자동 반려 검사 — 마스터 결과를 옮겨 적지 않고 직접 재실행 + 범위 확대

`innerHTML` **0** · `TODO`/`FIXME`(+`XXX`/`HACK`, 단어 경계) **0** · **(확대)** `outerHTML`/`insertAdjacentHTML`/`document.write`/`createContextualFragment`/`srcdoc` **0** · `fetch`/`XMLHttpRequest`/`WebSocket`/`sendBeacon`/`EventSource` **0** · `eval`/`new Function`/문자열 타이머 **0** · `localeCompare`/`new Date('문자열')` **0** · `jr.` 외 localStorage 키 **0** · `prefers-color-scheme` 분기 **0**.

**외부 URL grep 2건은 반려하지 않았습니다** — 둘 다 `http://www.w3.org/2000/svg` **XML 네임스페이스 식별자**(`ui.js` 의 `createElementNS` 인자 · `index.html` data: URI 파비콘의 `xmlns`)이며 가져올 주소가 아닙니다. **말로 끝내지 않고 실측으로 확증**: 2엔진 종합 스윕 동안 **`file://` 이외 요청 0건**.

### 실행 검증 — 실제로 돌렸습니다

**Chromium 141.0.7390.37 + Firefox · Playwright 1.56.1 · 실제 `file://` · 390×844/360×640 · 스크린샷 42장 · 재현 스크립트 19개**(`apps/2026-08-11/verify/`).

- **콘솔 에러 0건** — 종합 스윕(화면 왕복 3회·기록 12건·검증 오류 6종·메모 150자/이모지·월 이동 12회·카테고리 CRUD·키보드 25탭·새로고침 3회) + 개별 시나리오 20여 건, 2엔진 전부 0. 경고도 0.
- **화면 S-01~S-04** 전환 전부 · **기능 F-01~F-09** 전부(F-06 비율 합은 `3334/3333/3333`→`[34,33,33]` 등 **전 경계 케이스에서 정확히 100** · F-08 은 **실제 다운로드 970자 → 전체 삭제 → 가져오기로 완전 복원** · F-09 는 **2단계 확인 전부 통과해야만** 삭제 + 카테고리 기본 8종 복귀).
- **예외 8종 전부 실제로 잡힘** — 저장소 차단(메모리 모드+E-201, 메모리 모드에서도 저장 성공) · 용량 초과(`{ok:false,E-202}`, **예외 안 던짐**) · JSON 손상(**부팅 성공 + `jr.v1.corrupt.*` 격리, 전체 초기화로 도망가지 않음**) · 남의 JSON(E-405, **저장소 무접촉**) · 3연타(**1건만 저장**) · 새로고침(초안 복원+E-602 / 명시적 나가기는 삭제 / 24h 경과는 E-603) · 다운로드 차단(**텍스트 폴백 664자, F-08 실패 안 함**) · API 미지원(정적 폴백 + **E-001 문구 정본과 글자 그대로** / `FileReader` 부재 시 E-002+버튼 비활성+나머지 정상).
- **부수**: E-307 읽기전용(닫을 수 없는 배너·쓰기 차단·INT-30 빈 배열·버튼 비활성) · E-605 다른 탭 변경 토스트 · 44×44 전 화면 통과 · 360px 가로 스크롤 0 · `pointer-events:none`(Q-041 시정 반영).

### 이음매 — 두 사람이 서로를 못 봤으므로 여기를 집중 심사했습니다

**어긋남 0건.** `JR` 6모듈 겹침 0 · 전역은 `JR` 하나 · 로드 순서 7줄 계약대로(`defer`/`async`/`module` 0) · **INT-24**(`JR.io`→`JR.stats` 실행 호출 **0**, 등장 3건 전부 주석 / `applyImport`→`replaceAll` / `notifyChange('bulk')` 가 **성공·쓰기실패·예외 3경로 전부** 캐시 무효화) · **INT-25**(`monthIndex` 사설 `var`, 공개·외부참조 0) · **배너 우선순위 15단이 계약 순서와 완전 일치**하며 B 슬롯 15개와 일대일 · **INT-32 `scrollTo` 실동작 135→0** · **E-코드 64개**(중복 0 · 슬롯 T33/I15/B15/S1 · 대역 분포가 1차 호출 전수 계수와 **한 칸도 안 어긋남**) · **INT-33 문구 3건 글자 단위 완전 일치**(64/61/29자) · **INT-32 `E-413` 슬롯 B**(87자) · `frontend-dev` 함정 8개 **전부 회피**.

**Q-042 `getCategories`**: 파트장 확정대로 **`items`** 채택 + `fullstack-dev` 가 `categories` **별칭을 함께** 실어 어느 문서를 본 호출부도 빈 목록을 그리지 않게 했습니다. ② 판정이 어느 쪽이든 **코드 변경 0줄**.

### 파트장이 이번에 찾은 것 — 코드 밖의 결함 2건

| # | 발견 | 조치 |
|---|---|---|
| 1 | **`questions.md` Q 번호 충돌 6건** — 두 실무자가 병렬로 작업하며 둘 다 「최대 Q-044」를 읽고 045 부터 붙여 **Q-045~Q-050 이 각각 2개씩**(12개 질문이 6개 번호에) | **원인은 실무자가 아니라 분할안입니다** — 파일 소유는 나눴으나 **Q 번호 대역을 나누지 않았습니다.** `fullstack-dev` 분을 **Q-052~Q-058 로 번호만 이동**(내용 무수정, 정정 고지 삽입). **②·③·SA 는 새 번호로 답해 주십시오.** 부수 발견: **사이클 1 Q-018 도 같은 충돌** — 범위 밖이라 미조치, 마스터 판단 요청 |
| 2 | **기획서 §7-10 5) 표의 계산 착오** — 「T 슬롯 최장 = `E-121` 61자」라 적었으나 **같은 절이 `E-403` 을 64자로 확정**. 실제 T 최장은 `E-403` 64자 | **구현은 양쪽 다 정본 글자 그대로이고 둘 다 65자 상한 안** → **기능 영향 0**. 문구는 ② 소관이라 **④ 가 고치지 않았습니다** |

### 실무자 Q 6건에 파트장이 판정했습니다

**Q-048/Q-056**(초안 저장 주체) → **ⓐ 확정, `ui.js:1196~1201` 자체 등록으로 §6-6 방어선 채워져 있음(실행 확인)** · **Q-052**(`E-001` 슬롯 `'S'`) 승인 · **Q-053**(`items`+`categories`) 승인 · **Q-054**(`boot.js` DOM 3곳) **승인 — 분할안 §4-3 이 부정확했습니다**(INT-30(3) 이 3번째를 지시) · **Q-057**(직렬화 실패 → `E-501`) 확정 · **Q-058**(성능 재측정) 접수 + ④ 가 더 나쁜 쓰기 경로를 추가 발견(Y-4).
**미답 6건**(frontend Q-045·046·047·049·050 · fullstack Q-055)은 **②·③ 소관**이라 ④ 가 답하지 않았습니다. 전부 「진행 가능」.

### Y-4 를 반려하지 않은 근거

ⓐ **설계 목표(1,000건)에서 5~8배 여유**로 통과 ⓑ 구조설계 §4-4 가 **「미리 최적화하지 않습니다」**로 못박음 ⓒ 원인인 원자적 쓰기 순서(expenses→categories→settings + `.bak`)는 **§6-2 복구 사다리와 §6-3 손상 격리가 의존하는 설계**여서, 설정만 따로 쓰도록 쪼개면 **예외 2·3번의 보증이 흔들립니다 — 증상보다 위험한 수술.** ⓓ 5,000건은 상정 규모(수백~수천) 상단 밖. **정식 최적화 판정은 ② 소관**이며 ⑤ 는 **1,000건 기준으로 판정**하십시오.

### 승인 범위의 한계 — 숨기지 않습니다

- **Y-1(교차검토 미수행)이 가장 큰 한계**입니다. 이 승인은 **④ 파트장 단독 판단**입니다.
- **Safari 는 검증되지 않았습니다**(Y-2). ⑥ 에서 「전 브라우저 검증됨」으로 기술하면 안 됩니다.
- 실기 모바일·실제 다운로드 폴더·OS 파일 선택창·`env(safe-area-inset-*)` 실제 값·한글 시스템 폰트 렌더는 **확인 불가**로 남았습니다.
- **④↔⑤ 반복은 최대 3회.** 초과 시 중단하고 사용자에게 보고합니다.
