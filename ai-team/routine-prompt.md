# 07:07 정기 실행 프롬프트 — 저장소 사본

> 감사관 지적(2026-08-11 중대-2)에 따라 만든 파일입니다. 프롬프트가 저장소 밖에만 있어 **저장소만 보는 감사로는 검증할 수 없었습니다.**

| | |
|---|---|
| Routine ID | `trig_01R318Goz8QaBwiJ8nSPRjEi` |
| cron | `7 22 * * *` (UTC) = 매일 07:07 KST |
| 바인딩 | `session_01Q2Z7i57cwjVYEYqXL2wyjy` (새 창이 아니라 같은 대화가 이어짐) |
| 사본 기준 시각 | 2026-08-11 (트리거 `updated_at 2026-08-10T04:09Z`) |

## ⚠️ 이 사본과 실물이 어긋날 수 있습니다
사본은 저장소에, 실물은 Claude Routine 에 있습니다. **실물이 실제로 실행되는 것**입니다. 프롬프트를 바꾸면 이 파일도 함께 갱신하십시오.

## 확인된 드리프트 (2026-08-11 감사)

| 항목 | 실물 프롬프트 | 정본 | 결과 |
|---|---|---|---|
| 마감 단계 | `board.md` · `cycle.md` · `SESSION-LOG.md` 만 나열 | `daily-app/SKILL.md` 4-1 은 **`STATE.md` 갱신**도 요구 | 프롬프트가 **낡음** — `STATE.md` 규정(커밋 `f35f53e`, 08-10 12:07Z)이 트리거 갱신(08-10 04:09Z) **이후**에 생김 |
| D-012 준수 | 정본 4개 목록·노션 절차 3단계·RED 목록·YELLOW 규칙을 **본문째 복제** | "스킬·자동실행 프롬프트를 **정본 참조형으로 얇게**" | **위반** |

**왜 중요한가**: 2026-08-08 감사 1차 반려 사유가 "자동 실행 프롬프트가 구 6인 체제" 였고, 그 결과가 **산출물 0건**이었습니다. 프롬프트 복붙은 같은 사고를 반복시키는 구조입니다.

## 시정 제안 (사용자 지시 필요)
프롬프트 변경은 마스터가 임의로 할 수 없습니다. 아래 얇은 형태로 교체할 것을 제안합니다.

```
⏰ 매일 07:07(KST) 정기 실행입니다. 이 대화창에서 진행합니다.

1. 브랜치를 맞춥니다:
   git fetch origin claude/meeting-room-team-structure-knz8ob && git checkout claude/meeting-room-team-structure-knz8ob && git pull origin claude/meeting-room-team-structure-knz8ob
2. `/daily-app` 스킬을 실행합니다. 절차·규정·노션 동기화는 전부 그 스킬과
   스킬이 가리키는 정본에 있습니다. 여기에 복제하지 않습니다.
3. 사용자가 없다는 전제로 진행하고 멈춰서 대기하지 않습니다.
4. 마지막에 일일보고서 요약을 이 대화창에 남깁니다.
```

이렇게 하면 규정이 바뀔 때 정본만 고치면 되고, 프롬프트가 낡아 사고를 내는 경로가 사라집니다.

---

## 실물 프롬프트 전문 (2026-08-11 시점)

```
⏰ 매일 07:07(KST) 정기 실행입니다. AI 앱 개발팀의 하루치 사이클을 이 대화창에서 진행합니다.

## 0. 어제까지의 상태를 먼저 읽는다
- 저장소 브랜치를 맞춥니다 (팀 파일은 이 브랜치에만 있음):
  `git fetch origin claude/meeting-room-team-structure-knz8ob && git checkout claude/meeting-room-team-structure-knz8ob && git pull origin claude/meeting-room-team-structure-knz8ob`
- 정본 4개: `.claude/team-rules.md`(최우선) · `.claude/team-org.md` · `.claude/master-doctrine.md` · `ai-team/SESSION-LOG.md`
- 노션 도구가 이 실행에 **있으면** 「📓 작업 일지」(id `3b750286-df80-8173-a09e-f8dc7712e713`)를 fetch 해 어제 항목의 **"내일 이어받을 지점"** 을 확인합니다. **없으면** 저장소 파일만으로 진행합니다 — 저장소가 실제 상태, 노션은 기록입니다.

## 1. 진행
`/daily-app` 스킬을 실행합니다. 사용자가 없다는 전제로 `master-doctrine.md` 의 GREEN/YELLOW/RED 로 판단하고 **멈춰서 대기하지 않습니다.**
- 첫 실행이면 `team-master` 감사관을 호출해 복원(D-016)부터 감사합니다.
- 미결 Q-001(FocusNoise 산출물이 저장소에 없어 ⑤ 재개 불가): 사용자 답변이 있으면 따르고, 없으면 GREEN으로 결정한 뒤 `decisions.md` 에 기록합니다.

## 2. 마감 — 저장소 (생략 불가)
`board.md` · `cycle.md` · `SESSION-LOG.md` 갱신 → **커밋 후 `claude/meeting-room-team-structure-knz8ob` 브랜치로 푸시.** 커밋하지 않으면 컨테이너와 함께 전부 소멸합니다. PR은 사용자가 요청하지 않는 한 만들지 않습니다.

## 3. 마감 — 노션 정리 (생략 불가)
절차 정본은 **`ai-team/notion-sync.md`** 입니다. 반드시 그 파일을 읽고 따르십시오. 요약하면:
1. 오늘 항목을 `ai-team/notion-queue/<날짜>.md` 에 **먼저 씁니다** (노션 도구 유무와 무관하게 항상).
2. 노션 도구가 **있으면** `상태: 대기` 인 큐 파일을 날짜 오름차순으로 「📓 작업 일지」에 `insert_content` + `position:{"type":"end"}` 로 append 하고, 큐 파일 상태 줄을 `반영됨 <시각>` 으로 바꿉니다. 그다음 일지 상단 "현재 상태" 표를 오늘 값으로 갱신합니다.
3. 노션 도구가 **없으면** 큐에 남겨둔 채 `SESSION-LOG.md` 에 "노션 미동기화 N건"으로 적고 이 대화창에 보고합니다. **못 썼는데 썼다고 하지 않습니다.**

## 4. RED
결제·계정생성·자격증명·외부공개·되돌릴 수 없는 삭제·설치에 막히면 그 단계만 "차단됨"으로 기록하고 나머지를 끝냅니다.

면제(YELLOW) 항목은 매일 보고서 최상단에 계속 표기합니다. 마지막에 일일보고서 요약을 이 대화창에 남기십시오.
```
