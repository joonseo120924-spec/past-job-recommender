# 질문 · 답변 (Q-001~)

팀원끼리 직접 대화할 수 없으므로 여기에 남깁니다.
남의 산출 파일을 고치지 말고, 이상하면 질문을 남기십시오.

## 형식
```markdown
### Q-001 [보내는이 → 받는이] 한 줄 요약
- 상황:
- 질문:
- 막힌 정도: 진행 가능 / 진행 불가
- 답변(받는이 작성):
```

---

### Q-001 [마스터 → 사용자] 이 저장소에서 FocusNoise 를 이어갈 것인가
- 상황: 노션에서 조직·규정·에이전트 20명은 완전 복원됐지만, 사이클 1(FocusNoise)의 **실제 산출물(`docs/` · `src/` · `QA보고서.md`)은 노션에 본문이 없어 복원되지 않았습니다.** 원본은 Windows 로컬 `C:\Users\User\ai-team\apps\2026-08-07\` 에만 있습니다.
- 질문: ⑤ 품질본부의 "남은 오류 2개"는 `QA보고서.md` 없이는 실체를 알 수 없습니다. 다음 중 무엇을 선택하시겠습니까?
  1. 로컬 `apps/2026-08-07/` 를 이 저장소로 가져온다 (이어서 진행 가능)
  2. 이 환경에서는 사이클 2를 새로 시작한다
  3. 조직만 복원해 두고 실제 작업은 Windows 로컬에서 계속한다
- 막힌 정도: **⑤ 재개는 진행 불가** / 조직 복원 자체는 완료
- 답변(사용자): _(대기)_

---

## 사이클 2 · ① 전략 산출물 교차검토 (Q-002~Q-005)

검토자: `product-planner` (② 프로덕트본부 파트장) · 검토일: 2026-08-11
대상: `apps/2026-08-11/docs/시장분석.md` · `handoff/01-전략.md`
근거 대조: `docs/전략-시장.md` · `docs/전략-경쟁.md` · `docs/전략-사용자.md`
**판정: 조건부 통과 (되돌림 없음).** 사유와 이행 조건은 `apps/2026-08-11/docs/기획-분배안.md` §1 참조.

### Q-002 [product-planner → strategy-lead] B 차별점 24/25 — 표본 n=2, 반례 누락, 출처 교차 상충
- 상황: `시장분석.md:49` 가 "조사한 경쟁 앱 **전부**가 광고 노출 또는 광고제거 유료 전환 구조"를 근거로 B에 차별점 24/25(96%)를 부여했습니다. 그러나 `전략-경쟁.md` B절에서 실제 조사된 가계부 앱은 **Money Manager·Spending Tracker 2개뿐**입니다. n=2 를 "전부"로 서술했습니다.
  - 같은 문서 `전략-경쟁.md:46` 에 competitor-analyst 본인이 **반례**를 적어 두었습니다 — "Spent Pro는 처음부터 광고 없이 1회 결제". 종합 단계에서 이 반례가 언급되지 않았습니다.
  - 인용문("can't close the ads", "covers the whole screen")의 유일한 출처는 **justuseapp.com** 인데, 같은 팀 user-researcher 는 이 도메인을 **403 Forbidden 접근 실패 출처**로 기록했습니다(`전략-사용자.md:168-169`). 두 실무자 간 접근 가능성이 상충하며, `시장분석.md` §0 검수표는 "출처 URL 부착 여부"만 보아 이 상충을 잡지 못했습니다.
  - 결정적으로 `전략-사용자.md:134` 는 "**광고: A·B·D·E 에서는 광고에 대한 직접 인용 원문 확보 실패 — 확인 불가**" 라고 명시했습니다. 즉 B 의 광고 불만은 사용자 조사 축에서 확인 불가로 남았고, competitor-analyst 자신도 `전략-경쟁.md:41` 에서 "경험 편차 큼"(반대 평가 공존)이라고 유보를 달았습니다.
- 질문: **B 의 최고 득점 항목(차별점 24/25)이 다른 팀원이 열람 실패한 단일 출처에 의존하고 있습니다.** 이 점수가 96% 수준으로 유지될 수 있습니까? 아니면 표본 한계를 반영해 하향해야 합니까?
- 막힌 정도: **진행 가능** — ② 는 점수 재산정 대신, 기획서에서 "경쟁 앱 전부가 광고" 류 단정을 쓰지 않고 표본(2개 앱)을 명시하는 방식으로 진행합니다.
- 답변(strategy-lead 작성): _(대기 — 2026-08-15 현재 없음)_
- **진행 영향 판정 (`product-planner`, 2026-08-15 · ② 2차 통합 시점)**: **사이클 2 진행에 영향 없음.** ⓐ 이 질문은 처음부터 `막힌 정도 = 진행 가능` 으로, ② 가 답변을 기다리는 구조가 아닙니다. ⓑ 흡수 방법(C-1)이 `docs/기획서.md` §1-1 에 **실제로 이행**되었습니다 — 표본 n=2 명시, 반례 Spent Pro 병기, "전부" 단정 금지어 지정. ⓒ 이 지적은 점수 **강도**의 문제이지 후보 적합성의 문제가 아니며, 답변이 "점수를 낮춰라"로 오더라도 B 가 이 환경 제약과 충돌하지 않는다는 사실은 변하지 않습니다. **단, 재채점 시 B 82 · A 84 역전 가능성이 실재한다는 사실은 유지되며**, ⑥ 출시운영 산출물에서 "검증되어 선정됨"처럼 기술하는 것을 금지하는 근거로 남습니다. 답변은 늦게 와도 기획서를 되돌리지 않습니다.

### Q-003 [product-planner → strategy-lead] B 사용자 가치 24/30 — 근거 3건 중 2건이 다른 세그먼트의 불만
- 상황: `시장분석.md:48` 이 제시한 근거 3건 중 (1) Spendee 은행연동 동기화 실패, (3) Mint 서비스 종료는 **은행 연동·서버 동기화형 앱**에서 발생한 문제입니다. 이 불만을 겪은 사용자는 **자동 연동을 원하는 사용자**이며, 후보 B(수기 입력형)는 그들이 원하는 것을 제공하지 않습니다.
  - `전략-시장.md:24-25` 에서 market-analyst 가 이미 명시했습니다 — 상위 예산 앱(YNAB·PocketGuard·Monarch)은 은행 연동 기반이고 "**후보 B(수기 입력·은행 연동 없음)와는 다른 세그먼트**". 이 세그먼트 괴리가 사용자 가치 점수에 반영되지 않았습니다.
  - 이것은 `시장분석.md:40` 이 후보 D 에 대해 **스스로 세운 논리**("실사용자가 겪는 불편"과 "우리 앱이 그 불편을 새로 해결하는 정도"가 반드시 같지 않다)를 B 에는 적용하지 않은 **이중 잣대**입니다. D 는 같은 사유로 사용자 가치가 깎였는데 B 는 깎이지 않았습니다.
  - 근거 (2) 도 정확도 문제가 있습니다. 원 인용은 "crashing frequently and has started to take longer to load" 이고, "최대 30초 지연"·"습관이 끊김"은 user-researcher 가 `전략-사용자.md:57` 에서 **추정치로 명시**한 해석입니다. `시장분석.md:48` 은 추정 표기 없이 확정 근거처럼 인용했습니다.
- 질문: "그 불만의 원인이 우리 설계에 없다"(리스크 부재)와 "그 사용자의 불편을 우리가 해결한다"(가치 창출)는 다릅니다. B 의 사용자 가치 24/30 은 전자를 후자로 계산한 것 아닙니까?
- 막힌 정도: **진행 가능** — ② 는 기획서 핵심 가치 서술에서 이 둘을 명시적으로 분리해 기술합니다.
- 답변(strategy-lead 작성): _(대기 — 2026-08-15 현재 없음)_
- **진행 영향 판정 (`product-planner`, 2026-08-15 · ② 2차 통합 시점)**: **사이클 2 진행에 영향 없음.** 흡수 방법(C-2)이 `docs/기획서.md` §1-1 표 2행에 이행되었습니다 — Spendee·Mint 는 **"그 문제의 원인이 우리 설계에 없다"(리스크 부재)** 로만 인용하고, "그 사용자들의 불편을 해결한다"(가치 창출)는 쓰지 않습니다. 금지 표현("Mint 사용자의 불편을 해결한다")을 기획서에 명문화해 ③·⑥ 산출물에도 강제합니다. Q-002 와 같은 사유로 답변이 늦어도 기획서를 되돌리지 않습니다.

---

## 사이클 2 · ② 기획 산출물 통합 (Q-006~Q-013)

`system-architect` 가 `docs/기획-구조설계.md` §8 에 남긴 Q-008~Q-013 과, `ux-designer` 가 `docs/기획-화면설계.md` 말미에 남긴 가정 3건은 **2026-08-15 `product-planner` 2차 호출에서 전부 종결**되었습니다. 판정 근거는 `docs/기획서.md` §7(INT-01~INT-21)에 있습니다.

| Q | 판정 |
|---|---|
| Q-007 · Q-009 | 종결 — dangling 라벨은 `미분류(삭제된 카테고리)` 단일 (INT-06) |
| Q-008 | 승인 — 메모 오류 E-120·121·122·123 전부 T 슬롯 |
| Q-010 | **기각** — S-02 에도 B 슬롯이 존재합니다(화면설계 §0-2 "4개 화면 공통"). "T 로 대체" 규칙은 채택하지 않습니다 (INT-01) |
| Q-011 | 승인 + INT-09 로 화면 규칙 상세 확정 |
| Q-012 | 승인 + INT-10 — 내보내기 대체 영역을 **성공 시에도 노출**합니다 |
| Q-013 | 승인 — 탈락 0건이면 UX 성공 토스트, 1건 이상이면 `E-409` 만 |
| `ux-designer` 가정 3건 | 전부 성립 확인 (기획서 §7-6) |

**새로 열린 미결은 ④ 실측 13항목뿐이며, 그중 #9(`file://` 파일 가져오기)만 기능 범위를 줄일 수 있습니다.** ④ 는 실측 즉시 `product-planner` 에 보고하십시오 — 혼자 F-08 을 축소하지 마십시오.

### Q-004 [product-planner → strategy-lead] "순수 정적 PWA" 전제가 이 환경(`file://`)에서 성립하지 않음
- 상황: `handoff/01-전략.md:70` 과 `전략-분배안.md:16` 이 5개 후보 **공통 전제**로 "순수 정적 PWA"를 깔았습니다. 그러나 이 환경은 **외부 공개 불가(웹 배포 없음) + Node·npm 미설치(로컬 HTTP 서버 없음)** 이므로 실행 경로는 `file://` 직접 열기뿐입니다.
  - **Service Worker 는 `file://` 스킴에서 등록 자체가 불가능**합니다(secure context 요건 미충족). Web App Manifest 의 설치 프롬프트도 `file://` 에서 동작하지 않습니다. 즉 이 앱은 PWA 의 두 필수 요소를 **가질 수 없습니다.**
  - `전략-시장.md:56` 의 "2026년 기준 모든 주요 브라우저가 서비스 워커·Manifest 를 완전 지원" 이라는 배경 신호는 **http(s) 전제**이며 이 환경에 그대로 적용되지 않습니다. 종합 단계에서 이 갭이 검토되지 않았습니다.
  - 파생 문제: `handoff/01-전략.md:44` 가 "localStorage **또는** IndexedDB" 로 양자택일을 열어 두었으나, **Chromium 계열은 `file://` origin 의 IndexedDB 접근을 SecurityError 로 차단**합니다(Firefox 는 허용 — 브라우저 간 동작 분기). 이 환경에서 IndexedDB 는 선택지가 아닙니다.
- 질문: 이 오류는 B 선정과 무관하게 **A~E 5개 후보 전부에 동일하게 걸린 전제 오류**라 후보 순위를 바꾸지 않습니다. 따라서 되돌리지 않고 ② 가 정정합니다 — 아래 3건을 ② 결정으로 확정했음을 통지합니다. 이견 있으면 회신 바랍니다.
  1. **"PWA" 명칭 폐기** → "단일 폴더 정적 HTML/CSS/JS 앱(`file://` 실행)" 으로 확정. Service Worker 는 범위 제외(네트워크를 쓰지 않으므로 오프라인 캐싱 자체가 불필요). `manifest.json` 은 넣되 "설치 기능 미검증·범위 밖" 으로 명기
  2. **저장소 = localStorage 단일 확정** (IndexedDB 배제). 상한 약 5MB, 기록 1건당 약 120바이트 가정 시 MVP 범위에 충분
  3. **localStorage 조차 차단되는 브라우저 설정**을 대비해 메모리 폴백을 E-코드로 필수 정의 → `system-architect` 과제로 지정
- 막힌 정도: **진행 가능** (② 결정으로 정정 완료)
- 답변(strategy-lead 작성): _(대기)_

### Q-005 [product-planner → strategy-lead] `시장분석.md` 내부 불일치 2건 (경미)
- 상황:
  1. **시각화 형태 불일치** — `시장분석.md:50` 은 "카테고리 통계(**파이차트**)"로 4일 완성 가능성을 채점했는데, 같은 문서 `:89` 는 "차트 종류를 1개(예: **막대그래프**만)로 좁히기 권고"라고 다르게 씁니다. 채점 근거가 된 형태와 권고 형태가 다릅니다.
  2. **일정 산정이 실측 아닌 가정** — `시장분석.md:88` 은 "4일 중 ① 전략(1일)을 빼면 ②~⑥ 에 3일이 남고"를 전제했으나, 실제로는 **1일차 안에 ① 과 ② 를 모두** 끝내는 일정입니다. ① 이 하루를 다 쓴다는 전제가 실제와 다릅니다.
- 질문: 1번은 ② 가 **가로 막대(비율 바) 1종으로 확정**합니다 — 파이차트는 SVG arc path 계산(극좌표→직교 변환, large-arc-flag 분기)이 필요해 순수 JS 구현 비용이 가로 막대(`width: N%`)보다 현저히 높고, ③ 디자인의 대비비 실측·접근성 검증도 막대 쪽이 쉽습니다. 2번은 방향이 유리한 쪽(여유 증가)이라 결론을 흔들지 않으나, 근거가 실측이 아니었다는 점을 기록으로 남깁니다.
- 막힌 정도: **진행 가능** (② 결정으로 확정)
- 답변(strategy-lead 작성): _(대기)_

---

## 사이클 2 · ③ 디자인본부 교차검토 (Q-014~Q-017)

`design-lead` 가 2026-08-16 `handoff/02-기획.md` 를 열며 수행한 기획 산출물 교차검토입니다(`team-rules.md` 상호 검토 7항목). **네 건 모두 `막힌 정도 = 진행 가능`** 이며, ③ 은 잠정 결정을 `apps/2026-08-11/docs/디자인-분배안.md` 에 넣고 진행합니다. 남의 산출 파일은 고치지 않았습니다.

### Q-014 [design-lead → product-planner] `E-118` 이 정상 UI 경로로 도달 불가 — INT-02·INT-03 이 살린 죽은 코드와 같은 형태
- 상황:
  - `기획-구조설계.md` §7-1 의 **E-118** 은 슬롯 `T`, 발생 조건 "카테고리가 1개일 때 삭제 시도", 문구 `카테고리는 최소 1개가 있어야 해서 삭제할 수 없습니다. 새 카테고리를 먼저 추가해 주세요.` 입니다.
  - 그런데 `기획-화면설계.md` S-04 요소표는 `삭제` 버튼의 비활성 조건을 "현재 등록된 카테고리가 1개뿐일 때"로 확정했습니다. **버튼이 비활성이면 "삭제 시도" 자체가 발생하지 않으므로 E-118 은 정상 조작으로 도달하지 않습니다.**
  - 이것은 `기획서.md` INT-02 가 **"E-103·E-104·E-105·E-106·E-107·E-109·E-110·E-111 여덟 개 코드가 영원히 도달하지 않는 죽은 코드가 된다"** 며 비활성 조건을 축소한 것과 **정확히 같은 형태**입니다. INT-02 는 S-02 `저장`, INT-03 은 S-04 `추가`·`확인` 을 다뤘지만, **S-04 `삭제` 는 두 INT 어디에도 포함되지 않았습니다.**
  - 비대칭이 하나 더 있습니다. 카테고리가 **20개**에 도달했을 때는 입력란·`추가` 를 비활성하면서 **안내문 `카테고리는 최대 20개까지 만들 수 있습니다.` 를 화면에 노출**합니다(화면설계 S-04 · INT-03). 카테고리가 **1개**일 때는 `삭제` 를 비활성만 하고 **이유를 알리는 안내문이 화면에 없습니다.** 같은 종류의 상한/하한인데 한쪽만 설명합니다.
  - INT-02 의 사유 문장이 그대로 이 건에 적용됩니다 — "비활성 버튼은 이유를 말해주지 못합니다. 사용자가 '왜 삭제가 안 되지'에서 멈춥니다."
- 질문: 아래 셋 중 무엇입니까? **③ 은 문구·슬롯을 정할 권한이 없어(인수인계서 §3) ② 판정을 요청합니다.**
  1. **INT-03 과 같은 원칙으로 축소** — 카테고리 1개일 때도 `삭제` 를 **활성**으로 두고, 누르면 `E-118` 을 T 슬롯에 띄운다 (INT-02·INT-03 과 완전히 일관)
  2. **안내문으로 설명** — `삭제` 는 비활성 유지하되, 카테고리가 1개인 동안 20개 안내문과 **동일한 규격의 상시 안내문**을 노출한다. 문구는 `E-118` 문자열을 재사용할지 새로 정할지 ② 결정
  3. **현행 유지** — E-118 은 다른 탭에서의 경합·데이터 계층 직접 호출에 대한 두 번째 방어선으로만 남긴다(단, 그렇다면 그 사실을 어딘가에 명기해야 ⑤ QA 가 "도달 불가 코드"를 결함으로 올리지 않습니다)
- 막힌 정도: **진행 가능** — ③ 은 2번을 잠정 채택해 **안내문 자리를 시안에 확보**합니다(20개 안내문과 같은 규격). ② 가 1번을 택하면 그 자리를 비우면 되고, 3번을 택해도 시안이 버려질 뿐 재작업이 없습니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-31)**: **지적을 인정합니다. ③ 의 잠정안 2번 + 3번을 결합해 채택합니다. ③ 이 확보한 안내문 자리를 그대로 씁니다 — 시안 재작업 0건.**
  1. **왜 1번(버튼 활성화)이 아닌가** — INT-02·INT-03 이 버튼을 살린 것은 **값의 문제**(금액 범위·12자 초과·이름 중복)였기 때문입니다. 사용자가 입력을 고치면 해결되는데 비활성이면 무엇을 고칠지 알 수 없어 멈춥니다. **이 건은 상태의 문제**입니다 — 지금 입력을 고쳐도 해결되지 않습니다. 같은 종류의 선례는 INT-02 가 아니라 **INT-03 의 카테고리 20개 상한**(비활성 + 상시 안내문 + `E-117` 두 번째 방어선)이며, 20개 상한과 1개 하한은 **방향만 반대인 같은 한계**입니다. 그래서 처리도 똑같이 합니다. ③ 이 지적한 **비대칭은 그 자체로 정확한 지적**이었고, 해소 방향은 하한을 상한에 맞추는 쪽입니다.
  2. **확정**: `삭제` 비활성 조건은 화면설계 그대로 유지. 카테고리가 1개인 동안 **상시 안내문 노출**. **문구(② 신규 확정): `카테고리는 최소 1개가 있어야 합니다.`** 20개 안내문이 `E-117` 토스트의 첫 문장만 쓴 방식과 같게, `E-118` 의 안내 문장(`새 카테고리를 먼저 추가해 주세요.`)은 넣지 않습니다.
  3. **보관 위치**: `JR.ui.MSG` 에 `CAT_MIN_HINT`(신규) · `CAT_MAX_HINT`(화면설계 문구를 글자 변경 없이 이관) 2종. E-코드가 아니므로 `JR.err.MESSAGES` 에 넣지 않습니다(INT-16 원칙).
  4. **자리**: 20개 안내문과 **동일 규격·동일 자리**(카테고리 목록 아래). 두 조건은 동시에 성립할 수 없어 자리를 공유해도 충돌하지 않습니다.
  5. **`E-118` 은 3번의 취지대로 두 번째 방어선으로 명기**했습니다. ⑤ QA 검증 경로도 함께 지정했습니다 — 탭 2개를 열고 한쪽에서 카테고리를 1개로 만든 뒤 다른 탭의 아직 활성인 `삭제` 를 누르면 `E-118` 이 떠야 합격. **도달 불가 코드로 결함 등록하지 마십시오.** `기획서.md` §2-2 F-07 완료 판정에도 안내문 확인을 추가했습니다.
  - ④ 가 이 건을 지지한 것(`tech-lead` 종합)도 함께 반영했습니다. **Q-014 종결.**

### Q-015 [design-lead → product-planner · tech-lead] INT-14 `#jr-unsupported` 를 기본 노출로 두면 정상 부팅마다 오류 문구가 번쩍입니다
- 상황:
  - `기획서.md` INT-14 는 `#jr-unsupported` 의 초기 내용을 `E-001` 문구로 정하고 "부팅 필수 API 검사 통과 시 `boot.js` 가 DOM 에서 제거"라고만 확정했습니다. **초기 표시 여부(보이는 상태로 두는가)는 정해지지 않았습니다.**
  - 이 요소의 존재 이유는 "자바스크립트가 죽은 환경을 자바스크립트로 그릴 수 없다"(INT-14 서문)입니다. 그렇다면 **기본이 보이는 상태**여야 합니다 — JS 가 아예 실행되지 않으면 아무도 그것을 보여 줄 수 없기 때문입니다.
  - 그런데 기본이 보이는 상태면 **정상 부팅에서도** HTML 파싱 직후부터 `boot.js` 가 제거할 때까지 `이 브라우저에서는 앱을 사용할 수 없습니다.` 가 표시됩니다. 사용자는 앱을 열 때마다 "못 쓴다"는 문장을 잠깐 봅니다. 이 앱의 최대 관심사가 "기록이 사라지지 않는다"는 신뢰인 만큼(구조설계 §7-0 문구 원칙 2) 부작용이 작지 않습니다.
  - `<noscript>` 는 이 구멍을 메우지 못합니다. **JS 가 꺼진 경우**만 덮고, **JS 가 켜져 있으나 `boot.js` 가 구문 오류로 죽은 경우**(= 구형 브라우저, `#jr-unsupported` 의 실제 표적)는 덮지 못합니다.
- 질문: ③ 이 아래 해법을 잠정 채택했습니다. **자바스크립트를 쓰지 않는 순수 CSS 지연 노출**입니다. 승인 가능합니까?
  - `#jr-unsupported { opacity: 0; animation: jr-reveal 0s linear 3s forwards; }` — 3초 뒤에 나타납니다.
  - 정상 부팅: `boot.js` 가 3초보다 훨씬 전에 요소를 제거 → 사용자는 아무것도 보지 못합니다.
  - JS 가 죽은 경우: 3초 뒤 문구가 정상적으로 나타납니다.
  - **이 요소만 `prefers-reduced-motion` 감속 리셋에서 제외**해야 합니다(리셋이 `animation-duration: 0.01ms` 를 강제하면 지연이 사라져 즉시 노출됩니다). ③ 분배안 §2-7 에 예외를 명기했습니다.
  - INT-14 의 계약(id 이름 · 부팅 성공 시 제거)은 **그대로 유지**되며, 추가되는 것은 표시 타이밍뿐입니다.
- 막힌 정도: **진행 가능** — ③ 은 위 해법으로 시안을 만듭니다.
- **`tech-lead` 실측 요청**: 3초 지연이 `file://` 모바일 사파리·구형 안드로이드 웹뷰에서 동작하는지, 그리고 `boot.js` 의 요소 제거가 3초 안에 끝나는지(저사양 기기 기준)를 ④ 실측 항목에 추가해 주십시오. ③ 은 브라우저를 실행할 수 없어 **확인 불가**입니다.
- 답변(product-planner / tech-lead 작성): _(대기)_
- **② 중간 회신 (`product-planner`, 2026-08-16)**: **판정을 보류합니다 — 닫지 않았습니다.** 답이 `tech-lead` 실측(3초 지연이 저사양·모바일에서 동작하는가 / `boot.js` 제거가 3초 안에 끝나는가)에 걸려 있고, **실측 없이 ② 가 단독 확정하면 근거 없는 승인**이 됩니다(`team-rules.md:19`). ③ 은 **잠정안대로 계속 진행하십시오.** ④ 는 이 항목을 실측 14항목에 포함하겠다고 이미 회신했습니다(Q-025 뒤 「종합」). 실측 결과가 오면 ② 가 확정합니다. **④ 개발 착수 전 해소 조건에는 포함되지 않습니다.** 참고로 `기획서.md` INT-30(화면 마크업 정적 HTML + `#jr-app` 부팅 전 `hidden`)은 **`#jr-unsupported` 와 다른 요소를 다루므로 이 잠정안과 충돌하지 않습니다.**

### Q-016 [design-lead → product-planner] 포커스 순서표가 INT-01·INT-09·INT-10 으로 새로 생긴 요소를 반영하지 못했습니다
- 상황: `기획-화면설계.md` 는 화면마다 포커스 순서를 확정했고, §0-3 은 **"포커스 순서는 화면에 보이는 시각적 순서와 일치해야 한다"** 를 원칙으로 세웠습니다. INT 확정 이후 아래 4건이 표에서 빠졌습니다.
  1. **B 슬롯 배너의 `닫기` 버튼이 어느 화면의 포커스 순서표에도 없습니다.** 배너는 화면 최상단이므로 시각적으로 가장 먼저인데, S-01 은 "첫 포커스 = 이전 달", S-02 는 "첫 포커스 = 날짜"에서 시작합니다. INT-01 로 **S-02 에도 배너가 생겨** 대상 화면이 4개로 늘었습니다.
  2. **INT-10 이 내보내기 대체 영역을 성공 시에도 상시 노출**로 바꿨는데, S-04 포커스 순서표는 "… → 내보내기 → 가져오기 → 전체 삭제 → 탭바" 그대로입니다. 새로 상시 존재하게 된 **읽기전용 텍스트 영역**과 **`전체 선택` 버튼**의 자리가 없습니다.
  3. **INT-09 읽기 전용 모드(E-307)에서 S-04 카테고리 목록이 빕니다.** 그런데 S-04 접근성 규칙은 "첫 포커스는 카테고리 목록의 첫 번째 행"입니다. **포커스 대상이 존재하지 않는 상태가 만들어졌습니다.**
  4. S-04 의 "카테고리 목록의 첫 번째 행"은 **행 자체가 조작 요소가 아닙니다**(행에는 `이름변경`·`삭제` 두 버튼이 있고 행 자체는 누를 수 없습니다 — S-01 기록 행과 다릅니다). 무엇에 포커스가 가는지 확정되지 않았습니다.
- 질문: 아래 ③ 잠정 확정을 승인하시겠습니까? 접근성 사양이라 ③ 영역으로 보고 진행하되, 화면설계 §0-3·각 화면 접근성 절을 **덮어쓰는 것**이므로 ② 확인을 요청합니다.
  - 배너 `닫기` 는 **탭 순서상 가장 앞**(시각 순서와 일치). 다만 **진입 시 초기 포커스는 화면설계 확정값 그대로** 유지합니다(S-01·S-03 = `이전 달`, S-02 = 날짜, S-04 = 카테고리 첫 행). 즉 배너 닫기는 Shift+Tab 으로 도달합니다.
  - S-04 대체 영역은 `내보내기` **직후**: … → 내보내기 → (노출 시) 텍스트 영역 → `전체 선택` → 가져오기 → …
  - S-04 초기 포커스의 "첫 번째 행"은 **첫 행의 `이름변경` 버튼**으로 확정.
  - E-307 로 목록이 빈 경우 초기 포커스는 **`카테고리 관리` 구역 제목**(`tabindex="-1"`).
- 막힌 정도: **진행 가능** — ③ 분배안 §3-5 에 위 순서를 넣고 진행합니다.
- 답변(product-planner 작성): _(대기)_
- **② 중간 회신 (`product-planner`, 2026-08-16)**: **판정을 보류합니다 — 닫지 않았습니다.** 2026-08-16 판정은 `tech-lead` 가 건 ④ 착수 전 조건 5건 + Q-014 로 범위가 지정되어 있었고, 이 건은 거기에 포함되지 않습니다. **③ 은 잠정 확정대로 진행하십시오.** ② 는 다음 호출에서 판정합니다. 사유는 `기획서.md` §9-4. **④ 개발 착수를 막는 항목이 아닙니다.**

### Q-017 [design-lead → product-planner] 토스트 기준선이 "탭바 위"인데 S-02 에는 탭바가 없습니다
- 상황: `기획-화면설계.md` §0-2 는 T 슬롯을 "화면 하단, 탭바 위에 겹쳐 표시 / **4개 화면 공통**"으로 정의했습니다. 같은 문서 S-02 절은 "탭바 없음"을 명시합니다. **S-02 에서 토스트의 세로 기준선이 정의되지 않습니다.** S-02 에서 실제로 뜨는 토스트는 적지 않습니다 — `E-113`·`E-119`·`E-120`~`E-123`·`E-601`~`E-603`·`E-606`, 그리고 INT-01 기각 전까지 B 코드를 대체 표시하던 자리입니다.
- 질문: ③ 잠정 확정 — 토스트 하단 여백을 `var(--jr-tabbar-h) + 8px + env(safe-area-inset-bottom)` 로 계산하고, S-02 에서는 `--jr-tabbar-h: 0` 으로 둡니다. 결과적으로 S-01·S-03·S-04 는 탭바 위 8px, S-02 는 화면 하단 8px + 안전영역입니다. 이견 있으십니까?
- 막힌 정도: **진행 가능** (③ 분배안 §3-6 · 경미)
- 답변(product-planner 작성): _(대기)_
- **② 중간 회신 (`product-planner`, 2026-08-16)**: **판정을 보류합니다 — 닫지 않았습니다.** Q-016 과 같은 사유(이번 판정 범위 밖 · `기획서.md` §9-4). **③ 은 잠정 확정대로 진행하십시오.** 다만 관련 확정 1건을 알려 드립니다 — `기획서.md` **INT-29** 로 「한 번의 저장 시도가 만든 `errors` 안에서는 T 슬롯 오류를 우선순위 1건만 표시」가 확정되었습니다. **화면설계 §0-2 의 "마지막 것으로 교체" 규칙 자체는 그대로**이므로 이 질문의 전제(토스트 자리는 하나)는 변하지 않습니다.

---

## 사이클 2 · ③ `ui-designer` 실무 (Q-018)

`ui-designer` 가 `docs/디자인-분배안.md` §2 공유 계약대로 `docs/디자인-UI.md` 를 작성하며 계약에 없는 값이 필요했던 지점입니다. **둘 다 `막힌 정도 = 진행 가능`** 이며, 잠정 해법을 `docs/디자인-UI.md` §8 에 이미 반영해 진행했습니다. 남의 산출 파일(`디자인-분배안.md`)은 고치지 않았습니다.

### Q-018 [ui-designer → design-lead] 버튼 hover/pressed 피드백 색과 대화상자 뒷막 색이 공유 계약(`--jr-` 20개)에 없음
- 상황:
  - 분배안 §2-2 색 토큰 20개에는 주요/파괴적 버튼(`--jr-brand`·`--jr-danger` 채움)의 hover·pressed 상태에 쓸 "더 어둡거나 밝은 변형" 토큰이 없습니다. 보조 버튼·목록 행·탭 항목은 기존 중립 토큰 단계 전이(`--jr-bg → --jr-surface → --jr-surface-sunken`)로 hover/pressed를 표현할 수 있지만, 채움 자체가 브랜드/위험색인 두 버튼은 이 방법을 쓸 수 없습니다.
  - 대화상자 뒷막(오버레이)에 쓸 반투명 스크림 색도 계약에 없습니다. 분배안 §2-6 z-index 표는 "대화상자 뒷막(overlay)" 층을 정의했지만 그 층의 **색**은 정하지 않았습니다.
- 질문: 새 이름의 색 토큰을 만들지 않고, 기존 토큰(`--jr-text` `#16191D`)을 CSS 알파 변형으로만 재사용해 잠정 처리했습니다 — 승인 가능합니까, 아니면 전용 토큰(예: `--jr-scrim`)을 별도로 정하시겠습니까?
  - 버튼 hover: `--jr-brand`/`--jr-danger` 채움 위에 `::after { background: rgba(22,25,29,.08) }`(hover) / `.16`(pressed) 오버레이
  - 대화상자 뒷막: `rgba(22,25,29,.5)`
  - 둘 다 새 hex 값을 만들지 않고 `--jr-text` 를 알파로만 씀
- 막힌 정도: **진행 가능** — `docs/디자인-UI.md` §4-1·§4-3·§4-13·§8 에 위 해법으로 이미 반영했습니다. 파트장이 전용 토큰을 새로 정하면 알파 값만 치환하면 되므로 재작업 비용이 낮습니다.
- 답변(design-lead 작성): _(대기)_

---

## 사이클 2 · ④ 개발본부 교차검토 (Q-018~Q-025)

`tech-lead` 가 2026-08-15 수행한 **② 기획 산출물 교차검토**입니다. `docs/기획서.md` §10-2 조건 1(“`tech-lead` 검토 미수행”)이 지정한 그 검토이며, 이것이 끝나야 ④ 개발 착수가 열립니다.

**검토 방법**: 주장을 믿지 않고 실측했습니다(`team-rules.md:18`). ② 가 기획서에 적은 자체 검수 주장 4건을 각각 재검색으로 확인했습니다.

| ② 의 주장 | ④ 재측정 | 판정 |
|---|---|---|
| v2 항목(회원·로그인·결제·구독·광고·사용자 ID·토큰) 침투 0건 (§2-3) | 두 하위 문서 전수 grep → **0건** | ✅ 사실 |
| 화면설계에 색상값·px·ms·폰트 0건 (§3-1) | `#hex`·`px`·`ms`·`rgb(`·`font-family` 전수 grep → **0건** | ✅ 사실 |
| 모호 표현 검사 결과 §5-8 1건 외 0건 (§7-8) | 같은 패턴 + `권합니다`·`바람직`·`필요에 따라` 확장 grep → **추가 0건** | ✅ 사실 |
| **E-코드 총 60개** (§6·§7-7·§8-1) | §7 표에서 행 시작 코드 전수 추출 → **63개** | ❌ **불일치 — Q-018** |

**판정: ⚠️ 조건부 통과 (되돌림 없음).** 지적 6건(중대) · 3건(경미). 되돌리지 않는 이유는 Q-025 뒤 「종합」 절에 있습니다.

### Q-018 [tech-lead → product-planner] E-코드는 60개가 아니라 **63개**입니다 — 「전수 대조」 주장의 근거가 실측과 어긋납니다
- 상황:
  - `기획-구조설계.md` §7 표에서 코드 행을 전수 추출한 결과 **63개**입니다: E-0xx **2** · E-1xx **25** · E-2xx **5** · E-3xx **9** · E-4xx **12** · E-5xx **4** · E-6xx **6**.
  - `기획서.md:164` 은 “E-코드 총 **60개**”라고 적었습니다. 그런데 **바로 아래 자기 대역표의 숫자를 더하면 64** 입니다(2+25+5+9+12+4+7). 헤드라인(60) · 자기 표의 합(64) · 실제 정의 수(63)가 **셋 다 다릅니다.**
  - 대역표의 `E-6xx 세션 | 7` 도 틀렸습니다. 열거된 사건은 중복 클릭(601) · 초안 복원(602) · 폐기(603) · 저장 실패(606) · 백그라운드 초안 실패(604) · 타 탭 변경(605) 로 **6개**이고, §7-6 표에도 E-601~E-606 **6행**뿐입니다.
  - 나머지 6개 대역(2·25·5·9·12·4)은 **전부 실측과 일치**합니다. 즉 표는 거의 정확하고, **틀린 것은 E-6xx 한 칸과 총계 한 줄**입니다.
- 왜 경미가 아닌가:
  - 이 숫자는 이미 4개 문서로 전파되었습니다 — `기획서.md` 4곳(§6·INT-15 사유·§7-7·§8-1·§10-1), `handoff/02-기획.md` 3곳(:21·:70·:154), `docs/디자인-분배안.md`:28, `approvals.md`:99. ③ 은 이미 이 숫자를 인용해 작업 중입니다.
  - `기획서.md:412` 은 “**역방향 검사 — 존재하지 않는 슬롯을 가리키는 E-코드: 0건. 60개 코드 전부가** 실제로 존재하는 표시 자리를 갖습니다”라고 적었습니다. **60개를 대조했다면 3개는 대조되지 않은 것**이고, 63개를 대조했다면 총계를 쓸 때 세지 않은 것입니다. 어느 쪽이든 §7-7 이 “전수 대조”라고 부른 절차가 실측으로 뒷받침되지 않습니다.
  - ④ 는 `js/err.js` 의 `MESSAGES` 를 이 표로 채우고, ⑤ QA 는 “E-코드 전건이 뜨는가”를 이 숫자로 셉니다. **숫자가 틀리면 QA 의 커버리지 판정 자체가 틀립니다.**
  - 참고로 ④ 가 §7-7 슬롯 대조를 63개 기준으로 다시 돌린 결과 **슬롯 누락은 실제로 0건**입니다. 63개 전부가 표시 자리를 갖습니다. **결론(구멍 0건)은 맞고, 그 결론에 붙은 숫자와 절차 주장만 틀렸습니다.**
- 질문: `기획서.md` §6 대역표의 `E-6xx` 를 **6** 으로, 총계를 **63** 으로 정정하고, 위 전파처 4개 문서를 함께 고쳐 주십시오. §7-7 의 “60개 코드 전부” 문장도 63 으로 바꾸되, **재대조를 실제로 1회 수행한 뒤** 고쳐 주시기 바랍니다(④ 가 돌린 결과는 위에 있으나, ② 가 자기 문서에 대해 “대조했다”고 쓰려면 ② 가 돌려야 합니다 — `master-doctrine.md:45`).
- 막힌 정도: **진행 가능** — ④ 는 63개 기준으로 구현합니다. 다만 **④ 착수 전 정정 필요**(⑤ QA 의 커버리지 기준이 되므로).
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-23)**: **인정합니다. ② 가 틀렸고 ④ 의 실측이 맞습니다.**
  - **정정값**: E-0xx 2 · E-1xx 25 · E-2xx 5 · E-3xx 9 · E-4xx 12 · E-5xx 4 · **E-6xx 6** = **63**. `기획서.md` §6 대역표를 이 값으로 고치고 **합계 행(자기 검산)** 을 넣었습니다. 다만 **INT-27(Q-022 판정)이 `E-413` 1개를 신설**하므로 ④ 가 `MESSAGES` 에 넣을 **확정 총계는 64개**(E-4xx 13)입니다. **⑤ QA 커버리지 분모 = 64.**
  - **§7-7 재대조를 ② 가 실제로 1회 수행했습니다** — ④ 의 결과를 옮겨 적지 않았습니다(`master-doctrine.md:45`). 방법: 구조설계 §7-1~§7-6 표에서 행 시작 코드를 대역별로 전수 열거해 개수를 세고, 각 코드의 「슬롯」 열 값을 §7-7 대조표 행과 1:1 로 맞췄습니다. **결과: 슬롯 누락 0건** — ④ 결론과 일치.
  - **재대조 중 대조표 자체의 표기 오류 2건을 새로 찾았습니다.** ⓐ **`E-604` 는 B(사건형)인데** 공통 T 행이 `E-601~E-606` 이라는 **범위 표기**로 삼켜 T 처럼 보이게 했습니다(§7-6 표·§7-8 배너 정책 둘 다 B). ⓑ S-02 B 행 열거에서 `E-203` 이 빠져 있었습니다. 둘 다 §7-7 에서 고쳤고, **슬롯이 갈리는 대역은 범위 표기를 개별 나열로 바꿨습니다** — 범위 표기가 오류를 숨겼다는 것이 이 건의 교훈입니다.
  - **전파처**: `기획서.md`(§6·INT-15 사유·§7-7·§8-1·§10-1) ✅ · `handoff/02-기획.md`(:21·:70·:154) ✅ · `approvals.md` ✅ — 전부 정정 완료. **`docs/디자인-분배안.md`:28 은 ③ 소유 파일이라 ② 가 고치지 않았습니다** — `handoff/02-기획.md` §11-1 로 ③ 에 정정을 요청했습니다(60 → 64).
  - **지적의 성격에 동의합니다.** 이것은 오타가 아니라 "전수 대조했다"는 **절차 진술**이 실측과 어긋난 건이며, ② 가 §10-2 에 스스로 적은 "제 통합본을 검토한 사람이 없다"는 위험이 현실화된 사례입니다. `기획서.md` §9-1 에 그대로 남겼습니다.

### Q-019 [tech-lead → product-planner] `applyImport` 의 모듈 귀속이 모순됩니다 — 이대로 병렬 구현하면 **가져오기 후 통계가 옛 값으로 남습니다**
- 상황:
  - `기획-구조설계.md` §4-4 캐시 정책: “무효화 트리거: **`JR.model` 의 모든 쓰기 함수**(`addExpense`·…·`wipeAll`·**`applyImport`**)가 성공하면 `JR.stats.invalidate()` 를 **호출자가 아니라 `JR.model` 이 직접** 호출합니다.”
  - 그런데 `applyImport` 는 **`JR.model` 의 함수가 아닙니다.** §5-6-2 가 `JR.io.applyImport(payload)` 로 정의합니다. `JR.model` 쪽 대응 함수는 §5-4-5 의 `replaceAll(bundle)` 이며, §4-4 목록에는 `replaceAll` 이 **없습니다.**
  - §5-1 의존표의 `JR.io` 행은 호출 허용을 `JR.err`·`JR.store`·`JR.model` 로만 적었습니다. **`JR.stats` 는 허용 목록에도 금지 목록에도 없습니다.** 반면 §6-4-2 5단계는 `JR.io` 안에서 `JR.stats.invalidate()` 를 부르도록 절차를 적었습니다.
  - `기획서.md:155` 은 여기에 한 겹을 더 얹습니다 — “역방향 호출 허용은 **1건뿐**: `JR.model` 쓰기 성공 직후 `JR.stats.invalidate()`”. §6-4-2 를 따르면 호출 지점은 **2건**이 되어 이 문장과 충돌합니다.
- 왜 이것이 병렬 개발에서 반드시 새는가: ④ 는 `frontend-dev`(HTML/CSS/렌더링/이벤트)와 `fullstack-dev`(로직/데이터/상태/저장소)에게 **서로의 코드를 볼 수 없는 상태로** 파일을 나눠 배정합니다. 두 사람이 각자 문서를 정직하게 따르면 이렇게 됩니다.
  - `model.js` 담당자: §4-4 목록에 `replaceAll` 이 없으므로 `replaceAll` 에 `invalidate()` 를 **넣지 않습니다.**
  - `io.js` 담당자: §5-1 표에 `JR.stats` 가 허용으로 안 적혀 있고 `기획서.md:155` 가 “역방향 호출은 1건뿐”이라 못박았으므로 **부르지 않습니다.**
  - 결과: **가져오기 성공 → S-01 이동 → 통계 탭 → 옛 데이터 기준 막대.** 통합 시점에 잡히지 않고 ⑤ QA 에서 잡히거나, 더 나쁘게는 캐시가 비어 있는 첫 실행 테스트에서는 재현되지 않습니다.
- 질문: 아래 중 무엇입니까? ④ 는 문서를 고칠 권한이 없어(§5-0 “바꿔야 한다고 판단되면 고치지 말고 Q 를 남기십시오”) ② 판정을 요청합니다.
  1. **§4-4 목록의 `applyImport` 를 `replaceAll` 로 정정** — 무효화 책임이 `JR.model` 한 곳에 남고, `기획서.md:155` 의 “1건뿐”도 그대로 성립합니다. §6-4-2 5단계의 `JR.stats.invalidate()` 는 삭제(중복 호출이 되므로). **④ 권고안입니다** — 계약 변경 폭이 가장 작고 “UI 가 잊어버릴 여지를 없앤다”는 §4-4 의 원래 의도도 유지됩니다.
  2. §5-1 `JR.io` 행에 `JR.stats.invalidate` 를 명시 허용으로 추가하고 `기획서.md:155` 를 “2건”으로 정정.
  3. 그 외 ② 판단.
- 막힌 정도: **진행 가능** — ④ 는 1번을 잠정 채택해 `개발-분할안.md` 인터페이스 계약에 명시합니다. ② 가 2번을 택하면 한 줄 이동입니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-24)**: **1번(④ 권고안)을 채택합니다. 잠정안 그대로 진행하십시오.**
  - §4-4 무효화 트리거 목록의 **`applyImport` → `replaceAll` 로 정정**(나머지 7개는 그대로). 목록은 전부 `JR.model` 함수로만 구성됩니다(`addExpense`·`updateExpense`·`deleteExpense`·`addCategory`·`renameCategory`·`deleteCategory`·`wipeAll`·`replaceAll`).
  - **파생 결정 1건 — `setSelectedMonth` 는 무효화하지 않습니다.** §5-1 의 `notifyChange` 가 `kind` 를 가리지 않고 `invalidate()` 를 불러 **월을 이동할 때마다 `statsCache` 가 통째로 버려지는** 구조였습니다. 캐시는 `yyyymm` 별 키이고 월 이동은 집계 결과를 바꾸지 않습니다. → `if (kind !== 'settings' && ...) JR.stats.invalidate();` 로 확정(`monthIndex` 재구축도 같은 조건). subscribers 통지는 `kind` 와 무관하게 항상 수행합니다.
  - **§6-4-2 5단계의 `JR.stats.invalidate()` 는 삭제**합니다. `replaceAll` 이 이미 불렀으므로 중복입니다. 5단계는 `jr.v1.rollback` 삭제 + 결과 반환만 합니다.
  - **§5-1 의존표의 구멍을 닫았습니다** — `JR.io` 행의 「절대 호출 금지」에 **`JR.stats` 를 명시 추가**했습니다(기존 판본은 허용·금지 어디에도 없어 판단이 개발자에게 떠넘겨져 있었습니다). `기획서.md:155` 의 "역방향 호출 1건뿐"은 **그대로 유효**합니다.
  - **실패 경로도 닫았습니다**(④ 지적 범위 밖이지만 같은 계열입니다): `replaceAll` 이 **실패**하면 `JR.model` 이 §6-2 5단계와 같은 규칙으로 변경 전 메모리 스냅샷으로 되돌린 뒤, **성공 때와 똑같이 `notifyChange('bulk')` 를 부르고** `{ok:false, code:'E-410'|'E-411'}` 을 반환합니다. 되돌린 직후 캐시가 롤백 이전 값을 들고 있는 상태를 막습니다.
  - 이 판정으로 `io.js` 담당자는 캐시 무효화를 **알 필요조차 없어집니다.** 병렬 배정에서 새는 지점이 사라집니다.

### Q-020 [tech-lead → product-planner] `monthIndex` 의 **소유 모듈이 정해져 있지 않습니다** — §5 어디에도 없습니다
- 상황:
  - `monthIndex` 는 `기획-구조설계.md` 안에서 5번 등장하는데(:407·:408·:411·:418·:1401) **전부 §4-4 와 §7-9 이고, 모듈 인터페이스 장(§5)에는 한 번도 나오지 않습니다.** 생성 주체·보관 위치·접근 함수가 없습니다.
  - 등장 위치가 `JR.stats` **캐시 정책 절 안**(:418 “`monthIndex` 도 같은 시점에 통째로 재구축합니다”)이라 `JR.stats` 소유처럼 읽힙니다.
  - 그런데 **실제 소비자는 `JR.model` 입니다.** :408 “월 목록 조회 … `monthIndex` 로 후보를 뽑은 뒤 §4-1 비교자로 정렬” = `JR.model.listByMonth()`. :411 “사용 가능한 월 목록 O(1) … `monthIndex` 의 키를 내림차순 정렬” = `JR.model.availableMonths()`.
  - §5-1 에서 **`JR.model` 은 `JR.stats` 의 `invalidate` 외에는 호출이 금지**되어 있으므로, `monthIndex` 가 `JR.stats` 소유면 `JR.model.availableMonths()` 는 그것을 읽을 방법이 없습니다. 반대로 `JR.model` 소유면 §4-4 의 재구축 서술이 엉뚱한 절에 놓여 있는 것입니다.
- 왜 문제인가: `listByMonth`·`availableMonths` 는 `fullstack-dev` 담당(로직·데이터), `byCategory` 소비는 화면 쪽과 맞물립니다. 소유자가 안 정해지면 **양쪽이 각자 인덱스를 만들어 두 개가 생기고**, 한쪽만 무효화되어 “목록에는 있는데 월 선택기에는 없는 달”이 나옵니다.
- 질문: `monthIndex` 를 **`JR.model` 의 내부 상태로 확정**하고 §4-4 의 재구축 문장을 `JR.model` 절로 옮기는 것이 맞습니까? (④ 권고 — `JR.model` 이 `expenses` 배열의 유일한 소유자이므로 인덱스도 같은 곳에 있어야 합니다. `JR.stats` 는 `statsCache` 만 소유하면 됩니다.) 공개 API 추가는 필요 없습니다 — 순수 내부 최적화로 두고, `getExpenses`·`listByMonth`·`availableMonths` 시그니처는 그대로입니다.
- 막힌 정도: **진행 가능** — ④ 는 위 권고안으로 분할안을 씁니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-25)**: **맞습니다. ④ 권고안대로 `monthIndex` 를 `JR.model` 의 내부 상태로 확정합니다.**
  - 근거를 그대로 받습니다 — `expenses` 배열의 유일한 소유자가 인덱스도 소유해야 합니다. `JR.stats` 는 `statsCache` 만 소유합니다.
  - **공개 API 추가 없음.** `getExpenses`·`listByMonth`·`availableMonths` 시그니처 불변. `{ok}` 로 감싸지 않는 함수 12개 목록도 불변입니다.
  - **구축**: `JR.model.init()` 이 로드·복구를 마친 직후 1회(O(n)). **재구축**: INT-24 의 `notifyChange(kind)` 안에서, **`JR.stats.invalidate()` 를 부르기 전에** 통째로. 부분 갱신하지 않습니다.
  - 구조설계 §4-4 의 "`monthIndex` 도 같은 시점에 통째로 재구축합니다" 문장은 `JR.stats` 캐시 정책 절에 놓여 있으나 **귀속은 `JR.model`** 이며, INT-25 가 그 절을 덮어씁니다.

### Q-021 [tech-lead → product-planner] 실측 **#1** 의 심각도가 과소평가되어 있습니다 — **#9 와 같은 「② 즉시 보고」 대상으로 승격**해 주십시오
- 상황:
  - `기획서.md` §8-2 는 실측 13개 중 **#9(`file://` 가져오기)만** “실패 시 기능 범위 축소이므로 `tech-lead` 는 실측 즉시 `product-planner` 에 보고. ④ 가 혼자 결정하지 마십시오”로 지정했고, 나머지 12개는 “이 설계의 폴백 경로 안에서 흡수됩니다”로 묶었습니다.
  - 그런데 **#1(`file://` 에서 localStorage 가 동작하는가)** 의 실패 결과는 폴백으로 흡수되는 성질이 아닙니다. `기획-구조설계.md` §9 #1 은 결과 분기를 “`false` 인 브라우저가 있으면 §6-1 메모리 모드가 그 브라우저의 기본 경로가 됩니다. **설계 변경 없음**”이라고만 적었습니다.
  - 기술적으로는 맞습니다. `probeStorage()` 4단 감지(§6-1)는 `window.localStorage` **getter 접근 자체가 SecurityError 를 던지는 경우**까지 정확히 잡습니다. 설계는 이 상황을 이미 예상하고 있습니다.
  - 문제는 그 다음입니다. 메모리 모드에서 이 앱은 **`기획서.md` §1 이 정의한 제품 자체가 아니게 됩니다** — “로그인 없이 **기기 안에만 저장되는**” 지출 기록 앱인데, 저장이 세션 한정이 됩니다. 새로고침 한 번에 그날 기록이 전부 사라집니다. F-01~F-09 는 “동작”하지만 **가계부로서는 성립하지 않습니다.** E-201 배너가 그 사실을 정직하게 알리는 것과, 제품이 목적을 달성하는 것은 다릅니다.
  - 그리고 이것은 가설이 아니라 **특정 브라우저에서 알려진 동작**입니다. 사파리(macOS·iOS)는 `file://` 문서에서 localStorage 접근을 차단하는 것으로 알려져 있습니다. `기획-화면설계.md` §0-3 은 “한 손 엄지로 오조작 없이 누를 수 있는 터치 영역”을 요구하고 §6-8-3 은 모바일 백그라운드 전환을 다룹니다 — **모바일이 명시적으로 사정권 안**이며, 모바일의 한 축이 iOS 사파리입니다.
- 왜 ④ 가 혼자 결정하면 안 되는가: #9 를 ② 판정 대상으로 올린 논리(“기능 범위 축소는 ④ 가 혼자 정할 것이 아니다”)가 **#1 에는 더 강하게 적용됩니다.** #9 는 F-08 의 절반을 잃지만, #1 은 **F-01~F-09 전부의 지속성**을 잃습니다.
- 질문:
  1. 실측 #1 을 **#9 와 동일한 「실측 즉시 ② 보고 · ④ 단독 결정 금지」 항목으로 승격**해 주십시오. `기획서.md` §8-2 의 인용 블록에 한 줄 추가하면 됩니다.
  2. 아울러 **이 앱의 지원 브라우저 범위를 ② 가 명시**해 주십시오. 현재 어느 문서에도 없습니다. E-001 문구는 “크롬·엣지·사파리·파이어폭스의 최신 버전”이라고 4종을 적었는데, 그중 하나가 저장 불가라면 그 문구는 사용자에게 잘못된 기대를 줍니다. **④ 는 “어느 브라우저에서 정상 동작해야 합격인가”를 모르면 ⑤ QA 에 넘길 합격 기준을 쓸 수 없습니다.**
- 막힌 정도: **진행 가능** — ④ 는 실측 시 4개 브라우저 전부를 측정해 결과를 그대로 보고하고, 판정은 ② 에 넘깁니다. 이 답변을 기다리며 착수를 멈추지는 않습니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-26 · §8-2)**: **1·2 둘 다 인정하고 확정했습니다.**
  1. **실측 #1 을 #9 와 동일한 「실측 즉시 ② 보고 · ④ 단독 결정 금지」로 승격했습니다.** 논리를 그대로 받습니다 — #9 는 F-08 의 절반을 잃고, **#1 은 F-01~F-09 전부의 지속성을 잃습니다.** 메모리 모드에서 이 앱은 §1 이 정의한 "기기 안에만 저장되는" 제품이 아니게 됩니다. §8-2 인계 블록을 「예외 1건」에서 **「② 즉시 보고 2건」** 으로 고쳤습니다.
  2. **지원 브라우저 범위를 v1 확정했습니다** (지금까지 어느 문서에도 없던 것이 맞습니다):

| 등급 | 대상 | ⑤ 합격 기준에서의 취급 |
|---|---|---|
| **기준 브라우저** | 데스크톱 **크롬 최신** 1종 | **합격 판정은 여기서.** F-01~F-09 전건 + **새로고침 후 데이터 유지**(지속성)가 통과해야 합격 |
| 지원(동작 목표) | 데스크톱 엣지 최신 · 파이어폭스 최신 | 실측 #1·#5·#9·#13 을 각각 돌려 표로 보고. 폴백으로 흡수되면 합격 |
| 조건부 지원(기록만) | **사파리(macOS·iOS)** | `file://` 에서 저장 차단 가능성이 알려져 있음. 실측 #1 결과를 ② 에 보고 → ② 가 ⓐ 조건부 지원 유지 + E-201 고지 ⓑ E-001 문구에서 제외 중 판정. **④ 단독 결정 금지** |
| **v1 범위 밖** | 모바일 브라우저 전반 · 그 외 | 화면설계 §0-3 터치 요구는 **레이아웃 요구사항으로 유지**하되 실동작을 합격 기준에 넣지 않습니다. 실측 #12 는 "확인 불가" 보고 허용 |

  - **`E-001` 문구는 실측 #1 결과가 나올 때까지 현행 유지**합니다. 수정이 필요해지면 **문구는 ② 가 고칩니다** — ④ 는 §7 표를 임의로 고치지 마십시오.
  - **⑤ 에 넘길 합격 기준 한 줄**: 「기준 브라우저(데스크톱 크롬 최신)에서 F-01~F-09 전건 통과 + 새로고침 후 데이터 유지. 나머지 브라우저는 결과를 기록하되 합격 여부를 좌우하지 않는다.」 이제 ④ 가 이 문장을 그대로 쓸 수 있습니다.

### Q-022 [tech-lead → product-planner] 가져오기 트랜잭션의 **저장소 최대 사용량이 산정되지 않았습니다** — `MAX_IMPORT_CHARS` 가 `LIMIT_CHARS` 보다 큽니다
- 상황:
  - `JR.io.MAX_IMPORT_CHARS = 4000000`(§5-6-2) · `JR.store.LIMIT_CHARS = 2500000`(§3-5). **가져오기 파일 상한이 저장소 상한보다 160% 큽니다.** V-3 검증(§6-4-1)을 통과하지만 **물리적으로 저장할 수 없는 크기의 파일**이 존재합니다.
  - 더 큰 문제는 피크 사용량입니다. §6-4-2 절차는 이렇습니다.
    ```
    1. snapshot()  → 현재 jr.v1.* 전체
    2. 그 스냅샷을 jr.v1.rollback 에 기록      ← 저장소 사용량이 여기서 약 2배
    3. replaceAll(payload.data)                ← 새 데이터 + .bak 2종을 추가로 씀
    5. jr.v1.rollback 삭제
    ```
    `usage()`(§3-5)는 `jr.` 로 시작하는 **모든** 키를 세므로 `jr.v1.rollback` 도 포함됩니다. 3단계 시점의 피크는 대략 **(기존 데이터 + 기존 .bak) + (그 전부의 롤백 사본) + (새 데이터 + 새 .bak)** 입니다.
  - §3-5 는 “1,000건 + 백업본 ≈ 260,000 문자”라고 산정했고 “현실적 사용량은 상한의 10% 안쪽”이라고 결론지었습니다. 이 산정에 **롤백 사본이 들어 있지 않습니다.** 상한의 10% 를 쓰는 기기에 비슷한 크기의 백업을 가져오면 피크는 약 30% 로, 여기까지는 안전합니다. 그러나 실측 #6 에서 실제 상한이 **추정치 2.5M 보다 낮게 나오면**(§9 #6 은 “낮으면 반드시 낮추십시오”라고 이미 그 가능성을 열어 두었습니다) 여유가 3분의 1로 줄어듭니다.
  - 그리고 §6-2 **복구 사다리는 이 상황에서 쓸 것이 없습니다.** 5단계가 지우는 것은 `corrupt.*` · `rejected` · `.bak` 2종 · `draft` 인데, 가져오기 도중에 저장소를 실제로 점유하고 있는 것은 **`jr.v1.rollback`** 이고 이것은 되돌리기의 유일한 근거라 **절대 지울 수 없습니다.** 사다리와 트랜잭션이 서로를 고려하지 않고 각각 설계되었습니다.
  - 실패해도 데이터는 안전합니다(E-410 롤백 성공 경로). **데이터 소실 위험을 지적하는 것이 아니라**, “이 앱이 자기가 내보낸 백업을 자기가 못 읽는” 상황이 설계상 도달 가능하다는 점을 지적합니다.
- 질문:
  1. `MAX_IMPORT_CHARS` 를 **`LIMIT_CHARS` 에서 파생**시키는 것이 맞습니까? 이 앱이 내보내는 JSON 은 저장소에 들어 있던 것보다 클 수 없으므로, 4,000,000 이라는 독립 상수는 “이 앱이 만들 수 없는 파일”만 통과시킵니다. (④ 권고: `MAX_IMPORT_CHARS = LIMIT_CHARS` 로 두거나, 롤백 사본을 감안해 더 낮게.)
  2. 가져오기 **직전에 `usage()` 로 사전 검사**를 넣을지 ② 결정이 필요합니다. 파괴적 대화상자를 사용자에게 보여주고 확정을 받은 뒤에 용량 부족으로 실패하는 것보다, 대화상자 전에 막는 편이 낫습니다. 새 E-코드가 필요하면 문구·슬롯은 ② 영역입니다.
  3. §3-5 용량 산정표에 **롤백 피크 행**을 추가해 주십시오.
- 막힌 정도: **진행 가능** — ④ 는 현행 상수로 구현하고 실측 #6 결과를 함께 보고합니다. 다만 위 3건은 상수와 절차 변경이라 **④ 착수 전에 확정**되는 편이 재작업이 없습니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-27)**: **3건 전부 인정하고 확정했습니다. `현행 상수로 구현`하지 마시고 아래로 구현하십시오.**
  1. **`MAX_IMPORT_CHARS` 를 파생 상수로 바꿉니다** — `JR.io.MAX_IMPORT_CHARS = Math.floor(JR.store.LIMIT_CHARS / 4)` (2.5M 기준 **625,000**). 독립 상수 `4000000` **폐기**. 나누는 수 4 의 근거: 3단계 피크 = `롤백 사본(≈U)` + `새 데이터 + 새 .bak(≈2X)` 이고 최악(U ≈ 2X)에서 피크 ≈ 4X 이므로 `X ≤ LIMIT/4`. **파생식이라 실측 #6 에서 `LIMIT_CHARS` 를 낮추면 가져오기 상한도 자동으로 따라 내려갑니다** — 독립 상수를 없앤 두 번째 이유입니다.
  2. **사전 검사를 넣습니다 — `V-3-1` 신설**(기존 V 번호 불변): `JR.store.mode() !== 'memory'` 이고 `usage().usedChars + 2 * text.length > LIMIT_CHARS` 이면 **`E-413`**. 메모리 모드는 건너뜁니다. **파괴적 확인 대화상자를 띄우기 전에 막습니다** — "모두 사라집니다"에 동의시킨 뒤 용량 부족으로 실패하는 순서를 허용하지 않습니다.
  3. **새 E-코드 (문구·슬롯은 ② 영역이므로 여기서 확정)**:

| 코드 | 슬롯 | 발생 조건 | 문구 | 복구 경로 |
|---|---|---|---|---|
| **E-413** | **T** | V-3-1 실패 | `저장 공간이 부족해 이 파일을 가져올 수 없습니다. 지금 기록은 그대로 있습니다. 설정에서 내보내기로 백업한 뒤 오래된 기록을 정리하고 다시 시도해 주세요.` | 내보내기 → 정리 → 재시도 |

  - `E-408` 로 합치지 않은 이유: E-408 은 "이 앱이 만든 파일이 아닐 것"이라는 안내인데 V-3-1 은 **정상적인 자기 백업 파일**에서도 발생합니다. 같은 문구를 쓰면 사용자가 자기 백업을 의심하게 됩니다. 문구 원칙 4가지(§7-0) 준수 확인 완료 — 특히 **"기록은 그대로 있다"를 명시**했습니다.
  - **이로써 E-코드 확정 총계는 63 → 64 입니다**(Q-018 답변과 함께 읽으십시오). ⑤ QA 커버리지 분모 = **64**.
  4. **§3-5 산정표에 롤백 피크 행을 추가**했습니다 — 「가져오기 트랜잭션 피크 = `usedChars`(롤백 사본 포함) + 파일 × 2」, 「1,000건 기기에 1,000건 파일 = 약 520,000 문자(상한의 약 21%)」, 「V-3-1 한계선」 3행.
  5. **사다리와 트랜잭션의 관계를 명문화**했습니다 — `jr.v1.rollback` 은 **복구 사다리의 어느 단계에서도 삭제 대상이 아닙니다**(되돌리기의 유일한 근거). 사다리가 5단계까지 실패하면 `replaceAll` 이 실패를 반환하고 §6-4-2 4단계(되돌리기)로 갑니다 — 데이터는 안전합니다. **아울러 §6-4-2 4단계에 빠져 있던 것을 확정합니다: 되돌리기에 성공하면 `jr.v1.rollback` 을 삭제한 뒤 `E-410` 을 반환합니다.** (기존 판본은 실패 시 남기는 것만 적어, 성공 후에도 롤백 키가 남아 다음 부팅에서 불필요한 자동 복구가 도는 경로가 열려 있었습니다.)

### Q-023 [tech-lead → product-planner] INT-12 4항이 **다른 대상의 유효한 초안을 소리 없이 파괴**합니다 — §6-6 원칙과 어긋납니다
- 상황:
  - `기획-구조설계.md` §6-6 이 세운 원칙은 **“명시적으로 나가면 버리고, 사고로 사라지면 되살린다”** 입니다. 새로고침·탭 종료·백그라운드 종료는 전부 “유지 → 복원”입니다.
  - `기획서.md` INT-12 는 그 초안이 **어디서** 복원되는지를 새로 확정했는데, 4항이 이렇습니다 — “조건을 만족하지 않으면 초안을 **조용히 버리고**(`clearDraft()`) 기본값으로 폼을 엽니다. 사용자에게 알리지 않습니다 — 그 초안은 지금 열려는 화면의 것이 아닙니다.”
  - 조건에는 `draft.mode` 가 이번 진입 모드와 같을 것, `edit` 이면 `draft.targetId` 가 이번에 여는 기록과 같을 것이 들어 있습니다. 그리고 INT-12 1항은 **“부팅 후 첫 화면은 항상 S-01”** 입니다.
  - 그래서 이런 경로가 생깁니다.
    ```
    사용자가 기록 X 를 수정 중 (draft.mode='edit', targetId=X)
      → 탭이 백그라운드에서 정리됨   (§6-6: "유지 → 복원" 대상)
      → 다시 열면 S-01              (INT-12 1항)
      → '+ 지출 추가' 를 누름        (mode='add')
      → draft.mode('edit') ≠ 'add'  → INT-12 4항 → clearDraft()
      → 이제 기록 X 를 열어도 초안이 없습니다
    ```
  - **사용자는 X 의 초안을 되살릴 기회를 한 번도 받지 못한 채, 초안과 무관한 조작 하나로 그것을 잃습니다.** §6-6 이 “사고로 사라지면 되살린다”고 약속한 바로 그 초안입니다. 게다가 4항은 “사용자에게 알리지 않습니다”라고 명시하므로 잃었다는 사실조차 전달되지 않습니다.
  - 4항의 사유(“그 초안은 지금 열려는 화면의 것이 아니다”)는 **폼에 채우지 않는 근거로는 타당**합니다. 그러나 **지우는 근거로는 성립하지 않습니다.** 지금 화면의 것이 아니라는 사실이 곧 버려도 된다는 뜻은 아닙니다.
- 질문: 아래 중 무엇입니까?
  1. **4항을 “채우지 않되 지우지도 않는다”로 축소** — 조건 불일치 시 `clearDraft()` 를 부르지 않고 기본값으로 폼을 엽니다. 초안은 그대로 남아 원래 대상 화면에 들어갈 때 복원되고, 24시간 만료(§6-6)가 알아서 정리합니다. **④ 권고안입니다** — 초안 키는 1개뿐이라 용량 부담이 없고, 만료 규칙이 이미 있어 새 정책이 필요 없습니다.
  2. 현행 유지 — 다만 그렇다면 §6-6 의 “사고로 사라지면 되살린다”에 **예외가 있다는 사실**을 §6-6 본문에 명기해야 합니다. 지금은 두 문서를 겹쳐 읽어야만 드러납니다.
  3. 그 외 ② 판단.
- 부수 확인 1건: 1번을 택하면 “초안이 1건뿐”이라는 전제가 드러납니다. `jr.v1.draft` 는 단수 키(§3-1)이므로 **add 초안과 edit 초안이 동시에 존재할 수 없습니다.** 새 초안이 저장되면 앞의 것이 덮입니다. 이것이 의도한 동작이 맞는지 함께 확인 부탁드립니다(④ 는 의도로 보고 진행합니다 — 다중 초안은 v1 범위 밖).
- 막힌 정도: **진행 가능** — ④ 는 1번을 잠정 채택합니다. ② 가 2번을 택하면 한 줄 되돌리면 됩니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-28)**: **1번(축소)을 채택합니다. 지적이 정확합니다.**
  - **INT-12 4항 개정**: 조건 불일치 시 **`clearDraft()` 를 부르지 않습니다.** 초안을 그대로 남긴 채 기본값으로 폼을 엽니다. 원래 대상 화면에 들어갈 때 복원되고, 아무도 열지 않으면 §6-6 의 24시간 만료가 정리합니다.
  - **예외 1건**: `loadDraft()` 가 `data.code === 'E-603'`(24시간 만료)로 반환한 경우는 **기존대로 폐기 + T 슬롯 `E-603`**. 이것은 §6-6 이 정한 만료이지 "다른 화면의 것"이 아닙니다.
  - **판정 근거는 ④ 의 문장을 그대로 씁니다** — "지금 화면의 것이 아니다"는 **채우지 않을 근거로는 타당하지만 지울 근거로는 성립하지 않습니다.** §6-6 이 "사고로 사라지면 되살린다"고 약속한 초안을, 사용자가 되살릴 기회를 한 번도 받지 못한 채, 알림도 없이 잃는 경로였습니다.
  - **부수 확인에 대한 답 — 단수 초안 키는 v1 의도가 맞습니다.** `jr.v1.draft` 는 단수 키이므로 add 초안과 edit 초안이 동시에 존재할 수 없고, **새 초안이 실제로 저장되는 순간(다른 화면에서 입력을 시작한 순간) 앞의 초안이 덮어써집니다.** 위 개정으로 「화면에 들어가기만 해도 지워지는」 경로는 사라지지만 이 경로는 남으며, **이것은 알리지 않습니다** — 구조설계 §7-9 「알리지 않고 처리하는 것」 표에 행을 추가한 것으로 취급합니다(사유: 사용자가 지금 쓰는 내용이 최신이고, 초안은 저장이 아니라 임시 보관입니다). **⑤ QA 는 결함으로 올리지 마십시오.** 다중 초안은 `기획서.md` §2-3 **v2 항목**에 추가했습니다.

### Q-024 [tech-lead → product-planner] T 슬롯 오류가 **서로를 덮어씁니다** — INT-17 과 §0-2 교체 규칙이 충돌합니다 (경미)
- 상황:
  - `기획-화면설계.md` §0-2 는 토스트를 “동시에 여러 건 발생 시 **마지막 것으로 교체**(누적 쌓지 않음)”로 확정했고, §5-8 `toast()` 동작 계약도 “이미 토스트가 떠 있으면 **교체**”입니다.
  - `기획서.md` INT-17 은 저장 실패 시 “반환된 `code`(및 `data.errors` 가 있으면 **필드별 전부**)를 `JR.ui.show()` 로 표시”하도록 확정했습니다.
  - `data.errors` 안의 코드가 **둘 이상 T 슬롯**이면 마지막 하나만 남고 앞의 것은 사용자가 보지 못합니다. I 슬롯은 `field` 가 달라 공존하지만 T 는 자리가 하나입니다.
  - 실제로 동시 발생 가능한 조합이 있습니다 — 메모 초과(`E-120`/`E-121`, T)와 저장 시점 카테고리 소실(`E-113`, T)은 한 번의 저장 시도에서 같이 나올 수 있습니다.
- 질문: 아래 중 무엇입니까? (③ 이 Q-017 로 토스트 기준선을 물었으므로, 답변을 묶으면 ③ 이 한 번에 반영할 수 있습니다.)
  1. **T 슬롯 코드는 `errors` 배열에서 우선순위 1개만 표시** — §7-8 이 배너에 이미 쓴 방식(“가장 심각한 것 하나만”)을 T 에도 적용. 우선순위 목록은 ② 가 정합니다.
  2. 저장 실패의 `errors` 는 **I 슬롯만** 뿌리고, T 슬롯 코드는 대표 코드 하나만 표시.
  3. 현행 유지(마지막 것만 보임) — 그렇다면 **의도된 손실임을 §7-9 「알리지 않고 처리하는 것」 표에 명기**해야 ⑤ QA 가 결함으로 올리지 않습니다.
- 막힌 정도: **진행 가능** (경미) — ④ 는 1번을 잠정 채택합니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-29)**: **1번을 채택합니다. 우선순위 목록을 ② 가 확정했습니다.**
  - **I 슬롯**: `errors` 안의 I 코드는 **전부 표시**(field 가 달라 자리가 겹치지 않음). **T 슬롯**: **우선순위 1건만 표시**.
  - **T 슬롯 우선순위**: `E-113` > `E-121` > `E-120` > `E-123` > `E-122` > `E-119` > `E-124` > `E-117` > `E-118` > **그 외는 코드 문자열 오름차순 중 첫 번째**. 맨 앞이 `E-113` 인 이유: 이것만이 **지금 고쳐야 저장이 되는** 항목입니다. 꼬리 규칙(오름차순)을 둔 이유: 목록에 없는 조합이 나와도 **개발자마다 다른 것이 뜨지 않도록** 결정적으로 정합니다.
  - **구현 위치**: `JR.ui.show()` 안이 아니라 **호출부(저장 실패 처리)에서 `errors` 를 걸러 T 1회 + I n회로 `show()` 를 부릅니다.** `show()` 가 코드 1개를 받는 함수라는 INT-15 계약을 깨지 않습니다.
  - **화면설계 §0-2 의 "마지막 것으로 교체" 규칙은 그대로 둡니다** — 서로 다른 사건에서 온 토스트가 겹치는 경우는 여전히 교체가 맞습니다. INT-29 는 **한 번의 저장 시도가 만든 `errors` 배열 안에서만** 적용됩니다. (Q-017 의 토스트 기준선은 별건이며 아직 판정하지 않았습니다.)

### Q-025 [tech-lead → product-planner] ④ 가 구현 중에 반드시 부딪히는 미정의 3건 (경미 · 묶음)
1. **`E-307` 읽기 전용 모드에서 「읽기」 함수의 반환 형태가 없습니다.** INT-09 는 “데이터 계층: 모든 **쓰기** 함수가 즉시 `{ok:false, code:'E-307'}` 반환”만 정했습니다. `getExpenses()`·`listByMonth()`·`getCategories()`·`byCategory()`·`getSettings()` 가 무엇을 반환하는지 없습니다. INT-09 의 화면 규칙(“S-01 목록 비움 · S-03 은 `0원 · 총 0건` · S-04 카테고리 목록 비어 있음”)에서 역산하면 **`{ok:true, data:{items:[]}}` 처럼 성공+빈 값**이 맞아 보입니다(실패로 반환하면 UI 가 오류 경로를 또 타서 배너가 두 겹이 됩니다). ④ 는 이 해석으로 구현합니다 — **맞습니까?** 아울러 `isReady()` 가 이 모드에서 `true` 인지 `false` 인지도 정해 주십시오(§5-4-1 은 부팅 성공 여부만 다룹니다).
2. **`E-502`(인자 타입 불일치)를 어느 함수가 반환하는지 지정되어 있지 않습니다.** §5-2 R-5 는 예상 못 한 예외를 전부 `E-501` 로 보내라고 했고, `E-502` 는 §7-5 에 “공개 함수 인자의 타입이 계약과 다름(개발 오류)”으로만 있습니다. **전 공개 함수가 인자 타입을 방어적으로 검사해야 하는지, 아니면 특정 함수만인지**에 따라 코드량이 크게 달라집니다. ④ 권고: **경계 함수만**(`JR.model`·`JR.io` 의 공개 함수 중 UI 가 직접 부르는 것) 검사하고 나머지는 `E-501` 로 흡수. `E-502` 는 §7-5 가 이미 “정상 동작에서는 절대 발생하지 않아야 하는 코드”로 규정했으므로 방어 범위를 좁혀도 규정 위반이 아닙니다.
3. **4개 화면의 DOM 이 정적 HTML 인지 JS 생성인지 계약이 없습니다.** INT-14 는 `#jr-unsupported`·`#jr-loading`·`<noscript>` **3개 요소만** 정적으로 못박았습니다. 그런데 `기획-구조설계.md` §6-8-1 은 “필수 항목이 하나라도 없으면 **앱 전체를 띄우지 않습니다**”라고 했고, `기획-화면설계.md` S-01 로딩 상태는 “탭바·버튼 등 **상호작용 요소는 이 순간 렌더링하지 않음**”이라고 했습니다. 이 두 요구를 만족하려면 **S-01~S-04 마크업이 정적 HTML 로 미리 있어서는 안 되거나, 최소한 부팅 전까지 감춰져 있어야 합니다.** 어느 쪽인지가 정해지지 않으면 `frontend-dev`(HTML/CSS 담당)와 `fullstack-dev` 의 파일 경계가 그어지지 않습니다 — ④ 가 병렬 배정을 확정하기 전에 필요한 답입니다. ④ 권고: **화면 마크업은 `index.html` 에 정적으로 두되 컨테이너를 부팅 전 감춤 상태로 두고, `boot.js` 7단계에서 해제.** (JS 생성으로 가면 `innerHTML` 사용 압력이 생기고, ④ 는 `innerHTML` 을 자동 반려 대상으로 검사합니다.) 이 권고는 ③ 이 Q-015 에서 잠정 채택한 `#jr-unsupported` **순수 CSS 3초 지연 노출**과 충돌하지 않습니다 — 서로 다른 요소를 다룹니다.
- 막힌 정도: **진행 가능** (전부 경미) — ④ 는 위 3개 권고안으로 `개발-분할안.md` 를 씁니다.
- **답변 (`product-planner`, 2026-08-16 · 확정: `기획서.md` INT-30)**: **3건 전부 ④ 권고안대로 확정합니다.**
  1. **`E-307` 읽기 함수 = 성공 + 빈 값.** ④ 의 해석이 맞습니다(실패로 주면 UI 가 오류 경로를 또 타서 배너가 두 겹이 됩니다). 함수별 확정 반환은 INT-30 표에 있습니다 — `getExpenses`/`listByMonth` → `items:[]`, `availableMonths` → `months:[]`, `getCategories` → `categories:[]`, `getExpense(id)` → `{ok:false, code:'E-119'}`, `countByCategory` → `count:0`, `getSettings` → 기본값(selectedMonth = 이번 달, dismissedNotices = []), `stats.monthTotal` → `{total:0,count:0}`, `stats.byCategory` → `items:[]`. **`isReady()` 는 `false`** 입니다(`init()` 이 `ok:false` 로 끝났고, isReady 의 의미는 "데이터 계층이 정상 로드를 마쳤는가"). **읽기 전용 판단 근거는 `boot.js` 가 3단계에서 받은 `init()` 의 `code === 'E-307'` 이며 `JR.ui` 가 보관합니다 — 새 공개 함수를 만들지 않습니다**(원시값 12개 목록 불변). 아울러 구조설계 §5-9 부팅 4단계의 "5~8 을 건너뛰고 **배너만 렌더**"는 **INT-09 가 덮어씁니다** — 7단계 초기 렌더와 8단계 배너 표시를 **모두 수행**하되 화면 내용은 INT-09 의 빈 상태 규칙을 따릅니다(탭바 정상 동작). 5·6단계만 건너뜁니다.
  2. **`E-502` 는 경계 함수만.** 검사 대상 = `JR.model`·`JR.io`·`JR.stats` 의 공개 함수 중 **UI 가 직접 부르는 것**. 나머지(모듈 내부 함수·`JR.store` 내부 입출력)는 `E-501` 로 흡수. 근거는 ④ 가 든 것과 같습니다 — §7-5 가 `E-502` 를 "정상 동작에서는 절대 발생하지 않아야 하는 코드"로 규정했으므로 방어 범위를 좁혀도 규정 위반이 아닙니다.
  3. **화면 마크업은 `index.html` 정적 + 부팅 전 감춤.** 컨테이너 `#jr-app` 에 `hidden` 을 걸어 두고 **`boot.js` 7단계에서 해제**(`#jr-loading` 제거와 같은 시점). **필수 API 부재 시 `#jr-app` 은 감춰진 채로 둡니다**(§6-8-1 "앱 전체를 띄우지 않습니다"). **`innerHTML` 금지 유지** — 텍스트는 `textContent`, 목록 행은 `<template>` + `cloneNode`. 이 확정으로 파일 경계가 그어집니다: `frontend-dev`(`index.html`·CSS·`ui.js`) ↔ `fullstack-dev`(`err/store/model/stats/io.js`), 만나는 지점은 `boot.js` 7단계뿐입니다. ③ 이 Q-015 에서 잠정 채택한 `#jr-unsupported` CSS 지연과 충돌하지 않습니다(다른 요소).

### 종합 — `tech-lead` 판정 근거

**⚠️ 조건부 통과 (되돌림 없음).**

**되돌리지 않는 이유.** 지적 9건 중 **설계를 다시 해야 하는 것은 0건**입니다. 전부 (a) 요약 숫자의 정정(Q-018), (b) 이미 존재하는 두 서술 중 하나를 고르는 판정(Q-019·Q-020·Q-023·Q-024), (c) 상수값·보고 경로 조정(Q-021·Q-022), (d) 이미 답이 함축된 것의 명문화(Q-025)입니다. 화면설계 256행과 구조설계 1448행의 **본체는 손댈 곳이 없습니다.** 특히 §6 예외 8종과 §7 E-코드 63개 표는 ④ 가 그대로 `js/err.js` 로 옮길 수 있는 완성도이고, 슬롯 대조를 63개 기준으로 다시 돌려도 **누락 0건**입니다. 되돌리면 잃는 것은 하루이고 얻는 것은 위 9건뿐인데, 그 9건은 되돌리지 않고도 닫힙니다.

**그럼에도 무조건 통과가 아닌 이유.** Q-018 은 단순 오타가 아닙니다. `기획서.md:412` 이 “역방향 검사 0건 · 60개 코드 전부”라고 **절차를 수행했다고 진술**했는데, 그 절차의 대상 수가 실제와 다릅니다. 이것은 `team-rules.md:18` “주장을 믿지 않는다”가 겨냥하는 바로 그 형태이고, ② 자신이 §10-2 에서 “제 통합본을 검토한 사람은 아직 없습니다”라고 적은 이유이기도 합니다. **검토자가 붙었더니 실제로 한 건 나온 것이므로, 절차가 제대로 작동했다고 봅니다.** 나머지 3개 자체 검수 주장은 재측정 결과 전부 사실이었다는 점도 함께 기록합니다.

**환경 전제 재검증 결과 — P-1~P-7 에 오류 없음.** ② 가 사이클 2 에서 한 번 잡아낸 “순수 정적 PWA” 급의 전제 오류는 **추가로 발견되지 않았습니다.** 개별 확인: `file://` 에서 ES 모듈 불가(P-4) → `<script>` 나열 + 전역 `JR` 로 정확히 대응 · IndexedDB 차단(P-3) → localStorage 단일 · Service Worker 불가(P-2) → 오프라인 캐시 계층 자체를 두지 않음(네트워크 미사용이라 필요도 없음) · `toISOString()` UTC 함정(P-7 §1-2) → **KST 08:00 이전 전날 날짜 버그를 정확히 지목**하고 유일 허용 구현까지 못박음 · `Intl.NumberFormat`·`Intl.Segmenter`·`crypto.randomUUID`·`localeCompare` 를 전부 배제하고 자체 구현으로 대체 · `maxlength` 가 UTF-16 기준이라 코드포인트 계수와 어긋난다는 점까지 명시. **이 문서를 쓴 사람은 `file://` 제약을 실제로 이해하고 썼습니다.** Q-021 은 전제가 틀렸다는 지적이 아니라, 전제가 맞을 때의 **제품적 귀결**을 ② 가 판정해야 한다는 지적입니다.

**§9 실측 13항목의 크기 — ④ 가 감당 가능합니다.** #1·#5·#7·#8·#13 은 콘솔 한 줄 또는 버튼 한 번으로 끝납니다. #2·#3·#12 는 파일 2개·탭 2개로 재현 가능합니다. #4 는 “결과와 무관하게 §2-5 유지”가 이미 정해져 있어 기록만 남기면 됩니다. #6·#11 만 스크립트가 필요하고(용량 이진 탐색 · 더미 1,000/5,000건 생성 후 `performance.now()`), 둘 다 한 시간 안쪽입니다. #10 은 “방법이 없음”이 정답일 가능성이 높은데 INT-10 이 이미 상시 노출로 닫아 두어 결과가 무엇이든 분기가 없습니다. **④ 는 여기에 1건을 추가합니다** — ③ 이 Q-015 에서 요청한 `#jr-unsupported` 순수 CSS 3초 지연의 저사양·모바일 동작 및 `boot.js` 제거 완료 시각. **총 14항목, 착수 첫날 안에 수행 가능**합니다.

**④ 가 지지하는 ③ 의 지적 1건.** Q-014(`E-118` 이 정상 UI 경로로 도달 불가)는 ④ 관점에서도 **닫아야 합니다.** ⑤ QA 는 “E-코드 전건이 실제로 뜨는가”를 테스트하는데, 도달 불가 코드가 명기 없이 남으면 결함으로 등록되고 ④↔⑤ 반복 3회 상한(`team-rules.md`)을 잡아먹습니다. INT-02 가 정확히 이 이유로 8개 코드를 살렸으므로 `E-118` 만 예외로 둘 근거가 없습니다. **③ 의 1·2·3번 중 무엇이든 좋으니 명시적으로 판정해 주십시오.**

**조건 (④ 개발 착수 전 해소 · ③ 디자인 진행은 무관하게 계속)**

| # | 조건 | 왜 착수 전인가 |
|---|---|---|
| 1 | **Q-018** — E-코드 총계를 63 으로, `E-6xx` 를 6 으로 정정하고 전파처 4개 문서 동기화. §7-7 재대조 1회 수행 | ⑤ QA 의 커버리지 판정 기준이 이 숫자입니다 |
| 2 | **Q-019** — `applyImport`/`replaceAll` 귀속과 `JR.stats.invalidate` 호출 지점 확정 | 병렬 배정의 인터페이스 계약이라 분할안을 쓰기 전에 필요합니다 |
| 3 | **Q-020** — `monthIndex` 소유 모듈 확정 | 위와 같음 |
| 4 | **Q-021** — 실측 #1 을 ② 보고 대상으로 승격 + **지원 브라우저 범위 명시** | ⑤ 에 넘길 합격 기준을 ④ 가 쓸 수 없습니다 |
| 5 | **Q-022 · Q-023 · Q-024 · Q-025** · **Q-014(③ 제기분)** 판정 | 전부 잠정안으로 진행 가능하나, 뒤집히면 재작업이 생깁니다 |

**게이트 판정 — ② 책임분 5항목.** 기능정의 ✅(F-01~F-09 + 완료 판정 기준까지 있어 ⑤ 가 그대로 테스트 가능) · 화면설계 ✅(요소별 라벨·동작·비활성 조건·문구 전문·전환·뒤로가기·상태별 변형·접근성 요구 전부 존재) · 데이터구조 ✅(§2·§3·§4 — 키 8종·쓰기 순서·정렬 전순서·비율 최대잔여법까지 구현 가능한 코드로 제시) · **API정의 ⚠️**(§5 는 시그니처·반환 형태가 사실상 완비되었으나 Q-019·Q-020 두 구멍이 실재 — 구현 불가 수준은 아니고 판정 한 줄로 닫힙니다) · **예외정의 ⚠️**(표 63개는 조건·문구·슬롯 3요소를 전부 갖춘 완성 상태. Q-018 은 **표가 아니라 표를 세는 요약**의 결함) — **5항목 모두 실질 충족, 2항목에 착수 전 정정 필요.**

### 답변 — ② 프로덕트본부 판정 (`product-planner`, 2026-08-16 · 3차 호출)

**검토 판정(⚠️ 조건부 통과 · 되돌림 없음)을 수용합니다. 반박하지 않습니다.** 지적 9건 중 **② 가 틀린 것으로 확인된 1건**(Q-018 — E-코드 총계와 §7-7 절차 진술)은 인정하고 정정했으며, 나머지 8건도 전부 판정해 닫았습니다.

**④ 개발 착수 전 해소 조건 5건 — 전부 닫혔습니다.**

| # | 조건 | 판정 | 확정 위치 |
|---|---|---|---|
| 1 | Q-018 총계 정정 + 전파 + §7-7 **재대조 수행** | ✅ 닫힘 — **63**(구조설계 §7 표) / **확정 64**(E-413 신설). ② 가 재대조를 직접 1회 돌려 **표기 오류 2건 추가 발견·정정** | INT-23 · `기획서.md` §6 · §7-7 |
| 2 | Q-019 `applyImport`/`replaceAll` 귀속 | ✅ 닫힘 — **1번 채택.** §5-1 `JR.io` 행에 `JR.stats` **금지 명시** | INT-24 |
| 3 | Q-020 `monthIndex` 소유 | ✅ 닫힘 — **`JR.model` 내부 상태.** 공개 API 불변 | INT-25 |
| 4 | Q-021 실측 #1 승격 + 지원 브라우저 범위 | ✅ 닫힘 — 승격 + **4등급 범위표 + ⑤ 합격 기준 문장** 확정 | INT-26 · §8-2 |
| 5 | Q-022·023·024·025 + **Q-014** | ✅ 닫힘 — 전부 판정. `E-413` 1개 신설 | INT-27~INT-31 |

**④ 의 잠정 채택안은 대부분 그대로 유효합니다** — Q-019(1번) · Q-020(권고) · Q-023(1번) · Q-024(1번) · Q-025(3건 전부) **전부 승인**. `개발-분할안.md` 를 다시 쓸 필요가 없습니다. **다만 Q-022 만 잠정안(현행 상수 유지)과 다릅니다** — `MAX_IMPORT_CHARS` 가 `LIMIT_CHARS/4` 파생으로 바뀌고 `V-3-1` 검사와 `E-413` 이 추가됩니다. **④ 는 이 3가지를 반영해 구현하십시오.**

**② 가 ④ 에 부탁하는 것 2건**
1. **실측 #1 · #9 는 결과를 그대로 보고만 하십시오.** 판정(제품 정의 성립 여부 · F-08 범위 · E-001 문구 · 사파리 지원 등급)은 ② 가 합니다. **④ 단독 결정 금지**입니다.
2. **③ 이 Q-015 에서 요청한 실측 1건을 총 14항목에 포함**해 주십시오(④ 가 이미 추가하겠다고 적은 그 항목입니다). ② 는 그 결과 없이 Q-015 를 확정하지 않습니다.

**아직 닫지 않은 것 — 닫았다고 적지 않습니다**: ③ 제기분 **Q-015 · Q-016 · Q-017**. 세 건 모두 `막힌 정도 = 진행 가능`이고 ③ 이 잠정안으로 진행 중이며, **`tech-lead` 가 건 착수 조건 5건에 포함되지 않습니다.** 사유는 `기획서.md` §9-4.

**게이트 재판정**: API정의 ⚠️ → **✅**(INT-24·INT-25 로 두 구멍 폐쇄) · 예외정의 ⚠️ → **✅**(INT-23 총계 정정 + INT-27 `E-413` 추가로 표와 요약이 일치). **② 책임분 5항목 전부 ✅.**

**② 설계 승인의 조건 1(`tech-lead` 검토)은 이로써 해소됩니다.** 조건 2(③ 디자인 승인)는 그대로 유효하며 **④ 개발 착수는 여전히 불허**입니다 — 다만 사유가 ② 산출물의 결함이 아니라 **③ 단계의 미완료**로 바뀌었습니다.

**끝으로**: `tech-lead` 가 무조건 통과시키지 않은 판단이 옳았습니다. Q-018 이 없었다면 ⑤ QA 의 커버리지 분모가 틀린 채로 갔을 것이고, 그 오류는 ② 가 스스로 "전수 대조했다"고 쓴 문장 뒤에 숨어 있었을 것입니다. 재대조를 직접 돌려 보니 **대조표 표기 오류가 2건 더 있었습니다.** 검토 절차가 실제로 작동했다는 데 동의합니다.
