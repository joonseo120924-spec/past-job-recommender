# ⑤ → ④ 수정요청서 (사이클 2 · 되돌림 **3회차 = 마지막**)

발신: `qa-lead` (⑤ 품질본부 파트장) · 2026-08-27
수신: **④ 개발본부**(`tech-lead`) · 참조 **② 프로덕트본부**(`product-planner`) · 마스터
근거: `docs/QA보고서-2회차.md` · `docs/기획서.md` §7-12(INT-36) · `questions.md` Q-063 · Q-093
코드 기준선: **`d670faf`**

> ## 🚨 이번이 **마지막 되돌림**입니다
> `team-rules.md` 게이트: **④↔⑤ 반복은 최대 3회.** 1회차·2회차가 끝났고 **이번이 3회차**입니다.
> **3회차 재시험에서도 남은 오류가 있으면 ⑤ 는 승인하지 않고, 규정에 따라 진행을 중단하고 사용자에게 보고합니다.**
> 그래서 **이번 요청서는 의도적으로 짧습니다. 고칠 곳은 한 함수입니다.**

---

## 0. 먼저 — ④ 가 잘못한 것이 아닙니다

- **⑤ 가 지정한 12건은 12/12 조치가 확인되었습니다.** 하나도 되돌리지 않습니다.
- **진짜 회귀 0건.** 기존 스크립트 46개를 기준선과 양쪽 실행해 대조했습니다.
- **④ 의 Y-B 8건 주장은 전건 사실이었고, 그중 5건의 원인은 ⑤ 자신의 단언·계기 오류입니다.** §4 에 ⑤ 가 고칠 목록을 적었습니다.
- **④ 가 넓힌 2건**(`sanitizeList` 날짜 실재 · `wipeAll` 의 `jr.v1.rollback` 정리)은 **검증했고 통과입니다.** 되돌리지 마십시오(§3).
- **남은 2건은 `675840e` 에도 있던 기존 결함**이며 **④ 의 이번 조치와 무관**합니다. `git diff` 로 확인했습니다.

**그럼에도 반려인 이유**: 게이트 조건은 「누가 만들었는가」가 아니라 「지금 남아 있는가」입니다. ⑤ 는 직전 회차에 **똑같은 종류(`E-203` 빈 치환 자리)를 S3 결함으로 세어 되돌렸습니다.** 이번에 세지 않으면 판정이 아니라 편의가 됩니다.

---

## 1. 고칠 곳 — **`src/js/model.js` `loadKeyWithRecovery` 한 함수** (390~417행)

두 결함이 이 함수 안에 있습니다. **다른 파일은 건드리지 마십시오.**

```js
390  function loadKeyWithRecovery(key, fallback, notices) {
391    var r = S.getJSON(key), raw, b;
392    if (r.ok) {
393      if (r.data.value === null) { return { value: null, used: 'none' }; }
394      if (Object.prototype.toString.call(r.data.value) !== '[object Array]') {
395        notices.push('E-304');            // ← QA-S-009 : params 없음
396        b = S.getJSON(key + '.bak');
397        if (b.ok && …) { notices.push('E-303'); return { value: b.data.value, used: 'bak' }; }
401        return { value: fallback, used: 'default' };   // ← .bak 없음. 그래도 E-304 는 이미 들어감
402      }
...
405    /* 파싱 실패 */
411    notices.push('E-303');                // ← QA-S-010 : 무조건. 되살릴 백업본이 있는지 보기 전에
412    b = S.getJSON(key + '.bak');
413    if (b.ok && …) { return { value: b.data.value, used: 'bak' }; }
416    return { value: fallback, used: 'default' };   // ← 여기로 오면 아무것도 되살리지 못했음
417  }
```

---

## 2. 결함 2건

### 2-1. **QA-S-009 (S3)** — `E-304` 가 빈칸을 내보내고 문장 뒷부분이 사실과 다름 (= ④ 의 Y-A · Q-093)

**⑤ 재현 (3엔진 × 20사이클 전건 · 신규 설치에서 바로)**
```
신규 설치 부팅(.bak 없음) → localStorage['jr.v1.expenses'] = '{"not":"array"}' → 재부팅
배너: "저장된 기록 가운데 건을 읽을 수 없어 제외했습니다. 나머지 기록과 금액은 그대로입니다."
model 건수: 0
```
- **위반 1 (INT-36)**: 빈 치환 자리(`{count}`)가 화면에 나갑니다. ② 확정 — 「하나라도 남으면 미충족」.
- **위반 2 (⑤ 가 ④ 보고서보다 넓힘)**: 「**나머지 기록과 금액은 그대로입니다**」라고 말하는데 **남은 기록은 0건**입니다. 빈칸만이 아니라 **문장 자체가 사실과 다릅니다.** ④ 는 빈칸만 적었습니다.

### 2-2. **QA-S-010 (S2)** — 백업본이 없는데 「백업본으로 되살렸습니다」 · 전건 소실을 축소해 말함 (⑤ 신규)

**⑤ 재현 A — 신규 설치 (3엔진 × 20사이클 전건)**
```
신규 설치(.bak 없음) → localStorage['jr.v1.expenses'] = '{깨진 JSON' → 재부팅
배너: "저장된 데이터 일부가 손상되어 백업본으로 되살렸습니다. 가장 최근에 입력한 기록이 빠졌을 수 있으니 목록을 확인해 주세요."
실제: .bak = []  ·  남은 기록 = 0건
```

**⑤ 재현 B — 실제 데이터가 있는 경로 (가정이 아닙니다)**
```
① 지출 3건 저장(31,000+22,000+13,000) → .bak 2개 생성 실측
② jr. 밖 키로 localStorage 를 1바이트까지 채움 (실제 할당량 · 스텁 없음)
③ 지출 1건 더 저장 → §6-2 복구 사다리 3단계가 .bak 2개를 삭제 (store.js:233-237)
   실측: ["jr.v1.categories.bak","jr.v1.expenses.bak"] → []
④ 채운 키를 비우고 jr.v1.expenses 손상(쓰기 중단 흉내) → 재부팅
배너: "…백업본으로 되살렸습니다. 가장 최근에 입력한 기록이 빠졌을 수 있으니…"
실제: 남은 기록 0건 (4건 전부 소실)
```
- **왜 S2 인가**: **전 기록이 사라진 순간에 앱이 「되살렸다」고 말합니다.** 사용자는 자신이 내보내 둔 백업 파일로 복구하는 행동을 **하지 않게 됩니다.** 문구가 복구를 막습니다.
- **② 선례**: `E-605` 의 「사실과 다른 문구」를 ② 는 **시정 필수**로 판정했습니다(Q-063 #3 — 「② 는 구현 방식을 지정하지 않습니다. **"문구가 참이 되는 상태"만 요구**합니다」). 같은 기준입니다.
- **도달 조건은 이 앱이 대비하려고 만든 바로 그 상황**입니다(저장소 압박 → 복구 사다리, 쓰기 중단 → 손상 격리).

---

## 3. 되돌리지 말아야 할 것 — **④ 가 넓힌 2건은 검증 통과**

| ④ 확대 | ⑤ 검증 | 판정 |
|---|---|---|
| `model.js validExpenseRecord` 날짜 실재 (부팅 복구 경로) | 저장소에 `2026-02-30`·`2026-04-31` 을 직접 심고 재부팅 → 두 건 제외 · 정상 기록 `2026-08-10` 유지 · 그때 뜨는 `E-304` 는 **건수가 채워짐**(`2건`) | **유지** |
| `wipeAll` 이 `jr.v1.rollback` 정리 | 삭제 전 보유=true → 삭제 후 `null` → 재부팅 후 0건 · 원문 잔존 0. **INT-27(5) 불변식도 함께 확인** — 할당량을 채워 복구 사다리를 태워도 `rollback` 은 보존됨(`rollbackKept=true`) | **유지** (Q-091 은 ② 확정 대기) |

---

## 4. ⑤ 가 고칠 것 — **④ 는 손대지 마십시오** (Q-092 답변)

⑤ 소유 스크립트의 단언이 새 계약과 충돌합니다. **④ 가 옳고 ⑤ 가 틀렸습니다.** ⑤ 가 **3회차 재시험 직전에** 갱신합니다(지금 고치면 이번 회차의 A/B 대조 기준선이 사라집니다).

| 스크립트 | 현재 식 | 갱신할 식 | 사유 |
|---|---|---|---|
| `qa-lead-s2b.cjs:24` · `qa-sec-proto.cjs` | `getPrototypeOf(map) === Object.prototype` | `getPrototypeOf(map) === null` | INT-42 정답은 `null` |
| `qa-sec-field.cjs:80` | `memoLen >= 0 && memoLen <= 100` | `memoLen === -1 \|\| (memoLen >= 0 && memoLen <= 100)` | INT-43(4)-4 = 거부 |
| `qa-sec-integrity.cjs:40` | `ui.rows.length === 2` (결함 재현) | `ui.rows.length === 1` | 결함이 사라짐 |
| `qa-lead-s1.cjs:111` | 두 번째 `__proto__` 레코드 존재 가정 | 존재 확인 후 분기 | 결함이 사라져 CRASH |
| `qa-exc.cjs` · `qa-lead-func.cjs` | `warnings.indexOf('E-203')` | `warnings.some(w => w.code === 'E-203')` | INT-36 = 객체 배열 |
| `qa-lead-func.cjs:36` · `qa-exc.cjs` | `get/show('E-203')` 을 **params 없이** 부르고 숫자를 기대 | 삭제 — **⑤ 자신의 지시(「치환 엔진은 정상」)와 모순** | ⑤ 귀책 |
| `qa-lead-func.cjs` | `addExpense(v.data.expense \|\| v.data)` | `addExpense(원시입력)` | `validateExpense` 는 `.value` 를 돌려줍니다 — ⑤ 계기 오류 |
| `qa-extra.cjs` | INT-34 17개 명단 | INT-41 명단(계약 범위 18 · 전 표면 28) | Q-065 로 폐기된 기준선 |
| `qa-lead-s2.cjs` | — | **폐기 유지** | 이미 폐기 배너. 기준선 아님 |

---

## 5. 검토만 요청 — **결함으로 세지 않았습니다**

`wipeAll` 이 **`jr.` 키를 전부 지운 뒤** `persist` 실패를 판정하도록 순서가 바뀌었습니다(되돌림 전에는 지우기 **전에** 반환). 이 순서에서 `E-202`(「…**이전 기록은 그대로 있습니다**…」)가 나가면 실제 상태와 어긋납니다.

**⑤ 는 이것을 재현하지 못했습니다.** 3엔진에서 `jr.` 밖 키로 할당량을 1바이트까지 채운 뒤 `wipeAll` 을 20회 시도했으나 매번 성공했습니다(삭제로 확보되는 공간이 새로 쓰는 기본 상태보다 큼).
→ **남은 오류로 세지 않습니다.** ④ 가 코드를 읽고 판단해 주십시오. **고치지 않아도 3회차 승인을 막지 않습니다.**

---

## 6. 재검사 조건

| # | 요구 |
|---|---|
| 1 | §2 의 2건 조치 (또는 ② 판정에 따른 대안) |
| 2 | `git diff` 로 변경 파일·줄 명시 |
| 3 | **④ 가 직접 돌린 실행 로그 원문.** 「고쳤습니다」는 받지 않습니다 |
| 4 | **가시성·가려짐을 흉내 낸 시험이 있으면 그 사실을 로그 옆에 적어 주십시오.** 이번 회차에 `E-605`·`E-604` 재현이 `document.visibilityState` 를 `defineProperty` 로 덮어쓴 것이 보고서에 없었습니다 |
| 5 | §3 의 2건을 되돌리지 말 것 |
| 6 | `TODO`·`FIXME` 0건 유지 |

**검증 기준 (이것이 통과해야 완료)**
```
[QA-S-009]
  신규 설치 → jr.v1.expenses = '{"not":"array"}' → 재부팅
  → 배너에 빈 치환 자리 없음        (현재: "가운데 건을")
  → 배너 문장이 실제 상태와 일치     (현재: 0건인데 "나머지 기록과 금액은 그대로입니다")

[QA-S-010]
  신규 설치(.bak 없음) → jr.v1.expenses = '{깨진 JSON' → 재부팅
  → .bak 이 없으면 "백업본으로 되살렸습니다" 를 말하지 않음
  실데이터 경로: 3건 저장 → 할당량 채움 → 저장(사다리 3단계가 .bak 삭제) → 손상 → 재부팅
  → 전건 소실을 "가장 최근에 입력한 기록이 빠졌을 수 있으니" 로 축소해 말하지 않음

[회귀 금지]
  .bak 이 실제로 있고 되살린 경우에는 E-303 이 그대로 뜰 것
  ⑤ 가 이번에 통과시킨 12건이 전부 그대로일 것 (verify/qa2-a.cjs · qa2-b.cjs 로 ⑤ 가 재확인)
```

---

## 7. ② 프로덕트본부에 (병행 · ④ 를 막지 않습니다)

- **Q-093 (`E-304`)** — ④ 의 두 선택지(ⓐ 알리지 않음 ⓑ 없는 숫자를 지어냄) 는 둘 다 받을 수 없습니다. ⑤ 는 **세 번째**를 제안합니다: 이 분기의 문제는 「몇 건인지」가 아니라 **「이 키를 통째로 읽지 못했다」**입니다. **카테고리에는 `E-305` 라는 대응 코드가 이미 있고 지출에만 없습니다** — ② 가 설계 공백으로 확인한 그대로입니다. **E-코드 총수 64 를 바꿀지, 기존 코드로 표현할지는 ② 의 판정**입니다.
- **신규 Q-094 (`E-303`)** — 위 QA-S-010. ⑤ 가 새로 올립니다.
- **두 판정이 늦어져도 ⑤ 는 「② 대기」를 이유로 남은 오류에서 빼지 않습니다.** `team-rules.md:75` 때문입니다.
