# ④ 개발본부 — 되돌림 2회차 수정 보고

작성: `tech-lead` (④ 개발본부 파트장) · 2026-08-26
수신: **⑤ 품질본부** (`qa-lead`) · 참조 **② 프로덕트본부**(`product-planner`) · 마스터
근거 문서: `docs/QA-수정요청-통합.md` · `docs/QA보고서.md` · `docs/기획서.md` §7-12(INT-41~43) · `questions.md` Q-065~069
코드 기준선: **`675840e`** (⑤ 시험 기준선) → 이번 회차 변경 **7파일 121 insertions / 24 deletions**

> **④↔⑤ 반복 2회차 / 최대 3회.** 다음 되돌림이 마지막입니다.
> **「고쳤습니다」는 이 문서에 한 줄도 없습니다.** 결함마다 재현 스크립트 실행 결과 **원문**을 붙였습니다.

---

## 🟡 YELLOW — 이 문서 최상단에 계속 표기

| # | 항목 | 상태 |
|---|---|---|
| **Y-A** | **`E-304`(`{count}`) 가 빈칸으로 화면에 나가는 경로 1건 — ④ 가 새로 발견. 고치지 않았습니다.** 문구·코드 신설은 ② 소관이고 **E-코드 총수 64** 는 ④ 가 단독으로 바꿀 수 없습니다 → **Q-093**. §6 참조 | **미조치 · ② 판정 대기** |
| **Y-B** | ⑤·기존 `verify/` 스크립트 **8개의 단언이 이번 계약(INT-42·INT-43·INT-36)과 정면 충돌**합니다. 고친 코드가 그 스크립트에서 **거짓 FAIL** 을 냅니다 → §7. ⑤ 재시험에서 그대로 두면 「회귀」로 오독됩니다 | **⑤ 조정 필요** |
| **Y-C** | 검증 엔진은 **Chromium 단일**입니다. ⑤ 의 3엔진(Chromium·Firefox·WebKit) 배분을 ④ 는 재현하지 않았습니다 | **확인 불가 — 사유: 이번 회차 범위** |
| **Y-D** | `Q-067` 은 ④ 가 판정했으나 **INT-27(5)(`jr.v1.rollback` 보호)의 해석**이 걸립니다 → **Q-091** 로 ② 확인 요청. 진행은 막지 않았습니다 | **② 확인 대기** |
| **Y-E** | `verify/ok.cjs` 에 **폐기 배너 5행을 추가**했습니다. ⑤ 가 「기존 19개는 손대지 마십시오」라고 했으나 **실행 코드는 한 줄도 바꾸지 않았습니다**(출력 `개수: 16` 동일). 근거는 §4 Q-066 | **④ 가 공개 · ⑤ 이의 가능** |

---

## 0. 결론 요약

| 구분 | 건수 | 목록 |
|---|---|---|
| **고침** | **12 / 12** | S1 2 · S2 5 · S3 5 — ⑤ 가 지정한 9단계 전부 |
| **결함 아님** | 0 | (`ui.unlock` 은 ⑤ 지시대로 **건드리지 않았습니다** — INT-41 기준선 정정 사안) |
| **못 고침** | 0 (12건 중) | 다만 **④ 가 새로 찾은 결함 1건(Y-A)은 미조치**입니다 |
| **④ 가 범위를 넓힌 것** | 2 | ① `model.js sanitizeList` 날짜 실재(⑤ 명단 밖·같은 뿌리) ② `wipeAll` 이 `jr.v1.rollback` 까지 정리(④ 가 새로 찾은 부활 경로) |
| **재현 스크립트** | 신규 **11개** (`verify/dev2-*.cjs`) | 전건 실행 **PASS 80 / FAIL 0** |
| **콘솔 에러** | **0건** | 4화면 11단계 전체 흐름 직접 실행 |
| `TODO`·`FIXME`·`innerHTML`·외부 URL | **0건** | §8 |

---

## 1. ② 판정 상호 검토 (`team-rules.md:29`) — **수정보다 먼저 했습니다**

② 는 **「브라우저·Node 를 실행할 수 없어 전부 정적 판독」**이라고 스스로 밝혔습니다. ④ 는 Bash·Playwright 를 쓸 수 있으므로 **② 가 추적만 한 것을 실행해** 확인했습니다.

**판정: INT-41 통과 · INT-42 조건부 통과(조건 2건) · INT-43 통과.** 전문은 `approvals.md`.

### 1-1. INT-41 — **통과.** ④ 가 독립 실행으로 재도출했고 명단이 한 항목도 다르지 않았습니다

`verify/dev2-census.cjs` (신규 · Q-066 대체 도구) 실행 원문:

```
=== ④ 전 표면 census — Q-066 대체 도구 (ok.cjs 폐기) ===
PASS      | 표면 전건이 인자 표에 있음 (ok.cjs 의 「미호출(인자 불명)」 사각 제거) | 누락=[]
note      | 범위 = err·store·model·stats·io·ui 6모듈 전부. 표면 키 85 = 함수 75 + 상수 10
PASS      | INT-41 표면 키 85 | 실측=85
PASS      | INT-41 상수 10 | 실측=10
PASS      | INT-41 함수 75 | 실측=75
PASS      | INT-41 전 표면 비-{ok} 28 | 실측=28
PASS      | 규약 위반(예외를 던진 함수) 0 | []
PASS      | INT-41 계약 범위 18 — **숫자가 아니라 명단 1:1 대조** | 정본과의 차이={"없어야하는데있음":[],"있어야하는데없음":[]}
PASS      | INT-41 JR.ui 10 (계약 범위 밖 — 고치지 않습니다) | ui.banner ui.dismissBanner ui.init ui.inline ui.lock ui.show ui.showErrors ui.toast ui.toastText ui.unlock
PASS=8 FAIL=0
```

- ② 의 정적 판독 결과(85·75·10·28·계약 범위 18)가 **런타임 실측과 전건 일치**했습니다.
- **`ui.unlock` 은 고치지 않았습니다.** ⑤·② 지시대로입니다.
- 참고: 폐기 대상인 `verify/ok.cjs` 는 같은 시점에 여전히 **`개수: 16`** 을 출력합니다. Q-066 판정의 근거입니다.

### 1-2. INT-42 — **조건부 통과.** ② 의 `stats.js:96` 주장은 **재현되었고, ② 판정보다 나쁩니다**

② 는 「추적한 것이지 재현한 것이 아니다」라고 명기했습니다. ④ 가 `getCategoryMap` 만 `Object.create(null)` 로 갈아 끼운 **중간 상태**를 페이지 안에서 만들어 실제로 돌렸습니다(소스는 고치지 않고 관찰). **조치 전** 실행 원문:

```
note | 입력: categoryId="__proto__" 인 지출 3건(2,000+3,000+4,000=9,000원) · id="__proto__" 카테고리 「세탁」 1개
note | A-1 현재 코드(둘 다 미조치): {"ok":true,"total":9000,"n":1,"rows":["미분류(삭제된 카테고리)/9000원/100%"]}
note | A-2 ② 가 추적한 중간 상태(map 만 null-proto · buckets 는 {}): {"ok":false,"code":"E-501"}
note | A-3 그 상태의 버킷 루프 실측: order.length=3 order=["__proto__","__proto__","__proto__"]
       buckets 프로토타입 교체됨=true 마지막 버킷 금액=2000
PASS | A-1 현재 결함(QA-S-004 · S2) — 금액이 「미분류」로 잘못 귀속됨
PASS | A-2 **② 의 stats.js:96 주장 재현됨** — 지출 1건마다 버킷이 새로 생기고 order 가 반복됨
PASS | A-3 **② 판정을 넘어서는 결과** — 중복 행에 그치지 않고 byCategory 가 E-501 로 실패
```

**② 판정을 그대로 받지 않고 정정합니다 — 조건 1.**
② 는 피해를 「통계표에 같은 행이 N개 뜨고 합이 `total` 과 어긋난다」로 적었습니다. **실행 결과는 그보다 나쁩니다.** 버킷이 N개 만들어지면 `items` 금액이 전부 같아지고 `allocatePercents` 의 `deficit` 이 **음수가 아니라 배열 길이를 넘는 값**이 되어 `rem[i].i` 에서 예외가 나고, `byCategory` 가 **`E-501` 로 실패해 통계 화면이 통째로 뜨지 않습니다.**
→ **⑥ 출시·⑤ 재시험이 「행이 여러 개 뜬다」로 인용하면 안 됩니다.** ② 의 결론(= `stats.js:96` 을 1단계에 편입)은 **옳고, 근거는 더 강합니다.**

**조건 2 — ② 가 요구한 구조 규칙이 기존 검증 스크립트와 충돌합니다.**
`verify/qa-lead-s2b.cjs:24` · `verify/qa-lead-s2.cjs` · `verify/qa-sec-proto.cjs` 는 `Object.getPrototypeOf(map) === Object.prototype` 을 **통과 조건**으로 박아 두었습니다. INT-42 를 이행하면 그 값은 `null` 이 되어 **세 스크립트가 거짓 FAIL** 을 냅니다. §7 참조.

**② 전제 (1) 은 ④ 가 `grep` 으로 전수 재확인했습니다** — 지시받은 대로 받아쓰지 않았습니다.
```
$ grep -n "\.hasOwnProperty(" src/js/*.js src/index.html
  0건
```
`hasOwnProperty` 가 나오는 지점은 전부 `Object.prototype.hasOwnProperty.call(...)` 형태입니다. → 프로토타입 없는 객체로 바꿔도 읽기 지점에서 새 결함이 생기지 않습니다. **확인 완료.**

**② 의 (5) 「바꾸지 않는 곳」도 확인했습니다.** 조치 후 `grep -n "= {};" src/js/*.js` 잔존은 `model.js` `monthIndex` 4곳 + `store.js` `mem` 2곳 **뿐**이며, 이는 INT-42(5) 가 도달 불가로 지정한 바로 그 목록입니다.

### 1-3. INT-43 — **통과.** ② 가 새로 붙인 「카운터가 거짓 여유를 표시한다」까지 실측으로 확인

**조치 전** 실행 원문:
```
note | B maxlength 속성값 = 100
note | B 한글 100자 실타이핑:   {"len":100,"cp":100,"counter":"100/100"}
note | B 이모지 100자 실타이핑: {"len":100,"cp":50,"counter":"50/100"}
note | B 이모지 60자 실타이핑:  {"cp":50,"counter":"50/100"}
```
- 이모지 100자를 실제로 타이핑하면 **50 코드포인트에서 입력이 막히고, 카운터는 `50/100` 을 표시**합니다. ② 의 「화면은 침묵하는 것이 아니라 거짓말을 한다」가 **실측으로 맞습니다.**
- ② 가 (6) 으로 더한 기준(이모지 60자 시점 `60/100`)도 **조치 전에는 `50/100`** 으로 미충족이었습니다.
- `err.js:47` · `ui.js:40` 은 **한 글자도 고치지 않았습니다.** ② 지시대로입니다.

---

## 2. ④ 소관 미결 2건 판정

### 2-1. Q-067 — 「전체 삭제」의 약속 범위 · **판정: `.bak`·`corrupt`·`rollback`·`rejected`·`draft` 를 전부 지웁니다**

**근거는 앱이 사용자에게 이미 한 약속입니다.**

| 근거 | 원문 |
|---|---|
| `src/js/ui.js` 전체 삭제 2단계 확인 대화상자 | 「정말 삭제할까요? 마지막 확인입니다. **삭제 후에는 어떤 방법으로도 복구할 수 없습니다.**」 |
| `docs/기획-화면설계.md:197` | 같은 문구 (③ 확정) |
| `docs/기획서.md:77` F-09 | 「기록 전부 삭제 + 카테고리를 기본 8종으로 재설정」 |

- **「어떤 방법으로도 복구할 수 없습니다」는 앱 자신의 복구 경로를 포함합니다.** `.bak` 이 남고 `model.js loadKeyWithRecovery` 가 그것을 읽어 되살린다면, 사용자가 읽은 문장이 **거짓**이 됩니다. 그것이 `QA-S-001` 이 S1 인 이유입니다.
- **E-코드 정의와의 관계**: `E-302` 는 「원래 내용은 지우지 않고 따로 보관해 두었습니다」라고 말합니다. 이것은 **부팅 시점에 일어난 일에 대한 과거형 서술**이며, 그 뒤 사용자가 내린 **명시적 전체 삭제 명령**보다 약합니다. 뒤에 온 더 구체적인 사용자 명령이 이깁니다(`team-rules.md:38` ③ 사용자 가치).
- **복구 경로가 사라지는 것을 어떻게 알리는가 → 이미 알리고 있습니다.** 2단계 확인 문구가 바로 그 통지입니다. **새 E-코드를 만들지 않습니다. E-코드 총수 64 불변.**
- **평상시 백업은 그대로입니다.** `§3-4` 4단계의 `.bak` 쓰기는 살아 있고, `wipeAll` 만 예외 경로를 탑니다(§3 2단계 회귀 확인 참조).

**④ 가 새로 찾은 부활 경로 1건 — `jr.v1.rollback`.**
`model.js init()` 은 부팅 때 `jr.v1.rollback` 이 있으면 **`S.restore()` 로 통째로 되돌립니다**(중단된 가져오기 트랜잭션 자동 복구). 전체 삭제가 이 키를 남기면 **다음 부팅에서 삭제한 기록이 전부 살아납니다.** ⑤ 는 `.bak`·`corrupt` 만 지목했으나 이 경로가 더 직접적입니다. 함께 닫았고 재현 스크립트에 넣었습니다.

**계약 변경 소지 → Q-091 로 ② 에 올렸습니다(진행은 막지 않음).** `INT-27(5)` 는 「`jr.v1.rollback` 은 **복구 사다리의** 어느 단계에서도 삭제 대상이 아니다」라고 적었습니다. 전체 삭제는 복구 사다리가 아니므로 문언상 위반이 아니지만, **④ 가 혼자 판정할 자리가 아니므로 ② 확인을 요청**했습니다. **Y-D.**

### 2-2. Q-066 — `verify/ok.cjs` 처분 · **판정: 기준선 산출 도구에서 폐기. 고치지 않고 내립니다**

**고치지 않고 폐기한 이유**: 이 도구의 결함은 버그가 아니라 **설계**입니다. 인자를 모르는 함수를 「미호출(인자 불명)」로 빼면서도 `개수: N` 을 출력하고, `JR.ui` 를 범위에서 뺀 사실을 출력 어디에도 적지 않습니다. 고쳐 쓰면 **같은 도구가 계속 기준선의 출처로 인용**됩니다. 실제로 `INT-34` 의 「17」이 이 도구의 부분 출력(`개수: 16`)에 손으로 1개를 보태 만들어졌습니다(② 자기 귀책 기록).

**실행한 조치 2가지**
1. **대체 도구 `verify/dev2-census.cjs` 를 새로 만들었습니다.** 표면 키를 런타임에서 열거해 **인자 표에 없는 키가 하나라도 있으면 FAIL 로 중단**하고, **범위(모듈 명단)를 출력 줄에 함께 적으며**, INT-41 명단과 **1:1 대조**합니다(숫자 비교 아님).
2. **`verify/ok.cjs` 맨 앞에 폐기 배너 5행을 붙였습니다. 실행 코드는 한 줄도 고치지 않았습니다.** ⑤ 의 「기존 19개는 손대지 마십시오」는 **회귀 판정 기준선을 바꾸지 말라는 뜻**으로 읽었고, 출력은 배너 전후가 동일합니다(`개수: 16`). ⑤ 가 `qa-lead-s2.cjs` 에 붙인 폐기 배너와 같은 방식입니다. **이의가 있으면 되돌리겠습니다 — Y-E.**

---

## 3. 순차 수정 — ⑤ 가 지정한 순서대로 · **병렬 금지 준수**

**실무자에게 분배하지 않고 파트장이 직접 순차로 고쳤습니다.** 병렬 수정이 덮어쓰기를 만든 전례가 있고, 9단계 중 6단계가 같은 파일(`model.js`·`ui.js`)을 건드리기 때문입니다. 각 단계마다 재현 스크립트를 돌려 통과를 확인한 뒤 다음으로 넘어갔습니다.

### 1단계 — `__proto__` 계열 (INT-42) · **QA-S-002(S1) · QA-S-003(S2) · QA-S-004(S2)**

**변경**: ② 가 지정한 **8개 누산기 · 9개 선언 지점 전부** + `boot.js` 1행. 읽기 코드는 한 줄도 고치지 않았습니다.

| # | 파일:행 | 누산기 | ⑤ 명단 |
|---|---|---|---|
| 1·2 | `io.js:244` | `seenE` · `seenC` | ✅ |
| 3 | `io.js:274` | `nameSeen` | ✅ |
| 4 | `model.js:376` | `seen` (`sanitizeList`) | ✅ |
| 5 | `model.js:809` | `map` (`getCategoryMap`) | ✅ |
| 6 | `stats.js:98` | `buckets` | ② 가 확대 |
| 7 | `stats.js:15`·`:18` | `statsCache` | ② 가 확대 |
| 8 | `ui.js:74` | `locks` | ② 가 확대 |
| — | `boot.js:18` | `Object.create` 가용성 검사 1행 | ② INT-42(4) |

**실행 원문** (`verify/dev2-step1-proto.cjs`):
```
PASS | ⑤-1 id='__proto__' 2건 가져오기 → 저장 건수 1 | id별건수=["__proto__:1","dup1:1","constructor:1"]
PASS | ⑤-4 대조군 dup1·constructor 는 기존대로 1건 유지 (회귀 금지) | 총 저장=3건
PASS | ⑤-5 **사용자가 고른 기록만 삭제됨 (S1 본체)** | 고른것=건강검진비 삭제전=3건 삭제후=2건 / 남은것=["dup1|대조군A","constructor|대조군C"]
PASS | ⑤-2 name='__proto__' 2건 → 카테고리 1개 | __proto__=1 커피=1 constructor=1
PASS | ⑤-4 대조군 커피·constructor 회귀 0
PASS | ⑤-3 categoryId='__proto__' → getCategoryMap() 의 프로토타입이 교체되지 않음
       | proto=null(교체 불가) · map 이 __proto__ 를 own 키로 가짐=true · 전역 오염=false
PASS | ②-INT-42(8)-1 지출 3건이 전부 __proto__ 카테고리일 때 items 길이 = 1 | 행수=1 행=["세탁/9000원/100%"]
PASS | ②-INT-42(8)-2 items[0] 금액=합계 9,000원 · 이름=「세탁」(「미분류」로 새지 않음) | 행합=9000 total=9000
PASS | ②-INT-42(8)-3 boot.js requiredApisPresent() 에 Object.create 검사가 있음
       | boot.js:18 = if (typeof Object.create !== 'function') { return false; }   /* INT-42(4) */
PASS=10 FAIL=0
```
→ **⑤ 검증 기준 5줄 + ② 가 더한 3줄 전부 충족.** `⑤-5`(사용자가 고른 기록만 삭제됨)가 S1 의 본체이며 실측했습니다.

### 2단계 — 「전체 삭제」 잔존물 (QA-S-001 · **S1**) · Q-067 판정 이행

**변경**: `model.js wipeAll()` 이 **평상시 쓰기 경로와 다른 종단 경로**를 탑니다. `S.keys()` 로 `jr.` 전 키를 **먼저 비운 뒤** 새 기본 상태를 씁니다. 주 데이터 키가 비어 있으면 `store.js writeBak` 이 `prevExp·prevCat` 둘 다 `null` 이라 **`.bak` 을 만들지 않습니다.**
**공개 API 를 늘리지 않았습니다** — `store.wipeAll` 같은 함수를 새로 내보내면 INT-41 의 표면 85·함수 75 기준선이 깨집니다. 기존 `S.keys()`·`S.removeRaw()` 만 씁니다.

**실행 원문** (`verify/dev2-step2-wipe.cjs`):
```
note | 심기: 3건 · 총합 66000원 · 삭제 전 .bak 보유 키=["jr.v1.categories.bak","jr.v1.expenses.bak"]
PASS | ⑤-1 전체 삭제 후 localStorage 전 키에 삭제한 메모 원문 0건
       | 남은 키=["jr.v1.meta","jr.v1.settings","jr.v1.categories","jr.v1.expenses"] / 메모가 남은 키=[]
PASS | ⑤-2 전체 삭제 → 메인 키 손상 → 재부팅 → 화면 0행 (되돌림 전: 3행 · 66,000원)
       | model 건수=0 총합표시="0원" 목록에 메모 노출=false
PASS | ⑤-2 총합 0원 | 총합표시="0원"
PASS | ⑤-2 전체 삭제 → 메인 키 비-배열 → 재부팅 → 부활 없음 | model 건수=0
PASS | ④ 신규 — jr.v1.rollback 잔존 경로도 닫힘 (model.init 의 트랜잭션 자동 복구가 되살리지 못함)
       | 삭제 직후 메모가 남은 키=[] · 재부팅 후 model 건수=0
PASS | ⑤-3 회귀 금지 — 평상시 저장 경로의 .bak 백업은 그대로 동작 | .bak 키=["jr.v1.categories.bak","jr.v1.expenses.bak"]
PASS | ⑤-3 회귀 금지 — 평상시 손상 복구(.bak → E-303)는 그대로 동작 | 복구된 건수=3건
PASS | 콘솔·페이지 오류 0건 | []
PASS=8 FAIL=0
```
→ ⑤ 의 3시나리오(잔존 · 메인 키 손상 부활 · 비-배열 부활) **전부 닫혔고**, **평상시 백업과 복구는 회귀 0** 입니다.

### 3단계 — `E-605` 재렌더 (S2)

**변경**: `boot.js:171` 을 고치지 않고 **`model.init()` 이 끝에 `notifyChange('bulk')` 를 부르게** 했습니다(정상·복구 두 경로 모두).
이유: `JR.ui` 에 공개 재렌더 함수를 새로 내보내면 INT-41 의 `JR.ui` 10개 기준선이 깨집니다. 부팅 시점에는 아직 구독자가 없어 무해합니다(`JR.ui.init` 이 뒤에 구독).

**실행 원문** (`verify/dev2-step3-e605.cjs` · 같은 origin 두 탭):
```
note | 탭B 초기: {"domRows":1,"listText":"아직 기록이 없어요…","total":"0원","modelCount":0}
note | 탭A 저장: {"month":"2026-08","count":3,"total":66000}
note | 탭B 복귀 후: {"domRows":3,…,"total":"66,000원","modelCount":3}
PASS | 전제 — 탭B 의 JR.model 이 3건으로 갱신됨 | model 건수=3
PASS | ⑤-3단계 DOM 3행 (되돌림 전: 0행) | DOM 행수=3
PASS | ⑤-3단계 목록에 탭A 가 저장한 기록이 실제로 그려짐
PASS | ⑤-3단계 **총합이 실제 합계와 일치** (되돌림 전: "0원") | 화면 총합="66,000원" 실제 합계=66000원
PASS | E-605 통지 자체는 그대로 뜸 (회귀 금지) | 토스트="다른 창에서 기록이 바뀌어 화면을 새로 불러왔습니다."
PASS=6 FAIL=0
```

### 4단계 — 메모 100자 **단일 기준** (`E-123` S2 + `QA-S-006` S3) · INT-43 이행

**변경 4곳** (② 가 「고치지 말라」고 한 `err.js:47`·`ui.js:40`·`ui.js:661-662`·`model.js:24·297-303` 은 **손대지 않았습니다**):
- `index.html:86` — `maxlength="100"` **속성만 삭제**. `id`·`class`·`placeholder`·`aria-describedby` 그대로.
- `ui.js:1161` — **정본 판정 지점.** 실제로 잘랐을 때만 `show('E-123')`.
- `io.js:23·184` — `MEMO_MAX = 100` · `countChars(o.memo) > 100` 이면 **레코드 거부**(② INT-43(4)-4: 잘라서 저장 금지). 기존 `rejected` → `E-409`.

**실행 원문** (`verify/dev2-step4-memo.cjs`):
```
PASS | INT-43(4)-2 index.html:86 maxlength 속성 삭제됨 | maxlength=null
note | 이모지 100자 실타이핑: {"utf16":200,"cp":100,"counter":"100/100","toast":""}
PASS | ⑤-4단계-1 이모지 100자 입력 → 100자 그대로 들어감 (되돌림 전: 50) | countChars=100
PASS | 카운터가 100/100
PASS | 100자 정확히에서는 자르지 않았으므로 E-123 을 부르지 않음 (② INT-43(4)-3)
PASS | ②-INT-43(6) 이모지 60자 시점 카운터 60/100 · 61번째가 실제로 입력됨 (되돌림 전: 50/100 에서 입력 막힘)
note | 한글 101자 실타이핑: {"cp":100,"toast":"메모는 100자까지 입력할 수 있어 뒷부분을 잘랐습니다."}
PASS | ⑤-4단계-2 101자째 입력 → 잘림 + E-123 토스트 (되돌림 전: 통지 없음)
PASS | ②-INT-43(5) 5,000자 붙여넣기(input 발생)도 그 자리에서 100 코드포인트로 잘림 + E-123
PASS | 회귀 금지 — 저장 시점 model.js 2차 방어선(E-120)이 그대로 | 101자→E-120 / 100자→통과
PASS | ⑤-4단계-3 가져오기 memo 5,000자 → 레코드 거부 (되돌림 전: 5,000자 저장) | 저장된 id=["m2","m3"]
PASS | 회귀 금지 — 정상 메모·정확히 100자 메모는 그대로 저장 | rejectedCount=1
PASS | 거부 건이 기존 rejected 경로(E-409 {count})로 셈됨 · 새 E-코드 없음
PASS=12 FAIL=0
```

### 5단계 — `E-203` `{percent}` (S3) · INT-36 이행

**변경**: INT-36 이 명시적으로 허용한 **「같은 반환 안의 `noticeParams`/동등한 키」** 형태를 택했습니다.
- `store.js writeAll` — `codes` 는 코드 문자열 배열 그대로, **`codeParams` 를 함께 반환**. `E-203` 을 담을 때 그 시점의 `u.data.ratio` 로 `{percent}` 를 실음.
- `model.js` — `toWarnings()` 로 **`warnings` 원소를 `{code, params}` 객체로** 변환(⑤ 검증 기준). `E-122` 도 객체화. `init()` 의 `notices` 는 문자열 배열 그대로 두고 `noticeParams` 에 `mergeParams()` 로 합류(부팅 경로 회귀 0).
- `ui.js:869` — `show(warnings[0].code, warnings[0].params)`.

**`err.js` 치환 엔진은 한 줄도 고치지 않았습니다.** ⑤ 가 「치환 엔진은 정상입니다」라고 판정했고, ④ 도 같은 결론입니다.

**실행 원문** (`verify/dev2-step5-e203.cjs` · **실제 UI 저장 경로**로 확인):
```
note | 저장소 사용률 = 82% · warnings = [{"code":"E-203","params":{"percent":82}}]
PASS | ⑤-5단계-2 warnings 원소 typeof === 'object' && .code 존재 (되돌림 전: "string") | types=["object"]
PASS | ⑤-5단계-1 배너에 「저장 공간을 82% 썼습니다」 (되돌림 전: 「% 썼습니다」)
       | 배너 발췌="저장 공간을 82% 썼습니다."
PASS | 빈 치환 자리가 화면으로 나가지 않음
PASS | ⑤-5단계-3 E-111·E-116·E-120·E-203·E-304·E-409 치환 회귀 0 | 치환 안 된 코드=[]
PASS | 자리표시자 보유 코드가 여전히 정확히 6개 (기준선 불변)
PASS | 회귀 금지 — E-122 도 {code,params} 로 나가고 토스트가 그대로 뜸
PASS=8 FAIL=0
```
> **④ 자신의 계기 오류 1건을 기록합니다.** 첫 실행에서 이 스크립트가 FAIL 2건을 냈는데, 원인은 코드가 아니라 **스크립트가 `JR.model.addExpense` 를 직접 불러 `ui.onSave` 를 타지 않은 것**이었습니다. 되돌림 대상인 결함이 바로 그 `ui.onSave` 전달 경로이므로, **실제 화면으로 저장하도록 고쳐** 다시 돌렸습니다. FAIL 을 코드 탓으로 돌리지 않았습니다.

### 6단계 — `E-604` 발생 지점 (S3)

**변경**: `ui.js saveDraft(leaving)` — 화면이 가려지는 순간(`visibilitychange` hidden · `pagehide`)의 실패는 **B 슬롯 배너 `E-604`**, 그 밖의 실패는 기존 `E-606` 토스트(세션당 1회) 유지. `blur`/`change` 리스너가 이벤트 객체를 넘기므로 **`leaving === true` 로만** 판정합니다.
**저장 실패는 실제 `localStorage` 할당량을 채워 만들었습니다 — 스텁·모의 없음.**

**실행 원문** (`verify/dev2-step6-e604.cjs`):
```
note | 저장소 채움: {"filledMB":5,"blocks":8,"quotaReached":true}
PASS | 전제 — 브라우저 localStorage 할당량에 실제로 도달함 (스텁 없음)
note | 가려진 순간: 배너="앱이 화면에서 벗어나는 동안 작성 중이던 내용을 저장하지 못했습니다." 토스트=""
PASS | ⑤-6단계 초안 저장 실패가 **B 슬롯 배너 E-604** 로 뜸 (되돌림 전: show('E-604') 호출 0건)
PASS | ⑤-6단계 **복귀 후에도 배너가 남아 있음** (토스트였다면 전달되지 않았을 정보)
note | 평상시 실패: saveDraft 탐침=E-606 토스트="작성 중인 내용이 자동으로 저장되지 않습니다. 저장 버튼을 눌러야 기록됩니다." 배너=""
PASS | 회귀 금지 — 가려지는 순간이 **아닌** 실패는 E-606 토스트 그대로, E-604 배너를 띄우지 않음
PASS=5 FAIL=0
```

### 7단계 — 가져오기 날짜 실재 (`QA-S-005` · S2)

**변경**: `io.js` 에 `isRealDate()` 를 두고 `RE_DATE` 통과 뒤 **달력 실재 여부**를 확인합니다. 구현은 `model.js isRealDate`(UI 경로 `E-109` 가 쓰는 바로 그 함수)와 **동일**합니다.
**④ 가 범위를 넓힌 1건**: `model.js validExpenseRecord`(부팅 복구 경로)도 **형식만 보고 있었습니다.** ⑤ 명단에는 없지만 **같은 뿌리**이고, io 만 막으면 저장소를 직접 건드린 값이 그대로 들어옵니다. 함께 고쳤고 재현 스크립트에 넣었습니다.

### 8단계 — `store.restore()` 네임스페이스 (`QA-S-007` · S3)

**변경**: 쓰기 루프에 `if (k.indexOf('jr.v1.') !== 0) { continue; }` 1행. 삭제 루프와 **대칭**을 맞춘 것뿐입니다.

### 9단계 — 내보내기 대체 영역 (`QA-F-001` · S3)

**변경**: `ui.js goScreen()` 맨 앞에서 `#jr-export-fallback` 에 `hidden` 을 다시 걸고 `#jr-export-text`·`#jr-export-notice` 를 비웁니다. `onExport` 는 `goScreen` 이 아니라 `renderS04` 로 끝나므로 **방금 누른 결과를 지우지 않습니다.**

**7·8·9단계 실행 원문** (`verify/dev2-step789.cjs`):
```
PASS | ⑤-7단계 가져오기가 달력에 없는 날짜(2/30·4/31)를 거부함 | 통과한 날짜=[] / 저장된 것=["2024-02-29","2026-08-10"]
PASS | 회귀 금지 — 윤년 2024-02-29 와 평범한 날짜는 그대로 통과
PASS | 거부 2건이 기존 rejected 경로(E-409)로 셈됨 · 새 E-코드 없음 | rejectedCount=2
PASS | ④ 범위 확대 — 부팅 복구 경로(model.js sanitizeList)도 같은 기준을 적용 | 부팅 후 날짜=["2026-08-10"]
PASS | ⑤-8단계 restore() 가 jr.v1. 밖 키를 쓰지 않음 (되돌림 전: 전=null → 후='침입값') | 전=null 후=null
PASS | 회귀 금지 — jr.v1. 키 복원은 그대로 동작
PASS | 회귀 금지 — snapshot→restore 왕복(가져오기 되돌리기)이 그대로 동작 | 삭제 후=0건 · 복원 후=1건
note |   ① S-04 진입(누르기 전)   hidden=true  높이=0   JSON길이=0
note |   ② 내보내기 누른 직후      hidden=false 높이=309 JSON길이=792
note |   ③ S-01 로 이탈           hidden=true  높이=0   JSON길이=0
note |   ④ S-04 재진입 (안 눌렀음) hidden=true  높이=0   JSON길이=0
PASS | ① 누르기 전에는 접혀 있음
PASS | ② 내보내기를 누르면 열리고 내용이 들어감 (회귀 금지)
PASS | ③ 다른 화면으로 나가면 접히고 DOM 잔존도 사라짐
PASS | ⑤-9단계 **④ 재진입 시 누르지도 않은 기록 JSON 전문이 뜨지 않음** (되돌림 전: hidden=false·높이 309·JSON 788자)
PASS=12 FAIL=0
```

---

## 4. 결함 12건 개별 판정표

| ID | 심각도 | 판정 | 근거 (재현 스크립트) |
|---|---|---|---|
| **QA-S-001** | S1 | **고침** | `dev2-step2-wipe.cjs` 8/8 — 3시나리오 + rollback 경로 + 회귀 2건 |
| **QA-S-002** | S1 | **고침** | `dev2-step1-proto.cjs` — 「사용자가 고른 기록만 삭제됨」 실측 |
| **QA-S-003** | S2 | **고침** | `dev2-step1-proto.cjs` — `__proto__=1 커피=1 constructor=1` |
| **QA-S-004** | S2 | **고침** | `dev2-step1-proto.cjs` — `proto=null(교체 불가)` · 전역 오염 false |
| **QA-S-005** | S2 | **고침** | `dev2-step789.cjs` — 가져오기 + **부팅 복구 경로까지** |
| **E-605** | S2 | **고침** | `dev2-step3-e605.cjs` — DOM 3행 · 총합 66,000원 |
| **E-123** | S2 | **고침** | `dev2-step4-memo.cjs` — 이모지 100자 통과 · 101자째 잘림+토스트 |
| **QA-S-006** | S3 | **고침** | `dev2-step4-memo.cjs` — 5,000자 레코드 거부 |
| **QA-S-007** | S3 | **고침** | `dev2-step789.cjs` — 전=null 후=null |
| **E-203** | S3 | **고침** | `dev2-step5-e203.cjs` — 배너 「저장 공간을 82% 썼습니다」 |
| **E-604** | S3 | **고침** | `dev2-step6-e604.cjs` — 실제 quota 도달 · 복귀 후에도 배너 잔존 |
| **QA-F-001** | S3 | **고침** | `dev2-step789.cjs` — 재진입 hidden=true·JSON 0자 |

**「결함 아님」으로 분류한 것: 0건.** ⑤ 가 지목한 12건은 ④ 가 전건 재현했고 전건 고쳤습니다.
**`ui.unlock` 은 12건에 없습니다.** ⑤·② 지시대로 **건드리지 않았습니다.**

---

## 5. ④ 가 범위를 넓힌 것 — **2건 · 숨기지 않습니다**

| # | 무엇 | 왜 | 위험 |
|---|---|---|---|
| 1 | `model.js validExpenseRecord` 에도 날짜 실재 검증 | `io.js` 만 막으면 저장소를 직접 건드린 값이 부팅 경로로 그대로 들어옵니다. **같은 뿌리** | 달력에 없는 날짜를 가진 기존 기록이 있으면 부팅 때 제외되고 `E-304` 가 뜹니다. 그런 기록은 애초에 도달 불가 데이터입니다 |
| 2 | `wipeAll` 이 `jr.v1.rollback` 까지 정리 | `model.init` 이 그 키를 읽어 **삭제한 기록을 통째로 되살립니다**. ⑤ 명단(`.bak`·`corrupt`)보다 직접적 | INT-27(5) 해석 → **Q-091** 로 ② 확인 요청 |

---

## 6. 🟡 ④ 가 새로 찾은 결함 1건 — **고치지 않았습니다** (Y-A)

### `E-304` 의 `{count}` 가 빈칸으로 화면에 나가는 경로

**재현** (전체 삭제와 무관 · 신규 설치에서 바로 재현됩니다):
```
$ (신규 설치 상태) localStorage['jr.v1.expenses'] = '{"not":"array"}'  ·  .bak 없음  ·  재부팅
부팅 전 키: ["jr.v1.meta","jr.v1.settings","jr.v1.categories","jr.v1.expenses"]
배너: "저장된 기록 가운데 건을 읽을 수 없어 제외했습니다. 나머지 기록과 금액은 그대로입니다."
model 건수: 0
```

- **원인**: `model.js loadKeyWithRecovery` 의 「값이 배열이 아님」 분기가 `notices.push('E-304')` 를 **params 없이** 합니다. `.bak` 이 있으면 `E-303` 이 배너 우선순위에서 이겨 가려지지만, **`.bak` 이 없으면 그대로 화면에 나갑니다.**
- **INT-36 위반**: 「화면에 빈 치환 자리가 나가는 경로가 **하나라도** 남으면 미충족입니다.」
- **④ 가 만든 것이 아닙니다.** 해당 함수는 `675840e` 와 **바이트 단위로 동일**하며 ④ 는 한 줄도 고치지 않았습니다. 위 재현에 `wipeAll` 이 들어가지 않는 것이 증거입니다. ⑤ 의 자리표시자 전수 검증(`qa-ph.cjs`)이 이 경로를 놓친 것은, 그 시험이 `err.get('E-304',{count:5})` 라는 **API 수준**만 봤기 때문입니다.
- **왜 ④ 가 고치지 않았는가**: 고치는 방법이 둘뿐인데 **둘 다 ② 소관**입니다.
  ⓐ 그 자리의 `E-304` 를 빼면 → **키 전체 손실 + 백업본 없음** 상황을 사용자에게 **아무것도 알리지 않게** 됩니다(가계부에서 조용한 데이터 손실).
  ⓑ 숫자를 채우려면 → 셀 수 있는 건수가 **존재하지 않습니다**(값이 배열이 아니므로). 없는 숫자를 지어내는 것은 `team-rules.md` 기질 2 위반입니다.
  → 필요한 것은 **「이 키를 통째로 읽지 못해 기본값으로 시작합니다」를 알리는 코드**이고, 그것은 **새 E-코드 = E-코드 총수 64 변경 = ② 계약**입니다.
- **→ Q-093 으로 ② 에 올렸습니다. ⑤ 재시험에서 나올 것이므로 ④ 가 먼저 적어 둡니다.**

---

## 7. 🟡 기존 검증 스크립트와 이번 계약의 충돌 — **8건** (Y-B)

**⑤ 재시험 전에 반드시 읽어 주십시오.** 아래는 **회귀가 아니라 단언 충돌**입니다. ④ 는 ⑤ 의 지시대로 기존 스크립트를 고치지 않았습니다.

기준선 비교 방법: `git archive HEAD` 로 **되돌림 전 `src/` 사본**을 따로 만들고, 같은 스크립트를 경로만 바꿔 **양쪽에서 각각 실행**해 대조했습니다.

| 스크립트 | 단언 | 되돌림 전 | 조치 후 | 성격 |
|---|---|---|---|---|
| `qa-lead-s2b.cjs:24` | `getPrototypeOf(map) === Object.prototype` | FAIL(오염됨) | **FAIL(`null`)** | **INT-42 와 정면 충돌.** 계약대로 고치면 `null` 이 정답 |
| `qa-lead-s2.cjs` (⑤ 가 이미 폐기 표시) | 동일 + 메모 길이 | FAIL | FAIL | 동일 |
| `qa-sec-proto.cjs` | 동일 | FAIL | **FAIL(`null`)** | 동일 |
| `qa-sec-field.cjs` | 가져오기 memo 가 **잘려서** 저장 | FAIL(300,000자) | **FAIL(레코드 거부 → 없음)** | **INT-43(4)-4 와 충돌.** ② 가 「잘라서 저장하지 마십시오」로 확정 |
| `qa-sec-integrity.cjs` | 「중복 id 2건이 목록에 **2줄로 표시된다(결함 재현)**」 | PASS(결함 있음) | **FAIL(결함 없어짐)** | 결함이 있어야 통과하는 단언 |
| `qa-lead-s1.cjs:111` | 두 번째 `__proto__` 레코드가 존재한다고 가정 | 통과 | **CRASH** | 동일. `S-002 ①·②` 단언 자체는 그 전에 **PASS** |
| `qa-exc.cjs` (3건) · `qa-lead-func.cjs` (2건) | `warnings` 를 **문자열 배열**로 탐색 / `show('E-203')` 을 **params 없이** 부르고 숫자가 채워지길 기대 | FAIL | FAIL | **INT-36 및 ⑤ 자신의 수정요청서와 충돌.** ⑤ §2 5단계: 「치환 엔진은 정상입니다. `params` 전달 경로만 빠졌습니다」 |
| `qa-extra.cjs` | 비-`{ok}` **INT-34 17개** 명단 | FAIL | FAIL | **INT-41 로 폐기된 기준선.** 기존 실패 |

**되돌림 전과 조치 후가 똑같이 실패하는 것 = 이번 조치와 무관한 기존 실패** (④ 가 양쪽 실행으로 확인):
`a11y.cjs`(44×44 파일 입력 1건) · `exc2.cjs`(예외6 초안 복원 1건) · `func2.cjs`(F-06 3건·F-07 2건) · `int32.cjs`(E-413 경로 1건) · `qa-sec-store.cjs`(CSP = QA-S-008 S4 · Q-090) · `perf*.cjs`(성능 관측치).

**조치로 새로 통과하게 된 기존 스크립트 단언**: `qa-lead-func.cjs` 「INT-10/39 S-04 재진입 시 대체 영역이 접혀 있고 비어 있음」(FAIL→PASS) · `qa-ph.cjs` 「warnings 원소가 `{code,params}` 객체인가」(FAIL→PASS) · `qa-sec-field.cjs` 날짜 2건(FAIL→PASS) · `qa-sec-integrity.cjs` 3건(FAIL→PASS) · `qa-lead-s001-revive.cjs` 3시나리오 전부(부활 → 부활 없음).

---

## 8. 재검사 조건 (⑤ 통합요청서 §5) 대조

| # | ⑤ 요구 | ④ 이행 |
|---|---|---|
| 1 | 9단계 전부 조치 | ✅ 9/9 · 12결함 전건 |
| 2 | `git diff` 로 변경 파일·줄 명시 | ✅ §9 |
| 3 | **각 단계 검증 기준을 ④ 가 직접 돌린 실행 로그** | ✅ §1·§3 에 **원문** |
| 4 | `verify/` 기존 19개 + `qa-*.cjs` 20개 전건 재실행 | ✅ **56개 전건 실행.** 결과·분류 §7 |
| 5 | `TODO`·`FIXME` 0건 유지 | ✅ 아래 |

```
$ grep -rn "TODO\|FIXME\|XXX\|HACK" src/     → 0건
$ grep -rn "innerHTML" src/                  → 0건
$ grep -rEn "https?://" src/
  src/js/ui.js:9:  var SVGNS = 'http://www.w3.org/2000/svg';
```
> `SVGNS` 는 `createElementNS` 에 넘기는 **네임스페이스 문자열**이며 네트워크 요청이 아닙니다. `675840e` 와 동일하고 이번 회차에서 고치지 않았습니다.

**통합 후 직접 실행 검증** (`verify/dev2-smoke.cjs`):
```
note | 지나간 단계: S-01 진입 → 지출 3건 저장 → 기록 편집 → 월 이동 ←→ → S-03 통계 → S-04 설정
       → 카테고리 추가 → 내보내기 → S-01 왕복 → 재부팅 → 전체 삭제
note | 최종 상태: {"screen":"s01","count":0,"cats":8,"keys":["jr.v1.categories","jr.v1.expenses","jr.v1.meta","jr.v1.settings"]}
PASS | 전체 삭제 후 기록 0건 · 기본 카테고리 8개
PASS | 전체 삭제 후 남은 키가 주 데이터 4개뿐 (.bak·corrupt·rollback·rejected·draft 0건)
PASS | **콘솔 에러 0건** (전체 흐름 11단계) | []
PASS=3 FAIL=0
```

---

## 9. 변경 목록 (`git diff --stat` 원문)

```
 ai-team/apps/2026-08-11/src/index.html  |  2 +-
 ai-team/apps/2026-08-11/src/js/boot.js  |  1 +
 ai-team/apps/2026-08-11/src/js/io.js    | 21 ++++++++++--
 ai-team/apps/2026-08-11/src/js/model.js | 61 ++++++++++++++++++++++++++++-----
 ai-team/apps/2026-08-11/src/js/stats.js |  8 +++--
 ai-team/apps/2026-08-11/src/js/store.js | 13 +++++--
 ai-team/apps/2026-08-11/src/js/ui.js    | 39 +++++++++++++++++----
 7 files changed, 121 insertions(+), 24 deletions(-)
```

| 파일 | 단계 | 바뀐 자리 |
|---|---|---|
| `index.html` | 4 | `86` `maxlength="100"` 삭제 |
| `boot.js` | 1 | `18` `Object.create` 가용성 검사 1행 추가 |
| `io.js` | 1·4·7 | `23` `MEMO_MAX` · `30-40` `isRealDate` · `184` 날짜 · `187` memo 상한 · `244`·`274` 누산기 |
| `model.js` | 1·3·5·7 | `174-194` `toWarnings`·`mergeParams` · `316` `E-122` 객체화 · `347` 날짜 · `376`·`809` 누산기 · `600`·`607` `mergeParams` · `614-618`·`633` `notifyChange` · `680`·`709` `toWarnings` · `1017-1046` `wipeAll` |
| `stats.js` | 1 | `15`·`18` `statsCache` · `98` `buckets` |
| `store.js` | 5·8 | `247-250` `codeParams` · `298-303` `E-203` params · `372` 네임스페이스 |
| `ui.js` | 1·5·6·9 | `74` `locks` · `498-508` `goScreen` 대체 영역 · `812-828` `saveDraft(leaving)`·`E-604` · `869` `warnings[0].code` · `1161-1166` `E-123` · `1223`·`1226` `saveDraft(true)` |

**신규 파일 (전부 `verify/` · 기존 스크립트 무수정)**
`dev2-lib.cjs` · `dev2-census.cjs` · `dev2-int42-43-review.cjs` · `dev2-step1-proto.cjs` · `dev2-step2-wipe.cjs` · `dev2-step3-e605.cjs` · `dev2-step4-memo.cjs` · `dev2-step5-e203.cjs` · `dev2-step6-e604.cjs` · `dev2-step789.cjs` · `dev2-smoke.cjs`
**수정 1건**: `verify/ok.cjs` — 폐기 배너 5행. **실행 코드 0줄 변경**(Y-E).

---

## 10. ④ 가 하지 않은 것 · 확인하지 못한 것

- **3엔진 검증**: Chromium 만 돌렸습니다. Firefox·WebKit 은 **확인 불가 — 사유: 이번 회차 범위.** ⑤ 의 3엔진 배분이 정본입니다.
- **실기기·스크린리더·실제 다운로드 폴더**: **미수행.**
- **S4 3건**(`QA-S-008` CSP · `QA-F-003` 성능 · `QA-F-004` 두 창): ⑤ 지시대로 **손대지 않았습니다.**
- **`Q-087`(가져오기 날짜 관대함) · `Q-089`(`restore` 네임스페이스)** 는 ② 미판정입니다. ④ 는 ⑤ 통합요청서 7·8단계 지시를 이행했을 뿐이며, ② 가 다르게 판정하면 되돌려야 합니다.
- **④↔⑤ 반복은 이번이 2회차입니다. 다음이 마지막입니다.**
