# 감사 보고서 — 3차 4건 (D-030 · D-031 · D-032 · D-035)

- 감사관: `team-master` (독립 · 마스터까지 감사)
- 일자: 2026-08-30
- 대상: **B-07 일괄 감사 12건 중 3차 4건.** 1·2차 8건은 별도 호출이 다루므로 건드리지 않았습니다
- 근거 원칙: `.claude/team-rules.md` (최우선 정본) · `.claude/team-org.md`
- 이 감사는 **D-014 요건 ③** 에 해당합니다

```
판정: ❌ 반려
치명: 3건  중대: 5건  경미: 3건
판본 드리프트 검사: 4/6
```

## 건별 판정 — 곁가지가 사이클을 막지 않게 분리했습니다

| 건 | 판정 | 사유 |
|---|---|---|
| **D-030** 박수 감지기 · 음성 응답 루프 | ⚠️ **조건부 승인** | 핵심 주장(8종 검증)을 **감사관이 재현 확인**. 문서 인자 오기(중대-4)만 고치면 됩니다 |
| **D-031** 음성 브리핑 콘솔 | ⚠️ **조건부 승인** | 한계 서술이 페이지 안에 있음. 박아 넣은 수치가 낡음(경미-2) |
| **D-032** 상주 프로그램 단일화 | ⚠️ **조건부 승인** | 흡수·삭제 완료 확인. 배너에 옛 호칭 잔존(중대-1) |
| **D-035** 슈퍼베이스 팀 복원 | ❌ **반려** | 치명-2·치명-3 |
| **회의실 기록 경로** (D-037 시정 잔여) | ❌ **반려** | 치명-1 |

> 🟡 **곁가지 판단**: 음성 3건(D-030·D-031·D-032)의 결함은 **사이클을 막지 않습니다.**
> 반려의 사유는 전부 **팀 운영 핵심**(창 간 복원 · 회의실 기록)입니다.

---

## [치명-1] D-037 의 기록 경로 시정이 **회의실 스킬에 반만 적용**됐습니다 — 키 없으면 아무 데도 안 남습니다

- 근거: `.claude/skills/meeting-room/SKILL.md:118` — 「라운드마다 `supabase-sync.py event --kind 호출` 로 남깁니다」
- 근거: `.claude/skills/meeting-room/SKILL.md:149` — 「파트장·실무자 호출 완료 | `supabase-sync.py event --kind 호출`」
- 대조: **같은 파일** `.claude/skills/meeting-room/SKILL.md:156-159` — 「`event.sh` 가 `ai-team/events.log` 에 먼저 쓰고 … 2026-08-30 감사 치명-2 로 순서를 뒤집었습니다」
- 정본: `.claude/team-rules.md:104-116` 「**기록 경로 — 파일이 1차, 외부 저장소는 2차**」

**실측** (키 없는 이 컨테이너에서 직접 호출):
```
$ python3 ai-team/scripts/supabase-sync.py event --kind 호출 --title "테스트"
✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다.
종료코드 1        ← events.log 에도, 슈퍼베이스에도 아무것도 남지 않습니다
```

무엇이 문제인가: `:118` 은 **교차 논쟁**(D-028)의 라운드 기록 지시입니다. 교차 논쟁은
`team-rules.md:90-102` 가 정본으로 끌어올린 규정이고, 그 정본은 기록 수단을 「아래 기록 경로」
= `event.sh` 로 지정합니다. 스킬이 정본과 다른 수단을 지시하고 있으므로 **최우선 정본 우선순위상
`:118`·`:149` 의 지시는 무효**이며, 무효인 지시를 따르면 **기록이 통째로 사라집니다.**
D-021·D-028 이 「규정만 있고 실행 0건」이 된 원인이 정확히 이것이고(`decisions.md:50`),
D-037 은 그것을 고치려고 만들어졌습니다(`decisions.md:85-101`). **§4 표 아래 코드블록만 고치고
§교차 논쟁과 §4 표 첫 줄은 그대로 뒀습니다** — 1·2차 감사 치명-3 과 같은 「지목된 한 건만 고침」입니다.

- 요구: `.claude/skills/meeting-room/SKILL.md:118`·`:149` 를 `ai-team/scripts/event.sh <종류> <발언자> "<제목>" [참조]` 로 바꾸십시오.
- 요구: 같은 파일에서 `supabase-sync.py event` 를 **직접** 부르라는 지시가 더 없는지 전수 확인하고, 있으면 전부 바꾸십시오.
- 요구: `check-selftest.py` 의 CASES 에 「회의실 스킬이 event.sh 대신 supabase-sync.py event 를 지시」 회귀를 추가하십시오. 지금 점검기는 이 회귀를 못 잡습니다.

---

## [치명-2] D-035 — `BOOTSTRAP.md` 붙여넣기 덩어리가 정본 스크립트의 안전장치 3종을 **전부** 결여합니다 (D-012 중복 정의)

- 근거: `ai-team/BOOTSTRAP.md:24-48` (붙여넣기 덩어리)
- 대조: `ai-team/scripts/supabase-sync.py:326-360` (`cmd_restore`) · `:288-299` (`audit`)
- 대조: `ai-team/decisions.md:154` — D-035 가 스스로 적은 원칙 「`restore` 는 기본이 **예행**입니다. 정본은 저장소이므로 **로컬을 말없이 덮지 않습니다**」

| 안전장치 | `supabase-sync.py restore` | `BOOTSTRAP.md` 덩어리 |
|---|---|---|
| 불완전한 원격 거부 | ✅ `:333-336` 「반쯤 복원된 팀이 더 위험합니다」 | ❌ 없음. 다 쓰고 **나서** 경고만 (`:48`) |
| 기본 예행(dry-run) | ✅ `:351-354` | ❌ 없음. 즉시 덮어씀 (`:43`) |
| BOM · 첫 줄 `---` 검사 | ✅ `:288-296` | ❌ 없음 |
| 무엇이 바뀌는지 사전 표시 | ✅ `:344-350` | ❌ 없음 |

그리고 `ai-team/BOOTSTRAP.md:5` 는 「새 대화창에 저장소가 **있든 없든**」이라고 안내하며,
덩어리 `:24-28` 은 `git rev-parse --show-toplevel` 로 **기존 저장소 루트를 찾아 거기에 씁니다.**

**실측** (감사관이 가짜 원격 + 저장소가 **있는** 창을 만들어 직접 실행):
```
=== A. supabase-sync.py restore --force  (정본 스크립트) ===
  에이전트 0 / 31 · 🔴 없음: .claude/team-org.md …
  ✗ 불완전한 원격으로는 복원하지 않습니다. 반쯤 복원된 팀이 더 위험합니다.   종료코드 1
  로컬 규정 파일: '★ 로컬의 최신 규정 — 아직 push 안 함 ★'      ← 지켜졌습니다

=== B. BOOTSTRAP.md 붙여넣기 덩어리 (같은 원격 · 같은 창) ===
  ✓ 2개 파일 복원 · 에이전트 0명 → /tmp/repo-exists-…            종료코드 0
  로컬 규정 파일: '원격의 낡은 규정'                              ← 말없이 덮였습니다
  로컬 상태 파일: '원격의 낡은 상태'                              ← 말없이 덮였습니다
```

무엇이 문제인가: **정본은 저장소**입니다(`decisions.md:501`, `supabase.md:19`). 아직 push 하지 않은
로컬 최신본이 있는 창에서 이 덩어리를 붙여넣으면 **경고도 예행도 없이 원격의 낡은 사본으로 되돌아갑니다.**
08-15 병합 사고에서 가장 위험했던 동작이라 `pull` 에까지 `--force` 를 요구해 두고(`supabase.md:95-96`),
정작 「이게 전부입니다」라고 안내하는 경로에는 그 방어가 없습니다.

`selftest-supabase.py:120-130` 은 이 덩어리를 **빈 임시 디렉터리**에서만 돌립니다(`:120` `tempfile.mkdtemp`).
따라서 이 결함은 **자체 시험으로 영원히 잡히지 않습니다** — 시험 범위를 밝히지 않은 채 「같은 시험에 포함」
(`STATE.md:91`)이라고 적은 것이 2차 감사에서 지적된 기전 그대로입니다.

- 요구: 덩어리에 ① 에이전트 31명·필수 파일 확인을 **쓰기 전에** 넣고 미달이면 중단, ② 기본 예행 + `FORCE=1` 같은 명시적 동의, ③ 기존 파일과 내용이 다른 것의 목록 출력 — 셋을 넣으십시오.
- 요구: 또는 중복 정의를 없애십시오(D-012 본래 해법) — 덩어리가 `supabase-sync.py` 한 파일만 내려받아 그것을 실행하게 하면 로직이 한 곳이 됩니다.
- 요구: `selftest-supabase.py` 에 **저장소가 있는 창** 시나리오를 추가하고, 로컬 파일이 보존되는지 대조하십시오.

---

## [치명-3] D-023/D-035 — 「실제 슈퍼베이스 왕복을 실측했다」와 「한 번도 못 했다」가 같은 저장소에 함께 있습니다

**「했다」 쪽**
- `ai-team/supabase.md:104-113` — 「검증 기록 (2026-08-29, **실측**)」 표: 테이블 생성 ✅ · `push` ✅ 8/8 · `status` ✅ 8/8 일치 · `pull` ✅ · `event` ✅ · 뷰 `team_now` ✅ · `DELETE` 204/삭제 0건
- `ai-team/decisions.md:519-521` (D-023 상세) — 「**검증 완료 (2026-08-29 실측)**: 사용자가 `schema.sql` 을 실행한 뒤 왕복으로 확인했습니다」
- `ai-team/STATE.md:87` — 「✅ **왕복 실측 완료** (push 8/8 · status 일치 · pull 차이 0 · event · 뷰 조회)」

**「못 했다」 쪽**
- `ai-team/STATE.md:92` — 「🔴 **B-08 — 이 컨테이너에 키가 없어 실제 업로드는 아직 못 했습니다**」 ← **같은 파일 5줄 아래**
- `ai-team/decisions.md:162` (D-035) — 「🔴 **실제 업로드는 하지 못했습니다.**」
- `ai-team/board.md:22` · `ai-team/SESSION-LOG.md:80` — 「가짜 서버로만 실측했습니다」
- `ai-team/decisions.md:50` (D-021) — 「`supabase-sync.py event` 가 **키 부재(B-08)로 실행 즉시 실패**합니다」
- 감사관 실측: `ai-team/supabase/` 에 `.env` 없음(`.env.example`·`schema.sql` 뿐) · 환경변수 없음

무엇이 문제인가: 두 서술은 **동시에 참일 수 없습니다.** 어느 쪽이 참이든 하나는 **미완을 완료로 적은 것**입니다.
그리고 이것은 취향 문제가 아닙니다 — `push --team` 의 upsert 가 `Prefer: resolution=merge-duplicates`
만으로 `on_conflict` 없이 실제 PostgREST 에서 도는지, RLS 정책이 `insert`+`update` 둘 다 필요한 upsert 를
통과시키는지(`schema.sql:80-86`), 페이지네이션이 실제 응답에서 맞는지는 **가짜 서버로는 증명되지 않습니다.**
`selftest-supabase.py:24-56` 의 가짜 핸들러는 `Prefer` 헤더를 읽지도, `select=` 를 존중하지도 않습니다.
「✅ 완전」(`supabase-sync.py:319`)은 그 위에서 나온 판정입니다.

- 요구: 어느 쪽이 사실인지 **증거와 함께** 확정하십시오. 「했다」가 맞다면 `team_events` 에 남아 있을 2건과 그 시각을 근거로 제시하고 B-08 문구를 「지금 이 체크아웃에 키가 없다」로 정정하십시오. 「못 했다」가 맞다면 `supabase.md:104-113` 표와 `decisions.md:519-521` 을 **「가짜 서버 실측」으로 정정**하고 D-023 을 미완으로 되돌리십시오.
- 요구: 어느 경우든 `STATE.md:87` 과 `:92` 중 하나는 지워야 합니다. 지금은 같은 화면에 모순 두 줄이 있습니다.
- 요구: 실제 왕복이 아직이라면, ✅ 를 쓰지 말고 **「가짜 서버 한정 — 실제 PostgREST 미검증」** 을 표의 모든 행에 붙이십시오.

---

## [중대-1] D-034 의 「살아 있는 코드에서 옛 호칭 0건」이 거짓입니다 — 부팅 화면 첫 줄이 `J A R V I S` 입니다

- 근거: `ai-team/scripts/master-listen.py:189-193`
```python
BANNER = r"""
   ┌─────────────────────────────────────────────┐
   │   J A R V I S                               │
   │   AI 앱 개발팀 · 총괄                        │
```
- 노출 경로: `boot_check()` `:196-197` 이 맨 먼저 출력 → `--setup`(`:221`)·`--tune`·기동 **모든 실행**의 첫 화면
- 거짓이 된 주장: `ai-team/decisions.md:198` — 「살아 있는 문서·코드에서 `jarvis`·`자비스` **0건** (역사 기록 `decisions.md` 제외)」
- 점검기가 놓친 이유: `ai-team/scripts/team-check.sh:98` `-iname '*jarvis*'` (파일명만) · `:101` `grep -i 'jarvis\|자비스'` — **자간이 벌어진 `J A R V I S` 는 어느 패턴에도 걸리지 않습니다.** 그래서 `team-check.sh` 는 지금도 「✅ 통과」를 냅니다 (감사관 실행 확인)

D-034 의 「0건」은 **grep 결과를 그대로 옮긴 완료 주장**이었고, grep 의 범위를 밝히지 않았습니다.
1·2차 감사에서 반복 지적된 기전과 동일합니다.

- 요구: `master-listen.py:191` 을 「M A S T E R」 또는 「마스터」로 바꾸십시오.
- 요구: `team-check.sh:98`·`:101` 패턴에 자간 표기(`[Jj][ ._-]*[Aa][ ._-]*[Rr]…`)와 「자빗스」를 추가하십시오 (2차 감사 경미-3 이 이미 요구한 항목입니다 — **아직 반영되지 않았습니다**).
- 요구: `check-selftest.py` 에 「배너에 옛 호칭 부활」 회귀를 추가하십시오.

## [중대-2] D-035 의 복원 범위가 어디에도 적혀 있지 않고, 규정이 정한 **1차 기록처**가 범위 밖입니다

- 근거: `ai-team/scripts/supabase-sync.py:33-38` `TEAM_GLOBS`
- 범위 밖(감사관 전수 대조): `ai-team/events.log` · `ai-team/docs/*.md` · `ai-team/console/master-console.html` · `ai-team/apps/**` · `ai-team/archive/*.md` · `ai-team/voice-out/*`
- 그런데: `ai-team/scripts/supabase-sync.py:319` 는 「**✅ 완전** — 어느 창에서든 restore 하나로 팀이 나옵니다」를 찍습니다
- 정본: `.claude/team-rules.md:110` — `ai-team/events.log` 가 **1차 기록처** · `.claude/team-org.md:106-111` — `ai-team/apps/<날짜>/` 가 **모든 앱 산출물의 위치**

저장소가 통째로 없는 창(BOOTSTRAP 경로)은 팀 31명과 규정은 받지만 **회의실 이벤트 기록도, 앱 산출물도,
감사 보고서도 받지 못합니다.** 지금 `apps/` 가 비어 있어(D-024 초기화) 피해가 없을 뿐입니다.

- 요구: `TEAM_GLOBS` 에 `ai-team/events.log` 를 넣거나, 넣지 않을 이유를 `supabase.md` 에 명시하십시오.
- 요구: `supabase.md` 와 `BOOTSTRAP.md` 에 **「복원되지 않는 것」 목록**을 적으십시오. `verify` 의 「✅ 완전」 문구도 「팀 정의·상태 파일 한정」으로 좁히십시오.

## [중대-3] `event.sh` 의 `논쟁` 종류가 슈퍼베이스에 **영구히** 들어갈 수 없습니다

- 근거: `ai-team/scripts/event.sh:15`·`:19` — 허용 종류에 `논쟁` 포함
- 근거: `ai-team/scripts/supabase-sync.py:50` — `KINDS = ["호출","승인","반려","결정","막힘","기록","동기화","감사"]` — **`논쟁` 없음**
- 근거: `ai-team/supabase/schema.sql:32` — `check (kind in ('호출','승인','반려','결정','막힘','기록','동기화','감사'))` — **`논쟁` 없음**
- 실측:
```
$ python3 ai-team/scripts/supabase-sync.py event --kind 논쟁 --title x
error: argument --kind: invalid choice: '논쟁'
```
1차(파일)는 남지만 2차는 **키가 생겨도 실패**합니다. 하필 교차 논쟁(D-028)은 `team-rules.md:90-102` 가
정본으로 승격한 규정입니다.

- 요구: `supabase-sync.py:50` 의 `KINDS` 와 `schema.sql:32` 의 check 제약에 `논쟁` 을 추가하고, 스키마를 다시 적용해야 함을 `supabase.md` 에 적으십시오. (제약 변경이므로 `alter table … drop constraint … add constraint` 가 필요합니다 — `create table if not exists` 만으로는 기존 테이블에 반영되지 않습니다.)

## [중대-4] D-030 — `voice.md` 가 **존재하지 않는 인자 3종**을 안내합니다 (같은 저장소의 다른 안내서와 모순)

- 근거: `ai-team/voice.md:50` — `master-listen.py --setup` 을 「감지만 (키 안 누름)」으로 설명 → 실제 `--setup` 은 마이크·음성 점검(`master-listen.py:306`), 「감지만」은 `--quiet`(`:312`)
- 근거: `ai-team/voice.md:51` — `--key ctrl+shift+v` → 실제 인자는 `--hotkey`(`master-listen.py:309`)
- 근거: `ai-team/voice.md:66`·`:70` — `--test` → **존재하지 않습니다**
- 대조: `ai-team/음성-설치.md:96·115·124·161·183-189` 는 `--setup`·`--tune`·`--hotkey`·`--list`·`--quiet` 로 **정확**합니다
- 역사 기록 `ai-team/decisions.md:333` 의 「`--calibrate` → `--test` → `--key`」 는 `clap-listen.py` 시절 인자로, D-034 원칙상 소급 수정 대상이 아닙니다. **문제는 살아 있는 안내서 `voice.md` 입니다.**

사장님이 `voice.md` 대로 치면 argparse 오류가 납니다. D-032 가 두 파일을 하나로 합치면서
인자 체계를 바꿨는데 **안내서 두 개 중 하나만 갱신**한 것입니다.

- 요구: `ai-team/voice.md:48-51`·`:66`·`:70` 을 실제 인자로 고치십시오.
- 요구: `master-listen.py --help` 출력과 두 안내서를 대조하는 검사를 `team-check.sh` 에 넣으십시오. 문서가 코드보다 늦는 것이 이 팀의 반복 유형입니다.

## [중대-5] D-035 — 삭제가 원격에 전파되지 않고, `verify` 는 잔존물을 못 봅니다

- 근거: `ai-team/scripts/supabase-sync.py:196-231` (`cmd_push`) — upsert 전용. tombstone·prune 없음
- 근거: `ai-team/scripts/supabase-sync.py:288-299` (`audit`) — **에이전트 개수와 필수 11파일만** 봅니다. 예상 밖의 키는 검사하지 않습니다
- 관련 사실: D-032 가 `clap-listen.py` 를 **삭제**했고(`decisions.md:303`), D-034 가 6개 파일을 **개명**했습니다(`decisions.md:177-182`)

한 번이라도 옛 이름으로 push 된 뒤 개명·삭제가 일어나면, 원격에 **죽은 키가 남고 `restore` 가 되살립니다.**
에이전트 파일은 31명 계수(`:302`)에 걸리지만 **스크립트·문서는 안 걸립니다.** `verify` 는 그래도 「✅ 완전」입니다.
지금 피해가 0인 유일한 이유는 **아직 한 번도 실제로 push 하지 않았기 때문**(B-08)입니다 — 우연입니다.

- 요구: `push --team` 에 원격 잔존 키 보고(또는 `--prune`)를 넣으십시오.
- 요구: `audit()` 에 「기대 목록에 없는 `.claude/`·`ai-team/scripts/` 키」 경고를 추가하십시오.

## [경미-1] D-030·D-032 의 「합성 신호 8종 전항 통과」 — **주장은 사실이나 재현 수단이 없습니다**

감사관이 `Detector`(`master-listen.py:129-171`)를 직접 불러 8종을 재구성해 실행했습니다.

| # | 신호 | 기대 | 실측 |
|---|---|---|---|
| 1 | 정상 2회 (0.3s) | 발동 | ✅ 발동 |
| 2 | 1회만 | 미발동 | ✅ 미발동 |
| 3 | 간격 2초 | 미발동 | ✅ 미발동 |
| 4 | 3연타 | 1회만 | ✅ 1회 |
| 5 | 지속음 3초 | 미발동 | ✅ 미발동 |
| 6 | **말끝 + 박수 1회** | 미발동 | ✅ 미발동 (D-030 이 고쳤다는 결함) |
| 7 | 말끝 뒤 박수 2회 | 발동 | ✅ 발동 |
| 8 | 작은 소리 2회 | 미발동 | ✅ 미발동 |

**불일치 0 / 8.** D-030 `decisions.md:311-314` · D-032 `decisions.md:258` 의 주장은 **재현됩니다.**
「실제 마이크 미검증」(`decisions.md:327`·`voice.md:66`)도 정직하게 적혀 있습니다.

다만 시험 코드가 저장소에 없어, 다음에 `Detector` 를 고치면 아무도 회귀를 알 수 없습니다.
D-037 이 세운 「점검기 자체를 시험한다」 원칙(`decisions.md:85-101`)이 여기엔 적용되지 않았습니다.

- 요구: 8종 시험을 `ai-team/scripts/selftest-detector.py` 로 커밋하십시오. 감사관이 돌린 것과 같은 방식이면 충분합니다.

## [경미-2] D-031 — 콘솔에 박아 넣은 수치가 낡았습니다

- 근거: `ai-team/console/master-console.html:205-207` — 「B-07 · 감사 미수행 **7건**」
- 정본: `ai-team/board.md:21` · `ai-team/STATE.md:31` — **12건**
- 완화: `:167` 에 「2026-08-29 기준」 스탬프가 있고, D-031 이 「바뀌면 재발행」을 미리 적어 두었습니다(`decisions.md:285`)

- 요구: 재발행하거나, 재발행 전까지는 「낡을 수 있음」이 아니라 **마지막 갱신 시각과 정본 경로**를 페이지 상단에 넣으십시오.

## [경미-3] `BOOTSTRAP.md` 주석에 고정 숫자

- 근거: `ai-team/BOOTSTRAP.md:30` — 「행이 79개 이상입니다」 · 감사관 실측 **85행**
- 같은 교훈이 이미 `ai-team/STATE.md:90` 에 적혀 있습니다 — 「고정 숫자를 적으면 그 숫자가 낡습니다」
- 요구: 숫자를 지우고 「페이지로 끊어 받습니다」만 남기십시오.

---

## 판본 드리프트 검사 — **4 / 6**

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 단계 번호가 모든 문서에서 일치 | ✅ | 파이프라인은 `①`~`⑥` 로 일관. 「N단계」는 설치 안내(`음성-설치.md:14-154`)·사이클 마감(`STATE.md:1`) 등 **국소 문맥**이며 파이프라인 단계와 충돌하지 않습니다 |
| 2 | handoff 파일명 `01-전략`~`06-출시운영` 통일 | ✅ | 전 에이전트가 `<앱폴더>/handoff/0N-*.md` 사용. `ai-team/handoff/` 는 `team-rules.md:129` 에서 **폐기 경로로만** 언급 |
| 3 | 퇴역한 것을 아직 가리키는 문서 | ❌ | **중대-1** (`master-listen.py:191` `J A R V I S`) · **중대-5** (원격 잔존 키 경로). `clap-listen.py`·`jarvis.py` 문자열은 `decisions.md` 역사 기록에만 있고 D-034 대조표(`:177-182`)로 해소돼 있어 **문제 없음** |
| 4 | 실무자가 승인권을 주장 | ✅ | 실무자 정의 전건이 「승인권은 없습니다」를 명시 (`accessibility-auditor.md:7` 외 22건 확인) |
| 5 | 한 파일 안 자기모순 | ❌ | **치명-3** (`STATE.md:87` ↔ `:92`) · **치명-1** (`meeting-room/SKILL.md:118`·`:149` ↔ `:156-159`) |
| 6 | 산출 경로가 문서마다 다름 | ✅ | 전 에이전트가 `<앱폴더>/docs|handoff|src|tests/`, 감사실만 `ai-team/docs/감사-*` (`team-org.md:106-118` 과 일치) |

---

## 도구 실존 검증 — 추측하지 않고 직접 호출했습니다

| 도구 | 실행 결과 |
|---|---|
| `python3 ai-team/scripts/selftest-supabase.py` | **종료코드 0.** push 85행(상태 8·팀 77) → 빈 디렉터리 restore → **sha256 85/85 일치** → 복원본 `team-check.sh` 통과(31명) → BOOTSTRAP 덩어리 85파일·31명 → `verify` ✅ 완전 |
| `bash ai-team/scripts/team-check.sh` | ✅ 통과 — 31명 (**단, 중대-1 을 못 잡습니다**) |
| `python3 ai-team/scripts/check-selftest.py` | ✅ 회귀 **10종 전부 검출** |
| `bash ai-team/scripts/event.sh 논쟁 …` | 1차 기록 ✅ / 2차 「⏭ 건너뜀 — 키 없음(B-08)」 |
| `supabase-sync.py event --kind 논쟁` | ❌ `invalid choice: '논쟁'` (중대-3) |
| `supabase-sync.py event` (키 없음) | ❌ 종료코드 1, 아무 기록 없음 (치명-1) |
| `Detector` 8종 재현 | ✅ **8/8 일치** (경미-1) |
| BOOTSTRAP 덩어리 vs `restore` (저장소 있는 창) | ❌ 덩어리만 로컬을 말없이 덮음 (치명-2) |
| `push --team` 페이로드 실측 | 85행 443.8 KB · 최대 청크 211.6 KB — 크기 문제 없음 |

> **유보** (D-015 원칙 — 확인 못 한 것은 확인 못 했다고 씁니다)
> - **실제 Supabase(PostgREST) 왕복**: 키가 없어 감사관도 확인하지 못했습니다. 치명-3 은 「돌지 않는다」가 아니라 **「돌았다는 기록과 안 돌았다는 기록이 함께 있다」** 는 지적입니다
> - **실제 마이크·PowerShell·단축키 전송**: 이 컨테이너에 장치·셸이 없습니다. D-030·D-032 가 이미 ❌ 로 적어 두었고, 그 서술은 **정확합니다**
> - **노션 반영 상태**: 노션 도구를 이 감사에서 쓰지 않았습니다. `STATE.md:130` 의 「미반영 2건」은 대조하지 못했습니다

---

## 마스터 감사

| 항목 | 판정 | 근거 |
|---|---|---|
| RED 항목을 진행했는가 | ✅ 없음 | D-030·D-031·D-032·D-035 는 전부 `ai-team/` 내부. `.claude/` 미수정이므로 RED 예외 대상 아님. Python 설치는 **사용자가 하는 일**로 정확히 표기(`decisions.md:334`) |
| YELLOW 를 보고서 최상단에 표기 | ✅ | `ai-team/SESSION-LOG.md:7`·`:56` 최상단 🟡 |
| GREEN 결정을 `decisions.md` 에 기록 | ✅ | D-030 `:294` · D-031 `:268` · D-032 `:236` · D-035 `:139`. **지시 원문 4건 모두 포함** |
| 승인 없이 다음 단계로 넘어갔는가 | ✅ 아니오 | `board.md:8` 「사이클 착수 불가 — 감사 반려가 유효」 |
| 품질 게이트를 시간 부족으로 완화 | ✅ 해당 없음 | 사이클 미착수 |
| **미완을 완료로 보고했는가** | ❌ **예 — 2건** | **치명-3** (실제 왕복 실측 ✅ 표기) · **중대-1** (「옛 호칭 0건」) |
| D-014 요건 ①② | ✅ | 4건 모두 지시 원문 + 기록 있음 |
| D-014 요건 ③ | ✅ 이 감사로 충족 | **단, 반려된 D-035 는 시정 후 재감사가 필요합니다** |

**잘한 점도 기록합니다** (감사는 결함만 세는 일이 아닙니다).
- D-031 이 「팀 이동은 이미 되고 있었습니다. **없는 일을 새로 했다고 적지 않습니다**」(`decisions.md:277`)라고 적은 것은 이 감사가 반복해 요구해 온 태도입니다
- D-030·D-032 의 「검증한 것 / 못 한 것」 표(`decisions.md:325-331`·`:256-261`)는 **범위를 밝힌 완료 주장**의 모범입니다. 치명-3 이 지적하는 것과 정반대의 서술 방식입니다
- D-035 의 「곁가지로 발견·시정」(`decisions.md:159-160`) — `checkpoint.sh` 가 `'대기'` 만 찾아 **미반영 1건을 초록불로** 보고하던 것을 잡아 고쳤습니다. 초록불을 의심한 실측입니다

---

## 반려를 푸는 조건

**필수 (이것을 고치기 전에는 사이클 3 착수 불가)**
1. **치명-1** — `meeting-room/SKILL.md:118`·`:149` 를 `event.sh` 로. 전수 확인 + 회귀 시험 추가
2. **치명-2** — BOOTSTRAP 덩어리에 완전성 검사·예행·차이 표시. 또는 중복 정의 제거. `selftest` 에 「저장소 있는 창」 시나리오 추가
3. **치명-3** — 실제 왕복 여부를 증거로 확정하고 `supabase.md:104-113` · `decisions.md:519-521` · `STATE.md:87` 중 틀린 쪽을 정정

**음성 3건 조건부 승인의 조건 (사이클을 막지 않습니다 — 별도로 처리하십시오)**
4. 중대-1 배너 · 중대-4 `voice.md` 인자 · 경미-1 시험 커밋 · 경미-2 콘솔 재발행

**중대 잔여**
5. 중대-2 복원 범위 명시 · 중대-3 `논쟁` 종류 · 중대-5 삭제 전파

> 이 감사는 **반려**입니다. `.claude/team-rules.md` 와 `.claude/team-org.md` 에 따라
> **감사관이 반려한 단계를 진행하는 것은 RED 입니다.**
