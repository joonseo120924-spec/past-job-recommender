# 감사 보고서 — 3차 4건 (D-030 · D-031 · D-032 · D-035)

- 감사관: `team-master` (독립 · 마스터까지 감사)
- 일자: 2026-08-30 (재작성판 — 아래 「이 판이 이전 판을 정정한 곳」 참조)
- 대상: **B-07 일괄 감사 12건 중 3차 4건.** 1·2차 8건은 별도 호출이 다루므로 열지 않았습니다
- 근거 원칙: `.claude/team-rules.md` (최우선 정본) · `.claude/team-org.md` · `.claude/master-doctrine.md`
- 이 감사는 **D-014 요건 ③** 에 해당합니다

```
판정: ❌ 반려
치명: 4건  중대: 7건  경미: 5건
판본 드리프트 검사: 3 / 6
```

## 건별 판정 — 곁가지가 사이클을 막지 않게 분리했습니다

| 건 | 판정 | 사유 |
|---|---|---|
| **D-030** 박수 감지기 · 음성 응답 루프 | ❌ **반려** | 핵심 주장(8종 검증)은 **감사관이 재현 확인(8/8)**. 그러나 `.claude/` 를 17행 고쳐 놓고 **RED 로 기록하지 않았습니다**(치명-4) |
| **D-031** 음성 브리핑 콘솔 | ⚠️ **조건부 승인** | 한계 서술이 페이지 안에 정확히 있음. 박아 넣은 값이 하루 만에 낡음(중대-5) |
| **D-032** 상주 프로그램 단일화 | ⚠️ **조건부 승인** | 흡수·삭제 전수 확인. 부팅 배너에 옛 호칭 잔존(중대-1) |
| **D-035** 슈퍼베이스 팀 복원 | ❌ **반려** | 치명-2 · 중대-2 · 중대-3 · 중대-7 |
| **회의실 기록 경로** (D-037 시정 잔여) | ❌ **반려** | 치명-1 |
| **감사 진행 상태 기록** | ❌ **반려** | 치명-3 |

> 🟡 **곁가지 판단**: 음성 3건의 **완성도** 결함(중대-1·4·5, 경미-1·2)은 **사이클을 막지 않습니다.**
> 사이클을 막는 것은 ① 회의실 기록이 통째로 사라지는 구조(치명-1) ② 창 간 복원이 로컬 최신본을 말없이
> 덮는 구조(치명-2) ③ 감사 진행 상태 기록의 붕괴(치명-3) ④ RED 미분류(치명-4) 넷입니다.

---

## [치명-1] D-037 의 기록 경로 시정이 회의실 스킬에 **반만** 적용됐습니다 — 키가 없으면 아무 데도 안 남습니다

- 근거: `.claude/skills/meeting-room/SKILL.md:118` — 「라운드마다 `supabase-sync.py event --kind 호출` 로 남깁니다」
- 근거: `.claude/skills/meeting-room/SKILL.md:149` — 표 「파트장·실무자 호출 완료 | `supabase-sync.py event --kind 호출`」
- 대조: **같은 파일** `:156-159` — 「`event.sh` 가 `ai-team/events.log` 에 먼저 쓰고 … 2026-08-30 감사 치명-2 로 순서를 뒤집었습니다」
- 정본: `.claude/team-rules.md:76-86` 「**기록 경로 — 파일이 1차, 외부 저장소는 2차**」 · `:80` 「저장소 파일에 먼저 씁니다 — `ai-team/events.log`」
- 정본: `.claude/team-rules.md:72` — 교차 논쟁은 「**기록 수단은 아래 「기록 경로」** 를 따릅니다」

**실측** (키 없는 이 컨테이너에서 감사관이 직접 호출):
```
$ python3 ai-team/scripts/supabase-sync.py event --kind 호출 --title "감사 실측"
✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다.
종료코드 1        ← events.log 에도, 슈퍼베이스에도 아무것도 남지 않습니다

$ bash ai-team/scripts/event.sh 호출 strategy-lead "…"
✓ 1차 기록 — ai-team/events.log
⏭ 2차 반영 건너뜀 — 키 없음(B-08). **못 했다고 적습니다**      종료코드 0
```

무엇이 문제인가: `:118` 은 **교차 논쟁**(D-028)의 라운드 기록 지시입니다. 교차 논쟁은 `team-rules.md:65-74` 가
정본으로 끌어올린 규정이고, 그 정본이 지정하는 수단은 `event.sh` 입니다. 스킬이 정본과 **다른 수단**을 지시하므로
정본 우선순위상 `:118`·`:149` 의 지시는 **무효**이며, 무효인 지시를 그대로 따르면 **기록이 통째로 사라집니다.**
D-021·D-028 이 「규정만 있고 실행 0건」이 된 원인이 정확히 이것이고, D-037 은 그것을 고치려고 만들어졌습니다.
그런데 **§4 표 아래 코드블록(`:156`)만 고치고 §교차 논쟁(`:118`)과 §4 표 첫 줄(`:149`)은 그대로 뒀습니다** —
1·2차 감사에서 반복 지적된 「지목된 한 건만 고침」입니다.

- 요구: `.claude/skills/meeting-room/SKILL.md:118`·`:149` 를 `ai-team/scripts/event.sh <종류> <발언자> "<제목>" [참조]` 로 바꾸십시오.
- 요구: 같은 파일에서 `supabase-sync.py event` 를 **직접** 부르라는 지시가 더 없는지 전수 확인하십시오.
- 요구: `ai-team/scripts/check-selftest.py` 에 「회의실 스킬이 `event.sh` 대신 `supabase-sync.py event` 를 지시」 회귀를 추가하십시오. **지금 점검기는 이 회귀를 못 잡습니다** (감사관 실행: 회귀 10종 전부 게이트·명부 항목이며 기록 경로 항목이 없음).

---

## [치명-2] D-035 — `BOOTSTRAP.md` 붙여넣기 덩어리가 정본 스크립트의 안전장치를 **전부** 결여합니다 (D-012 중복 정의)

- 근거: `ai-team/BOOTSTRAP.md:18-49` (붙여넣기 덩어리)
- 대조: `ai-team/scripts/supabase-sync.py:325-360` (`cmd_restore`) · `:288-300` (`audit`)
- 대조: `ai-team/decisions.md:150` — D-035 가 **스스로 적은 원칙** 「`restore` 는 기본이 **예행**입니다. 정본은 저장소이므로 로컬을 말없이 덮지 않습니다」

| 안전장치 | `supabase-sync.py restore` | `BOOTSTRAP.md` 덩어리 |
|---|---|---|
| 불완전한 원격 거부 | ✅ `:332-334` 「반쯤 복원된 팀이 더 위험합니다」 | ❌ 없음. **다 쓰고 나서** 경고만 (`:48`) |
| 기본 예행(dry-run) | ✅ `:351-354` | ❌ 없음. 즉시 덮어씀 (`:43`) |
| BOM · 첫 줄 `---` 검사 | ✅ `:295-298` | ❌ 없음 |
| 무엇이 바뀌는지 사전 표시 | ✅ `:345-350` | ❌ 없음 |

그리고 `ai-team/BOOTSTRAP.md:5` 는 「새 대화창에 저장소가 **있든 없든**」이라고 안내하며,
덩어리 `:24-28` 은 `git rev-parse --show-toplevel` 로 **기존 저장소 루트를 찾아 거기에 씁니다.**

**실측** (감사관이 가짜 원격 + 저장소가 **있는** 창을 만들어 직접 실행 — 원격에는 낡고 불완전한 사본만 심었습니다):
```
=== A. supabase-sync.py restore --force  (정본 스크립트) ===
  에이전트 0 / 31 · 🔴 없음: .claude/team-org.md … (필수 11파일 전부)
  ✗ 불완전한 원격으로는 복원하지 않습니다. 반쯤 복원된 팀이 더 위험합니다.   종료코드 1
  로컬 규정 파일: '★ 로컬의 최신 규정 — 아직 push 안 함 ★'      ← 지켜졌습니다

=== B. BOOTSTRAP.md 붙여넣기 덩어리 (같은 원격 · 같은 조건) ===
  ✓ 2개 파일 복원 · 에이전트 0명 → /tmp/B-repo-exists-…            종료코드 0
  로컬 규정 파일: '원격의 낡은 규정'                              ← 말없이 덮였습니다
  로컬 상태 파일: '원격의 낡은 상태'                              ← 말없이 덮였습니다
```

무엇이 문제인가: **정본은 저장소입니다** (`ai-team/supabase.md:18-22`). 아직 push 하지 않은 로컬 최신본이 있는
창에서 이 덩어리를 붙여넣으면 **경고도 예행도 없이 원격의 낡은 사본으로 되돌아갑니다.** 08-15 병합 사고에서
가장 위험했던 동작이라 `pull` 에까지 `--force` 를 요구해 두고(`supabase-sync.py:264-267`), 정작
「이게 전부입니다」라고 안내하는 경로(`BOOTSTRAP.md:16`)에는 그 방어가 없습니다.

`ai-team/scripts/selftest-supabase.py:120` 은 이 덩어리를 **빈 임시 디렉터리**(`tempfile.mkdtemp`)에서만 돌리고,
`:125-129` 는 **파일 개수와 스킬 존재만** 봅니다. 따라서 이 결함은 **자체 시험으로 영원히 잡히지 않습니다.**
`ai-team/STATE.md:91` 이 「같은 시험에 포함」이라고만 적은 것이 2차 감사에서 지적된 「검증 범위를 밝히지 않은
완료 주장」 기전 그대로입니다.

- 요구 (택1): ① 덩어리에 **쓰기 전** 완전성 검사(31명·필수 11파일) → 미달 시 중단, 기본 예행 + `FORCE=1` 명시 동의, 기존 파일과 다른 것의 목록 출력을 넣으십시오. ② 또는 **중복 정의를 없애십시오** — 덩어리가 `supabase-sync.py` 한 파일만 내려받아 그것을 실행하게 하면 복원 로직이 한 곳이 됩니다 (D-012 본래 해법이며 이쪽을 권합니다).
- 요구: `selftest-supabase.py` 에 **「저장소가 있는 창」 시나리오**를 추가하고, 로컬 파일이 보존되는지 대조하십시오.

---

## [치명-3] 감사 진행 상태 기록이 **세 갈래로 어긋납니다** — 미완과 완료의 경계가 무너졌습니다

| 기록 | 무엇이라 적혀 있는가 |
|---|---|
| `ai-team/STATE.md:14` | 「감사 12건 중 **8건 감사됨** … **3차 4건(D-030~D-032·D-035) 미착수**」 |
| `ai-team/STATE.md:40` (B-07) | 「1차(D-021·D-025·D-026·D-027) 판정 ❌ 반려 … **2·3차 미실시**」 ← 같은 파일 26줄 아래, **8건과 모순** |
| `ai-team/events.log:7` | 「2026-08-30 09:33 KST 감사 team-master **3차 감사(D-030·D-031·D-032·D-035) ❌ 반려** — 치명 3 · 중대 5 · 드리프트 4/6」 |
| `ai-team/docs/감사-규정-2026-08-30-3.md` | **311행짜리 3차 감사 보고서가 커밋 `0af6c16` 로 이미 저장소에 있었습니다** |
| 이번 감사 지시서 (마스터 → 감사관) | 「일괄 감사 12건 중 **3차 4건**을 요청합니다 (앞선 두 호출은 세션 한도로 중단됐습니다)」 |

한 저장소 안에서 3차 감사는 **미착수이자(`STATE.md:14`) 미실시이자(`:40`) 반려 완료(`events.log:7` + 보고서)** 입니다.
`STATE.md` 는 2026-08-30 커밋 `6e813f0` 에서 실제로 수정됐는데(노션 미반영 3건 → 0건),
`:14`·`:40` 의 감사 상태와 `:10` 「최종 갱신 **2026-08-29**」는 그대로 두었습니다.

무엇이 문제인가: `.claude/team-rules.md:34` 「**주장을 믿지 않는다** — "완료했습니다"는 무효」와
`team-org.md` 「끊기면 **미완으로 기록**합니다. 완료로 보고하지 않습니다」가 겨냥하는 바로 그 실패입니다.
지금은 반대 방향으로도 틀렸습니다 — **끝난 것을 미완으로 적어 두었고, 그 잘못된 전제로 감사관을 다시 불렀습니다.**
`STATE.md` 는 `:1` 이 스스로 「어느 기기에서 열든 **이 한 장이 팀의 현재 상태**」라고 선언한 파일입니다.

- 요구: `STATE.md:14`·`:40` 을 `events.log` 와 `ai-team/docs/` 실물에 맞추십시오. 「8건」과 「2·3차 미실시」 중 하나는 반드시 틀렸습니다.
- 요구: `STATE.md:10` 최종 갱신을 실제 갱신일로 고치십시오.
- 요구: 감사 건수·차수는 **`events.log` 와 `ai-team/docs/` 파일 목록에서 세게** 하십시오. 사람이 손으로 적은 숫자가 두 곳에 있으면 반드시 갈라집니다 (D-036 이 게이트에서 이미 내린 결론입니다).

---

## [치명-4] D-030 은 **`.claude/` 를 17행 고쳤습니다** — RED 인데 RED 로 분류되지 않았습니다

- 근거: 커밋 `2c303f7` (「D-030 박수 감지기 + 음성 응답 루프」) 가 **`.claude/jarvis.md` 에 +17행** — 「🔊 음성 보고 (D-030)」 절 신설
- 현존: 그 절은 지금도 살아 있습니다 — `.claude/master.md:40-54` (D-034 개명 후)
- 정본: `.claude/master-doctrine.md:34` — 「**`.claude/` 아래 파일 수정** — 자기 통제 장치를 스스로 바꿀 수 없음」 = **RED**
- 정본: `.claude/master-doctrine.md:37-38` — 예외는 3요건 **전부** 충족 시만
- 대조: `ai-team/decisions.md` 의 D-030 상세(`:294-336`)에는 **RED 판정도, D-014 3요건 검토도 없습니다.** 같은 문서에서 D-033(`:232`)·D-034(`:205`)는 「`.claude/` 수정이므로 **RED**」를 명시했습니다

무엇이 문제인가: D-030 은 마스터의 **정체성 정본**에 「음성 보고를 이렇게 한다」는 새 행동 규약을 심었습니다.
그것이 바로 「자기 통제 장치를 스스로 바꾸는」 행위입니다. D-033·D-034 는 같은 성질의 변경을 스스로 RED 로
분류해 B-07 에 넣었는데, **D-030 만 분류에서 빠졌습니다.** 그래서 B-07 12건 목록에는 D-030 이 「음성 곁가지」로
올라 있을 뿐 RED 로 셈되지 않았고, `master-doctrine.md:40-44` 가 못 박은 「되돌릴 수 있음은 요건 ③ 을
대신하지 못한다」는 검토 자체를 받지 못했습니다.

이것은 곁가지의 완성도 문제가 아니라 **분류 누락**이므로 사이클을 막습니다.

- 요구: `ai-team/decisions.md` 의 D-030 상세에 「`.claude/jarvis.md`(현 `master.md:40-54`) 수정 — **RED**. D-014 3요건 ①②③ 상태」를 D-033·D-034 와 같은 형식으로 추가하십시오.
- 요구: `ai-team/board.md:21`·`ai-team/STATE.md:31` 의 B-07 목록에서 D-030 을 **`.claude/` 수정 건**으로 표기하십시오.
- 요구: `.claude/` 를 고친 커밋을 `git log --name-only -- .claude/` 로 전수 재확인해, 같은 누락이 D-026~D-032 사이에 더 없는지 대조하십시오. 감사관은 D-030~D-032·D-035 네 건만 확인했습니다 (D-031·D-032·D-035 커밋은 `.claude/` 미수정 — 실측 확인).

---

## [중대-1] D-034 의 「살아 있는 코드에서 옛 호칭 0건」이 거짓입니다 — 부팅 화면 첫 줄이 `J A R V I S` 입니다

- 근거: `ai-team/scripts/master-listen.py:189-193`
```python
BANNER = r"""
   ┌─────────────────────────────────────────────┐
   │   J A R V I S                               │
   │   AI 앱 개발팀 · 총괄                        │
```
- 노출 경로: `boot_check()` `:196-197` 이 맨 먼저 출력 → `--setup`·`--tune`·기동 **모든 실행의 첫 화면**
- **두 번째 위치**: `ai-team/음성-설치.md:102` — 같은 배너를 「**이렇게 나오면 정상입니다**」로 실어 두었습니다. 사용자 안내서가 옛 호칭을 **정상 동작으로 가르칩니다**
- 거짓이 된 주장: `ai-team/decisions.md:198` — 「살아 있는 문서·코드에서 `jarvis`·`자비스` **0건** (역사 기록 `decisions.md` 제외)」
- 점검기가 놓친 이유: `ai-team/scripts/team-check.sh:98` 은 `-iname '*jarvis*'` (**파일명만**), `:101` 은 `grep -i 'jarvis\|자비스'` — **자간이 벌어진 `J A R V I S` 는 어느 패턴에도 걸리지 않습니다.** 감사관 실행 결과 지금도 「✅ 팀 정의 점검 통과」입니다

D-034 의 「0건」은 grep 결과를 그대로 옮긴 완료 주장이었고, **grep 이 무엇을 못 보는지를 밝히지 않았습니다.**

- 요구: `master-listen.py:191` 과 `음성-설치.md:102` 를 「M A S T E R」로 바꾸십시오. **두 곳 다입니다.**
- 요구: `team-check.sh:98`·`:101` 패턴에 자간 표기(`[Jj][ ._-]*[Aa][ ._-]*[Rr]…`)를 추가하십시오.
- 요구: `check-selftest.py` 에 「배너에 옛 호칭 부활」 회귀를 추가하십시오.

## [중대-2] D-035 의 복원 범위가 어디에도 적혀 있지 않고, 규정이 정한 **1차 기록처가 범위 밖**입니다

- 근거: `ai-team/scripts/supabase-sync.py:33-39` `TEAM_GLOBS`
- 범위 밖 (감사관 전수 대조 — 글롭 대 실제 파일 트리):
  `ai-team/events.log` · `ai-team/docs/*.md` (감사 보고서 6건) · `ai-team/console/master-console.html` ·
  `ai-team/archive/cycles-1-2.md` · `ai-team/voice-out/*` · `ai-team/apps/**`
- 그런데 `ai-team/scripts/supabase-sync.py:320` 은 「**✅ 완전** — 어느 창에서든 restore 하나로 팀이 나옵니다」를 찍습니다
- 정본: `.claude/team-rules.md:80` — `ai-team/events.log` 가 **1차 기록처** · `.claude/team-org.md` 「산출 경로 규칙」 — `ai-team/apps/<날짜>/` 가 모든 앱 산출물의 위치
- 정본: `.claude/agents/team-master.md:47` · `.claude/agents/evidence-auditor.md:26` — 감사 보고서는 `ai-team/docs/`

저장소가 통째로 없는 창(BOOTSTRAP 경로)은 팀 31명과 규정은 받지만 **회의실 이벤트 기록도, 앱 산출물도,
감사 보고서도 받지 못합니다.** 지금 `apps/` 가 비어 있어(D-024 초기화) 피해가 드러나지 않을 뿐입니다.
`.claude/` 는 전량 포함됨을 감사관이 실측 확인했습니다 (37파일 전부 글롭에 걸림).

- 요구: `TEAM_GLOBS` 에 `ai-team/events.log` 를 넣거나, 넣지 않을 이유를 `supabase.md` 에 명시하십시오.
- 요구: `supabase.md` 와 `BOOTSTRAP.md` 에 **「복원되지 않는 것」 목록**을 적고, `:320` 의 「✅ 완전」 문구를 「**팀 정의·상태 파일 한정**」으로 좁히십시오.

## [중대-3] `event.sh` 의 `논쟁` 종류는 슈퍼베이스에 **영구히** 들어갈 수 없습니다

- 근거: `ai-team/scripts/event.sh:15`·`:19` — 허용 종류에 `논쟁` 포함
- 근거: `ai-team/scripts/supabase-sync.py:50` — `KINDS = ["호출","승인","반려","결정","막힘","기록","동기화","감사"]` — **`논쟁` 없음**
- 근거: `ai-team/supabase/schema.sql:32` — `check (kind in ('호출',…,'감사'))` — **`논쟁` 없음**
- 실측: `supabase-sync.py event --kind 논쟁` → `error: argument --kind: invalid choice: '논쟁'`

1차(파일)는 남지만 2차는 **키가 생겨도 실패**합니다. 하필 교차 논쟁(D-028)은 `team-rules.md:65-74` 가
정본으로 승격한 규정입니다.

**더 중요한 함정**: `ai-team/supabase.md:54` 는 스키마 적용을 「**여러 번 실행해도 안전합니다**」라고 안내하는데,
`schema.sql:13`·`:26` 은 `create table if not exists` 입니다. 테이블은 사용자가 2026-08-29 에 이미
만들었으므로, `schema.sql:32` 에 `논쟁` 을 추가하고 다시 Run 해도 **기존 check 제약은 바뀌지 않습니다.**

- 요구: `supabase-sync.py:50` 과 `schema.sql:32` 에 `논쟁` 을 추가하고, **`alter table … drop constraint … add constraint`** 를 별도로 넣으십시오. `supabase.md:54` 에 「제약 변경은 재실행으로 반영되지 않는다」를 적으십시오.

## [중대-4] D-030 — `voice.md` 가 **존재하지 않는 인자 4종**을 안내합니다 (같은 저장소의 다른 안내서와 모순)

- 근거: `ai-team/voice.md:50` — `--setup` 을 「감지만 (키 안 누름)」으로 설명 → 실제 `--setup` 은 마이크·음성 점검(`master-listen.py:306`), 「감지만」은 `--quiet`(`:312`)
- 근거: `ai-team/voice.md:51` — `--key ctrl+shift+v` → 실제 인자는 `--hotkey`(`:309`)
- 근거: `ai-team/voice.md:66`·`:70` — `--test` → **없습니다**
- 근거: `ai-team/voice.md:66` — `--factor` · `--floor` → **인자가 아니라 설정 파일 키**입니다 (`master-listen.py:38-39`, `master-listen.config.json`). argparse 에 없습니다
- 대조: `ai-team/음성-설치.md:180-188` 「명령 요약」은 `--setup`·`--tune`·`--say`·`--list`·`--hotkey`·`--quiet` 로 **argparse 와 정확히 일치**합니다

사용자가 `voice.md` 대로 치면 argparse 오류가 납니다. D-032 가 두 파일을 하나로 합치면서 인자 체계를 바꿨는데
**안내서 두 개 중 하나만 갱신**한 것입니다.

- 요구: `ai-team/voice.md:48-51`·`:66`·`:70` 을 실제 인자로 고치십시오.
- 요구: `master-listen.py --help` 출력과 두 안내서를 대조하는 검사를 `team-check.sh` 에 넣으십시오. **문서가 코드보다 늦는 것이 이 팀의 반복 유형입니다.**

## [중대-5] D-031 — 콘솔이 낡은 값을 **소리로 읽습니다** (곁가지 · 비차단)

- 근거: `ai-team/console/master-console.html:179` 「감사 대기 **7건**」 · `:207-208` 「감사 미수행 **7건** / D-021 · D-025 ~ **D-030**」
- 근거: `:260-261` — **낭독 원고**에 「규정 파일을 **일곱 번** 고쳤는데」 · 「첫째, 감사 **일곱 건**」
- 정본: `ai-team/board.md:21` · `ai-team/STATE.md:31` — **12건** (D-021 · D-025 ~ D-035)
- 완화: `:167` 에 「2026-08-29 기준」 스탬프가 있고, D-031 이 「바뀌면 재발행」을 미리 적어 두었습니다(`decisions.md:285`)
- 잘한 점: 「하지 못하는 일」이 `:245` 에 페이지 안에서 정확히 서술돼 있습니다 (CSP 로 외부 요청 불가 → 복사·붙여넣기)

이 페이지의 **유일한 목적이 음성 브리핑**인데, 그 브리핑이 하루 만에 틀린 숫자를 말합니다.
D-012(중복 정의)가 값을 두 곳에 두면 갈라진다고 한 그대로입니다. 다만 **곁가지이므로 사이클을 막지 않습니다.**

- 요구: 재발행하거나, 재발행 전까지 **마지막 갱신 시각과 정본 경로**를 페이지 상단·낭독 첫 문장에 넣으십시오.

## [중대-6] 복원 진입점이 **셋**이고, 그중 하나가 감사관의 정본 산출 경로를 「폐기」라고 선언합니다

- 근거: `ai-team/RESTORE.md:24` — 「산출물은 `ai-team/apps/<날짜>/` 아래에만. **`ai-team/docs/` 는 폐기 경로**」
- 대조: `.claude/agents/team-master.md:47` — 「앱 무관 감사는 **`ai-team/docs/감사-규정-<날짜>.md`**」
- 대조: `.claude/agents/evidence-auditor.md:26` — 「앱 무관 감사는 **`ai-team/docs/감사-증거대조-<날짜>.md`**」
- 실물: `ai-team/docs/` 에 감사 보고서 **6건**이 이미 있고 `ai-team/approvals.md:19` 가 그 경로로 인용합니다

즉 **이 보고서가 저장되는 경로 자체가 한 문서에서는 폐기 경로**입니다. 판본 드리프트 6번(산출 경로가 문서마다 다름)의
정면 사례이며, `.claude/agents/` 를 고쳐야 하므로 감사관이 직접 고칠 수 없습니다(RED).

덧붙여 복원 진입점이 셋입니다 — `ai-team/RESTORE.md`(노션에서 손으로 재작성) · `ai-team/BOOTSTRAP.md`(슈퍼베이스
붙여넣기) · `supabase-sync.py restore`. 셋이 서로를 가리키지 않고, 치명-2 가 보인 대로 **동작도 다릅니다.**

- 요구: `RESTORE.md:24` 를 「**감사 보고서는 `ai-team/docs/`, 앱 산출물은 `ai-team/apps/<날짜>/`**」로 고치십시오.
- 요구: 세 진입점의 관계를 `ai-team/README.md` 에 한 줄로 정리하십시오 — 어떤 상황에 어느 것을 쓰는가.

## [중대-7] D-035 의 새 경로는 **실제 PostgREST 를 한 번도 거치지 않았습니다** — 「실측」이라는 낱말이 그것을 가립니다

먼저, 감사관이 확인한 **사실 관계**부터 적습니다. 이 항목은 이전 판이 「모순」이라 단정한 곳이고, 그 단정은 틀렸습니다.

| | 실제로 있었던 일 | 근거 |
|---|---|---|
| **D-023** (상태 파일 8개) | **진짜 슈퍼베이스 왕복이 있었습니다.** 사용자가 대시보드에서 `schema.sql` 을 실행한 뒤 `push` 8/8 → `status` 8/8 → `pull` 차이 0 → `event` → 뷰 조회 | 커밋 `0fd169f` (2026-08-29 05:26Z, 세션 `session_013K7vyHdjfbrh3uj1LECNv2`) · `ai-team/supabase.md:104-113` · `ai-team/decisions.md:516-517` |
| **D-035** (`push --team`·`restore`·`verify`·BOOTSTRAP) | **한 번도 실제 서버에 닿지 않았습니다.** 가짜 서버만 | `ai-team/STATE.md:92` · `ai-team/decisions.md:162` · `ai-team/board.md:22` · 감사관 실측: `ai-team/supabase/.env` 없음, 환경변수 없음 |

따라서 「했다」와 「못 했다」는 **대상이 다르므로 동시에 참일 수 있습니다.** 진짜 결함은 다른 데 있습니다.

- `ai-team/decisions.md:155-156` (D-035 실측): 「push 80파일 → sha256 **80/80** 일치 → `verify` **완전**」 — **어디에도 「가짜 서버」라고 쓰여 있지 않습니다.** 바로 위 `:153` 에서 「가짜 PostgREST」를 언급했으나, 결과를 적은 줄은 D-023 의 진짜 왕복과 **같은 어투**입니다
- `ai-team/STATE.md:89` 는 「**가짜 서버** 왕복 실측」으로 정확히 적었습니다. 두 기록의 정직도가 다릅니다
- 가짜 서버가 흉내내지 않는 것 (`selftest-supabase.py:36-56`): `Prefer: resolution=merge-duplicates` 헤더를 **읽지 않고**, `select=` 를 존중하지 않으며, RLS 도 `on_conflict` 도 없습니다. `do_POST` 는 그냥 파이썬 dict 에 대입합니다
- 즉 `push --team` 의 86행 3청크 upsert · `remote_all` 의 offset/limit 페이지네이션은 **실제 PostgREST 에서 검증되지 않았습니다.** (RLS 자체는 `schema.sql:84-86` 에 insert·update 정책이 둘 다 있어 upsert 가 통과할 **설계**이지만, 도는 것을 본 사람은 없습니다)
- 그 위에서 `supabase-sync.py:320` 이 「✅ 완전」을 찍습니다

- 요구: `decisions.md:155-156` 에 「**가짜 서버 한정 — 실제 PostgREST 미검증**」을 붙이십시오 (D-034 소급 미수정 원칙과 충돌하지 않습니다 — 사실을 지우는 것이 아니라 범위를 밝히는 것입니다).
- 요구: 키를 넣는 즉시 `push --team` → `verify` → **다른 창에서 `restore`** 를 1회 돌리고, 그 결과를 `supabase.md` 에 D-023 과 **구분해** 적으십시오. 그 전까지 B-08 은 열어 두십시오.
- 요구: `board.md:22`·`STATE.md:92` 의 B-08 문구를 「**D-035 의 `push --team` 은 아직 실제 서버에 올라간 적이 없습니다**」로 좁히십시오. 지금 문장은 D-023 왕복까지 부정하는 것처럼 읽힙니다 — **이번 감사 지시서가 실제로 그렇게 읽었습니다** (「실제 슈퍼베이스 왕복은 한 번도 없습니다(B-08)」).

---

## [경미-1] D-030·D-032 의 「합성 신호 8종 전항 통과」 — **주장은 재현되나 시험 코드가 없습니다**

감사관이 `Detector`(`ai-team/scripts/master-listen.py:129-171`)를 직접 불러 8종을 재구성해 실행했습니다.

| # | 신호 | 기대 | 실측 |
|---|---|---|---|
| 1 | 정상 2회 (0.3s) | 발동 | ✅ 1회 발동 (t=1.34) |
| 2 | 1회만 | 미발동 | ✅ 미발동 |
| 3 | 간격 2.0초 | 미발동 | ✅ 미발동 |
| 4 | 3연타 | 1회만 | ✅ 1회 (t=1.34) |
| 5 | 지속음 3초 | 미발동 | ✅ 미발동 |
| 6 | **말끝 + 박수 1회** | 미발동 | ✅ 미발동 (D-030 이 고쳤다는 결함) |
| 7 | 말끝 뒤 박수 2회 | 발동 | ✅ 1회 발동 (t=2.64) |
| 8 | 작은 소리 2회 | 미발동 | ✅ 미발동 |

**불일치 0 / 8.** D-030(`decisions.md:314`·`:326`)·D-032(`decisions.md:259`)·`voice.md:61-62`·`음성-설치.md:201` 의
주장은 **재현됩니다.** 「실제 마이크 미검증」도 네 곳에 정직하게 적혀 있으며 **그 서술은 정확합니다.**

다만 시험 코드가 저장소에 없어, 다음에 `Detector` 를 고치면 아무도 회귀를 알 수 없습니다.
D-037 이 세운 「점검기 자체를 시험한다」 원칙이 여기엔 적용되지 않았습니다.

- 요구: 8종 시험을 `ai-team/scripts/selftest-detector.py` 로 커밋하십시오.

## [경미-2] `BOOTSTRAP.md` 주석에 고정 숫자
- 근거: `ai-team/BOOTSTRAP.md:30` — 「행이 **79개 이상**입니다」 · 감사관 실측 **86행** (상태 8 · 팀 정의 78)
- 같은 교훈이 이미 `ai-team/STATE.md:90` 에 있습니다 — 「고정 숫자를 적으면 그 숫자가 낡습니다」
- 요구: 숫자를 지우고 「페이지로 끊어 받습니다」만 남기십시오.

## [경미-3] `RESTORE.md` 검증 스크립트에 20명 시절 잔재
- 근거: `ai-team/RESTORE.md:53` 「`# 개수 — 20`」 바로 아래 `:54` 「`# 31 이어야 합니다`」
- 요구: `:53` 을 지우거나 31 로 고치십시오.

## [경미-4] `STATE.md` 최종 갱신일이 낡음
- 근거: `ai-team/STATE.md:10` 「최종 갱신 **2026-08-29**」 · 실제 커밋 `6e813f0`(2026-08-30 23:00 KST)에서 `:130` 수정
- 요구: 치명-3 시정과 함께 고치십시오.

## [경미-5] D-035 기록의 「80파일」
- 근거: `ai-team/decisions.md:155-156` · `ai-team/SESSION-LOG.md:71`·`:74` — 「80파일 · 80/80」 · 감사관 실측 **86**
- 판단: **정정을 요구하지 않습니다.** D-034 의 「기록을 소급해 고치면 창작이 된다」 원칙이 적용됩니다. 대신 「(2026-08-29 당시 값)」을 덧붙이면 충분합니다

---

## 판본 드리프트 검사 — **3 / 6**

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 단계 번호가 모든 문서에서 일치 | ✅ | 파이프라인은 `①`~`⑥` 로 일관. `N단계` 전수 검색 결과 — `daily-app/SKILL.md` 는 `## 0.`~`## 7.` 절 번호와 `:53` 「6단계」가 일치, `음성-설치.md` 는 `## 0단계`~`## 7단계` 안에서 `:204` 「4단계(--setup)→5단계(--tune)」가 `:93`·`:121` 과 일치, `source-verifier.md:23` 「판정 4단계」는 판정 등급 수. **파이프라인 단계와 충돌하는 것 없음** |
| 2 | handoff 파일명 `01-전략`~`06-출시운영` 통일 | ✅ | 전 문서가 `handoff/01-전략.md`~`06-출시운영.md` 형식(17건). 에이전트 정의는 전건 `<앱폴더>/handoff/`. `ai-team/handoff/` 는 `team-rules.md:129` 에서 **폐기 경로로만** 언급 |
| 3 | 퇴역한 것을 아직 가리키는 문서 | ❌ | **중대-1** — `master-listen.py:191` · `음성-설치.md:102` 의 `J A R V I S`. **단, 파일명 참조는 깨끗합니다** — `clap-listen.py`·`jarvis.py`·`jarvis-voice.ps1`·`jarvis-console.html`·`JARVIS-설치.md` 전수 검색 결과 역사 기록(`decisions.md`) 밖 **0건** (D-032 흡수·삭제와 D-034 개명이 실제로 반영됨) |
| 4 | 실무자가 승인권을 주장 | ✅ | 에이전트 31명 중 29명이 승인권을 명시. 파트장 6명만 「승인권이 **있습니다**」, 실무자 23명 전원 「승인권은 **없습니다**」. 감사실 2명은 거부권 서술 |
| 5 | 한 파일 안 자기모순 | ❌ | **치명-3** (`STATE.md:14` ↔ `:40`) · **치명-1** (`meeting-room/SKILL.md:118`·`:149` ↔ `:156-159`) |
| 6 | 산출 경로가 문서마다 다름 | ❌ | **중대-6** (`RESTORE.md:24` 가 `ai-team/docs/` 를 폐기 경로라 선언 ↔ `team-master.md:47`·`evidence-auditor.md:26` 이 정본으로 지정). 에이전트 정의의 앱 산출 경로 자체는 깨끗합니다 — 74건 전부 `<앱폴더>/docs\|handoff\|src\|tests/` 접두 |

---

## 도구 실존 검증 — 설정 파일을 추측하지 않고 **직접 호출**했습니다

| 도구 | 실행 결과 |
|---|---|
| `python3 ai-team/scripts/selftest-supabase.py` | **종료코드 0.** push 86행(상태 8·팀 78) → 빈 디렉터리 restore → **sha256 86/86 일치** → 복원본 `team-check.sh` 통과(31명) → BOOTSTRAP 덩어리 86파일·31명 → `verify` ✅ 완전 |
| `bash ai-team/scripts/team-check.sh` | ✅ 통과 — 31명 (**중대-1 을 못 잡습니다**) |
| `python3 ai-team/scripts/check-gates.py` | 종료코드 0 · 침묵 (이상 없음) |
| `python3 ai-team/scripts/check-selftest.py` | ✅ 회귀 **10종 전부 검출** (**기록 경로·배너 회귀는 목록에 없습니다**) |
| `bash ai-team/scripts/event.sh 호출 …` | 1차 `events.log` ✅ / 2차 「⏭ 건너뜀 — 키 없음(B-08)」 · 종료코드 0 |
| `supabase-sync.py event` (키 없음) | ❌ 종료코드 1, **아무 기록 없음** (치명-1) |
| `supabase-sync.py event --kind 논쟁` | ❌ `invalid choice: '논쟁'` (중대-3) |
| `Detector` 8종 재현 | ✅ **8 / 8 일치** (경미-1) |
| BOOTSTRAP 덩어리 vs `restore` (저장소 **있는** 창) | ❌ 덩어리만 로컬 최신본을 말없이 덮음 (치명-2) |
| `TEAM_GLOBS` 대 실제 파일 트리 전수 대조 | `.claude/` 37파일 전량 포함 · `ai-team/` 14파일 범위 밖 (중대-2) |
| `git log --name-only` (D-030~D-035 커밋) | `2c303f7` 이 `.claude/jarvis.md` +17행 (치명-4). 나머지 3건은 `.claude/` 미수정 |
| `git status` (감사 종료 시) | **깨끗** — 감사 실행이 저장소를 건드리지 않았습니다 (커밋 `051a85b` 사고 재발 없음) |

> **유보** (D-015 원칙 — 확인 못 한 것은 확인 못 했다고 씁니다)
> - **실제 Supabase(PostgREST) 왕복**: 키가 없어 감사관도 확인하지 못했습니다. 중대-7 은 「돌지 않는다」가 아니라 **「돈 적이 없다」** 는 지적입니다
> - **D-023 의 2026-08-29 왕복**: 커밋 `0fd169f` 의 서술과 그때 적힌 구체값(차단 6건·이벤트 2건)이 근거이며, 감사관이 원격을 열어 본 것은 아닙니다. **거짓이라 볼 근거는 찾지 못했습니다**
> - **실제 마이크·PowerShell·단축키 전송**: 이 컨테이너에 장치·셸이 없습니다. D-030·D-032 가 이미 ❌ 로 적어 두었고 그 서술은 정확합니다
> - **노션 반영 상태**: 노션 도구를 이 감사에서 쓰지 않았습니다. `STATE.md:130` 의 「미반영 0건」은 대조하지 못했습니다
> - **`.claude/` 수정 커밋 전수**: D-030~D-035 네 건만 확인했습니다. D-026~D-029 는 이번 범위 밖입니다 (치명-4 요구에 포함)

---

## 마스터 감사

| 항목 | 판정 | 근거 |
|---|---|---|
| **RED 항목을 진행했는가** | ❌ **예 — 1건** | **치명-4.** D-030 이 `.claude/jarvis.md` 를 17행 고치고 RED 로 분류하지 않았습니다 (`2c303f7` · 현 `.claude/master.md:40-54`). D-031·D-032·D-035 는 `.claude/` 미수정 — 실측 확인 |
| YELLOW 를 보고서 최상단에 표기 | ✅ | `ai-team/STATE.md:29-33` · `ai-team/SESSION-LOG.md:56` 최상단 🟡 |
| GREEN 결정을 `decisions.md` 에 기록 | ✅ | D-030 `:294` · D-031 `:268` · D-032 `:236` · D-035 `:139`. **지시 원문 4건 모두 포함** |
| 승인 없이 다음 단계로 넘어갔는가 | ✅ 아니오 | `ai-team/board.md:8` 「사이클 착수 불가 — 감사 반려가 유효」 |
| 품질 게이트를 시간 부족으로 완화 | ✅ 해당 없음 | 사이클 미착수 |
| **미완을 완료로 보고했는가** | ❌ **예 — 3건** | ① **치명-3** 3차 감사를 「미착수」로 적어 둔 채 감사관을 재호출 (반대 방향의 같은 오류) ② **중대-1** D-034 「옛 호칭 0건」(`decisions.md:198`) ③ **중대-7** 「가짜 서버」임을 밝히지 않은 실측 서술(`decisions.md:155-156`) |
| **감사관에게 사실과 다른 전제를 제시했는가** | ❌ **예 — 1건** | 이번 지시서 「실제 슈퍼베이스 왕복은 한 번도 없습니다(B-08)」 ↔ 커밋 `0fd169f`·`supabase.md:104-113`·`decisions.md:516-517`. **B-08 문구가 과도하게 넓은 것이 뿌리**이며, 그 문구를 마스터가 그대로 믿었습니다 (중대-7) |
| D-014 요건 ①② (D-030~D-032·D-035) | ✅ | 4건 모두 지시 원문 인용 + 기록 있음 |
| D-014 요건 ③ | ⚠️ **이 감사로 수행** | 단, **반려**이므로 요건 ③ 은 「검증 완료」가 아니라 「검증 결과 반려」입니다. 시정 후 재감사가 필요합니다 |

**잘한 점도 기록합니다** (감사는 결함만 세는 일이 아닙니다).
- `ai-team/decisions.md:277` — D-031 이 「팀 이동은 **이미 되고 있었습니다. 없는 일을 새로 했다고 적지 않습니다**」라고 적은 것은 이 감사가 반복해 요구해 온 태도입니다
- D-030·D-032 의 「검증한 것 / 못 한 것」 표(`decisions.md:325-331`·`:255-261`)와 `음성-설치.md:195-202` 는 **범위를 밝힌 완료 주장**의 모범입니다. 8종 재현이 8/8 로 맞은 것도 그 덕입니다
- `ai-team/decisions.md:158-159` — D-035 가 곁가지로 `checkpoint.sh` 의 노션 검사가 `'대기'` 만 찾아 **미반영 1건을 초록불로** 보고하던 것을 잡아 고쳤습니다. **초록불을 의심한 실측**입니다
- `supabase-sync.py` 의 안전장치 4종(40개 미만 거부 · 불완전 원격 거부 · 기본 예행 · BOM 검사)은 실측 결과 **전부 실제로 동작합니다.** 문제는 스크립트가 아니라 그것을 우회하는 붙여넣기 덩어리입니다

---

## 이 판이 이전 판(커밋 `0af6c16`, 311행)을 정정한 곳

감사관은 자기 앞 판도 감사합니다. 이전 판에는 **감사관 자신의 규정 위반**이 있었습니다.

| 이전 판 | 실제 | 성격 |
|---|---|---|
| 「정본: `team-rules.md:104-116` 기록 경로 · `:110` events.log · `:90-102` 교차 논쟁」 | **각각 `:76-86` · `:80` · `:65-74`** | **근거 줄번호 오기.** `team-rules.md` 는 그 시점 이후 수정된 적이 없습니다(마지막 커밋 `c8fde92`). 「근거를 **파일경로:줄번호**로 제시한다」는 감사관 자신의 원칙 위반 |
| 치명-3 「두 서술은 **동시에 참일 수 없습니다**」 | **동시에 참입니다.** D-023(상태 8파일)은 진짜 왕복, D-035(팀 정의)는 미수행 — 대상이 다릅니다 | **단정 과잉.** 커밋 `0fd169f` 를 확인하지 않고 모순으로 판정했습니다. 중대-7 로 **범위를 좁혀 재기술**했습니다 |
| 드리프트 6번 ✅ | ❌ — `RESTORE.md:24` 가 `ai-team/docs/` 를 폐기 경로라 선언 (중대-6) | 검사 누락 |
| 「D-030~D-035 는 전부 `ai-team/` 내부, `.claude/` 미수정 → RED 아님」 | ❌ — `2c303f7` 이 `.claude/jarvis.md` 수정 (치명-4) | **검사 누락.** 커밋을 보지 않고 결정 문서 서술만 믿었습니다 |
| 「푸시 페이로드 85행」 · 「BOOTSTRAP 79행 vs 실측 85」 | **86행** | 값 오차 |
| 중대-1 배너 1곳 | **2곳** (`음성-설치.md:102` 누락) | 전수 미흡 |

기전은 두 판 모두 같습니다 — **결정 문서의 서술을 읽고 실물을 열지 않은 것.** 이전 판이 마스터에게 지적한 바로 그 기전입니다.

---

## 반려를 푸는 조건

**필수 — 이것을 고치기 전에는 사이클 3 착수 불가**
1. **치명-1** — `meeting-room/SKILL.md:118`·`:149` 를 `event.sh` 로. 전수 확인 + `check-selftest.py` 회귀 추가
2. **치명-2** — BOOTSTRAP 덩어리에 완전성 검사·예행·차이 표시를 넣거나 **중복 정의를 제거**. `selftest` 에 「저장소 있는 창」 시나리오 추가
3. **치명-3** — `STATE.md:10`·`:14`·`:40` 을 `events.log`·`ai-team/docs/` 실물에 맞추고, 감사 건수를 실물에서 세게 하십시오
4. **치명-4** — D-030 을 `.claude/` 수정 = **RED** 로 재분류하고 D-014 3요건 상태를 명시. `.claude/` 수정 커밋 전수 재확인

**중대 — 함께 처리하십시오 (2~7 은 사이클을 막지 않습니다)**
5. 중대-2 복원 범위 명시 + 「✅ 완전」 문구 축소 · 중대-3 `논쟁` 종류와 `alter table` · 중대-6 `RESTORE.md:24` · 중대-7 「가짜 서버 한정」 표기와 B-08 문구 축소
6. 중대-1 배너 **2곳** · 중대-4 `voice.md` 인자 · 중대-5 콘솔 재발행

**경미**
7. 경미-1 8종 시험 커밋 · 경미-2 BOOTSTRAP 고정 숫자 · 경미-3 `RESTORE.md:53` · 경미-4 갱신일

> 이 감사는 **반려**입니다. `.claude/master-doctrine.md:35` 에 따라
> **감사관이 반려했는데 진행하는 것은 RED 입니다.**
> 시정 후 재감사에서는 **치명-4 의 전수 재확인 결과**를 먼저 확인하겠습니다.
