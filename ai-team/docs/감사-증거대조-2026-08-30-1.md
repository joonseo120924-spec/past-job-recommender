# 🎖️ 증거 대조 감사 — B-07 일괄 12건 중 1차 4건

| | |
|---|---|
| 감사관 | `evidence-auditor` (감사실 · 증거) |
| 대상 | **D-021** · **D-025** · **D-026** · **D-027** |
| 일자 | 2026-08-30 (KST) |
| 대조 개시 HEAD | `8919dfb` → 대조 중 `051a85b` 로 이동 (마스터 세션이 같은 시각 커밋. 아래 §4-2 에 기록) |
| 브랜치 | `claude/meeting-room-team-structure-knz8ob` |
| 병렬 감사 | `team-master` 가 같은 4건을 **규정** 관점으로 `ai-team/docs/감사-규정-2026-08-30-1.md` 에 별도 작성 중. 이 문서는 그 파일을 열지 않았습니다 |

> 이 감사는 **주장을 믿지 않고 파일을 직접 열어** 수행했습니다. 스크립트는 실제로 실행했고, 노션 3개 페이지는 실물을 열어 대조했습니다.
> **증거 부족(⚠️)과 불일치(🔴)를 구별합니다.** 증거가 없다는 것은 거짓이라는 뜻이 아닙니다.

## 판정 요약

| 판정 | 건수 |
|---|---|
| ✅ 증거 있음 | **22** |
| ⚠️ 증거 부족 | **9** |
| 🔴 주장과 실물 불일치 | **6** |
| 합계 | 37 |

**결론: 4건 모두 「기능적 실물은 주장대로 존재」합니다.** 에이전트 31명·형식 검증 6종·로스터·훅 스크립트는 전부 재현됐습니다.
문제는 **실물이 아니라 기록** 쪽에 몰려 있습니다 — 🔴 6건 중 5건이 「같은 사실을 적은 두 문서가 서로 다르다」 유형이고, ⚠️ 9건 중 4건이 「규정은 썼는데 그 규정을 지킨 실행 기록이 없다」 유형입니다.

---

# 1. D-021 — 회의실 진행 중 실시간 백업

| # | 주장 (출처) | 판정 | 대조 결과 |
|---|---|---|---|
| 1-1 | `.claude/skills/meeting-room/SKILL.md` 「4. 진행 중 즉시 백업」 에 반영됨 (`decisions.md` 「D-021 상세」) | ✅ **증거 있음** | `.claude/skills/meeting-room/SKILL.md:139` 이 정확히 `## 4. 진행 중 즉시 백업 (D-021 · 생략 불가)`. 본문 139–156행에 이벤트 5종(호출·승인/반려·결정·막힘·감사) 대응표와 실행 예시 존재 |
| 1-2 | 지시 원문 "회의실이 진행중 일때 바로바로 노션에 자동으로 백업해놔 어디서든 불러도 모든 내용이 동기화 되게" | ✅ **증거 있음** | 노션 「📓 작업 일지」 §2026-08-15 항목의 6번 문단에 **한 글자까지 같은 문장**. `.claude/skills/meeting-room/SKILL.md:140` 인용문과도 일치 |
| 1-3 | 로컬 판(노션만)과 달리 슈퍼베이스 `team_events` 를 **1차 기록처**로 두고 노션은 도구가 붙었을 때만 | ✅ **증거 있음** | `.claude/skills/meeting-room/SKILL.md:145~153` 이 전부 `supabase-sync.py event`, 157행이 "노션 도구가 이 세션에 붙어 있으면 … 없으면 큐에 남기고 **못 했다고 적습니다**". `.claude/team-rules.md:18` 도 같은 서술 |
| 1-4 | **그 규정이 실제로 지켜진 실행 기록** | ⚠️ **증거 부족** | **이행 기록 0건입니다.** ① `ai-team/docs/` 는 `.gitkeep` 뿐, `ai-team/apps/` 도 `.gitkeep` 뿐 — 회의록·출석부 산출물이 하나도 없습니다 ② 저장소 전체에서 `supabase-sync.py event` 문자열이 나오는 곳은 **규정·문서·스크립트 자체 6곳뿐**이고 호출 흔적은 없습니다 ③ `ai-team/supabase/.env` 가 없어 지금 `python3 ai-team/scripts/supabase-sync.py event --kind 감사 --title "테스트"` 를 돌리면 `✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다` 로 끝납니다 (직접 실행 확인). **즉 이 컨테이너에서는 규정을 지키려 해도 지킬 수 없는 상태**입니다 (B-08) |
| 1-5 | D-014 요건 ③ 감사관 검증 = ❌ 미수행 | ✅ **증거 있음** | `decisions.md` 「D-021 상세」 표 · `ai-team/board.md` B-07 행 · `ai-team/STATE.md` 「🟡 YELLOW」 절이 모두 미수행으로 일치. **못 한 것을 했다고 적지 않았습니다** |

> **1-4 는 반려 사유가 아닙니다.** D-021 은 「규정을 넣는다」가 지시였고 규정은 실물로 있습니다. 다만 **「지켜진다」는 아직 아무 근거가 없으므로**, 다음 회의실 개최 시 `event` 호출 로그를 남기는 것이 이 항목을 ✅ 로 바꾸는 유일한 길입니다.

---

# 2. D-025 — 조직 개편 v3 (20명 → 31명)

## 2-1. 인원·명부 — 전수 대조

| # | 주장 | 판정 | 대조 결과 (직접 실행) |
|---|---|---|---|
| 2-1 | 에이전트 **31명** | ✅ **증거 있음** | `ls .claude/agents/*.md \| wc -l` → **31**. `ai-team/scripts/roster.sh` 실행 → `👥 팀 31명` |
| 2-2 | 6본부 29명 + 감사실 2명 구성 | ✅ **증거 있음** | `.claude/team-org.md:23~29` 의 31개 이름을 정렬해 `.claude/agents/` 실제 파일명과 `diff` → **IDENTICAL** (한 명도 남거나 모자라지 않음). 본부별 5·5·4·5·5·5·2 = 31 |
| 2-3 | 본부별 증감 (① 4→5, ② 3→5, ③ 3→4, ④ 3→5, ⑤ 3→5, ⑥ 3→5, 감사실 1→2) | ✅ **증거 있음** | 현재 값이 로스터 실측과 전부 일치. 신설 11명(`source-verifier`·`ux-writer`·`data-analyst`·`accessibility-auditor`·`performance-engineer`·`security-architect`·`compatibility-tester`·`test-automation-engineer`·`privacy-compliance`·`tech-writer`·`evidence-auditor`) 파일 11개 모두 실존 |

## 2-2. 「검증 (실측)」 5종 — **감사관이 직접 다시 셌습니다**

`SESSION-LOG.md` 「2026-08-29 (2) … 검증 (실측)」 절과 노션 일지 같은 항목의 주장을 그대로 재현했습니다.

| # | 주장값 | 감사관 실측값 | 판정 |
|---|---|---|---|
| 2-4 | BOM **0건** | 31개 파일 앞 3바이트 `ef bb bf` 검사 → **0건** | ✅ **증거 있음** |
| 2-5 | 첫 줄 `---` **31/31** | `head -1` 비교 → **ok=31 bad=0** | ✅ **증거 있음** |
| 2-6 | Windows 절대경로 **0건** | `.claude/` 전체에서 `X:\` · `C:/Users` · `\Users\` → **0건** | ✅ **증거 있음** |
| 2-7 | 접두사 없는 산출 경로 **0건** | `.claude/agents/*.md` 에서 `docs/` 로 시작하는 무접두 경로 → **0건** | ✅ **증거 있음** |
| 2-8 | `tools:` 금지 **16/16 준수** | `^tools:` 없는 파일 **16개** = `accessibility-auditor` `brand-designer` `compatibility-tester` `design-lead` `evidence-auditor` `frontend-dev` `fullstack-dev` `functional-tester` `performance-engineer` `qa-lead` `security-tester` `strategy-lead` `team-master` `tech-lead` `test-automation-engineer` `ui-designer` → `.claude/team-rules.md:123` 의 16개 명단과 **집합이 완전히 동일**. 나머지 15개는 `tools:` 보유 | ✅ **증거 있음** |

**5종 전부 주장값과 감사관 실측값이 같습니다.** 이 절은 지어낸 숫자가 아닙니다.

## 2-3. 부수 주장

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 2-9 | `roster.sh --full` 총 31명 = **opus 17 · sonnet 14** (`decisions.md` 「D-028 상세」 3절) | ✅ **증거 있음** | `grep ^model:` 집계 → **opus 17 · sonnet 14**. 나아가 31명 **개별 모델값이 노션 「👥 팀원 31명 역할 명세」와 한 명도 어긋나지 않습니다** |
| 2-10 | 게이트 상향 — `tech-lead` 8→**10항목** · `qa-lead` 5→**6자료** (D-033) | ✅ **증거 있음** | `ai-team/scripts/team-check.sh` 실행 → `✅ 팀 정의 점검 통과 — 31명, 형식·명부·경로·게이트 일치` (exit 0). 이 스크립트가 게이트 수치를 정본과 대조합니다 |
| 2-11 | **지시 원문**: "위와 같음" (= D-026 의 「항상」 지시) — `decisions.md` 「D-025 상세」 첫 줄 | 🔴 **주장과 실물 불일치** | `decisions.md` 에서 D-025 상세 바로 **위**에 있는 절은 「D-026 상세」이고 그 원문은 *"노션 페이지 업데이트 하고 항상 슈퍼베이스에 저장하고…"* 입니다. 그러나 **D-025 의 실제 지시 원문은 다른 문장**입니다 — `SESSION-LOG.md` 「2026-08-29 (2) · 초기화 + 조직 개편 v3」 의 「지시 원문」 줄과 노션 「📓 작업 일지」 §2026-08-29 (2) 가 **둘 다** *"지금 까지 에이전트 팀에서 만든거 초기화시키고 팀 구성 재정비하자"* 로 기록하고 있습니다. **D-014 요건 ②(지시 원문을 그대로 인용해 기록)를 ✅ 로 표시한 근거가 엉뚱한 지시를 가리킵니다.** |

> **2-11 이 왜 중요한가**: D-025 는 `.claude/` 전면 개편이라 RED 이고, 진행 근거가 D-014 예외 3요건입니다. 그중 ② 의 인용이 다른 지시를 가리키면 **예외의 근거 자체가 서류상 성립하지 않습니다.** 실제 지시가 존재한다는 것은 다른 두 기록으로 확인되므로 **거짓은 아니고 인용 오류**입니다. `decisions.md` 「D-025 상세」의 「지시 원문」 줄을 `SESSION-LOG` 의 문장으로 바꾸면 해소됩니다.

---

# 3. D-026 — 「항상」 규칙 4종을 훅으로 강제

## 3-1. 훅 실물

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 3-1 | `.claude/settings.json` 에 `Stop` · `SessionStart` 훅 2개, 기존 SessionStart 훅은 지우지 않고 병합 | ✅ **증거 있음** | `.claude/settings.json:3~24` SessionStart(3개 명령: `cat ai-team/STATE.md` · `roster.sh` · `team-check.sh --quiet`), `:25~36` Stop(`checkpoint.sh --hook`). 기존 STATE 명령이 9행에 살아 있음 |
| 3-2 | `checkpoint.sh` 가 슈퍼베이스 push + GitHub 커밋·푸시 + 노션 큐 확인을 한 번에 | ✅ **증거 있음** | `ai-team/scripts/checkpoint.sh:29~45`(슈퍼베이스) `:47~67`(커밋·푸시·2·4·8초 재시도) `:69~77`(큐). `--hook` 분기는 `:79~83` |
| 3-3 | `[checkpoint]` 태그 커밋으로 사람 커밋과 구별 | ✅ **증거 있음** | `git log --all --grep='^\[checkpoint\]'` → **16건**. 형식은 `checkpoint.sh:52` 의 `MSG` 와 일치 |

## 3-2. 「훅 발화 확인」 — 이 감사의 핵심 쟁점

`STATE.md` 「「항상」 규칙 (D-026 · 2026-08-29 신설)」 절 끝의 두 줄과, 노션 일지 §2026-08-29 (4) 검증표가 **훅 발화를 ✅ 확인**으로 적었습니다. 그런데 **절차 정본 `ai-team/always-on.md:100` 은 같은 항목을 `⚠️ 미검증` 으로 적고 있습니다.** 어느 쪽이 맞는지 실물로 갈랐습니다.

### 3-2-1. `Stop` 훅 — ✅ 증거 있음 (단, 근거는 주장보다 훨씬 좁습니다)

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 3-4 | "`Stop` 훅 발화 확인 — `[checkpoint] 자동 저장` 커밋이 히스토리에 남아 있습니다" | ✅ **증거 있음** | **하지만 16건 전부가 근거는 아닙니다.** `checkpoint.sh:19~25` 상 훅은 `--hook` 만 넘기므로 `REASON` 이 비고, `:52~53` 에 따라 훅이 만든 커밋은 **`· 사유` 접미가 붙을 수 없습니다.** 16건을 분류하면 **접미 없는 것은 단 2건** — `20c932a`(2026-08-30 00:15) 과 `e54d8c6`(2026-08-30 04:23). 나머지 **14건은 `checkpoint.sh "사유"` 수동 실행분**입니다. 결정적 증거는 `e54d8c6` 으로, **이 감사가 진행되는 동안** 발생했고 뒤이은 `051a85b` 커밋 본문이 *"Stop 훅이 그것을 판단 없이 커밋·푸시했다"* 라고 같은 사건을 기술합니다. **훅은 실제로 뜁니다.** |

> **기록 정정 권고**: `STATE.md` 의 근거 문장을 *"`[checkpoint]` 커밋이 있다"* 에서 *"접미 없는 `[checkpoint]` 커밋 `20c932a`·`e54d8c6`"* 로 좁히십시오. 현재 문장은 **수동 실행 14건까지 훅의 증거인 것처럼 읽힙니다.** 이 팀이 D-026 커밋 SHA 를 확인 없이 적었다가 정정한 전례와 같은 축입니다.

### 3-2-2. `SessionStart` 훅 — ⚠️ 증거 부족

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 3-5 | "`SessionStart` 훅 발화 확인 (2026-08-29) — 세션 시작 시 STATE + 로스터가 자동 출력됐습니다" | ⚠️ **증거 부족** | **저장소에 남는 산출물이 하나도 없습니다.** 이 훅의 3개 명령(`cat`·`roster.sh`·`team-check.sh --quiet`)은 **전부 표준출력만 내고 파일을 쓰지 않습니다** — 사후 대조가 원리적으로 불가능합니다. 근거는 마스터 세션의 화면 관찰 1회뿐이고, **같은 항목을 절차 정본 `ai-team/always-on.md:100` 은 지금도 `⚠️ 미검증`으로 적고 있습니다.** 두 문서가 정반대입니다. 이 팀에는 **「SessionStart 훅을 노션에만 작동 확인으로 적었다가 08-11 중대-6 으로 지적받은」 동일 유형의 전례**가 있습니다 (노션 일지 §2026-08-10 (2) 에 그 정정이 남아 있음). 거짓이라는 뜻이 아니라, **관찰을 남기지 않아 대조할 수 없다**는 뜻입니다 |

> **⚠️ 를 ✅ 로 바꾸는 방법**: SessionStart 훅에 `date >> ai-team/.hook-log` 같은 **파일에 남는 흔적**을 한 줄 추가하면 다음 세션부터 대조 가능해집니다. (감사 대상이 흔들리므로 이번 감사 중에 고치지 마십시오.)

## 3-3. 나머지 주장

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 3-6 | 노션 미반영 큐 **1건** — `2026-08-29-6.md` (`STATE.md` 「노션 동기화」 절) | 🔴 **주장과 실물 불일치** | `checkpoint.sh:72` 와 **똑같은 정규식**(`^상태:.*(미반영\|대기\|⏳)`)으로 세면 **2건**입니다 — `ai-team/notion-queue/2026-08-29-6.md`(D-034) **와** `ai-team/notion-queue/2026-08-29-7.md`(D-035). 두 파일 모두 3행이 `상태: ⏳ 미반영`. 두 파일은 `STATE.md` 를 마지막으로 고친 **같은 커밋 `daa788d` 에 함께 들어왔습니다** — 즉 STATE 를 쓰는 시점에 이미 2건이었습니다. **D-026 이 자랑하는 「미반영 건수 감시」의 사용자 노출 숫자가 바로 그 자리에서 틀렸습니다** |
| 3-7 | `ai-team/always-on.md` 「훅 구성 (`.claude/settings.json`)」 표 | ⚠️ **증거 부족** | 표가 SessionStart 명령을 **2개**(`cat STATE` · `roster.sh`)만 적었으나 실물 `.claude/settings.json:17~21` 에는 **3번째 `team-check.sh --quiet`** 가 있습니다 (D-033 에서 추가). 거짓 서술은 아니고 **갱신 누락**입니다. 절차 정본이므로 복원 시 훅 하나가 빠질 수 있습니다 |
| 3-8 | `ai-team/always-on.md:91` — "훅이 푸시하는 브랜치는 `69oi8o`" | 🔴 **주장과 실물 불일치** | 실제 푸시 대상은 `claude/meeting-room-team-structure-knz8ob` 입니다. `checkpoint.sh:48` 이 `HEAD` 를 그대로 쓰고, `git rev-parse HEAD` = `origin/claude/meeting-room-team-structure-knz8ob` 로 동일. `origin/claude/notion-ai-agent-team-import-69oi8o` 의 끝은 `e74f1fa`(D-034)에서 멈춰 있고 이후 8개 커밋이 `knz8ob` 에만 있습니다 |
| 3-9 | 지시 원문 인용 (`decisions.md` 「D-026 상세」) | ⚠️ **증거 부족** | 저장소 인용은 *"…항상 부를 수 있게 해"* 에서 끝나는데 노션 일지 §2026-08-29 (3) 의 같은 원문은 *"…항상 부를 수 있게 해 **계획자세히 잘 짜**"* 로 4어절 더 깁니다. 두 사본이 다르고 **원본(사용자 메시지)에 접근할 수 없어 어느 쪽이 온전한지 확정할 수 없습니다.** D-014 요건 ②는 "그대로 인용"을 요구하므로 최소한 두 사본을 일치시켜야 합니다 |

---

# 4. D-027 — 회의실 전원 점호 + 회의록 형식

## 4-1. 점호 — 실행해서 확인

| # | 주장 | 판정 | 대조 결과 (직접 실행) |
|---|---|---|---|
| 4-1 | `roster.sh --unit N` 이 31명 전원을 🗣/⏳/👁 로 출석부에 낸다 | ✅ **증거 있음** | `ai-team/scripts/roster.sh --unit 1` → 첫 줄 `🔔 출석 31 / 31`, 7개 본부 전원 표시. `--unit all` 도 정상 |
| 4-2 | `roster.sh --unit 1` = **🗣5 / ⏳24 / 👁2** (노션 일지 §2026-08-29 (4) 검증표) | ✅ **증거 있음** | 실측 — 🗣 ① 전략 5명 · ⏳ ②③④⑤⑥ 5+4+5+5+5=**24명** · 👁 감사실 **2명**. 합 31. **주장값과 완전 일치** |
| 4-3 | 발언 안 하는 사람도 **대기 사유**가 나온다 | ✅ **증거 있음** | `roster.sh:35~39` 의 `WAIT` 사전, 출력에 `← ② 설계 승인 후` `← ③ 디자인 승인 + 착수 10항목 충족 후` 등이 실제로 붙음 |
| 4-4 | 「호출하지 않은 사람의 발언을 지어내지 않는다」 규칙 | ✅ **증거 있음** | `.claude/skills/meeting-room/SKILL.md:83~84` ("에이전트가 실제로 한 말을 옮깁니다" · "호출하지 않은 사람의 발언을 만들지 않습니다") · 노션 「👥 팀원 31명 역할 명세」 §회의실 절에 같은 취지의 문장 |

## 4-2. 노션 매뉴얼 교체 — **노션 실물을 열어 대조했습니다**

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 4-5 | 노션 「팀원 명세」를 「옛 20명 + 부록 11명」 → **31명 단일 명세 v3** 로 전면 재작성 | ✅ **증거 있음** | 노션 페이지 「👥 팀원 31명 역할 명세 (복원용)」(`3b750286-df80-814b-8c9f-eb6ed22f7dce`) 를 직접 열었습니다. 제목이 「31명」, 머리말이 `판번호 v3 · 2026-08-29 (D-025 · D-027)`, 본문이 **① 전략(5) ~ 감사실(2) 로 이어지는 단일 명세**이며 「부록 11명」 구조가 없습니다. **주장대로입니다** |
| 4-6 | 그 명세의 내용이 저장소 실물과 맞는가 (감사관 추가 검증) | ✅ **증거 있음** | ① `tools:` 금지 16개 명단이 저장소 실측 16개와 **집합 완전 일치** ② **31명 개별 모델(opus/sonnet) 이 저장소 `.claude/agents/*.md` 의 `model:` 값과 한 명도 어긋나지 않음** ③ 산출 경로가 전부 `<앱폴더>/` 접두. **복원 소스로서 유효합니다** |
| 4-7 | 지시 원문 "내가 회의실 하면 모든 에이전트가 나와서 대화 하며 작업해" | ⚠️ **증거 부족** | 노션 일지 §2026-08-29 (4) 는 같은 지시를 *"**노션 에이전트 메뉴얼을 최신형으로 바꾸고** 내가 회의실 하면 모든 에이전트가 나와서 대화 하며 작업해 **그리고 팀구성을 보여줘**"* 로 기록합니다. `decisions.md` 「D-027 상세」의 인용은 **가운데 토막만** 떼어냈습니다. 3-9 와 같은 유형 |
| 4-8 | 회의실 규정이 실제로 운용된 기록 | ⚠️ **증거 부족** | 저장소에서 `출석 31 / 31` 문자열이 나오는 곳은 `.claude/skills/meeting-room/SKILL.md` **한 곳(명세 예시)** 뿐이고, 실제 개최분 회의록·출석부 산출물은 **0건**입니다. 1-4 와 뿌리가 같습니다 |

---

# 5. 교차 대조 — 4건에 걸친 사실 주장

| # | 주장 | 판정 | 대조 결과 |
|---|---|---|---|
| 5-1 | **일괄 감사 건수** | 🔴 **주장과 실물 불일치** | `ai-team/STATE.md` **한 파일 안에서 세 값이 다릅니다** — 「현재 상태」 표 "다음 단계" 칸은 **12건**, 「🟡 YELLOW」 절 둘째 항목은 **9건**, 「🔴 지금 막힌 것」 표 B-07 행은 **11건**. `ai-team/board.md` B-07 행은 **12건**, 노션 일지 상단 「현재 상태」 표 감사관 칸은 **10건**. 실제로 세면 D-021 + D-025~D-035 = **12건**이 맞습니다. **감사 대상 건수는 감사의 범위 그 자체인데 기준 문서가 스스로 세 값을 말합니다** |
| 5-2 | **정본 브랜치 = `8tlqr8` ↔ `69oi8o` 분기 중** (`STATE.md` 「현재 상태」 표 · B-06) | 🔴 **주장과 실물 불일치** | 실제 작업 브랜치는 **제3의 브랜치 `claude/meeting-room-team-structure-knz8ob`** 입니다. `69oi8o` 는 `e74f1fa` 에서 멈춰 있고 이후 8개 커밋(`cf8bb7c` `20c932a` `daa788d` `5d67c3e` `35ff351` `8919dfb` `e54d8c6` `051a85b`)이 `knz8ob` 에만 있습니다. **B-06 은 「2갈래 분기」로 적혀 있으나 실물은 3갈래**이며, 자동 푸시(D-026)가 매 턴 새 갈래를 더 벌리고 있습니다. `always-on.md:91`(3-8)·노션 팀 홈·노션 팀원 명세 머리말도 전부 `69oi8o` 로 낡았습니다 |
| 5-3 | "슈퍼베이스 왕복 실측 완료 — **push 8/8** · status 일치 · pull 차이 0 · event · 뷰 조회" (`STATE.md` 「슈퍼베이스 (D-023)」 절 · `decisions.md` 「D-023 상세」 · `ai-team/supabase.md` 「검증 기록」) | ⚠️ **증거 부족** | **「8」은 셀 수 있습니다** — `ai-team/scripts/supabase-sync.py:27~28` 의 `FILES` 가 정확히 8개(`STATE.md` `cycle.md` `board.md` `approvals.md` `decisions.md` `questions.md` `SESSION-LOG.md` `README.md`)이고, 실행해 보면 `state files now = 8` 입니다. **그러나 「실제 서버와 왕복했다」는 부분은 자기 보고뿐입니다** — 응답 로그·상태코드·타임스탬프가 저장소 어디에도 보존돼 있지 않고, `supabase-sync.py` 는 이력 파일을 남기지 않으며, 이 컨테이너에 키가 없어 **재현이 불가능**합니다(직접 실행 확인). 08-29 05:26 UTC 커밋 `0fd169f` 의 메시지가 유일한 서술입니다. **거짓이라는 근거는 없습니다.** 다만 이 팀의 기준("실행해서 확인했다 → 실행 로그가 있는가")으로는 미검증입니다 |
| 5-4 | D-035 "**80파일** push → sha256 **80/80** 일치 → 복원본 `team-check.sh` 통과(31명) → `verify` 완전" | ⚠️ **증거 부족** | **메커니즘은 감사관이 직접 재현했습니다** — `python3 ai-team/scripts/selftest-supabase.py` 실행 결과 `✓ 82개 파일 업로드 — 상태 8 · 팀 정의 74` → `대조: sha256 일치 82 / 82` → `복원본 team-check: ✅ … 31명` → `✅ 완전`. **다만 숫자가 80이 아니라 82입니다.** `daa788d` 시점의 팀 정의 파일은 74개(+상태 8 = 82)였고 그 뒤 `gate-check.sh` 가 늘었으므로, 80은 **측정 시점 이후 같은 커밋 안에서 파일이 더 늘어난 것**으로 설명됩니다. 그러나 **주장된 숫자 그대로는 재현되지 않습니다.** 재현 가능한 것은 「82/82 일치」이며 이쪽이 더 강한 증거입니다 |
| 5-5 | 노션 「🏢 AI 앱 개발팀」 팀 홈의 하위 문서 설명 | 🔴 **주장과 실물 불일치** | 팀 홈 「📚 하위 문서」 표가 팀원 명세를 **"기존 20명 명세 + 맨 아래 「신설 11명」 — 둘 다 있어야 31명입니다"** 로 설명합니다. 이는 **D-027 이 폐기했다고 선언한 바로 그 옛 구조**이며, 4-5 에서 확인한 실제 페이지(31명 단일 명세 v3)와 어긋납니다. 같은 표의 작업 일지 설명도 "의사결정 D-001~**D-025**"(실제 D-035)로 낡았습니다. **여기가 「노션에서 팀 가져와」의 진입점**이므로, 낡은 설명은 다음 복원자에게 "20명 명세도 찾아야 한다"는 잘못된 지시가 됩니다 |
| 5-6 | 노션 일지 상단 「현재 상태」 표 — "훅 발화는 ✅ **검증 완료(SessionStart·Stop·team-check 셋 다)**" | ⚠️ **증거 부족** | Stop 은 ✅(3-4), SessionStart 는 ⚠️(3-5), **`team-check` 훅 발화는 저장소·노션 어디에도 근거가 없습니다** — `team-check.sh --quiet` 는 이상이 없으면 침묵하도록 만들어져(`STATE.md` 「재발 방지」 절) **정상 동작 시 아무 흔적도 남기지 않습니다.** 「셋 다」는 실제로 확인된 것보다 넓은 주장입니다. 저장소 정본 `ai-team/always-on.md:100` 은 여전히 「미검증」이라 **사람이 읽는 노션이 저장소보다 강하게 적혀 있습니다** — 08-11 중대-6 과 같은 방향의 편차입니다 |

---

# 6. 감사관이 대조 중 관측한 사건 (기록)

이 감사가 도는 동안 `HEAD` 가 `8919dfb` → `e54d8c6` → `051a85b` 로 두 번 움직였습니다. 감사 대상이 감사 중에 바뀐 것이므로 사실만 남깁니다.

- `e54d8c6` 은 **접미 없는 `[checkpoint]` 커밋**이며, `ai-team/scripts/gate-check.sh`(신규 75행) 와 `ai-team/scripts/master-listen.config.json`(1행 변경)을 함께 담았습니다. 이것이 3-4 의 결정적 증거입니다.
- 뒤이은 `051a85b` 은 그 커밋에 **아무도 의도하지 않은 설정 변경**(`master-listen.config.json` 의 `hotkey` 가 `""` → `"x"`)이 섞였음을 스스로 기록하고 되돌렸습니다. 원인은 병렬 감사 중인 `team-master` 가 검증을 위해 `master-listen.py` 를 실행한 것이고, `Stop` 훅이 그것을 판단 없이 커밋·푸시했습니다.
- 감사관 소견: **이것은 D-026 「자동 커밋의 대가」가 실제로 청구된 첫 사례**이며, 그 대가를 `decisions.md` 「D-026 상세」가 미리 감수한다고 적어 둔 범위 안에 있습니다. 되돌림과 원인 기술이 정확해 **은폐가 아닙니다.** 다만 5-2 와 겹쳐 **훅이 매 턴 낡은 브랜치가 아닌 세 번째 브랜치로 푸시하고 있다**는 사실을 다시 확인시킵니다.
- 대조 중 감사관이 실행한 `selftest-supabase.py` 는 임시 디렉터리에만 썼고, 실행 후 `git status --porcelain` 은 비어 있었습니다. **이 감사는 저장소를 바꾸지 않았습니다** (이 보고서 파일 제외).

---

# 7. 판정

| 대상 | 실물 | 기록 | 감사관 판정 |
|---|---|---|---|
| **D-021** | ✅ 규정 실물 존재·정확 | ⚠️ 이행 기록 0건 | **조건부 — 증거 있음.** 반려 사유 없음. 「지켜진다」는 아직 미검증이므로 다음 회의실에서 `event` 로그 필수 |
| **D-025** | ✅ 31명·검증 5종 전부 재현됨 | 🔴 지시 원문 인용이 다른 지시를 가리킴 | **조건부 — 실물 합격, 인용 1건 정정 요구.** 개편 자체를 되돌릴 근거는 없습니다 |
| **D-026** | ✅ 훅 실물·`Stop` 발화 확인 | 🔴 미반영 건수 오기 · 🔴 브랜치 서술 · ⚠️ `SessionStart` 발화 | **조건부 — 정정 3건 요구.** 특히 5-1·5-2 는 **감사 범위와 푸시 대상**에 관한 것이라 먼저 고쳐야 합니다 |
| **D-027** | ✅ 점호 실행 확인 · 노션 명세 실물 확인 | ⚠️ 인용 절단 · ⚠️ 개최 기록 0건 | **증거 있음.** 4건 중 실물 근거가 가장 두껍습니다 |

## 감사관이 마스터에게 요구하는 정정 (6건 · 전부 기록 수정이며 코드 변경 아님)

1. `decisions.md` 「D-025 상세」의 「지시 원문: 위와 같음」 → `SESSION-LOG.md` 의 실제 문장으로 교체 **(2-11)**
2. `ai-team/STATE.md` 의 감사 건수를 **12건**으로 통일 (현재 12/9/11 세 값) **(5-1)**
3. `ai-team/STATE.md` 「노션 동기화」 절의 미반영 건수 **1건 → 2건** **(3-6)**
4. `ai-team/STATE.md` B-06 · `ai-team/always-on.md:91` 의 브랜치 서술에 **`knz8ob`** 반영 (2갈래 → 3갈래) **(5-2 · 3-8)**
5. `ai-team/STATE.md` 의 `Stop` 훅 근거를 **`20c932a`·`e54d8c6` 두 커밋으로 좁히고**, `SessionStart` 는 `always-on.md` 와 같이 **⚠️ 미검증**으로 되돌릴 것 **(3-4 · 3-5)**
6. 노션 팀 홈 「📚 하위 문서」 표의 팀원 명세 설명을 **31명 단일 명세**로, 작업 일지 설명을 **D-035** 까지로 갱신 **(5-5)** — 미반영 큐 2건과 함께 처리

> **감사관은 승인하지 않습니다. 반려하지도 않습니다.**
> 위 6건은 전부 **되돌릴 수 있는 기록 수정**이고, 실물은 4건 모두 주장대로 존재합니다. 최종 진행 판정은 `team-master` 의 규정 감사와 합쳐 마스터가 내립니다.
> **이 보고서의 모든 판정 근거는 파일 경로 또는 실행한 명령입니다.** 근거를 대지 못한 항목은 ⚠️ 로 남겼고, 추측으로 🔴 를 붙이지 않았습니다.
