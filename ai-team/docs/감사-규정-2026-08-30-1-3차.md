# 🎖️ 규정 감사 — 3차 재감사 (2026-08-30)

| 항목 | 내용 |
|---|---|
| 감사관 | `team-master` (독립 · 어느 본부에도 속하지 않음) |
| 대상 | 재감사 잔존 치명 1건(요구 3건) · 중대 4건 · 커밋 `c8fde92` · `f9a1f9f` · `b82ba2f` |
| 방법 | 파일 직접 열람 + git diff 전수 + **감사관이 직접 만든 회귀 34종을 `/tmp` 사본에 주입**. 마스터의 서술도, 마스터의 `check-selftest.py` 결과도 근거로 채택하지 않음 |
| 선행 보고서 | `ai-team/docs/감사-규정-2026-08-30-1.md` · `-재감사.md` · `-2.md` (**고치지 않았습니다**) |

```
판정: ⚠️ 조건부 승인
치명: 0건   중대: 3건   경미: 3건
판본 드리프트 검사: 5/6
```

> **1차·재감사 반려를 해제합니다.** 잔존 치명 1건(「요구 2건 중 0건 이행」)은 닫혔습니다.
> 다만 **사이클 3 착수는 여전히 불가**입니다 — 사유는 이 판정이 아니라 **B-07 12건 중 4건(D-030~D-032·D-035)이 미감사**이기 때문입니다.

> 감사관은 저장소를 편집하지 않았습니다. 주입 시험은 전부 `/tmp` 사본에서 했고
> `git status --porcelain` 무출력으로 원본 무변경을 확인했습니다.

---

## 0. 요구 3건 — 전부 이행 확인

| 요구 | 판정 | 근거 |
|---|---|---|
| ① `master-doctrine.md` RED 절과 대원칙 2 **양쪽** | ✅ 이행 | `.claude/master-doctrine.md:10-13`(대원칙 2 아래) · `:40-43`(RED 예외 절 아래). 문안이 요구를 넘어 「차단됨 기록 + 사용자 보고가 정상 행동」까지 명시 |
| ② 결정 절에 판정 병기 6건 | ⚠️ **5건 정확 · 1건 대상 오지정** | 아래 [중대-1] |
| ③ 점검기를 값 대조로 | ⚠️ **크게 개선됐으나 범위가 6게이트 중 2게이트** | 아래 [중대-2] |

**`:451` 처리에 대한 판정**: 원문을 지우지 않고 그 자리에 판정을 병기한 처리는 **옳습니다**
(`ai-team/decisions.md:490`). 기록을 고쳐 쓰면 그 다음 감사가 무엇을 대조할 대상을 잃습니다.
이 방식을 앞으로의 표준으로 인정합니다.

**커밋 범위 검증**: `git diff 053e296 b82ba2f -- .claude` 를 전수 대조했습니다.
에이전트 31개는 필독 안내 1줄의 기계적 치환, 나머지는 `master-doctrine`(2절 추가) ·
`team-rules`(교차 논쟁·기록 경로) · `daily-app`/`meeting-room` SKILL(2차 감사 지적분)입니다.
**감사 요구 범위를 벗어난 변경은 없습니다.**

---

## [중대-1] 대원칙 2 각주가 지목한 6건과 실제 병기된 6건이 다릅니다 — 교리의 근거가 틀렸습니다

`.claude/master-doctrine.md:13` 은 반복 사례를 **D-016·D-021·D-025·D-026·D-027·D-035** 로 적었습니다.
그러나 `decisions.md` 에 실제로 병기된 6곳은 다음입니다.

| 병기 위치 | 소속 절 |
|---|---|
| `ai-team/decisions.md:58` | D-021 상세 ✅ |
| `ai-team/decisions.md:203` | **D-034 상세** (168–208) ← 교리 목록에 없음 |
| `ai-team/decisions.md:422` | D-027 상세 ✅ |
| `ai-team/decisions.md:454` | D-026 상세 ✅ |
| `ai-team/decisions.md:487` | D-025 상세 ✅ |
| `ai-team/decisions.md:637` | D-016 상세 ✅ |

**D-035 상세(`:139-167`)에는 판정이 병기되어 있지 않습니다.** 그리고 D-035 자신의 기록은
`ai-team/decisions.md:164` 에서 이렇게 말합니다 — "**`.claude/` 는 건드리지 않았으므로 RED 예외 대상은 아니지만**".

감사관이 실측했습니다.

```
$ for c in daa788d 5d67c3e 35ff351 8919dfb; do git show --stat --format="" $c -- .claude | wc -l; done
0 / 0 / 0 / 0      ← D-035 관련 커밋 4개 전부 .claude/ 무변경
$ git log --oneline --all -- .claude | grep e74f1fa
e74f1fa 총괄 호칭을 「마스터」로 되돌림 (D-034)   ← .claude/ 변경 있음
```

즉 **병기 위치가 맞고 교리의 목록이 틀렸습니다.** D-035 는 「되돌릴 수 있으므로 `.claude/` 수정을
밀어붙인」 사례가 아니며, 실제 사례인 D-034 가 목록에서 빠졌습니다.

- **왜 중대인가**: 이 각주는 이번 감사의 핵심 교훈을 담는 문장이고, RED 판단의 근거로 인용될 자리입니다.
  근거 사례가 틀린 교리는 다음 세션에서 "D-035 는 `.claude/` 를 안 건드렸는데?" 한 번으로 통째로 신뢰를 잃습니다.
  또한 교리(`master-doctrine.md:13`)와 기록(`decisions.md:164`)이 **정면으로 모순**합니다 — 판본 드리프트 5번 부류입니다.
- **요구**: `master-doctrine.md:13` 의 목록을 **D-016·D-021·D-025·D-026·D-027·D-034** 로 정정하거나,
  D-035 를 남기려면 그 근거(어느 커밋이 `.claude/` 를 건드렸는지)를 제시하십시오.
  D-035 상세에는 「`.claude/` 미변경이므로 이 6회에 포함되지 않음」을 한 줄 적어 다음 감사가 헷갈리지 않게 하십시오.

---

## [중대-2] 점검기 — 값 대조가 **6게이트 중 2게이트**에만 걸려 있습니다

마스터의 중대-3 자인은 **받아들입니다.** `check-gates.py` 는 재감사 때보다 확실히 강해졌고,
감사관이 재주입한 회귀도 대부분 잡았습니다. 그러나 마스터가 이번에 쓴 문장은 다시 실측보다 넓습니다.

> `check-gates.py:10-11` — "정본(team-rules.md)의 게이트 표에서 항목 이름을 뽑아 / 그 항목을 실제로 심사하는 파트장 파일이 같은 이름을 전부 갖고 있는지"

**실제 구현은 ④ 개발 착수(`:69-77`)와 ⑤ QA 착수(`:80-100`) 두 게이트뿐입니다.**
② 설계 완료 · ③ 디자인 완료 · ⑤ QA 완료 · ⑥ 출시 착수는 `:64` 의 **줄 존재 확인**만 받습니다.
조건 칸의 내용은 아무도 보지 않습니다.

### 감사관 독립 주입 — 34종 중 **20종 미검출**

`/tmp` 사본에 감사관이 직접 주입하고 `team-check.sh` + `check-gates.py` 를 돌린 결과입니다.

**A. 마스터가 「감사관이 쓴 8종」이라 부른 것들 (감사관이 재주입)** — 8종 중 7종 검출

| 주입 | 결과 |
|---|---|
| 정본 ④ 에서 `출처검증` 삭제 | ✅ 검출 |
| 정본 ⑤ `보안설계서` → `보안검토서` | ✅ 검출 |
| `team-org.md` 에 옛 게이트 표 부활 | ✅ 검출 |
| `tech-lead` description 8항목 회귀 | ✅ 검출 |
| `qa-lead` description 5자료 회귀 | ✅ 검출 |
| `design-lead` 실무자 3인 → 2인 | ✅ 검출 |
| `product-planner` 2차 통합 4인 → 2인 | ✅ 검출 |
| `strategy-lead` **description 줄**의 「실무자 4인」 → 3인 | 🔴 **통과** |

마지막 줄이 핵심입니다. `check-gates.py:120` 은 「**우리 본부 실무자 N인**」 제목 줄만 봅니다.
`strategy-lead.md:3` 의 `description:` 줄에도 같은 숫자가 있는데 그 줄은 대조 대상이 아닙니다.
**게이트 숫자는 `description:` 을 보면서(`team-check.sh:82-83`) 인원 숫자는 안 봅니다** —
같은 파일 안에서 3인과 4인이 동시에 성립합니다.

**B. 감사관 신규 회귀 26종 중 19종 미검출**

| 주입한 회귀 | 결과 | 실제 위험 |
|---|---|---|
| 정본 ⑤ QA 완료에서 **「남은 오류 0개」 삭제** | 🔴 통과 | `team-rules.md:136` 이 「어떤 경우에도 면제 대상이 아니다」라고 못 박은 **유일한 절대 조항**이 무방비 |
| 면제 조항에서 **「오류 0개는 면제 불가」 줄 삭제** | 🔴 통과 | 위와 짝. 두 줄만 지우면 오류를 남긴 채 출시 가능 |
| ⑤ QA 완료 **「20회 이상」 → 「5회 이상」** | 🔴 통과 | 품질 게이트 완화가 무검출 |
| ⑥ 출시 착수 **「데이터안전 대조표」 삭제** | 🔴 통과 | |
| ③ 디자인 완료 **「접근성 실측표」 삭제** | 🔴 통과 | `accessibility-auditor` 신설 근거가 통째로 사라짐 |
| ② 설계 완료 **「문구(모든 E-코드에 1:1)」 삭제** | 🔴 통과 | `ux-writer` 신설 근거가 사라짐 |
| ⑥ 출시 착수 **조건 칸을 `—` 로 비움** | 🔴 통과 | 줄만 있으면 통과. `check-gates.py:64` 는 `None` 만 봅니다 |
| ④↔⑤ **반복 「최대 3회」 → 「제한 없음」** | 🔴 통과 | |
| `team-org.md` 게이트 표 부활 — **제목에서 띄어쓰기만 제거**(`④ 개발착수`) | 🔴 **통과** | **D-036 이 막으려던 사고가 여전히 우회 가능** |
| 조직 표에서 **③ 디자인 본부 행 통째 삭제** | 🔴 통과 | 명부를 그 표에서 파생하므로, 행을 지우면 그 본부 검사가 **조용히 사라집니다** |
| 조직 표 실무자 1명 삭제 + 파트장 「4인→3인」 동시 위조 | 🔴 통과 | 조직 정본 축소가 무검출 |
| handoff 파일명 `05-QA.md` → `05-품질.md` | 🔴 통과 | 판본 드리프트 **2번 항목 자체가 점검기 밖** |
| 「`ai-team/handoff/` 는 폐기된 경로」 → 「써도 됩니다」 | 🔴 통과 | |
| 「**실무자는 승인권이 없습니다**」 → 「실무자도 승인권을 가집니다」 | 🔴 통과 | 판본 드리프트 **4번 항목이 점검기 밖** |
| `master-doctrine.md` RED 에서 **「`.claude/` 아래 파일 수정」 삭제** | 🔴 **통과** | |
| `master-doctrine.md` RED 에서 **「감사관이 반려했는데 진행」 삭제** | 🔴 **통과** | |
| RED 예외 **3요건 → 2요건**(「③ 감사관 검증」 삭제) | 🔴 **통과** | |
| (검출된 7종: ⑤ 착수 자료 6→5개 · ④ 항목명 변경 · 10항목 낱말 회귀 · 게이트 표 이전 · `tech-lead` 인수인계 절 삭제 · `qa-lead` 최신코드 개명 · 조직 표 실무자 단독 삭제) | ✅ | |

### 가장 무거운 것 — **`master-doctrine.md` 를 읽는 점검기가 하나도 없습니다**

```
$ grep -rn "master-doctrine" ai-team/scripts/
ai-team/scripts/supabase-sync.py:43:   ".claude/master-doctrine.md", ...   ← 백업 대상 목록일 뿐
```

`team-check.sh` 도 `check-gates.py` 도 이 파일을 열지 않습니다. 그래서 **이번 감사가 그 파일에 새로
써 넣은 두 절(`:10-13` · `:40-43`)도, RED 목록 8줄도, 예외 3요건도 지워지면 아무도 모릅니다.**
자동 실행 중 마스터를 막는 최후의 문장들이 점검기의 사각지대에 통째로 있습니다.

- **요구 (우선순위 순)**
  1. `master-doctrine.md` 의 **RED 8줄 · 예외 3요건 · 대원칙 2 각주**의 존재를 점검기가 확인할 것.
     낱말이 아니라 **줄 개수와 항목 집합**으로 보십시오 (RED 8개, 요건 3개)
  2. **6게이트 전부**를 항목 집합으로 대조할 것. ④·⑤만 배열로 두고 나머지를 존재 확인으로 남기면
     지금 표의 절반이 지워져도 초록불입니다. 최소한 ⑤ QA 완료의 **「남은 오류 0개」·「20회 이상」**,
     ⑥ 의 **「데이터안전 대조표」**, ③ 의 **「접근성 실측표」**, ② 의 **「문구」** 는 문자열로라도 고정하십시오
  3. `team-org.md` 게이트 표 부활 검사를 **제목 문자열 일치**에서 **정규식**(`^\|.*(항목|자료).*(전부|:)`)으로 바꿀 것.
     띄어쓰기 하나로 우회됩니다
  4. 조직 표 **행 개수 6개 + 감사실 1행**을 먼저 확인한 뒤 파생할 것. 파생원을 검증하지 않는 파생은
     원본이 지워질 때 **더 조용해집니다**
  5. `description:` 줄의 **인원 숫자**도 대조 대상에 넣을 것
  6. 위 6종을 `check-selftest.py` 에 회귀로 넣고 **검출 로그를 제출**할 것

---

## [중대-3] 게이트 사본 4벌 유지 — **조건부 인정**. 다만 사본 하나는 **이미 드리프트했습니다**

마스터가 판정을 요청한 사안입니다. 판정합니다.

**원칙적으로 인정합니다.** 심사자가 손에 목록을 들고 있어야 한다는 논지는 옳고, 사본을 지우면
파트장이 정본을 열어 읽는 한 번의 왕복이 추가돼 실제로는 「안 읽고 넘어가는」 경로가 늘어납니다.
**단, 인정 범위는 「값 대조가 실제로 덮는 사본」에 한합니다.** 대조 밖의 사본은 사본이 아니라 **제2의 정본**이고,
그것이 D-036 치명-2 의 기전 그 자체였습니다.

| 사본 | 값 대조 | 판정 |
|---|---|---|
| `.claude/agents/tech-lead.md` ④ 10항목 | ✅ `check-gates.py:69-77` | **인정** |
| `.claude/agents/qa-lead.md` ⑤ 6자료 | ✅ `:80-88` | **인정** |
| `.claude/agents/tech-lead.md` 인수인계 절 6자료 | ✅ `:93-100` | **인정** |
| `ai-team/scripts/gate-check.sh` g2~g6 | ❌ 없음 | 🔴 **불인정 — 이미 어긋났습니다** |
| `.claude/agents/product-planner.md:46` ②책임 5항목 | ❌ 없음 | ⚠️ 조건부 |
| `ai-team/scripts/roster.sh:37` · `roster-full.py:27,30` | ❌ 없음 | ⚠️ 조건부 |

**증거 — 대조 밖 사본은 이미 드리프트했습니다.**

```
ai-team/scripts/gate-check.sh:57   g5() { echo "⑤ QA 착수 게이트 — 6자료"
ai-team/scripts/gate-check.sh:59      … printf "  ❌ %-12s %s 없음 (5자료가 여기 실립니다)\n" …
```

**한 함수 안에서 6자료와 5자료가 동시에 적혀 있습니다.** 판본 드리프트 검사 5번(한 파일 안 자기모순)
위반이며, 이 파일은 `team-rules.md:113` 과 `team-org.md:80` 이 파트장에게 쓰라고 지정한 **실측 보조 도구**입니다.
사본을 남기겠다는 판단 자체는 인정하지만, **그 판단의 전제(「어긋나면 잡힌다」)는 이 한 줄로 반증됩니다.**

- **요구**: ① `gate-check.sh:59` 의 `5자료` 를 `6자료` 로 즉시 정정 ② `gate-check.sh` 의 g2~g6 항목
  이름을 `check-gates.py` 의 대조 대상에 포함 ③ 그때까지 `team-rules.md:113` 의 「실측 보조」 안내
  옆에 「이 스크립트의 목록은 정본 대조 밖입니다」를 명시.

---

## 나머지 중대 3건 — 이행 확인

| | 지적 | 판정 | 근거 |
|---|---|---|---|
| 중대-1 | D-021 「✅ 반영됨」 → 실측대로 | ✅ 이행 | `ai-team/decisions.md:26`(요약표) · `:50`(상세). **B-08 명기 확인**. 「규정만 반영됨 (2026-08-29). 실제 백업 실행은 0건」 |
| 중대-3 | 점검기 값 대조 | ⚠️ 부분 — 위 [중대-2] |
| 중대-4 | 브랜치 경고 배너 | ✅ 이행 | `ai-team/local-windows.md:1-5` · `ai-team/음성-설치.md:1-3`. 둘 다 최상단, `reset --hard` 위험까지 명시. **B-06 이 사용자 결정 사항이라 문서를 통째로 바꾸지 않고 경고만 단** 판단도 적절 |

---

## 경미 3건

| | 지적 | 근거 |
|---|---|---|
| 경미-1 | 「당신이 쓴 8종」이 정확하지 않습니다 | 재감사 보고서 `:190-199` 의 8종 중 **「정본 ⑥ 출시 착수 행 삭제」가 `check-selftest.py` 에 없고**, 「`strategy-lead` 2차 통합 4인→2인」이 **`product-planner`** 로 바뀌었습니다(`check-selftest.py:32-33`). 감사관이 확인한 결과 두 경우 모두 **검출력 손실은 없습니다**(⑥ 행 삭제는 `check-gates.py:64` 가 잡습니다). 그래도 **감사 증거의 구성을 사실과 다르게 서술한 것**이므로 기록합니다 |
| 경미-2 | `check-gates.py` 의 `10`·`6` 이 상수로 박혀 있습니다 (`:73`·`:84`) | 게이트를 정당하게 올리면 점검기가 **정상 상태를 오검출**합니다. 지금은 이 상수가 닻 역할을 하므로 유지가 맞지만, 바꿀 때 스크립트도 함께 고쳐야 한다는 것을 `team-rules.md` 게이트 절에 한 줄 남기십시오 |
| 경미-3 | `.claude/` 수정의 **감사 시정 경로**가 규정에 없습니다 | `master-doctrine.md:36` 의 3요건은 ①「사용자가 대화 세션에서 직접 지시」를 요구하는데, **감사 반려 시정은 사용자 지시가 아니라 감사관 요구의 이행**입니다. 문언 그대로면 시정 자체가 RED 가 되어 「반려 → 시정 불가 → 영구 반려」의 교착이 생깁니다(D-013 부류). 이번 3커밋은 이 결함 때문이지 마스터의 위반이 아니라고 판정합니다 — **치명으로 세지 않습니다.** 다음에 확대 해석되지 않도록 「감사관이 반려 사유로 **명시 요구한 범위 내**의 수정은 허용하되 즉시 ③ 검증을 받는다」를 예외 절에 명문화하십시오 |

---

## 마스터 감사 — 6항목

| 항목 | 판정 | 근거 |
|---|---|---|
| RED 항목을 진행했는가 | ✅ 아니오 | `.claude/` 3파일 수정은 감사 요구 범위 내(diff 전수 확인). 규정 공백은 경미-3 |
| YELLOW 를 최상단에 표기했는가 | ✅ | `ai-team/board.md:7` 「사이클 착수 불가 — 감사 반려가 유효합니다」 |
| GREEN 결정을 `decisions.md` 에 기록했는가 | ✅ | D-037 신설(`:85-112`), 요약표 반영 |
| 승인 없이 다음 단계로 넘어갔는가 | ✅ 아니오 | `ai-team/STATE.md:14` · `board.md:9` 모두 착수 불가 유지 |
| 품질 게이트를 시간 부족으로 완화했는가 | ✅ 아니오 | 게이트 수치·조건 모두 상향 상태 유지 |
| **미완을 완료로 보고했는가** | ⚠️ **범위 과대 진술 1건** | 「점검기가 값 일치를 강제합니다」의 재발판 — `check-gates.py:10-11` 의 서술이 6게이트 전체처럼 읽히나 구현은 2게이트(중대-2). **다만 이번에는 마스터가 직전 주장의 거짓을 스스로 인정하고 반증을 자청했습니다.** 1차의 「고쳤다고만 보고」, 재감사의 「실측 없이 넓은 주장」 유형은 재발하지 않았습니다 |

**감사관이 인정하는 것**: 「뚫리면 그게 이번 감사의 성과입니다」라고 먼저 적고 반증을 요청한 것,
자기 주장이 거짓이었다고 먼저 쓴 것(`중대-3`), `:451` 의 불리한 문장을 지우지 않고 남긴 것은
**규정이 요구하는 수준 이상**입니다. 조건부 승인의 근거입니다.

---

## 판본 드리프트 검사 — 5/6

| # | 항목 | 결과 |
|---|---|---|
| 1 | 단계 번호 일치 (`N단계` 포함) | ✅ `daily-app` 6단계 · `board.md:19` · `STATE.md:1` 일치 |
| 2 | handoff 파일명 `01-전략`~`06-출시운영` | ✅ 전 문서 6종만 출현, 이형 0건 |
| 3 | 퇴역 에이전트 호출 | ✅ 문서가 참조하는 백틱 역할명 중 `.claude/agents/` 에 없는 것 **0건**(31/31) |
| 4 | 실무자가 승인권을 주장 | ✅ 0건. `team-org.md:31` 명문 유지 |
| 5 | **한 파일 안 자기모순** | 🔴 **`ai-team/scripts/gate-check.sh:57` vs `:59`** (6자료/5자료). 부수적으로 `master-doctrine.md:13` ↔ `decisions.md:164` (중대-1) |
| 6 | 산출 경로 문서마다 다름 | ✅ `team-master.md:47` · `evidence-auditor.md:26` 모두 `<앱폴더>/` 접두 |

재감사 4/6 → **5/6**.

---

## 유보 해소 — M-4 (② 설계 완료·⑥ 출시 착수 게이트 신설)

**마스터의 반론을 받아들입니다. 유보를 해소하고 신설이 아님으로 확정합니다.**

- 1차 보고서 원문에 있습니다 — `ai-team/docs/감사-규정-2026-08-30-1.md:113`
  "`team-rules.md` 의 게이트 표에는 **② 설계 완료 게이트와 ⑥ 출시 착수 게이트가 통째로 없습니다**
  (`team-org.md:77` · `:82` 에만 존재)"
- 조건 자체도 새로 만든 것이 아닙니다. `git show 053e296^:.claude/team-org.md` 의 `:77`·`:82` 와
  현 `team-rules.md:105`·`:110` 의 조건이 일치합니다. ⑥ 의 `approvals.md` 가 `ai-team/approvals.md` 로
  바뀐 것은 **B-02 부류의 개선**입니다.

---

## 3차 승인 조건 (닫기 전에는 다음 감사를 열지 않습니다)

1. **중대-1** — `master-doctrine.md:13` 의 6건 목록 정정. D-035 상세에 「`.claude/` 미변경」 한 줄
2. **중대-3** — `gate-check.sh:59` 의 `5자료` 즉시 정정 (드리프트 5번을 닫는 유일한 항목)
3. **중대-2** — 요구 6종. 그중 **1번(`master-doctrine.md` 를 점검기가 읽게 할 것)과 3번(게이트 표 부활 정규식)이 선결**입니다.
   나머지는 순차 가능하나, 추가할 때마다 **회귀를 주입한 로그를 함께** 제출하십시오
4. 경미 3건은 다음 감사에서 함께 봅니다

**미감사 4건(D-030·D-031·D-032·D-035)이 남아 있으므로 사이클 3 착수는 계속 불가입니다.**
이는 이번 판정과 무관한 별개 사유입니다.

---

## 감사 방법 기록 (재현용)

```
$ python3 ai-team/scripts/check-gates.py        # rc=0
$ bash ai-team/scripts/team-check.sh            # ✅ 31명
$ python3 ai-team/scripts/check-selftest.py     # ✅ 회귀 10종 전부 검출 (마스터 주장 재현됨)
$ python3 <감사관 독립 주입기 34종>              # 20종 미검출
$ git status --porcelain                        # 무출력 — 원본 무변경
```

감사관의 주입기는 `/tmp` 사본에만 작용하며 저장소에 남기지 않았습니다.
재현이 필요하면 이 보고서의 [중대-2] 표에 있는 (파일, 찾을 것, 바꿀 것) 그대로 주입하면 됩니다.
