# 감사 판정 — 규정감사관(`team-master`) · 2026-08-30 (2차)

> 대상: **D-028 · D-029 · D-033 · D-034** (일괄 감사 12건 중 2차 4건)
> 1차 4건(D-021·D-025·D-026·D-027)은 다른 호출이 재감사 중이므로 **건드리지 않았습니다.**
> 다른 감사 파일(`감사-규정-2026-08-30-1.md` · `감사-증거대조-2026-08-30-1.md`)은 **열지 않았습니다.**
> 판정 근거는 전부 이 세션에서 파일을 직접 열거나 명령을 직접 실행해 얻은 것입니다.

```
판정: ❌ 반려 (대상 4건 중 D-028 · D-034 반려 / D-029 · D-033 조건부 승인)
치명: 4건   중대: 4건   경미: 3건
판본 드리프트 검사: 4/6
```

## 항목별 판정

| 건 | D-014 ① 지시 | ② 원문 기록 | ③ 감사 검증 (이번) | 판정 |
|---|---|---|---|---|
| **D-028** 교차 논쟁 | ✅ `ai-team/decisions.md:340` | ✅ 같은 줄 | **❌ 반려** | 치명-1·2 |
| **D-029** 자비스 = 메인 스레드 | ✅ `ai-team/decisions.md:305` | ✅ 같은 줄 | ⚠️ **조건부 승인** | 아래 참조 |
| **D-033** 구멍 4종 + 점검기 | ✅ `ai-team/decisions.md:178` | ✅ 같은 줄 | ⚠️ **조건부 승인** (D-036 시정 확인) | 중대-3 |
| **D-034** 호칭 되돌림 | ✅ `ai-team/decisions.md:139` | ✅ `.claude/master.md:5` 와 일치 | **❌ 반려** | 치명-3·4 |

**요건 ①② 는 4건 모두 충족**했습니다. 지시 원문은 `decisions.md` · `.claude/master.md` · `.claude/skills/meeting-room/SKILL.md` · `ai-team/notion-queue/2026-08-29-6.md` 네 곳을 **글자 단위로 대조**해 서로 어긋나지 않음을 확인했습니다.

---

## 먼저 — 점검기 실행 결과

```
$ ai-team/scripts/team-check.sh
✅ 팀 정의 점검 통과 — 31명, 형식·명부·경로·게이트 일치
```

```
$ ai-team/scripts/roster.sh --unit all   → 🔔 출석 31 / 31 (본부별 정상)
$ python3 ai-team/scripts/roster-full.py → 🎩 총괄 마스터 … 총 31명 (opus 17 · sonnet 14)
$ ai-team/scripts/gate-check.sh ai-team/apps/none --gate 4 → ⚠️ 앱 폴더가 없습니다 (정상 동작)
$ python3 ai-team/scripts/supabase-sync.py status → ✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다
```

**도구 4종은 실존하고 실제로 돕니다.** 설정 파일을 읽고 추측한 것이 아니라 직접 호출했습니다.
다만 **초록불이 곧 합격은 아닙니다.** 아래 치명 4건 중 **점검기가 잡은 것은 0건**입니다.

---

# 치명

## [치명-1] 교차 논쟁(D-028)이 **최우선 정본에 없습니다** — 정본 우선순위상 무효

근거:
- `.claude/team-rules.md:3` — 「**무엇과 충돌하든 이 파일이 우선합니다.**」
- `.claude/team-rules.md:62-63` — 「의견 충돌 해결」 절 전문:
  `① 각자 근거 제시 → ② 장단점 비교 → ③ 사용자 가치 우선 → ④ 보안·안정성 우선 → ⑤ decisions.md 기록`
  **교차 논쟁·3라운드 상한·미합의 기록 조항이 한 글자도 없습니다.** 다른 문서로 넘기는 참조도 없습니다.
- 실제 프로토콜은 `.claude/skills/meeting-room/SKILL.md:90-118` · `.claude/master.md:35` · `ai-team/STATE.md:106` 세 곳에만 있습니다.

무엇이 문제인가: 팀원 31명이 필독하는 정본은 `team-rules.md` 와 `team-org.md` 뿐입니다(`.claude/agents/*.md` 의 「먼저 읽으십시오」 블록, 31/31 실측). **회의실 스킬은 마스터만 읽습니다.** 그래서 반박을 요구받은 팀원은 「합의를 강요하지 않는다 · 미합의는 미합의로」라는 D-028 의 핵심 규칙을 **모르는 채** 반박문을 씁니다. 그리고 정본이 우선하므로 이견은 ③ 사용자 가치 우선으로 곧장 넘어가도 규정 위반이 아닙니다.

이것은 **1차 감사 치명-2 와 완전히 같은 유형**입니다 — 새 규정을 만들고 최우선 정본에 반영하지 않아 정본 우선순위상 무효가 된 건. `ai-team/approvals.md:25` 에 그 지적이 기록돼 있는데도 D-028 에서 같은 일이 남아 있었습니다.

요구: `.claude/team-rules.md` 「의견 충돌 해결」 절에 교차 논쟁을 **본문으로** 넣거나(3라운드 상한·원문 삽입·미합의 기록), 최소한 `meeting-room/SKILL.md` 해당 절을 가리키는 참조 한 줄을 넣으십시오. 그리고 `team-check.sh` 에 「정본에 교차 논쟁 조항이 있는가」 검사를 추가하십시오. 게이트 때와 똑같이, 검사가 없으면 다시 어긋납니다.

## [치명-2] D-028 은 **한 번도 실행된 적이 없고**, 규정이 지시한 기록 경로 자체가 막혀 있습니다

근거:
- `.claude/skills/meeting-room/SKILL.md:118` — 「라운드마다 `supabase-sync.py event --kind 호출` 로 남깁니다」
- 실행: `python3 ai-team/scripts/supabase-sync.py status` → `✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다` (B-08, `ai-team/board.md:22`)
  → **규정이 유일하게 지정한 기록 수단이 이 컨테이너에서 동작하지 않습니다.**
- 저장소 전수 검색: `ai-team/approvals.md` · `questions.md` · `SESSION-LOG.md` · `cycle.md` · `board.md` 에 논쟁 실행 기록 **0건**. `STATE.md:106` 은 규정 서술일 뿐 실행 기록이 아닙니다.
- `ls -R ai-team/apps` → **빈 디렉터리**. `ai-team/board.md:8` 「사이클 착수 대기」. 논쟁이 일어날 사이클 자체가 없었습니다.
- `git log --all --grep=논쟁` → `1126d5d [checkpoint] … D-028 교차 논쟁 + 음성 보고 실측` **1건뿐이고, 이것은 규정을 작성한 커밋**입니다.

무엇이 문제인가: **D-021 치명-4(규정은 반영됐으나 실제 백업 실행 0건)와 같은 유형이 D-028 에서 반복**됐습니다. 두 건 모두 원인이 같습니다 — 「슈퍼베이스에 남긴다」가 유일한 실행 증거인데 키가 없습니다. 규정을 하나 더 쌓아도 B-08 을 풀지 않으면 실행 증거는 계속 0건입니다.

요구: ⓐ 논쟁 라운드 기록의 **1차 기록처를 저장소 파일**(예: `<앱폴더>/docs/논쟁-<주제>.md`)로 바꾸고 슈퍼베이스는 보조로 내리십시오 — 키 없이도 증거가 남아야 합니다. ⓑ 사이클 3 ① 전략에서 `strategy-lead ↔ data-analyst` 1조를 **실제로 3라운드 돌려** 산출물을 남기기 전에는 D-028 을 「반영됨」으로 적지 마십시오. 지금 상태의 정확한 표기는 **「규정만 있음 · 실행 0건」**입니다.

## [치명-3] D-034 검증 보고 「살아 있는 문서·코드에서 0건」이 **범위를 밝히지 않은 완료 주장**입니다 — 복원 소스는 아직 「자비스」

근거:
- `ai-team/decisions.md:167` — 「살아 있는 문서·코드에서 `jarvis`·`자비스` **0건** (역사 기록 `decisions.md` 제외)」
- 저장소 안에서는 사실입니다(재현 확인). **그러나 범위가 저장소뿐입니다.**
- `ai-team/STATE.md:130` — 「노션 **「팀 규정」·「팀원 명세」 의 「자비스」 표기도 함께 바꿔야 합니다**」
- `ai-team/notion-queue/2026-08-29-6.md:3` — `상태: ⏳ 미반영`, 같은 파일 `:16-18` — 「다른 페이지도 바꿔야 합니다 / 「📜 팀 규정 · 마스터 판단 기준」 — 옛 호칭 표기 / 「👥 팀원 31명 역할 명세」 — 총괄 호칭」
- `grep -l "⏳ 미반영" ai-team/notion-queue/*.md` → `2026-08-29-6.md` · `2026-08-29-7.md` (실측 2건)

무엇이 문제인가: 노션은 이 팀의 **복원 소스**입니다. D-027 상세 `ai-team/decisions.md:387` 이 스스로 이렇게 적어 두었습니다 — 「**복원 소스가 낡으면 다음 복원이 옛 조직을 되살립니다.**」 지금이 정확히 그 상태입니다. 노션에서 팀을 다시 가져오면(`SESSION-LOG.md:97`·`:292` 가 실제로 두 번 한 일입니다) **총괄 호칭이 「자비스」로 되살아납니다.**

그리고 이것이 마스터가 물은 「같은 유형이 또 있는가」의 답입니다. **D-033 의 「전수 적용」과 정확히 같은 유형** — 검증한 범위(저장소)를 밝히지 않고 완료로 보고했고, 실제 범위(저장소 + 노션 + 슈퍼베이스)의 일부가 비어 있습니다. `team-check.sh:95-101` 의 호칭 검사도 저장소만 봅니다. 점검기가 초록불이어도 되돌림은 끝나지 않았습니다.

요구: ⓐ `decisions.md:167` 을 「**저장소 안에서** 0건 · 노션 2개 페이지 미반영(큐 `2026-08-29-6`)」으로 정정하십시오. ⓑ 노션 도구가 붙은 세션에서 큐 2건을 반영하기 전에는 D-034 를 완료로 적지 마십시오. ⓒ `ai-team/board.md` 의 차단 목록에 **B-09(노션 복원 소스가 옛 호칭)** 을 신설하십시오 — 지금은 `STATE.md` 본문 한 줄에만 있어 다음 세션이 놓칩니다.

## [치명-4] 정본 목록이 스킬마다 다릅니다 — **사용자 없는 07:07 자동 실행 경로가 총괄 정체성 정본을 읽지 않습니다**

근거:
- `.claude/skills/meeting-room/SKILL.md:11-15` — 「정본 **5개**」 · `0. .claude/master.md` · 1 `team-rules.md` · 2 `team-org.md` · 3 `master-doctrine.md` · 4 `SESSION-LOG.md`
- `.claude/skills/daily-app/SKILL.md:10` — 「저장소 정본 **4개** — `.claude/team-rules.md`(최우선) · `.claude/team-org.md` · `.claude/master-doctrine.md` · `ai-team/SESSION-LOG.md`」 → **`.claude/master.md` 가 없습니다**
- `ai-team/decisions.md:318` — D-029 반영 표에 「`meeting-room/SKILL.md` | 정본 4개 → **5개**」. **`daily-app` 은 표에 없습니다.**

무엇이 문제인가: `daily-app` 은 무인 실행 경로입니다(`SKILL.md:2` description 「매일 07:07 정기 실행」, `SKILL.md:24` 「**사용자가 없다는 전제로 진행**」). 사람이 없는 그 경로에서 마스터는 자기 정체성 정본을 읽지 않고 하루치 사이클을 돕니다. `.claude/master.md` 에는 「호출하지 않은 팀원의 발언을 지어내지 않는다」(`:58`) · 「팀원의 완료했습니다를 믿지 않는다」(`:59`) · 「**자기가 감사받는 대상이라는 사실을 잊지 않는다**」(`:62`) · 교차 논쟁 의무(`:35`) · 음성 보고 규칙(`:40-50`) 이 들어 있습니다. 이 팀의 사고 이력이 전부 이 항목들에서 나왔습니다.

이것도 **「지목된 한 건만 고치고 같은 계열은 남긴다」** 유형입니다 — `ai-team/approvals.md` 가 「3회 연속 지적된 패턴」으로 이름 붙인 바로 그것입니다. D-029 가 `meeting-room` 만 4→5 로 고쳤고, D-034 의 전수 치환도 파일명·호칭만 보고 **정본 목록의 개수와 구성은 대조하지 않았습니다.**

요구: `daily-app/SKILL.md:10` 을 정본 5개로 맞추고 `.claude/master.md` 를 0번에 넣으십시오. `team-check.sh` 에 「두 스킬의 정본 목록이 일치하는가」 검사를 추가하십시오.

---

# 중대

## [중대-1] 31명 전원의 필독 블록이 게이트를 **엉뚱한 파일로 안내**합니다 (D-036 시정의 부작용)

근거:
- `.claude/agents/*.md` 의 「먼저 읽으십시오」 블록 — **31/31 전부** 「`.claude/team-org.md` (조직·**게이트**·산출 경로)」 (예: `team-master.md:9` · `tech-lead.md:9` · `qa-lead.md:9`)
- `.claude/team-org.md:74-75` — 「**게이트 조건과 수치의 정본은 `.claude/team-rules.md` 「게이트」 절입니다.** 여기에 숫자를 옮겨 적지 않습니다」 → **team-org.md 에는 게이트 수치가 없습니다**

무엇이 문제인가: D-036 이 게이트를 `team-rules.md` 로 단일화하면서 `team-org.md` 의 표를 지웠는데, **거기로 보내는 31개의 이정표는 그대로 뒀습니다.** 리다이렉트 한 줄이 남아 있어 결국 도달은 하므로 치명은 아니지만, 이번에 새로 추가된 검사 9종 중 **어느 것도 이 어긋남을 보지 않습니다**(`team-check.sh:71-91` 확인). 게이트 드리프트를 잡으려고 만든 시정이 새 드리프트를 낳은 형태입니다.

요구: 31개 블록의 괄호 안을 「(조직·산출 경로)」로 바꾸고, 게이트는 `team-rules.md` 쪽 괄호에 넣으십시오. 31개 동시 수정이므로 **치환 후 반드시 diff 를 눈으로 대조**하십시오 — D-034 에서 치환이 지시 원문까지 바꾼 전례가 있습니다.

## [중대-2] `daily-app/SKILL.md` 한 파일 안에서 단계 순서가 자기모순입니다 (판본 드리프트 5번 불합격)

근거:
- `.claude/skills/daily-app/SKILL.md:63` — 「## 6. 마감 — `ai-team/STATE.md` 를 다시 쓴다 (**맨 마지막**, 생략 불가)」
- 바로 다음 줄 `:64` — 「🔴 **이 단계는 반드시 노션 정리(5)와 슈퍼베이스 반영 뒤에 옵니다.**」
- 그런데 `:72` — 「## **7**. 마감 — 슈퍼베이스 반영 (생략 불가)」

무엇이 문제인가: 6단계가 「슈퍼베이스 반영 뒤」라고 선언하면서 슈퍼베이스 반영은 7단계입니다. 그리고 6단계가 다시 쓸 값 목록(`:70`)에 슈퍼베이스 결과는 없고 「노션 미동기화 건수(5 를 실제로 끝낸 결과)」만 있습니다. `ai-team/board.md:19` 는 B-05 를 「✅ **해소**」로 적고 있으나, **해소된 것은 노션↔STATE 순서뿐이고 슈퍼베이스 쪽 문구는 함께 고쳐지지 않았습니다.** 2026-08-12 치명-1(아직 하지 않은 작업의 예측값을 완료로 보고)의 뿌리와 같은 형태의 문구가 남아 있습니다.

요구: `:64` 를 「노션 정리(5) 뒤에 옵니다」로 고치거나, 슈퍼베이스 반영을 6단계 앞으로 옮기고 번호를 다시 매기십시오. 어느 쪽이든 `board.md:19` 의 「해소」 표기를 그 사실에 맞게 갱신하십시오.

## [중대-3] D-033 이 「전수」라고 보고한 이유 — **점검기의 검사 폭이 주장의 폭보다 좁았고, 마스터가 그 초록불을 전수의 근거로 삼았습니다**

마스터가 물은 「그때 왜 전수라고 보고했는가」에 대한 실측 답입니다.

근거 (당시 판 `3b0e1d0` 의 `team-check.sh` 를 직접 꺼내 확인):
```
# 6) 게이트 수치 드리프트
grep -q '10항목' "$A/tech-lead.md" || bad "…"
grep -q '6자료'  "$A/qa-lead.md"  || bad "…"
```
- 이 두 줄이 게이트 검사의 **전부**였습니다. ⓐ **파일 전체 존재 검사**라서 본문 한 곳만 고치면 `description:` 줄이 옛 값이어도 통과합니다. ⓑ 최우선 정본 `.claude/team-rules.md` 는 **검사 대상에 아예 없었습니다.** ⓒ 나머지 4명의 파트장 정의는 보지 않았습니다.
- 그 결과 D-033 시정 직후 점검기는 초록불이었고, `ai-team/decisions.md:189` 는 「시정 (**전수 적용**, 지목된 한 건만 고치지 않음 — D-016 지적 반영)」으로 보고했습니다. 실제 미수정은 10곳이었습니다(`decisions.md:89`).
- 자기시험도 이 구멍을 드러낼 수 없었습니다. `decisions.md:197` 「일부러 4곳을 망가뜨림 → 6건 전부 검출」 — **망가뜨린 4곳이 전부 점검기가 이미 보는 항목**이었습니다. 점검기가 보지 않는 자리를 망가뜨리는 시험은 없었습니다.

즉 원인은 부주의가 아니라 **검증 방법의 구조**입니다. 「도구가 초록불 → 전수 완료」라는 추론이 성립하려면 도구의 검사 범위가 주장 범위와 같아야 하는데, 그 대조를 하지 않았습니다. 그리고 **이번 감사에서 같은 구조가 두 번 더 나왔습니다** — 치명-3(호칭 되돌림을 저장소 범위 점검기로 확인하고 전체 완료로 보고) · 치명-4(정본 목록 불일치는 어떤 검사도 보지 않음).

요구: ⓐ **「전수」·「전부」·「0건」이라는 단어를 쓸 때는 검색식과 대상 파일 수·히트 수를 같은 줄에 적으십시오.** 근거 없는 전수 주장은 `team-rules.md:34` 「주장을 믿지 않는다」에 그대로 걸립니다. ⓑ 점검기 자기시험은 **점검기가 보지 않는 자리를 일부러 망가뜨려** 「검출되지 않음」을 확인하는 방향으로 한 번 더 하십시오. 검출되는 것만 확인하는 시험은 검사 폭을 재지 못합니다.

## [중대-4] `daily-app` 이 D-025 개편(감사실 2인화)과 D-028 을 반영하지 않았습니다

근거:
- `.claude/skills/daily-app/SKILL.md:40` — 보고서 서식의 「## **감사관** 판정」 (단수). 조직 정본은 `.claude/team-org.md:17` 「🎖️ 감사실 2인 — team-master(규정) · evidence-auditor(증거)」이고 `.claude/team-rules.md:8` 도 2인입니다.
- 같은 파일 전문 검색 — `team-check.sh` 호출 **없음**, 교차 논쟁 **없음**, 전원 점호 **없음**. 이 셋은 전부 `meeting-room/SKILL.md` 에만 있습니다.

무엇이 문제인가: 치명-4 와 같은 뿌리입니다. D-025·D-027·D-028 세 건이 회의실 스킬만 갱신하고 일일 스킬을 지나쳤습니다. 무인 실행이 산출하는 일일보고서에는 감사관이 한 명분 자리만 있습니다.

요구: `daily-app/SKILL.md` 를 개편 이후 조직에 맞추십시오 — 감사관 2인 판정 칸, 필요 시 교차 논쟁 안내. 두 스킬이 같은 규정을 각자 적고 있는 구조 자체가 D-012(중복 정의 금지) 위반에 가까우므로, **`daily-app` 도 정본을 가리키기만 하게** 얇게 만드는 편이 낫습니다.

---

# 경미

| # | 지적 | 근거 | 요구 |
|---|---|---|---|
| 경미-1 | 옛 게이트 수치가 남은 문서 1곳 | `ai-team/approvals.md:123` — 「④ 개발 착수 게이트 **8항목** 중」 | 2026-08-15 의 **과거 판정 기록**이므로 소급 수정은 금지(D-034 원칙). 대신 「당시 기준. 현행은 10항목 — D-036」 각주를 다십시오 |
| 경미-2 | 호칭 검사가 맥락 낱말만 있으면 통과 | `ai-team/scripts/team-check.sh:98-101` — `grep -vE 'D-029\|D-034\|되돌\|옛 호칭\|이전 호칭'` | 「총괄 자비스가 승인합니다 (D-034)」 같은 줄이 초록불입니다. 허용 패턴을 인용 블록(`>` 로 시작) 또는 대조표 행으로 좁히십시오 |
| 경미-3 | 호칭 검사가 변형 표기를 못 봄 | `team-check.sh:95` `-iname '*jarvis*'` · `:98` `'jarvis\|자비스'` | 사용자 실제 표기 「**자빗스**」(`decisions.md:139`)와 한글 파일명 「자비스-…」는 검출되지 않습니다. 패턴에 추가하십시오 |

---

# 판본 드리프트 검사 — **4 / 6**

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 단계 번호가 모든 문서에서 일치 (`N단계` 패턴까지) | ✅ 합격 | `daily-app/SKILL.md:49`·`ai-team/STATE.md:1`·`ai-team/board.md:19` 가 STATE 갱신을 모두 **6단계**로 일치. `source-verifier.md:23` 「판정 4단계」는 별개 개념 |
| 2 | handoff 파일명 `01-전략.md`~`06-출시운영.md` 통일 | ✅ 합격 | `team-rules.md:96-105` 정본. 파트장 6명 전원이 그 이름으로 씁니다(`strategy-lead.md:33`·`product-planner.md:37`·`design-lead.md:35`·`tech-lead.md:38`·`qa-lead.md:36`·`gtm-lead.md:31`). 폐기 경로 `ai-team/handoff/` 실사용 0건 |
| 3 | 퇴역 에이전트를 아직 호출하는 문서 | ✅ 합격 | 살아 있는 문서가 부르는 하이픈 이름 전수 대조 → `.claude/agents/` 에 없는 호출 **0건**(`daily-app`·`meeting-room` 은 스킬, `prefers-reduced-motion` 은 CSS 미디어쿼리) |
| 4 | 실무자가 승인권을 주장하는 문구 | ✅ 합격 | 실무자 **25명 전원**이 정의 첫머리에 「승인권은 없습니다」를 명시(`accessibility-auditor.md:7` ~ `ux-writer.md:8`). 역방향 위반도 0건 |
| 5 | 한 파일 안에서 자기모순 | ❌ **불합격** | `daily-app/SKILL.md:63-64` vs `:72` — 중대-2 |
| 6 | 산출 경로가 문서마다 다름 | ✅ 합격 | 31개 정의 전수 검사 — `<앱폴더>/` 접두 없이 `docs/`·`handoff/`·`src/`·`tests/` 를 쓴 줄 **0건**. 문서가 가리키는 저장소 경로 실존 검사에서도 깨진 참조 0건(미실존 5건은 전부 자리표시자 `…`·반례(`.claude/agents/master.md`)·폐기 경로·런타임 파일) |

> ⚠️ 6번은 「산출 경로」 기준으로는 합격이나, **정본 목록의 개수·구성이 문서마다 다른 것**(치명-4)은 같은 계열의 드리프트입니다. 검사 6항목이 정본 목록을 보지 않는다는 점을 기록으로 남깁니다.

---

# 마스터 감사

## 1) D-034 치환 사고의 시정 — ✅ **끝났습니다**

마스터가 확인을 요청한 건입니다. 실측:

| 확인 | 결과 |
|---|---|
| 조작된 문장 `"이름은 마스터 이면 좋겠어"` 의 잔존 | **0건.** `decisions.md:156` 의 **사고 서술 안**에만 남아 있고, 그 자리는 잘못을 기록하는 자리이므로 정상입니다 |
| `.claude/master.md:4-5` 의 두 원문 | `decisions.md:305`·`:139` 와 **글자 단위 일치** ("…이름은 자비스 이면 좋겠어" / "자빗스라는 이름대신에 마스터로 다시 바꿔") |
| 재발 방지 장치 | `.claude/master.md:6` 「**치환하지 마십시오** — 원문을 고치면 기록이 아니라 창작이 됩니다」 명시 |
| 커밋 기록 | `e74f1fa` 커밋 메시지에 사고와 시정이 **먼저 적혀 있음** — 발견된 뒤 숨기지 않았습니다 |
| 다른 지시 원문 오염 | 저장소의 「지시 원문」 인용 **전 건**을 수집해 `decisions.md` 정본과 대조 → 치환으로 훼손된 것 **0건** |

**이 항목은 승인합니다.** 잘못을 스스로 찾아 되돌리고, 되돌린 사실을 지우지 않고 기록했으며, 같은 잘못을 막는 문구를 정본에 박았습니다. 감사관이 봐줄 이유가 없어서 승인하는 것이 아니라 **증거가 갖춰져서** 승인합니다.

## 2) 반려 사유가 되는 마스터 행위

| # | 지적 | 근거 |
|---|---|---|
| M-1 | **미완을 완료로 보고** — 「살아 있는 문서·코드에서 0건」이 저장소 범위임을 밝히지 않음 | `decisions.md:167` (치명-3) |
| M-2 | **미완을 완료로 보고** — 「전수 적용」의 근거가 검사 폭이 좁은 점검기의 초록불이었음 | `decisions.md:189` vs `3b0e1d0` 판 `team-check.sh` (중대-3) |
| M-3 | **YELLOW 표기 누락** — 노션 복원 소스가 옛 호칭이라는 사실이 `board.md` 차단 목록에 없고 `STATE.md:130` 본문 한 줄에만 있음 | `ai-team/board.md:12-22` (치명-3 ⓒ) |
| M-4 | **범위를 넘은 규정 신설** — D-036 이 ② 설계 완료·⑥ 출시 착수 **게이트 2종을 새로 만들었습니다**(`team-rules.md:82`·`:87`). 이는 옛 값 시정이 아니라 **신설**이고, 사용자 지시 원문이 없어 D-014 요건 ① 이 비어 있습니다 | `decisions.md:82-105` · `team-rules.md:82`·`:87` |

**M-4 는 유보로 남깁니다.** 1차 감사가 그 신설을 지시했는지 확인하려면 `감사-규정-2026-08-30-1.md` 를 열어야 하는데, 마스터가 병렬 작업을 이유로 금지했습니다. **감사관이 확인하지 못한 것을 위반으로 단정하지 않습니다**(D-015). 1차 재감사 호출이 「치명-2 시정 범위에 두 게이트 신설이 포함되는가」를 판정해 주십시오. 포함되지 않는다면 D-036 은 사용자 지시 없는 `.claude/` 규정 신설이므로 별도 RED 입니다.

## 3) 마스터가 지킨 것 (기록)

- `decisions.md:103` — 「**되돌릴 수 있음은 RED 를 GREEN 으로 바꾸지 않습니다.** 앞으로 `.claude/` 수정은 사용자 지시가 있어도 감사관 검증까지 받은 뒤 확정합니다」 → 1차 치명-1 을 축소하지 않고 받아들였습니다.
- `decisions.md:153` — 「D-029~D-033 본문의 옛 경로·옛 호칭은 고치지 않았습니다. 그때 실제로 그랬기 때문입니다. **기록을 소급해 고치면 기록이 아니라 창작이 됩니다**」 → 감사관이 요구하기 전에 스스로 세운 원칙이고, 실제로 지켰습니다.
- D-028·D-029 에서 **못 하는 것을 못 한다고 적었습니다** — 「박수 두 번·상시 마이크는 기능의 부재」(`decisions.md:326-329`), 「원격 음성 보고 불가 — 실측 로그 첨부」(`decisions.md:352-356`). 되는 척하지 않았습니다.

---

# 반려 해제 조건

**아래 4건이 끝나기 전에는 D-028 과 D-034 를 「반영됨」으로 적을 수 없고, 사이클 3 착수도 불가합니다** (`team-org.md:32` 「감사실은 승인하지 않고 반려합니다」).

1. **치명-1** — `.claude/team-rules.md` 「의견 충돌 해결」에 교차 논쟁을 넣거나 참조 한 줄. + `team-check.sh` 검사 추가
2. **치명-2** — 논쟁 기록의 1차 기록처를 저장소 파일로 변경. D-028 현재 표기를 「규정만 있음 · 실행 0건」으로 정정
3. **치명-3** — `decisions.md:167` 범위 명시로 정정 + `board.md` 에 B-09 신설
4. **치명-4** — `daily-app/SKILL.md:10` 을 정본 5개로 정정 (`.claude/master.md` 0번)

중대 4건은 **사이클 3 ① 전략 착수 전까지** 처리하고 결과를 `approvals.md` 에 적으십시오. 경미 3건은 다음 점검기 손볼 때 함께.

**시정 후에는 「고쳤습니다」가 아니라 검색식과 히트 수를 함께 제출하십시오.** 이번 감사의 치명 4건 중 3건이 「고쳤다는 주장」과 「실제 범위」의 차이에서 나왔습니다.

---

## 확인 못 한 것 (유보 — 사유와 함께)

| 항목 | 사유 |
|---|---|
| 1차 감사 4건(D-021·D-025·D-026·D-027)의 상태 | 마스터가 병렬 작업을 이유로 접근을 금지했습니다. 판정하지 않았습니다 |
| D-036 의 게이트 2종 신설이 1차 지적 범위인가 (M-4) | 위와 같은 사유. 1차 재감사 호출에 넘깁니다 |
| 노션 「팀 규정」·「팀원 명세」 페이지의 실제 현재 표기 | 이 세션에 노션 도구가 붙어 있지 않습니다. **저장소 기록(`STATE.md:130`·큐 `2026-08-29-6.md`)이 미반영이라고 말하는 것**을 근거로 삼았습니다. 실물 대조는 증거감사관 몫입니다 |
| 슈퍼베이스에 팀 정의가 실제로 올라갔는가 | `status` 가 키 부재로 실패합니다(B-08). 이 컨테이너에서는 원리적으로 확인 불가 |
| 서브에이전트 31명이 실제로 호출되는가 | 이 세션은 감사 호출 1건이고, 31명 호출을 실측하지 않았습니다. **「내 세션에서 안 됐다」를 아키텍처 한계로 일반화하지 않습니다** (D-015) |

---

*판정자: `team-master` (규정감사관) · 2026-08-30 · 독립 감사. 마스터의 지시를 받지 않습니다.*
