# 규정 재감사 — 1차 반려(치명 4) 시정에 대한 판정

| | |
|---|---|
| 감사관 | `team-master` (규정) — 독립 감사관 |
| 일시 | 2026-08-30 (재감사) |
| 대상 | 1차 반려 `ai-team/docs/감사-규정-2026-08-30-1.md` 의 치명 4 · 중대 8 · 경미 6 |
| 체크아웃 | 브랜치 `claude/meeting-room-team-structure-knz8ob` · HEAD `053e296` (= `@{u}`, 작업 트리 깨끗) |
| 방법 | 파일 직접 열람 + 스크립트 직접 실행 + **점검기에 회귀를 주입해 검출력 실측**. 마스터의 서술을 근거로 채택하지 않음 |

> **1차 보고서(`…-1.md`)는 손대지 않았습니다.** `git diff HEAD -- ai-team/docs/` 출력 없음으로 확인했습니다.

---

## 판정

```
판정: ❌ 반려 (치명 1건 잔존)
치명: 1건  중대: 4건  경미: 7건
판본 드리프트 검사: 5/6   (1차 4/6 → 5/6)
```

**치명 4건 중 2건은 완전히 닫혔고, 1건은 실질적으로 닫혔으며, 1건이 남았습니다.**

| 1차 치명 | 재감사 판정 | 근거 |
|---|---|---|
| 치명-1 RED 진행 | 🔴 **미해소 — 유일한 잔존 치명** | 요구 2건 중 **0건 이행**. 아래 |
| 치명-2 게이트 정본 | ✅ **해소** | `.claude/team-rules.md:80-88` 6게이트 완전판 · `team-org.md:74-80` 표 제거·정본 참조. 실측 `grep -c '^| \*\*④ 개발 착수\*\*' .claude/team-org.md` → **0** |
| 치명-3 10곳 전수 | ✅ **해소 — 10/10 실측 확인** | 아래 대조표 |
| 치명-4 D-021 실행 0건 | ⚠️ **실질 해소, 형식 미이행** → 중대로 강등 | 색인 행은 정정, **지목한 상세 절은 그대로**. 아래 중대-1 |

**제출된 실측 로그를 감사관이 재현했습니다 — 위조 없음.**
- `bash ai-team/scripts/team-check.sh` → `✅ 팀 정의 점검 통과 — 31명, …` (EXIT=0)
- 사본에서 `qa-lead` description 을 5자료로 되돌림 → `🔴 qa-lead description 이 아직 5자료` / `⚠️ 팀 정의 드리프트 1건` **동일 문구로 재현**
- `grep -c '^| \*\*' .claude/team-rules.md` → **21** (제출값 일치)
- `python3 ai-team/scripts/supabase-sync.py event …` → `✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다.` **EXIT=1** (1차에는 EXIT=0 이었음 — 경미-2 해소)

> 감사관은 저장소를 편집하지 않았습니다. 회귀 주입 시험은 전부 `/tmp` 사본에서 했고, 원본은 `git status --porcelain` 무출력으로 확인했습니다.

---

## 치명-3 — 지목한 10곳 전수 대조 (감사관 실측)

1차가 표로 지목한 10곳을 **한 줄씩 다시 열어** 확인했습니다. **10/10 시정.**

| # | 위치 | 1차 실측값 | 현재값 | |
|---|---|---|---|---|
| 1 | `.claude/team-rules.md:84` | 8항목 | **10항목 전부** (항목 10개 전부 나열) | ✅ |
| 2 | `.claude/team-rules.md:85` | 5자료 | **6자료 전부** (보안설계서 포함) | ✅ |
| 3 | `.claude/agents/tech-lead.md:3` (description) | 8항목 | **10항목** | ✅ |
| 4 | `.claude/agents/tech-lead.md:50` | 5자료 | **⑤ QA 착수 6자료 전부** | ✅ |
| 5 | `.claude/agents/qa-lead.md:3` (description) | 5자료 | **6자료** | ✅ |
| 6 | `.claude/agents/design-lead.md:52` | 8항목 | **10항목** | ✅ |
| 7 | `.claude/agents/product-planner.md:46` | 8항목 | **10항목** | ✅ |
| 8 | `.claude/agents/product-planner.md:30` | "두 산출물" | **실무자 4인의 산출물** | ✅ |
| 9 | `.claude/agents/product-planner.md:35` | "두 실무자" | **실무자 4인에게** | ✅ |
| 10 | `.claude/agents/design-lead.md:28` | "두 산출물" | **실무자 3인의 산출물** | ✅ |

전수 검색으로 잔존 0건을 확인했습니다.
```
$ grep -rn "8항목\|5자료\|두 실무자\|두 산출물" .claude/
.claude/agents/tech-lead.md:20  > 8항목에서 **10항목으로 올렸습니다** (D-025) …
.claude/agents/qa-lead.md:21    > 5자료에서 **6자료로 올렸습니다** (D-025) …
.claude/team-rules.md:72        > 2026-08-30 감사 치명-2: … 옛 값(8항목·5자료)으로 …
```
남은 3건은 전부 **경위 설명 안의 인용**이며, 규정으로 읽히는 자리가 아닙니다. **정상입니다** — 기록 소급 수정 금지 원칙(`decisions.md:153`)에 부합합니다.

**감사관 확인**: 4벌의 게이트 사본이 **값까지 일치**합니다 — ④ 10항목 10/10 동일, ⑤ 6자료 6/6 동일(`team-rules.md:85` ↔ `qa-lead.md:18` ↔ `tech-lead.md:51`). 지금 잘못된 심사가 나지는 않습니다. 구조적 위험은 중대-2 로 별도 지적합니다.

---

## 판본 드리프트 검사 — 5/6

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 단계 번호 일치 | ✅ | `daily-app/SKILL.md:49` · `STATE.md:1` · `board.md:19` 전부 **6단계** |
| 2 | handoff 파일명 | ✅ | `team-rules.md:96-104`. 폐기 경로 실사용 0건 |
| 3 | 퇴역 에이전트 호출 | ✅ | 퇴역 0명, 미존재 호출 0건 |
| 4 | 실무자 승인권 주장 | ✅ | `승인권이 없습니다` 미기재 8개는 **파트장 6 + 감사관 2** 뿐. 실무자 23명 전원 명시 |
| 5 | 한 파일 안 자기모순 | ❌ **불합격** | `decisions.md:26`↔`:49` (중대-1) · `decisions.md:25`↔`:569` (경미-4) |
| 6 | 산출 경로 불일치 | ✅ **해소** | `team-master.md:46-48` 산출물 절 신설 → 감사관 2인 대칭 확보 (`<차수>` 누락은 경미-6) |

---

## 치명

### [치명-1] RED 진행 — 1차 요구 2건 중 **0건 이행**. 문서화 위치까지 편향됐습니다

마스터는 이 항목을 "시정이 아니라 인정"으로 제출했습니다. **인정 자체는 진실하며 그 점은 인정합니다.**
그러나 1차가 요구한 것은 사과가 아니라 **재발 차단**이었고, 두 요구 모두 이행되지 않았습니다.

**요구 1 — 5개 결정 절에 각각 RED 진행 사실을 감사 판정으로 기록: ❌ 미이행**

기록은 `decisions.md:102-104` (D-036 상세) 한 곳과 `approvals.md:5-33` 두 곳뿐입니다. 지목된 5개 절은 1차와 동일합니다.

| 절 | 현재 문구 | 있어야 할 것 |
|---|---|---|
| `ai-team/decisions.md:55` (D-021) | "③ 감사관 검증 ❌ **여전히 미수행** — D-025 개편 감사와 함께 받습니다" | + RED 진행이었다는 감사 판정 |
| `ai-team/decisions.md:389` (D-027) | "D-014 요건 ③ 은 **여전히 미수행**" | 〃 |
| `ai-team/decisions.md:417` (D-026) | "③ 감사관 검증 ❌ **미수행**" | 〃 |
| `ai-team/decisions.md:448` (D-025) | "③ **감사관 검증** ❌ **미수행**" | 〃 |
| `ai-team/decisions.md:199` (D-033) | "③ **감사관 검증 비어 있음**. B-07 일괄 감사에 10건째로 추가합니다" | 〃 |
| `ai-team/decisions.md:451` | **"되돌릴 수 있으므로 진행했습니다"** — 1차가 확대 해석으로 판정한 바로 그 문장이 **주석 없이 그대로** | 판정 병기 |

**감사관이 추가로 찾은 6회째**: `ai-team/decisions.md:598` (D-016 상세, 2026-08-10) — "되돌리려면 커밋을 되돌리면 됩니다 — 되돌릴 수 있으므로 진행했습니다". 1차가 센 5회보다 **앞선 최초 사례**입니다. D-016 은 이후 2026-08-12 재감사에서 ③ 을 받았으나(`ai-team/approvals.md:58`), **논리 자체는 그때 교정되지 않아 그대로 5회 더 재생산됐습니다.** 이것이 개별 절 주석보다 규정 본문 수정이 필요한 이유입니다.

**"③ 미수행"과 "③ 미수행 상태로 시행한 것이 위반이었다"는 다른 문장입니다.** 앞의 것은 1차 감사 전에도 이미 적혀 있었고, 1차는 그것으로 부족하다고 판정했습니다. 결정 절만 읽는 사람은 지금도 위반이 있었다는 사실을 알 수 없습니다.

**요구 2 — `master-doctrine.md` 에 "되돌릴 수 있음은 RED 예외 사유가 아니다" 한 줄: ❌ 미이행**

```
$ git log --oneline -1 --stat 053e296 | grep master-doctrine
(출력 없음)
$ ls -la .claude/master-doctrine.md
-rw-r--r-- 1 root root 3117 Aug 29 15:10 .claude/master-doctrine.md   ← 개편 이전 시각 그대로
```

- `master-doctrine.md:9` 대원칙 2 「되돌릴 수 있으면 진행, 없으면 정지」 — **무조건 그대로**
- `master-doctrine.md:28` RED 「`.claude/` 아래 파일 수정」 — 그대로
- `master-doctrine.md:31-34` 예외 3요건 + 「확대 해석 금지」 — 그대로

**같은 오독을 6회 낳은 텍스트가 한 글자도 바뀌지 않았습니다.** 다음 세션의 마스터는 `decisions.md` 를 끝까지 읽지 않아도 `master-doctrine.md` 는 반드시 읽습니다. 규정 쪽을 막지 않으면 7회째가 납니다.

**감사관이 지적하는 편향 — 이것이 이 항목을 치명으로 유지하는 이유입니다.**

커밋 `053e296` 은 `.claude/` 아래 **8개 파일**을 고쳤습니다 — `team-rules.md`·`team-org.md`·`meeting-room/SKILL.md`·`tech-lead`·`qa-lead`·`design-lead`·`product-planner`·`team-master`.
같은 커밋에서 **마스터를 구속하는 단 하나의 `.claude/` 파일만** 손대지 않았습니다.

동시에 `decisions.md:104` 에는 "앞으로 `.claude/` 수정은 감사관 검증까지 받은 뒤 확정합니다"라고 적었습니다. 그 원칙을 지켰다면 이 커밋의 8개 파일도 확정하지 못했어야 합니다. **자기 구속 규정에만 선택적으로 적용된 원칙은 원칙이 아닙니다.**
의도를 단정하지는 않습니다 — 누락일 수 있습니다. 그러나 결과는 「팀 규정은 고치고 마스터 규정은 남긴 개정」이며, 이는 감사관이 가장 먼저 보아야 할 형태입니다.

**요구 (재감사 3차 조건)**
1. `.claude/master-doctrine.md` 에 한 줄 명시. **이 편집에 한해 감사관이 사전 검증을 부여합니다** — 본 보고서가 D-014 요건 ③ 입니다. 문안 예시:
   > `대원칙 2 는 RED 목록의 상위 규정이 아닙니다. 「되돌릴 수 있음」은 RED 예외의 4번째 요건이 아니며, .claude/ 수정의 예외는 3요건 전부 충족 시만입니다 (2026-08-30 감사 치명-1, 동일 오독 5회).`
   RED 절(`:28` 아래)과 대원칙 2(`:9`) **양쪽**에 걸리게 두십시오. 한쪽만 고치면 이번 치명-2 와 같은 구조가 됩니다.
2. 위 표 5개 절 각각에 한 줄씩. `:451` 은 문장을 지우지 말고 **판정을 병기**하십시오 (기록 소급 수정 금지 원칙 준수).
3. `team-check.sh` 에 `master-doctrine.md` 의 그 문구 존재 검사를 넣으십시오. 넣지 않으면 다음 개편에서 다시 사라집니다.

---

## 중대

### [중대-1] 치명-4 가 지목한 상세 절이 그대로 — 색인만 고쳤습니다

1차 요구는 명시적이었습니다: **"`decisions.md:48` 의 '✅ 반영됨' 을 '⚠️ 절차만 반영 — B-08 로 실제 기록 0건' 으로 정정"**.

```
ai-team/decisions.md:26  | **D-021** | … 규정은 반영됐으나 **실제 백업 실행 0건**(키 없음, B-08) …   ← 정정됨 ✅
ai-team/decisions.md:49  **이 저장소 상태**: ✅ **반영됨 (2026-08-29).** …                        ← 지목된 줄, 무변경 ❌
ai-team/decisions.md:55  | ③ 감사관 검증 | ❌ 여전히 미수행 — D-025 개편 감사와 함께 받습니다 |   ← B-08 언급 0
```

`grep -n "B-08" ai-team/decisions.md` → `:26` 과 `:131` 뿐. **D-021 상세 절(`:43-57`) 안에는 B-08 이 한 번도 나오지 않습니다.**
같은 파일이 한 항목을 두고 색인에서는 "실행 0건", 상세에서는 "✅ 반영됨"이라고 말합니다 — **드리프트 5 불합격의 주된 근거**입니다.

치명에서 강등한 이유: 미실행 사실이 `:26`·`STATE.md:92`·`board.md:22` 에 드러나 있어 **은폐 상태는 아닙니다.** 그러나 D-021 을 조회하는 사람이 실제로 여는 곳은 상세 절입니다.

**요구**: `:49` 를 정정하고, `:55` 3요건 표 아래에 "규정 절은 있으나 실행 경로가 닫혀 있음(B-08)"을 한 줄 추가.

### [중대-2] D-012 미충족 — **마스터가 물은 항목에 대한 답입니다**

마스터의 질문: *"`10항목` 은 파트장 정의에 **문장 안 참조**로만 남았고 표는 하나뿐입니다. D-012 를 충족합니까?"*

**충족하지 않습니다. 그리고 「문장 안 참조뿐」이라는 전제가 실측과 다릅니다.**

```
.claude/team-rules.md:84   | **④ 개발 착수** | **10항목 전부**: 시장분석 · 출처검증 · … · 디자인승인 |   ← 정본
.claude/agents/tech-lead.md:18   1. 시장분석 2. **출처검증** 3. 아이디어 승인 4. 기능정의 5. 화면설계
                                 6. 데이터구조 7. API정의 8. 예외정의 9. **문구확정** 10. **디자인 승인**
.claude/team-rules.md:85   | **⑤ QA 착수** | **6자료 전부**: 최신코드 · 변경내역 · … · 보안설계서 |     ← 정본
.claude/agents/qa-lead.md:18     최신 코드 · 변경 내역 · 기능 목록 · 테스트 방법 · 알려진 제한사항 · **보안설계서**
.claude/agents/tech-lead.md:51   최신 코드 위치 · 변경 내역 · 기능 목록 · 테스트 방법 · 알려진 제한사항 · **보안설계서**
```

이것은 참조가 아니라 **전량 재기술**입니다. 게이트 내용의 사본이 정본 포함 **4벌** 존재합니다.
마크다운 표가 아니라 번호 목록·가운뎃점 나열이라는 차이뿐이며, **D-012 「중복 제거 — 정본 참조형으로 얇게」가 막으려는 것은 표 문법이 아니라 중복 정의**입니다.

**왜 중대인가**: 치명-2·3 의 발생 기전이 그대로 남아 있습니다. 정본을 고치고 `tech-lead`·`qa-lead` 를 잊으면, **게이트를 실제로 심사하는 두 사람**이 옛 기준으로 통과시킵니다. 위치만 `team-org.md` 에서 파트장 정의로 옮겨 갔습니다.

**정상 참작**: 현재 4벌의 **값은 전부 일치**합니다(항목별 대조 완료 — 10/10, 6/6, 6/6). 지금 잘못된 판정이 나지는 않습니다. 그래서 치명이 아니라 중대입니다.

**요구** — 둘 중 하나를 고르고 `decisions.md` 에 기록하십시오.
- (a) `tech-lead.md:18`·`:51`·`qa-lead.md:18` 의 항목 나열을 지우고 "`.claude/team-rules.md` 「게이트」 절의 ④ 10항목 전부" 식 참조로 바꾼다 — D-012 그대로의 해법
- (b) 사본을 남기되 **점검기가 정본과 항목 단위로 대조**하게 한다 — 중대-3 을 먼저 해결해야 성립

### [중대-3] `team-check.sh` 는 **값 일치를 강제하지 못합니다** — 마스터 주장의 전제가 성립하지 않음

마스터는 D-012 충족 근거로 "아래 점검기가 값 일치를 강제합니다"를 들었습니다. **회귀를 주입해 실측했습니다.**

| 주입한 회귀 | 검출 | 실제 위험 |
|---|---|---|
| `team-org.md` 에 옛 게이트 표(8항목·5자료) **부활** | ❌ **통과** | **D-036 이 막으려던 사고 그 자체가 무검출** |
| 정본 표 `10항목` → `8항목` | ✅ 검출 (2건) | — |
| 정본 ⑥ 출시 착수 행 삭제 | ✅ 검출 | — |
| 정본 `보안설계서` → `보안검토서` 개명 | ❌ **통과** | 6자료의 내용이 바뀌어도 무검출 |
| 정본 ④ 행에서 `출처검증` 삭제 | ❌ **통과** | 신설 `source-verifier` 가 다시 게이트에서 빠져도 무검출 |
| `strategy-lead` 실무자 **4인 → 3인** | ❌ **통과** | 치명-3 후반부(파트장이 부하를 옛 인원으로 전제)가 숫자 형태로 재발해도 무검출 |
| `strategy-lead` 2차 통합 **4인 → 2인** | ❌ **통과** | 〃 |
| `design-lead` 실무자 **3인 → 2인** | ❌ **통과** | 〃 |

**8종 중 5종 미검출.** 점검기가 보는 것은 `10항목`·`6자료`라는 **낱말 세 개와 게이트 제목 문자열**이며, 게이트의 **값**도 파트장의 **인원 숫자**도 보지 않습니다.
인원 검사는 `grep -q '두 실무자\|두 산출물'` 로 **파트장 6명 중 2명만**, 그것도 「두」라는 한자어 표기에만 걸립니다.

**왜 중대인가**: 1차 치명-3 의 요구는 "점검기가 `description:` 줄을 보게 할 것"이었고 그 부분은 이행됐습니다(재현 확인). 그러나 마스터는 그 위에 **"값 일치를 강제한다"** 는 더 넓은 주장을 얹고, 그 주장을 D-012 면제의 근거로 썼습니다. **점검기가 통과했다는 것이 값이 맞다는 뜻은 아닙니다** — 1차 보고서가 「자동 점검기가 놓친 것」으로 지적한 구조가 규모만 줄어 남았습니다.

**요구**
1. `team-org.md` 에 게이트 표가 다시 생기면 실패하도록: `grep -qE '^\|.*(항목|자료).*(전부|:)' .claude/team-org.md && bad …`
2. ④ 10항목·⑤ 6자료의 **항목 이름을 배열로 두고 정본과 대조**
3. 파트장 6명 각각에 대해 `.claude/agents/` 실측 인원 ↔ 정의의 「실무자 N인」 숫자 일치 검사
4. 검사를 추가할 때마다 **회귀를 주입해 검출을 확인**하고 그 로그를 남기십시오. 이번에 마스터가 시험한 것은 9종 중 1종뿐이었습니다

### [중대-4] `local-windows.md` 가 20명 조직 브랜치 체크아웃과 `reset --hard` 를 사용자에게 지시 — 지목된 한 건만 고침

1차 중대-4 는 `routine-prompt.md` 를 지적했고, 그 7곳은 `knz8ob` 로 정정됐습니다(실측 확인 — `:31`·`:49`·`:59`). **같은 결함이 같은 폴더의 이웃 파일에 남아 있습니다.**

```
ai-team/local-windows.md:19-20   │  claude/notion-ai- │   ← 다이어그램상 "정본" 이 8tlqr8
                                 │  team-import-8tlqr8│
ai-team/local-windows.md:40      git checkout claude/notion-ai-team-import-8tlqr8
ai-team/local-windows.md:55      1. `claude/notion-ai-team-import-8tlqr8` 브랜치를 pull
ai-team/local-windows.md:106     git push origin claude/notion-ai-team-import-8tlqr8
ai-team/local-windows.md:116-117 git fetch origin claude/notion-ai-team-import-8tlqr8
                                 git reset --hard origin/claude/notion-ai-team-import-8tlqr8
ai-team/음성-설치.md:75          git checkout claude/notion-ai-agent-team-import-69oi8o   ← 정지 브랜치
```

실측: `git ls-tree -r --name-only origin/claude/notion-ai-team-import-8tlqr8 -- .claude/agents/ | wc -l` → **20**, `git rev-list --left-right --count …8tlqr8...HEAD` → **22 33**.

사용자가 이 문서대로 하면 **20명 조직**을 받고, `:117` 의 `reset --hard` 는 문서 스스로 "되돌릴 수 없습니다"라고 적은 명령입니다. 1차가 `routine-prompt.md` 를 중대로 본 이유("사용자가 그대로 반영하면 개편 이전 조직으로 회귀")가 **더 강하게** 적용됩니다.

**정상 참작**: `STATE.md:39`·`board.md:20` 의 B-06 이 "`local-windows.md` 는 `8tlqr8` 을 가리킨다"를 정확히 적고 있어 은폐가 아닙니다. 그러나 **문서 자체에는 경고가 한 줄도 없고**, 그 문서를 여는 사람은 상태 파일을 먼저 읽지 않습니다.

**요구**: B-06 이 미해소인 동안 `local-windows.md` 와 `음성-설치.md` 맨 위에 🔴 경고 배너를 넣으십시오 — "이 문서의 브랜치는 **개편 이전 20명 조직**입니다. B-06 결정 전에는 그대로 실행하지 마십시오." 브랜치 일원화는 사용자 결정 사항이므로 **감사관은 브랜치 변경을 요구하지 않습니다.**

---

## 경미

| # | 지적 | 근거 | 상태 |
|---|---|---|---|
| 경미-1 | 훅 구성표가 3행인데 실제 훅은 4개 (`SessionStart` → `team-check.sh` 누락) | `ai-team/always-on.md:73-78`(3행) ↔ `.claude/settings.json` 훅 4개 | 1차 경미-1 **미시정** |
| 경미-2 | 복원 절차가 감사관 1명만 호출 — 감사실 2인 규정 미반영 | `ai-team/RESTORE.md:65` | 1차 경미-4 **미시정** |
| 경미-3 | 노션 페이지명이 「팀원 **20명** 역할 명세」 | `ai-team/notion-sync.md:23` ↔ `decisions.md:387` | 1차 경미-3 **미시정** |
| 경미-4 | D-020 BOM 예외 범위가 색인과 상세에서 다름 | `decisions.md:25`("`team-sync.ps1` 에 한해") ↔ `:569`(".ps1 파일에 한정") · 실측 BOM 4개(`team-sync`·`supabase-sync`·`speak-report`·`master-voice`) | 색인 행 정정 필요 — 드리프트 5 부차 근거 |
| 경미-5 | `git add -A` 처리 방침이 **결정으로 기록되지 않음** | `grep -rn "git add -A\|미추적" ai-team/decisions.md ai-team/always-on.md` → **출력 없음**. `checkpoint.sh:54-60` 에 주석과 커밋 매니페스트만 | 1차 부록 요구 **부분 이행** (코드는 개선, 기록은 없음) |
| 경미-6 | 산출 경로에 `<차수>` 누락 | `.claude/agents/team-master.md:47` 은 `감사-규정-<날짜>.md` — 실제 파일은 `…-1.md` · `…-1-재감사.md`. 1차 요구는 `<날짜>-<차수>` | 절은 신설됐으므로 드리프트 6 은 합격, 형식만 보정 |
| 경미-7 | D-026 상세의 "훅 발화 미검증"이 `always-on.md` 와 갈림 | `decisions.md:419` ↔ `always-on.md:100`(정정됨) | **유보 처리** — 그 시점의 사실 기록이며 「기록 소급 수정 금지」(`decisions.md:153`)와 충돌. 절차 정본이 갱신됐으므로 실질 해소로 봅니다 |

---

## 중대 8건 재확인 — 6건 해소

| 1차 중대 | 판정 | 근거 |
|---|---|---|
| 중대-1 `STATE.md` B-07 건수 3중 | ✅ 해소 | `STATE.md:14`·`:31`·`:40` 전부 **12건**, 대상 `D-021 · D-025~D-035` 명시 |
| 중대-2 노션 미반영 건수 | ✅ 해소 | `STATE.md:130` **2건** = 실측 `2026-08-29-6.md`·`-7.md` |
| 중대-3 브랜치 서술 반대 | ✅ 해소 | `always-on.md:91` `knz8ob` 정정 · `STATE.md:16`·`board.md:20` 3갈래로 재기술. 실측 일치 |
| 중대-4 B-03 교체안 브랜치 | ⚠️ **부분** | `routine-prompt.md` 3곳 정정 확인. 같은 결함이 `local-windows.md`·`음성-설치.md` 에 잔존 → **중대-4(신규)** |
| 중대-5 `roster.sh` 명령 불일치 | ✅ 해소 | `meeting-room/SKILL.md:32-33` → `--unit N` / `--unit all` + 경고 주석 |
| 중대-6 훅 발화 검증 상태 | ✅ 해소 | `always-on.md:100` `Stop` ✅(`e54d8c6`) · `SessionStart` ⚠️ 사후 대조 불가. **정직한 서술** |
| 중대-7 승인 원장 공백 | ✅ 해소 | `approvals.md:5-33` 「조직 개편 v3 일괄 감사」 신설. 치명 4건과 시정까지 표로 |
| 중대-8 `team-master` 산출 경로 | ✅ 해소 | `team-master.md:46-48` 신설 (`<차수>` 는 경미-6) |

---

## 마스터 감사 (6항목)

| 항목 | 판정 | 근거 |
|---|---|---|
| RED 항목을 진행했는가 | ❌ **미해소** | 치명-1. 인정은 했으나 요구 2건 모두 미이행. 같은 커밋에서 `.claude/` 8개를 고치며 자기 구속 파일만 남김 |
| YELLOW 를 보고서 최상단에 표기했는가 | ✅ 지킴 | `SESSION-LOG.md:7` 항목 첫 줄 🟡 |
| GREEN 결정을 `decisions.md` 에 기록했는가 | ✅ 지킴 | D-036 신설(`:82-104`). 시정 경위·오검출 사실까지 기록 |
| 승인 없이 다음 단계로 넘어갔는가 | ✅ 넘어가지 않음 | `STATE.md:40` 「반려가 유효한 동안 사이클 3 착수 불가」 유지. `ai-team/apps/` 여전히 비어 있음 |
| 품질 게이트를 시간 부족으로 완화했는가 | ✅ 완화하지 않음 | 정본이 6게이트 완전판으로 상향 확정 |
| **미완을 완료로 보고했는가** | ⚠️ **1건** | 치명-3 은 실측 그대로 정확했습니다(10/10 확인 — 이 팀에서 처음입니다). 다만 **"점검기가 값 일치를 강제합니다"** 는 실측과 다릅니다 — 8종 중 5종 미검출(중대-3). 미완 보고가 아니라 **검증 범위 과대 진술**로 판정 |

**1차 대비 개선 (감사관이 확인함)**: 실측 명령과 출력을 함께 제출했고 **재현했더니 전부 일치**했습니다. 점검기를 일부러 망가뜨려 시험한 뒤 원복한 것, 처음 나온 2건이 문서가 아니라 검사식의 오검출이었다고 **자기에게 유리하지 않은 방향으로** 적은 것, `STATE.md` 의 고정 숫자(80파일)를 "낡을 값"이라며 스스로 지운 것은 규정 이상입니다. **1차의 「고쳤다고만 보고」 유형은 이번에 재발하지 않았습니다.**

---

## 유보 (확인하지 못한 것)

| # | 항목 | 왜 |
|---|---|---|
| 유보-1 | 서브에이전트 31명의 실제 호출 성공 | 감사관은 서브에이전트를 호출할 수 없습니다. 파일·형식·명부는 실측했으나 호출은 미확인 (D-015) |
| 유보-2 | 노션 4개 페이지 실제 내용 | 이 세션에 노션 도구 없음. 경미-3 은 `notion-sync.md` 기재만 근거 |
| 유보-3 | 07:07 Routine 실물 프롬프트 | 저장소 밖 |
| 유보-4 | 슈퍼베이스 실물 왕복 | B-08 미해소. 키는 사용자만 넣을 수 있음 — **마스터 책임 아님** |
| 유보-5 | `*.ps1` 동작 | 컨테이너에 PowerShell 없음 |
| 유보-6 | D-028~D-035 (2·3차 대상) | 이번 차수 아님 |

---

## 3차 감사 조건

**치명-1 하나입니다.**

1. `.claude/master-doctrine.md` 에 「되돌릴 수 있음은 RED 예외 사유가 아니다」 명시 — **본 보고서가 이 편집의 D-014 요건 ③ 입니다.** 감사관 검증을 기다리느라 막히지 마십시오
2. `decisions.md` 5개 절에 RED 진행 판정 병기 (`:451` 은 삭제 아닌 병기)
3. `team-check.sh` 에 1번 문구의 존재 검사 추가 + **회귀 주입 로그** 제출

중대 4건은 3차에서 함께 봅니다. **중대-3(점검기 검출력)은 중대-2(D-012)의 선결 조건**이므로 순서를 지키십시오.

**이 반려가 유효한 동안 사이클 3 착수는 불가입니다** (`master-doctrine.md:29` · `team-org.md:32`).
다만 잔존 치명은 1건이며 전부 **문서 편집**으로 닫힙니다. 새 조사나 사용자 결정을 요구하지 않습니다.
