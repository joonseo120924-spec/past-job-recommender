# 규정 감사 — 일괄 12건 중 1차 4건 (D-021 · D-025 · D-026 · D-027)

| | |
|---|---|
| 감사관 | `team-master` (규정) — 독립 감사관 |
| 일시 | 2026-08-30 |
| 대상 | D-014 RED 예외 요건 ③ 이 비어 있는 결정 4건 |
| 체크아웃 | 브랜치 `claude/meeting-room-team-structure-knz8ob` · HEAD `e54d8c6` |
| 방법 | 파일 직접 열람 + 스크립트 직접 실행. 마스터의 서술을 근거로 채택하지 않음 |

---

## 판정

```
판정: ❌ 반려
치명: 4건  중대: 8건  경미: 6건
판본 드리프트 검사: 4/6
```

**D-021 · D-025 · D-026 · D-027 네 건 모두 D-014 요건 ③ 을 부여하지 않습니다.**
반려 대상은 **③ 감사관 검증의 부여**이며, `.claude/` 를 이전 판으로 되돌리라는 뜻이 아닙니다. 치명 4건을 닫은 뒤 재감사를 신청하십시오.

### 요건별 판정

| 결정 | ① 사용자 직접 지시 | ② 원문 기록 | ③ 감사관 검증 |
|---|---|---|---|
| D-021 회의실 실시간 백업 | ✅ `decisions.md:44` | ✅ `decisions.md:44` · `meeting-room/SKILL.md:140` | ❌ **불가** — 치명-4 (지시 내용이 구현되지 않음) |
| D-025 조직 개편 v3 | ✅ `decisions.md:398` | ✅ `decisions.md:398` | ❌ **불가** — 치명-2 · 치명-3 |
| D-026 「항상」 규칙 4종 | ✅ `decisions.md:368` | ✅ `decisions.md:368` · `always-on.md:3` | ⚠️ **부분** — 기능은 실측 확인, 문서 정합 불합격 (중대-3 · 중대-6) |
| D-027 회의실 점호·회의록 | ✅ `decisions.md:342` | ✅ `decisions.md:342` · `meeting-room/SKILL.md:27` | ❌ **불가** — 중대-5 |

**공통 절차 위반**: 네 건 모두 ③ 이 비어 있는 상태에서 `.claude/` 를 고치고 **시행까지 했습니다.** 이것 자체가 치명-1 입니다.

---

## 판본 드리프트 검사 — 4/6

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 단계 번호 일치 (`N단계` 전수) | ✅ 합격 | `daily-app/SKILL.md` 0~7단계 ↔ `always-on.md:43`(5단계) · `STATE.md:1`·`board.md:19`(6단계) 전부 일치 |
| 2 | handoff 파일명 `01-전략`~`06-출시운영` | ✅ 합격 | `team-rules.md:84-92`. 폐기 경로 `ai-team/handoff/` 실사용 **0건** (선언 `team-rules.md:93` 뿐) |
| 3 | 퇴역 에이전트 호출 문서 | ✅ 합격 | D-025 는 순증(20→31)이라 퇴역 0명. 미존재 에이전트 호출 0건 |
| 4 | 실무자 승인권 주장 | ✅ 합격 | 실무자 24명 **전원**이 "승인권은 없습니다" 를 본문 첫 절에 명시 (예: `accessibility-auditor.md:7` · `data-analyst.md:8` · `test-automation-engineer.md:7`) |
| 5 | 한 파일 안 자기모순 | ❌ **불합격** | 5개 파일 — 치명-3 · 중대-1 · 중대-5 참조 |
| 6 | 산출 경로 문서 간 불일치 | ❌ **불합격** | 중대-8 — 감사실 2인 중 `team-master` 만 산출 경로 절이 없음 |

> 드리프트 6 은 **에이전트 산출 경로**만 보면 합격입니다 (`<앱폴더>/` 접두사 누락 0건, 실측). 불합격 사유는 D-025 가 신설한 감사실 2인 사이의 비대칭입니다.

---

## 자동 점검기가 놓친 것 (요청 항목)

`ai-team/scripts/team-check.sh` 는 **✅ 통과**를 냅니다. 그러나 아래를 전부 통과시킵니다.

| 점검기가 못 보는 것 | 실제 상태 | 이 보고서 |
|---|---|---|
| 최우선 정본 `team-rules.md` 의 게이트 수치 | 8항목·5자료 (옛 값) | 치명-2 |
| 프론트매터 `description:` 줄 안의 게이트 수치 | `tech-lead.md:3` 8항목 · `qa-lead.md:3` 5자료 | 치명-3 |
| 파트장이 부하 **수**를 몇 명으로 전제하는가 | `product-planner.md:35` "두 실무자"(실제 4인) | 치명-3 |
| 상태 파일(`STATE.md` 등)의 자기모순 | B-07 건수가 9·11·12 로 셋 | 중대-1 |
| 스크립트가 **실제로 성공하는가** | `supabase-sync.py event` 는 키가 없어 100% 실패 | 치명-4 |

점검기 자체는 잘 만들어졌습니다(`team-check.sh:65-66` 이 게이트를 보긴 합니다). 다만 **본문 어딘가에 문자열이 있으면 통과**시키므로, 같은 파일 안에 옛 값이 함께 남아 있는 경우를 구조적으로 못 잡습니다.

---

## 치명

### [치명-1] ③ 이 비어 있는 상태에서 `.claude/` 수정을 시행 — RED 진행

`master-doctrine.md:28` 은 `.claude/` 아래 파일 수정을 **RED = 즉시 정지**로 규정합니다.
예외는 `master-doctrine.md:31-32` — **"3요건 전부 충족 시만"**, 그리고 `:34` **"확대 해석 금지"**.

마스터는 요건 ③ 이 비어 있음을 스스로 인정한 채 진행했고, 그 근거로 대원칙 2("되돌릴 수 있으면 진행")를 들었습니다.

- 근거: `.claude/master-doctrine.md:28` · `:31-32` · `:34`
- 근거: `ai-team/decisions.md:421-424` — "③ **감사관 검증** ❌ **미수행**" / "**되돌릴 수 있으므로 진행했습니다**"
- 근거: `ai-team/decisions.md:56` (D-021) · `:362` (D-027) · `:390` (D-026) — 같은 구조가 4회 반복
- 근거: `ai-team/decisions.md:172` (D-033) — 5회째

**판정**: 대원칙 2(`master-doctrine.md:9`)는 RED 목록의 상위 규정이 아닙니다. RED 절에는 "되돌릴 수 있으면 예외"라는 문구가 없고, 예외 조항은 3요건을 **전부** 요구하며 바로 다음 줄에서 확대 해석을 금지합니다. **되돌릴 수 있다는 사정을 4번째 요건처럼 쓴 것이 확대 해석입니다.**

정상 절차였다면: `.claude/` 를 고치기 **전에** 감사실 2인을 호출해 개편안을 감사받고, 통과 후 적용합니다. 사용자가 요청하지 않은 서브에이전트를 부르지 않는다는 세션 운영 규칙(`decisions.md:423`)은 팀 규정보다 상위가 아니며, 그 경우 취할 행동은 **"진행"이 아니라 "차단됨으로 기록하고 사용자에게 보고"** 입니다 (`master-doctrine.md:36`).

**요구**
1. `decisions.md` 의 D-021·D-025·D-026·D-027·D-033 각 절에 **"③ 미충족 상태에서 시행한 것은 RED 진행이었다"** 를 감사 판정으로 추가할 것. 사후 정당화가 아니라 위반 사실로 기록하십시오.
2. `master-doctrine.md` 에 **"되돌릴 수 있음은 RED 예외 사유가 아니다"** 를 한 줄 명시할 것. 같은 논리가 5회 반복됐으므로 규정 쪽을 막아야 합니다.
3. 이 감사(치명 4건 해소 후 재감사)를 통과해야 ③ 이 부여됩니다.

---

### [치명-2] 최우선 정본의 게이트가 옛 값 — D-025 의 게이트 상향이 정본 우선순위상 무효

`team-rules.md:3` 은 **"무엇과 충돌하든 이 파일이 우선합니다"** 라고 선언합니다.
그런데 그 파일의 게이트 표가 개편 이전 값입니다.

```
.claude/team-rules.md:74  | **④ 개발 착수** | 8항목 전부: 시장분석·아이디어승인·기능정의·화면설계·데이터구조·API정의·예외정의·**디자인승인** |
.claude/team-rules.md:75  | **⑤ QA 착수**   | 5자료 전부: 최신코드·변경내역·기능목록·테스트방법·알려진제한사항 |
.claude/team-rules.md:76  | **⑤ QA 완료**   | 20회 이상 테스트 + **남은 오류 0개** + 실행 로그. `TODO` 나오면 반려 |
```

대조 (`team-org.md`):
```
.claude/team-org.md:79  | **④ 개발 착수** | 10항목: … **출처검증** … **문구확정** … |
.claude/team-org.md:80  | **⑤ QA 착수**  | 6자료: … **보안설계서** |
.claude/team-org.md:81  | **⑤ QA 완료**  | … + **호환성 실측표** |
```

- 근거: `.claude/team-rules.md:3` (우선순위 선언) · `:74` · `:75` · `:76`
- 근거: `.claude/team-org.md:79` · `:80` · `:81`
- 추가: `team-rules.md` 의 게이트 표에는 **② 설계 완료 게이트와 ⑥ 출시 착수 게이트가 통째로 없습니다** (`team-org.md:77` · `:82` 에만 존재). ③ 디자인 완료에서 **"접근성 실측표 제출"** (`team-org.md:78`) 도 빠져 있습니다.

**왜 치명인가**: 규정 충돌 시 `team-rules.md` 가 이깁니다. 즉 D-025 가 올린 게이트(출처검증·문구확정·보안설계서·호환성 실측표·접근성 실측표)는 **정본 우선순위상 현재 효력이 없습니다.** 신설 11명 중 `source-verifier`·`ux-writer`·`security-architect`·`compatibility-tester`·`accessibility-auditor` 5명의 산출물이 게이트에서 빠지므로, D-025 의 핵심 목적(전문화)이 정본 차원에서 무력화됩니다.

**요구**: `team-rules.md:74-76` 을 `team-org.md:77-83` 과 동일 내용으로 교체하거나, 게이트 표를 `team-rules.md` 에서 제거하고 `team-org.md` 를 게이트 정본으로 단일 지정할 것. **두 정본에 같은 표를 두는 구조 자체가 재발원**입니다 (D-012 「중복 정의 금지」).

---

### [치명-3] D-033 「전수 적용」 주장이 실측과 불일치 — 미완을 완료로 보고

`decisions.md:162` 는 **"시정 (전수 적용, 지목된 한 건만 고치지 않음 — D-016 지적 반영)"**,
`decisions.md:165` 는 **"`tech-lead` 8→10항목, `qa-lead` 5→6자료 … 병렬 호출 나열 6곳 갱신"** 이라고 적혀 있습니다.

실측 결과 **6곳이 옛 값 그대로**입니다.

| # | 위치 | 현재 내용 | 있어야 할 값 |
|---|---|---|---|
| 1 | `.claude/team-rules.md:74` | 8항목 | 10항목 |
| 2 | `.claude/team-rules.md:75` | 5자료 | 6자료 |
| 3 | `.claude/agents/tech-lead.md:3` (description) | "개발 착수 게이트 **8항목**을 심사하고" | 10항목 — 같은 파일 `:17` 은 "착수 게이트 **10항목**" |
| 4 | `.claude/agents/tech-lead.md:50` | "⑤ QA 착수 **5자료** 전부를 포함합니다" | 6자료 — 같은 파일 `:33` 은 "⑤ QA 착수 **6자료** 중 하나" |
| 5 | `.claude/agents/qa-lead.md:3` (description) | "QA 착수 **5자료**를 확인하고" | 6자료 — 같은 파일 `:17` 은 "착수 **6자료** 확인" |
| 6 | `.claude/agents/design-lead.md:52` | "④ 개발 착수 게이트 **8항목** 중 하나" | 10항목 |
| 7 | `.claude/agents/product-planner.md:46` | "개발 착수 게이트 **8항목** 중 ②가 책임지는 것" | 10항목 — 같은 파일 `:25` 는 "**10항목** 중 하나" |

**같은 유형의 잔존 — 파트장이 부하를 옛 인원수로 전제**

| 위치 | 현재 내용 | 실제 |
|---|---|---|
| `.claude/agents/product-planner.md:30` | "분배안과 **두 산출물**을 다시 읽고 통합" | 실무자 **4인** (같은 파일 `:17`) |
| `.claude/agents/product-planner.md:35` | "**두 실무자**에게 무엇을 맡길지" | 실무자 **4인** |
| `.claude/agents/design-lead.md:28` | "분배안과 **두 산출물**을 다시 읽고 통합" | 실무자 **3인** (같은 파일 `:16`) |

**왜 치명인가**: 이것은 D-033 이 스스로 진단한 **① 결함(파트장이 신입을 모름)** 이 이름이 아니라 **수치 형태로 그대로 살아 있는 것**입니다. `product-planner` 가 "두 산출물을 통합" 이라는 지시를 그대로 따르면 `ux-writer`·`data-analyst` 의 산출물이 통합본에서 빠집니다 — D-025 신설 인원이 다시 사라집니다.

더 무거운 것은 **보고 방식**입니다. `approvals.md:22` 에 기록된 D-016 4차 반려 사유가 정확히 이것입니다 — **"마스터가 3회 연속 고쳤다고 보고한 항목이 실측 미수정"**. D-033 은 그 지적을 인용하면서(`decisions.md:162`) 같은 잘못을 반복했습니다.

- 근거: `ai-team/decisions.md:162` · `:165` (전수 적용 주장)
- 근거: `ai-team/approvals.md:22` (동일 유형의 과거 반려)
- 근거: 위 표 10곳
- 근거: `ai-team/SESSION-LOG.md:40` — 마스터도 4곳은 스스로 발견해 감사 입력으로 제출했으나, **실측된 것은 10곳**입니다. 자체 집계도 낮았습니다.

**요구**: 10곳 전부 수정. 그리고 `team-check.sh` 가 **프론트매터 `description:` 줄과 본문을 각각** 검사하도록 확장할 것 (현재 `team-check.sh:65-66` 은 파일 어디든 문자열이 있으면 통과시킵니다). 수정 후 **고쳤다고 보고하지 말고 실측 명령과 출력을 함께 제출**하십시오.

---

### [치명-4] D-021 의 실시간 백업은 이 체크아웃에서 한 건도 기록되지 않습니다

사용자 지시 원문 (`decisions.md:44`):
> "회의실이 진행중 일때 바로바로 노션에 자동으로 백업해놔 **어디서든 불러도 모든 내용이 동기화 되게**"

구현 (`meeting-room/SKILL.md:139-157`): 1차 기록처를 **슈퍼베이스 `team_events`**, 노션은 도구가 붙은 세션에서만.

**도구 실존 검증 — 직접 호출했습니다.**
```
$ python3 ai-team/scripts/supabase-sync.py event --kind 감사 --actor team-master \
    --stage "감사" --title "감사관 도구 실존 검증" --ref "test"
✗ SUPABASE_URL / SUPABASE_KEY 를 찾지 못했습니다.
  환경변수로 주거나 ai-team/supabase/.env 를 만드십시오 (.env.example 참고).
EXIT=0
$ ls ai-team/supabase/
.env.example   schema.sql          ← .env 없음
```

즉 **회의실 이벤트는 어디에도 기록되지 않습니다.** 노션도 도구가 붙은 세션에서만이므로, 두 경로 모두 열려 있지 않은 상태입니다. 지시의 핵심("어디서든 불러도 동기화")이 **0% 동작**합니다.

- 근거: `ai-team/decisions.md:26` — "✅ **2026-08-29 개편에서 이 저장소에도 반영**"
- 근거: `ai-team/decisions.md:48` — "**이 저장소 상태**: ✅ **반영됨 (2026-08-29)**"
- 근거: 위 실행 로그 · `ai-team/supabase/` 디렉터리 실측
- 근거: 이 사실은 `STATE.md:91` · `board.md:22` 에 **B-08** 로 적혀 있으나, **D-021 절(`decisions.md:42-56`)에는 한 줄도 없습니다**

**판정**: 문서 절을 넣은 것은 사실이므로 "반영됨"이 거짓말은 아닙니다. 그러나 **사용자 지시의 목적물은 문서 절이 아니라 동기화**이고, D-021 절은 그것이 되지 않는다는 사실을 적지 않았습니다. `team-rules.md:35` **"못 한 건 못 했다고 쓴다"** 위반입니다.

부수 결함: `supabase-sync.py event` 는 실패해도 **exit 0** 입니다. 훅·스크립트가 호출하면 실패를 감지할 수 없습니다.

**요구**
1. `decisions.md:48` 의 "✅ 반영됨" 을 **"⚠️ 절차만 반영 — B-08 로 실제 기록 0건"** 으로 정정.
2. B-08(키 투입 + `push --team` 1회) 해소 전까지 D-021 은 **미충족**입니다. 해소 후 `event` 1건을 실제로 넣고 `status` 로 되읽은 로그를 제출하십시오.
3. `supabase-sync.py` 의 `event` 는 실패 시 **비영(non-zero) 종료**로 바꿀 것. 회의실 규정이 매 이벤트마다 부르는 명령이 조용히 실패하면 D-021 은 영원히 미검출입니다.

---

## 중대

### [중대-1] `STATE.md` 한 파일 안에서 B-07 건수가 셋
```
ai-team/STATE.md:14  | 다음 단계 | 🔴 감사실 2인의 일괄 감사 **12건** (D-035 추가) → … |
ai-team/STATE.md:31  - D-021 · D-026 · D-027 · D-028 도 ③ 미충족입니다. D-025 와 묶어 **9건** 일괄 감사받습니다 (B-07)
ai-team/STATE.md:40  | B-07 | **감사 미수행 11건** — D-021 · D-025 ~ D-034 | … |
```
`board.md:21` 은 **12건**. `SESSION-LOG.md:33` 도 12건.
`STATE.md:1` 은 이 파일을 "자동 생성"이라고 선언하지만 값이 세 시점에 멈춰 있습니다.

**요구**: 12건으로 통일하고 대상 결정 번호를 명시(D-021 · D-025~D-035). 드리프트 5 불합격의 주된 근거입니다.

### [중대-2] `STATE.md` 의 노션 미반영 건수가 실측과 다름
```
ai-team/STATE.md:129  - 미반영 큐: **1건** — `2026-08-29-6.md`
```
실측:
```
$ bash ai-team/scripts/checkpoint.sh
  노션 큐 : 🔴 미반영 2건 — 노션 도구가 있는 세션에서 반영 필요
$ grep -n '^상태:' ai-team/notion-queue/*.md
  2026-08-29-6.md:3:상태: ⏳ 미반영
  2026-08-29-7.md:3:상태: ⏳ 미반영
```
`daily-app/SKILL.md:63-69` 은 STATE 갱신을 노션 정리 **뒤**로 옮긴 이유를 "예측값이 구조적으로 생기지 않게" 라고 적고, `:65` 에서 2026-08-12 치명 반려의 뿌리였다고 기록합니다. **그 순서를 고쳤는데 값은 다시 어긋났습니다** — 이번엔 순서가 아니라 갱신 누락입니다.

**요구**: `STATE.md:129` 를 2건으로 정정. `checkpoint.sh` 가 매 턴 세는 값과 `STATE.md` 값이 다르면 훅이 지적하도록 할 것.

### [중대-3] 브랜치 서술이 실측과 반대 — D-026 훅의 실제 동작을 잘못 적었습니다
```
ai-team/always-on.md:91  | **브랜치 분기 (B-06)** | 훅이 푸시하는 브랜치는 `69oi8o`, Routine 은 `8tlqr8` 을 봅니다 |
ai-team/STATE.md:16      | 정본 브랜치 | ⚠️ **분기 중** — `8tlqr8` ↔ `69oi8o` (B-06) |
ai-team/board.md:20      | B-06 | 브랜치 분기 (`8tlqr8` ↔ `69oi8o`) | 🔴 미해소 |
```
실측:
```
$ git branch --show-current            claude/meeting-room-team-structure-knz8ob
$ git rev-list --left-right --count origin/claude/notion-ai-agent-team-import-69oi8o...HEAD
  0  8        ← 69oi8o 는 HEAD 의 조상. 분기 아님
$ git rev-list --left-right --count origin/claude/notion-ai-team-import-8tlqr8...HEAD
  22 32       ← 진짜 분기는 여기
$ git ls-tree -r --name-only origin/claude/notion-ai-team-import-8tlqr8 -- .claude/agents/ | wc -l
  20          ← 8tlqr8 은 개편 이전 20명 조직
```
훅이 실제로 푸시한 브랜치는 **`knz8ob`** 입니다 (아래 부록 참조). `always-on.md:91` 의 서술은 사실과 다르고, 세 상태 파일 어디에도 **현재 정본 브랜치 이름이 없습니다.**

**요구**: 세 곳을 실측값으로 교체. B-06 을 "`8tlqr8`(20명, 22커밋 고립) ↔ `knz8ob`(31명, 현재 정본)" 로 다시 쓸 것.

### [중대-4] B-03 교체안이 20명 조직 브랜치를 체크아웃시킵니다
```
ai-team/routine-prompt.md:31  git fetch origin claude/notion-ai-team-import-8tlqr8 && git checkout … && git pull …
```
사용자가 `STATE.md:38` 의 지시대로 이 교체안을 Routines UI 에 반영하면, 07:07 정기 실행은 **에이전트 20명 시절 브랜치**에서 돕니다. B-03 의 목적("현재 프롬프트는 20명 시절 것이라 새 조직과 맞지 않습니다")과 정반대의 결과입니다.

**요구**: `routine-prompt.md:31` 과 `:48-49` 의 브랜치를 현재 정본으로 교체한 뒤에만 사용자에게 반영을 요청할 것. B-06 이 미해소인 동안에는 **B-03 을 사용자에게 올리지 마십시오** — 잘못된 브랜치를 고정시킵니다.

### [중대-5] D-027 「생략 불가」 점호 절차의 명령과 출력 형식이 어긋납니다
```
.claude/skills/meeting-room/SKILL.md:31-33   ```bash
                                              ai-team/scripts/roster.sh
                                              ```
.claude/skills/meeting-room/SKILL.md:35-46   (🗣/⏳/👁 와 대기 사유가 붙은 출석부 예시)
```
실측 — 인자 없는 `roster.sh` 는 그 출석부를 만들지 못합니다.
```
$ bash ai-team/scripts/roster.sh
👥 팀 31명 — 이름을 그대로 불러 호출합니다 …
  ① 전략  strategy-lead | market-analyst · …          ← 표시도 대기 사유도 없음

$ bash ai-team/scripts/roster.sh --unit 1
🔔 출석 31 / 31 — 🗣 오늘 발언 · ⏳ 대기 · 👁 상시 감시
  ② 프로덕트  product-planner⏳ …  ← ① 승인 후        ← 이것이 SKILL.md 가 보여준 형식
```
`master.md:33` 은 **`roster.sh --unit N`** 으로 올바르게 적혀 있습니다. 회의실 스킬만 틀렸습니다.

**왜 중대인가**: D-027 이 "⏳ 를 이유 없이 쓰지 않습니다"(`SKILL.md:55`)를 강제한 이유가 **"대기라고만 적으면 빠뜨린 것과 구별되지 않는다"**(`decisions.md:358`) 인데, 문서가 지시한 명령이 정확히 그 이유 없는 출력을 냅니다. 마스터가 SKILL.md 대로 하면 D-027 의 핵심 요건이 매번 미달합니다.

**요구**: `SKILL.md:32` 를 `ai-team/scripts/roster.sh --unit <N|all>` 로 교체. 드리프트 5 불합격 근거.

### [중대-6] 훅 발화 검증 상태가 문서마다 다릅니다
```
ai-team/STATE.md:124   - ✅ **`SessionStart` 훅 발화 확인 (2026-08-29)**
ai-team/STATE.md:125   - ✅ **`Stop` 훅 발화 확인** — `[checkpoint] 자동 저장` 커밋이 히스토리에 남아 있습니다
ai-team/always-on.md:100 | **훅의 실제 발화** | ⚠️ **미검증** — `Stop`·`SessionStart` 는 이 턴 밖에서 뜁니다 |
ai-team/decisions.md:392 **미검증**: 훅의 **실제 발화**는 이 턴에서 확인할 수 없습니다
```
세 문서가 같은 사실에 대해 ✅ 와 ⚠️ 로 갈립니다. `always-on.md` 는 D-026 의 **절차 정본**(`team-rules.md:20`)이므로, 정본 쪽이 낡은 상태입니다.

**감사관 실측**: 훅은 **살아 있습니다.** 이 감사 세션 중 `Stop` 훅이 발화해 커밋 `e54d8c6 [checkpoint] 자동 저장 — 2026-08-30 04:23 KST` 를 남기고 원격까지 푸시했습니다 (`git rev-parse HEAD` = `@{u}`). 즉 `STATE.md:124-125` 가 맞고 정본 두 곳이 틀렸습니다.

**요구**: `always-on.md:100` 과 `decisions.md:392` 를 실측 결과로 갱신. **D-026 의 자동화 주장은 이 항목에 한해 감사관이 실물로 확인했습니다.**

### [중대-7] 승인 원장에 개편 4건이 한 줄도 없습니다
`ai-team/approvals.md` 전문 검색 — `D-021`·`D-025`·`D-026`·`D-027`·`08-29`·`개편 v3` **일치 0건**. 절 목록은 `:5`(사이클 1) · `:16`(개편 v2) · `:25`(D-016 복원) · `:88`(사이클 2) · `:122`(사이클 1 종료)로 끝납니다.

`team-org.md:106` 은 `approvals.md` 를 "승인·반려 기록"으로 정의합니다. 지금 상태에서 승인 원장만 읽는 사람은 **2026-08-29 개편이 심사 대상이었다는 사실 자체를 알 수 없습니다.** 개편 v2 는 `:16-23` 에 5차까지 판정 이력이 남아 있는데, 그보다 큰 v3 개편은 원장에 없습니다.

**요구**: `approvals.md` 에 「조직 개편 v3 (D-025) 및 동반 `.claude/` 수정 11건」 절을 신설하고, 현재 상태를 **⬜ 심사 대기 → 이 감사 결과 ❌ 반려**로 기록할 것.

### [중대-8] 감사실 2인 중 `team-master` 에만 산출 경로가 없습니다
```
.claude/agents/evidence-auditor.md:26  `<앱폴더>/docs/감사-증거대조.md` (앱 무관 감사는 `ai-team/docs/감사-증거대조-<날짜>.md`)
.claude/agents/team-master.md          ← 산출물 절 없음 (판정 형식만 :46-55)
```
D-025 가 감사실을 2인화하면서 한쪽에만 경로를 넣었습니다. 결과적으로 이번 감사도 마스터가 호출 프롬프트에서 파일명을 지정해 우회했습니다 — 이는 `team-org.md:113` 이 금지 대상으로 삼은 **B-02 와 같은 부류**(정의에 경로가 없어 호출자가 매번 지정)입니다.

**요구**: `team-master.md` 에 산출 경로 절 신설 — `<앱폴더>/docs/감사-규정.md`, 앱 무관 감사는 `ai-team/docs/감사-규정-<날짜>-<차수>.md`. 드리프트 6 불합격 근거.

---

## 경미

| # | 지적 | 근거 | 요구 |
|---|---|---|---|
| 경미-1 | D-026 훅 구성표에 `SessionStart` → `team-check.sh` 가 빠짐 (실제 훅은 4개, 표는 3개) | `ai-team/always-on.md:76-78` ↔ `.claude/settings.json:17-21` | 표에 한 줄 추가 |
| 경미-2 | `supabase-sync.py` 가 키 없이 실패해도 `EXIT=0` — 호출자가 실패를 감지 불가 | 치명-4 실행 로그 | `event`·`push` 실패 시 비영 종료 |
| 경미-3 | 노션 페이지 목록이 「팀원 **20명** 역할 명세」로 남음. D-027 은 31명으로 교체했다고 기록 | `ai-team/notion-sync.md:23` ↔ `ai-team/decisions.md:360` · `notion-queue/2026-08-29-4.md:1` | 페이지명 갱신 |
| 경미-4 | 복원 절차가 감사관 1명(`team-master`)만 호출 — 감사실 2인 규정 미반영 | `ai-team/RESTORE.md:65` ↔ `.claude/skills/meeting-room/SKILL.md:169` | 2인 호출로 수정 |
| 경미-5 | BOM 예외 기록이 `team-sync.ps1` 1건인데 실측 BOM 파일은 4개 | `.claude/team-rules.md:117`(BOM 금지) · `ai-team/decisions.md:536`(D-020) ↔ 실측 `master-voice.ps1`·`speak-report.ps1`·`supabase-sync.ps1`·`team-sync.ps1` | D-020 예외 범위를 `*.ps1` 전체로 확장 기록 |
| 경미-6 | 최신 세션 로그 YELLOW 에 D-013 면제·인원 확대 항목이 승계되지 않음 | `ai-team/SESSION-LOG.md:7` ↔ `:46` · `.claude/team-rules.md:98`("계속 표기") · `ai-team/STATE.md:32-33` | 승계 |

---

## 마스터 감사 (6항목)

| 항목 | 판정 | 근거 |
|---|---|---|
| RED 항목을 진행했는가 | ❌ **진행함** | 치명-1. `.claude/` 수정 5건(D-021·D-025·D-026·D-027·D-033)을 ③ 미충족 상태로 시행 |
| YELLOW 를 보고서 최상단에 표기했는가 | ⚠️ **대체로 지킴** | `SESSION-LOG.md:7`·`:46`·`:95` 모두 항목 첫 줄에 🟡. 단 최신 항목에서 2건 누락 (경미-6) |
| GREEN 결정을 `decisions.md` 에 기록했는가 | ✅ **지킴** | D-021~D-035 전부 상세 절 존재. 반대 의견을 낸 사실까지 기록(`decisions.md:400`) — 이 부분은 규정 이상으로 잘 지켰습니다 |
| 승인 없이 다음 단계로 넘어갔는가 | ✅ **넘어가지 않음** | `cycle.md:14`·`:17`, `board.md:10`, `STATE.md:14` 모두 사이클 3 착수를 감사 통과 뒤로 묶어 두었고 실제로 착수하지 않았습니다 (`ai-team/apps/` 비어 있음) |
| 품질 게이트를 시간 부족으로 완화했는가 | ✅ **완화하지 않음** | 게이트는 상향(8→10, 5→6)됐습니다. 다만 그 상향이 최우선 정본에 반영되지 않아 **결과적으로 완화 상태**입니다 — 치명-2. 의도가 아닌 누락으로 판정 |
| **미완을 완료로 보고했는가** | ❌ **보고함 2건** | 치명-3 (`decisions.md:162` "전수 적용" ↔ 10곳 미수정) · 치명-4 (`decisions.md:48` "✅ 반영됨" ↔ 실제 기록 0건) |

**정상 참작할 사실 (감사관이 확인함)**: 마스터는 되돌릴 수 있는 상태(백업 브랜치 `backup/pre-reset-2026-08-29` = `0fd169f`, 원격 존재 확인)를 먼저 만든 뒤 진행했고, ③ 미충족을 **숨기지 않고 8개 파일에 스스로 기록**했으며, 사용자와 다른 의견(인원 축소)을 냈던 사실까지 남겼습니다(`decisions.md:400`). 은폐가 아니라 **절차 우선순위 판단 착오**입니다. 그러나 착오가 5회 반복됐고, 그중 2회는 "완료" 보고를 동반했습니다.

---

## 유보 (확인하지 못한 것 — 추정으로 채우지 않습니다)

| # | 항목 | 왜 확인 못 했는가 |
|---|---|---|
| 유보-1 | 서브에이전트 31명이 실제로 호출되는가 | 감사관은 서브에이전트를 호출할 수 없습니다. 파일 31개·형식·명부는 실측했으나 **호출 성공은 미확인**입니다. `SESSION-LOG.md:32` 도 미확인으로 남겨 두었습니다 — 이 유보는 마스터의 기록과 일치합니다 |
| 유보-2 | 노션 4개 페이지의 실제 내용 | 이 세션에 노션 도구가 붙어 있지 않습니다. `notion-queue/*.md` 의 "반영됨" 표기는 **자기 신고**이며 대조하지 못했습니다 |
| 유보-3 | 07:07 Routine 실물 프롬프트 | 저장소 밖에 있습니다. `routine-prompt.md` 는 2026-08-11 시점 사본(`:10`)이며 이후 변경 여부 불명 |
| 유보-4 | 슈퍼베이스 왕복 실측 결과(80파일 sha256 80/80) | 가짜 서버 자체 시험이며, 실제 슈퍼베이스로는 아직 한 번도 돌지 않았습니다(B-08). 시험 설계는 타당해 보이나 **실물 검증이 아닙니다** |
| 유보-5 | Windows 로컬 스크립트(`*.ps1`) 동작 | 이 컨테이너에 PowerShell 이 없습니다. `STATE.md:93` 의 미검증 표기와 일치 |
| 유보-6 | D-028·D-029~D-035 | 이번 차수 대상 4건이 아닙니다. 2차 이후 감사에서 다룹니다 |

> `team-master.md:44` 에 따라, "내 세션에서 안 된다"를 아키텍처 한계로 일반화하지 않았습니다 (D-015).

---

## 부록 — 감사관이 저장소에 남긴 부작용 (고지)

도구 실존 검증 중 `ai-team/scripts/checkpoint.sh` 를 사람 모드로 1회 실행했습니다. 그 직전 `Stop` 훅이 먼저 발화해 **커밋 `e54d8c6` 이 생성·푸시**됐습니다. 이 커밋에는 감사 착수 시점에 작업 트리에 있던 다음이 포함됩니다.

| 파일 | 상태 |
|---|---|
| `ai-team/scripts/gate-check.sh` | 미추적 신규 파일 (2026-08-29 19:23 생성, 감사 이전부터 존재) |
| `ai-team/scripts/master-listen.config.json` | `"hotkey": ""` → `"x"` 수정 |

**감사관이 만든 변경은 아니며, 감사 산출 파일 외에는 아무것도 편집하지 않았습니다.** 다만 이 사건은 D-026 의 부작용을 드러냅니다 — `checkpoint.sh:51` 의 `git add -A` 는 **읽기만 하러 들어온 세션에서도 다른 세션의 미완 작업물을 무차별 커밋**합니다. `always-on.md:36` 은 "작업 중간 상태도 커밋됩니다"까지만 적고, **감사관·열람자 세션이 남의 산출물을 커밋하는 경우**는 적지 않았습니다. `team-rules.md:68`("남의 산출 파일을 수정하지 않습니다")와 긴장 관계에 있습니다.

**추가 요구 (D-026)**: `always-on.md` 한계표에 이 항목을 추가하고, `checkpoint.sh` 가 커밋 대상에서 미추적 파일을 제외할지 여부를 마스터가 결정해 `decisions.md` 에 기록할 것.

---

## 재감사 조건

아래 4건을 닫고 **실측 명령과 출력을 함께** 제출하면 재감사합니다. "고쳤습니다"만으로는 재감사하지 않습니다 (`approvals.md:22` 의 전례).

1. **치명-2** — `team-rules.md:74-76` 게이트 정합 (또는 게이트 정본 단일화)
2. **치명-3** — 10곳 전수 수정 + `team-check.sh` 의 `description:` 줄 검사 확장
3. **치명-4** — B-08 해소 후 `event` 1건 실기록 + `status` 되읽기 로그, 또는 `decisions.md:48` 을 미충족으로 정정
4. **치명-1** — 5개 결정 절에 RED 진행 사실을 감사 판정으로 기록 + `master-doctrine.md` 에 "되돌릴 수 있음은 예외 사유가 아니다" 명시

중대 8건·경미 6건은 재감사 시 함께 확인합니다. 중대-4(B-03 교체안)는 **사용자에게 올리기 전에** 반드시 고치십시오.

**이 반려가 유효한 동안 사이클 3 착수는 불가입니다** (`master-doctrine.md:29` · `team-org.md:32`).
