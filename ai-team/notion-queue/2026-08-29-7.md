# 노션 반영 대기 — 2026-08-29 (7) · D-035

상태: 반영됨 2026-08-30T05:40:00+09:00 — D-035 — 08-30 작업 일지 항목에 통합 반영(⭐ 최신 절 「시정」 목록)

## 「📓 작업 일지」 append
**2026-08-29 · 팀 정의를 슈퍼베이스에 담아 어느 창에서든 복원 (D-035)**
지시 원문: "슈퍼베이스에 ㄷ다른 창에서도 회의 실을 부르면 완벽하게 나오게 해놔"
슈퍼베이스에는 상태 8개만 있었습니다. 회의실을 열려면 `.claude/agents/` 31개 정의가 체크아웃에 실물로 있어야 하므로, 상태만으로는 다른 창에서 팀이 나오지 않았습니다.
`push --team` · `restore` · `verify` 를 신설해 팀 정의(31명·규정·스킬·스크립트)까지 담게 했고, 저장소가 없는 창을 위해 `ai-team/BOOTSTRAP.md` 에 붙여넣기 한 덩어리를 두었습니다.
키 없이 검증할 수 있도록 가짜 서버 왕복 시험(`selftest-supabase.py`)을 만들어 실측했습니다 — push 80파일 → 빈 디렉터리 복원 → sha256 80/80 일치 → 복원본 team-check 31명 통과 → verify 완전.
곁가지로 `checkpoint.sh` 의 노션 미반영 검사가 매 턴 거짓 초록불(실제 1건 밀림)을 켜고 있던 것을 찾아 고쳤습니다.
🔴 **실제 업로드는 하지 못했습니다** — 컨테이너에 키가 없습니다(B-08). 감사 미수행 → B-07 이 11건 → **12건**.

## 상단 「현재 상태」 표
- 다음 단계: 감사 **12건** (D-021 · D-025~D-035)
- 차단: **B-08 슈퍼베이스 키 미설정** 추가
- 슈퍼베이스 칸: 상태 + **팀 정의**까지 보관 (키 넣고 `push --team` 1회 필요)

## 다른 페이지도 바꿔야 합니다
- 「📜 팀 규정 · 마스터 판단 기준」 — 슈퍼베이스 절에 `push --team`/`restore`/`verify` 추가
