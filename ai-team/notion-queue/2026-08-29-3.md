상태: 대기
대상: 📓 작업 일지 (3b750286-df80-8173-a09e-f8dc7712e713) — 항목 append + 상단 표 갱신 · 규정이 바뀌었으므로 📜 팀 규정 도
---
# 2026-08-29 (3) · 「항상」 규칙 4종을 훅으로 강제 (D-026)

**🟡 YELLOW**: D-026 도 감사 미수행 (D-025·D-021 과 함께 B-07) · **훅의 실제 발화는 미검증**

**지시 원문**: "노션 페이지 업데이트 하고 항상 슈퍼베이스에 저장하고 중간중간 마다 항상 git hub에 올리고 항상 에이아이 에이전트가 팀에 있아사 항상 부를 수 있게 해 계획자세히 잘 짜"

## 핵심 판단 — 「항상」은 기억으로 못 지킵니다
이 팀의 사고는 전부 "규정은 있었는데 그 실행에서 빠졌다" 유형입니다.
13일 공백 · 6일 트랙 분기 · 노션 상단 표 미갱신 치명 반려 — **셋 다 마스터가 몰라서가 아니었습니다.**
그래서 4가지를 **하네스가 강제하는 훅**으로 넣었습니다.

## 만든 것
- `ai-team/scripts/checkpoint.sh` — 슈퍼베이스 push + GitHub 커밋·푸시 + 노션 큐 확인을 한 번에
- `ai-team/scripts/roster.sh` — 팀 명단을 `.claude/agents/` 에서 **실측**해 출력
- `.claude/settings.json` — `Stop` 훅(체크포인트) + `SessionStart` 훅에 로스터 추가 (기존 STATE 훅은 **지우지 않고 병합**)
- `ai-team/always-on.md` — 절차 정본

## 자동화 수준 (정직하게)
| 규칙 | 수단 | 수준 |
| --- | --- | --- |
| 슈퍼베이스 항상 저장 | `Stop` 훅 (매 턴) | ✅ 완전 |
| GitHub 중간중간 푸시 | 같은 훅 · `[checkpoint]` 태그 커밋 | ✅ 완전 |
| 노션 항상 갱신 | 큐 + **미반영 건수 매 턴 감시** | ⚠️ **부분** |
| 에이전트 항상 호출 가능 | `SessionStart` 훅 · 실측 로스터 | ✅ 완전 |

**노션만 부분인 이유**: 노션 MCP 는 세션마다 붙기도 하고 안 붙기도 하며, 07:07 Routine 에 커넥터가 안 붙을 수 있다는 것이 이미 확인된 제약입니다. **못 하는 것을 된다고 적지 않습니다.** 대신 미반영 건수를 매 턴 띄워 잊히지 않게 했습니다 — 13일 공백이 조용했던 이유가 그 감시의 부재였습니다.

## 검증 (실측)
| 항목 | 결과 |
| --- | --- |
| `checkpoint.sh` | ✅ 슈퍼베이스 push · 커밋 1건 · 푸시 · 큐 0건 (실제 부작용 확인) |
| `checkpoint.sh --hook` | ✅ JSON 한 줄 출력 |
| `roster.sh` | ✅ 31명 · 7개 본부 · 누락 0 |
| `settings.json` | ✅ `jq -e` 로 Stop·SessionStart 양쪽 스키마 확인 |
| **훅의 실제 발화** | ⚠️ **미검증** — 턴 밖에서 뜁니다 |

## 승인 / 반려
없음 — 인프라·규정 작업이며 파이프라인 단계 이동이 없습니다.

## 감사관 판정
**미수행.** D-026 도 D-014 요건 ③ 이 비어 있어 D-025·D-021 과 함께 B-07 로 묶었습니다.

## 🔴 이것으로도 안 되는 것
1. **다른 저장소·폴더에서 연 세션** — 훅도 에이전트도 이 저장소 안에 있습니다
2. **B-03 Routine 프롬프트** — 자동화가 붙어도 **07:07 은 20명 시절 프롬프트로 돕니다.** 사용자가 Routines UI 에서 교체해야 합니다
3. **B-06 브랜치 분기** — 자동 푸시가 `69oi8o` 로만 가므로, 일원화 전까지 분기가 더 벌어집니다
4. **Windows 로컬** — `checkpoint.sh` 는 bash. Git Bash 없으면 안 돕니다 (미검증)

## 다음 이어받을 지점
1. 다음 턴 끝에 `📌 체크포인트` 가 뜨는지 확인 → 훅 발화 검증
2. 감사실 2인으로 D-025·D-021·D-026 감사 (B-07)
3. B-03 · B-06 사용자 결정
