상태: 대기
대상: 📓 작업 일지 (3b750286-df80-8173-a09e-f8dc7712e713) — 항목 append + **상단 「현재 상태」 표 갱신** 둘 다 필요
추가 대상: 🏢 팀 홈 · 👥 팀원 명세 (조직이 바뀐 날이므로 — notion-sync.md 「대상 노션 페이지」)
---
# 2026-08-29 (2) · 초기화 + 조직 개편 v3 — 20명 → 31명

**🟡 YELLOW**: **D-025 개편은 감사 미수행**(D-014 요건 ③) · D-021 도 ③ 미충족인 채 반영 · **마스터는 축소를 권했으나 사용자가 확대를 택함**

**지시 원문**: "지금 까지 에이전트 팀에서 만든거 초기화시키고 팀 구성 재정비하자"

## 결론부터
앱 산출물 3,296행을 비우고 조직을 **6본부 29명 + 감사실 2명 = 31명** 으로 개편했습니다.
**지우기 전에 백업 브랜치를 원격에 먼저 푸시**해 되돌릴 수 있게 만든 뒤 진행했습니다.

## 한 일
1. 실측 인벤토리 → 사용자에게 범위 3개 안 제시 → **「앱 산출물만」** 선택받음
2. **백업 `backup/pre-reset-2026-08-29`(`0fd169f`) 원격 푸시** — 이 전에는 아무것도 지우지 않았습니다
3. 산출물 12파일 삭제 + `ai-team/archive/cycles-1-2.md` 에 복구 절차 기록
4. **신설 11명** 작성 · 기존 20명 결함 수정 · 정본 3종·스킬 2종 갱신 · 상태 파일 리셋

## 신설 11명 — 전부 이 팀이 실제로 겪은 사고에서
| 신설 | 근거 |
| --- | --- |
| `source-verifier` | 1위 아이디어 최고 득점 근거가 **403 으로 안 열리는 단일 출처**였음 |
| `ux-writer` | 화면설계와 구조설계가 문구를 나눠 가져 E-코드 전까지 미완 |
| `data-analyst` | 7점 차가 근거로 지탱되지 않음 (재채점 시 역전 가능) |
| `accessibility-auditor` | 접근성이 파트장 고유 업무로 몰려 D-013 면제가 미검증 확정 |
| `performance-engineer` | 두 사이클 통틀어 성능을 본 사람이 없었음 |
| `security-architect` | 프로토타입 오염 방어가 **있는 척만 하고 미동작** |
| `compatibility-tester` | `file://` SW 불가 · IndexedDB 차단 · iOS 오디오 중단을 전부 뒤늦게 발견 |
| `test-automation-engineer` | 수동 20회 사이클이 세션 한도로 중단 |
| `privacy-compliance` | 방침·약관을 제출 입력값 담당이 겸함 |
| `tech-writer` | 사용자가 읽을 문서가 한 줄도 없었음 |
| `evidence-auditor` | 감사관 1명이 마스터까지 감시 → 3차까지 가는 병목 |

## 차단 4건 동시 해소
- **B-01** `team-org.md` 의 "자동 실행이 동작하지 않습니다" → 정확한 서술로 교체
- **B-02** 전 에이전트 산출 경로에 `<앱폴더>/` 접두사(22파일) + 경로 규칙 명문화
- **B-04** `tech-lead`·`qa-lead` handoff 필독 신설 → 01~05 전 구간 연결
- **B-05** `daily-app` 의 STATE 갱신을 노션 정리 **뒤**로 이동 (예측값이 구조적으로 안 생기게)

## 게이트도 함께 올렸습니다
④ 개발 착수 8항목 → **10항목**(출처검증·문구확정) · ⑤ QA 착수 5자료 → **6자료**(보안설계서) · 상호 검토 6행 → **15행**

## 실측이 잡은 기존 문서 오류
정본의 "`tools:` 쓰면 안 되는 역할 **10개**" 목록이 실제와 달랐습니다. 실측하니 **11개**(`strategy-lead` 누락)였고, 신설 5명을 더해 **16개**로 갱신했습니다.

## 승인 / 반려
없음 — 조직 개편이며 파이프라인 단계 이동이 없습니다.

## 검증 (실측)
에이전트 **31/31** · BOM **0건** · 첫 줄 `---` **31/31** · Windows 절대경로 **0건** · 접두사 없는 산출 경로 **0건** · `tools:` 금지 16/16 준수

## 감사관 판정
**미수행 (B-07).** 이 세션의 운영 규칙상 사용자가 요청하지 않은 서브에이전트를 부르지 않습니다. **D-025·D-021 둘 다 D-014 요건 ③ 이 비어 있습니다.** 되돌릴 수 있어 진행했으며 **사이클 3 착수 전 첫 작업이 이 감사**입니다.

## 🔴 남은 차단 3건 (사용자)
1. **B-03 Routine 프롬프트** — 이 환경에서 수정 불가. **현재 프롬프트는 20명 시절 것이라 새 조직과 맞지 않습니다**
2. **B-06 브랜치 분기** — `8tlqr8` ↔ `69oi8o`
3. **B-07 개편 감사**

## 🔴 노션 쪽 할 일
「👥 팀원 20명 역할 명세」 페이지가 **아직 20명 기준**입니다. 이 상태로 복원하면 **옛 조직이 되살아납니다.**

## 다음 이어받을 지점
1. 감사실 2인으로 D-025·D-021 감사
2. 노션 팀원 명세 31명으로 갱신
3. B-03·B-06 사용자 결정
4. 통과 후 사이클 3 ① 전략 (실무자 4인 병렬)
