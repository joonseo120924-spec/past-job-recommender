# 🔊 voice-out — 마스터가 소리 내어 읽을 보고문

사장님이 **음성 보고**를 요청하면 마스터가 이 폴더에 `<날짜시각>-<주제>.txt` 를 쓰고 커밋·푸시합니다.
사장님 PC 에서 `master-voice.ps1` 이 돌고 있으면 **새 파일을 받아 SAPI 로 읽습니다.**

- 마스터는 **읽기 좋은 문장**으로 씁니다 — 표·코드블록·기호 없이, 짧은 문장으로
- 이미 읽은 파일은 다시 읽지 않습니다 (`.spoken` 목록, git 추적 안 함)
- 지연은 폴링 주기만큼입니다 (기본 20초). 실시간이 아닙니다
