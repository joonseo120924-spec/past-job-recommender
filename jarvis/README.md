# J.A.R.V.I.S. — 4단계 시스템

말 한마디로 하루를 굴리는 개인 비서. **브레인 · 기억 · 보이스 · HUD** 네 부품만
있습니다. 외부 API를 호출하지 않고, 음성은 브라우저 밖으로 나가지 않습니다.

```bash
pip install -r requirements.txt
uvicorn jarvis.main:app --reload --port 8010
# http://127.0.0.1:8010  ← HUD 한 화면
```

볼트를 옵시디언 금고로 바꾸려면:

```bash
JARVIS_VAULT=~/Documents/MyVault uvicorn jarvis.main:app --port 8010
```

## 네 개의 부품

| 파트 | 하는 일 | 코드 |
|---|---|---|
| 브레인 | 요청을 스킬 하나로 연결 (INTENT → SKILL) | `jarvis/skills_registry.py`, `jarvis/skills/*/SKILL.md` |
| 기억 | 모든 기록이 마크다운으로 쌓임 | `jarvis/vault.py`, `vault/` |
| 보이스 | 로컬 STT/TTS, 스페이스바로 말하기 | `jarvis/static/hud.js` (Web Speech) |
| HUD | 한 화면에서 상태·명령·일정 | `jarvis/static/` |

## STEP 1 — 브레인

스킬 하나는 역할 하나만 맡습니다. 큰 프롬프트 하나보다 작은 스킬 다섯 개가 낫습니다.

| 스킬 | 역할 |
|---|---|
| `inbox` | 아침 브리핑 |
| `plan` | 오늘의 상위 3개 |
| `metrics` | 지표 확인 |
| `trends` | 흐름 스캔 |
| `vault` | 기억 읽기/쓰기 |
| `status` | 현재 상황 보고 |
| `agents` | 노션 AI 앱 개발팀 현황 |

각 스킬은 `jarvis/skills/<name>/SKILL.md` 하나로 정의됩니다. 프론트매터의
`triggers` 가 라우팅 키워드이고, 파일을 고치면 **서버 재시작 없이** 다음 요청부터
반영됩니다. 어느 트리거에도 안 걸리면 `vault` 로 떨어집니다 — 모를 때의 기본값은
"모르겠다"가 아니라 "기억을 뒤져 본다"입니다.

## 노션 연동 — AI 앱 개발팀

노션의 팀 정본(팀 홈 · 작업 일지 · 팀원 명세 · 팀 규정)을 볼트로 끌어와, 물으면
바로 답합니다.

```
자비스, 에이전트팀 어떻게 돼?
→ AI 앱 개발팀 현황입니다. 6본부 20명, 사이클 2 1일차, ② 프로덕트에서 구조설계
   미완입니다. FocusNoise는 동결… 감사관 판정은 3차 조건부 승인, 치명 0건 중대
   3건입니다. 사용자 지시가 필요한 차단이 5건 있습니다. 첫 번째는…
   이 상태는 2026-08-15 기준이라 7일 지났습니다.
```

두 갈래로 동작합니다.

| 상황 | 동작 |
|---|---|
| `NOTION_TOKEN` 있음 | `POST /api/notion/sync` 가 노션에서 다시 읽어 `wiki/` 미러와 스냅샷 시각을 갱신 |
| 토큰 없음 | 볼트의 스냅샷(`vault/data/agent-team.json`)으로 답하고, **며칠 지난 값인지 함께 말함** |

토큰은 노션 **내부 통합(Internal Integration)** 을 만들고 팀 페이지를 그 통합에
공유한 뒤 발급받으면 됩니다. 없다고 자비스가 입을 다물지는 않습니다 — 대신
오래된 스냅샷을 최신인 척하지 않습니다.

`vault/data/agent-team.json` 은 사람이 직접 고쳐도 됩니다. 사이클·앱 상태·감사관
판정·🔴 차단 목록·다음 이어받을 지점이 들어 있고, HUD 의 **AGENT TEAM** 패널과
`agents` 스킬이 같은 파일을 읽습니다.

## 현재 상황 보고

"지금 상황 보고해" 한 마디에 아는 것을 전부 정리해 말합니다 — 시스템(CPU·메모리),
볼트(노트 수·링크율), 오늘 흐름 진행, 열린 할 일, 지표 변화, 에이전트팀 한 줄.
측정할 수 없는 값은 **"측정 불가"** 라고 말합니다. 빈칸을 0으로 채우지 않습니다.

## STEP 2 — 기억

```
vault/
  raw/      원본 수집 (캡처, 받은 것)
  wiki/     정제된 지식
  outputs/  결과물 아카이브 (브리핑·계획·회고)
  data/     지표 스냅샷 (metrics.jsonl)
```

노트는 `---` 프론트매터가 붙은 평범한 마크다운입니다. DB도 인덱스도 없어서 옵시디언
으로 그대로 열립니다. `[[노트id]]` 로 링크하면 백링크가 잡히고, 링크된 노트 비율이
HUD 의 **VAULT SYNC** 수치입니다 — 링크 없는 노트는 결국 다시 안 읽히니까요.

검색은 형태소 분석 없이 부분 문자열로 합니다. 한국어에서 어설픈 토크나이징보다
이 편이 덜 틀립니다. 점수는 제목 3배, 태그 2배, 본문 1배(최대 5회).

## STEP 3 — 보이스

- **"자비스"라고 부르면 됩니다.** 상시 대기 상태에서 계속 듣고 있다가, 웨이크워드가
  섞인 발화만 명령으로 넘깁니다. 이름만 부르면 "네, 듣고 있습니다"로 답합니다
- **스페이스바를 누른 채로** 말하면 웨이크워드 없이 바로 명령입니다 (마이크 버튼도 동일)
- STT/TTS 모두 브라우저 로컬 엔진(Web Speech, `ko-KR`) — 오디오는 서버로 가지 않고,
  서버가 받는 건 인식된 **텍스트 한 줄**뿐입니다
- 답하는 도중 새 명령이 들어오면 말을 끊고 새 요청을 받습니다
- 파형은 마이크 레벨만 그립니다. 녹음도, 저장도 하지 않습니다
- 상시 대기와 소리 답변은 각각 끌 수 있고, Web Speech 미지원 브라우저에서는 입력창으로
  그대로 씁니다

### 대화가 이어집니다

오간 말은 `raw/conversation-YYYY-MM-DD` 에 쌓입니다. "다시 말해줘"는 마지막 답을 다시
읽고, 화면을 새로 고쳐도 오늘 대화가 그대로 남아 있습니다.

## STEP 4 — HUD

한 화면, 탭 없음. 부팅 시퀀스로 시작해서 — 마이크와 스피커는 사용자의 첫 동작이
있어야 열리므로 "시스템 시작"이 그 문을 겸합니다 — 아크 리액터 코어, SYSTEM
VITALS(CPU/RAM/IO, `/proc` 직접 읽기), VAULT SYNC, AUDIO I/O 파형, COMMAND DECK,
SCHEDULE, CONVERSATION, LIVE FROM THE VAULT 가 한 화면에 있습니다.

코어의 밝기는 상태를 그대로 비춥니다 — 대기 중엔 어둡고, 들을 때는 마이크 입력
레벨을 따라가고, 말할 때 가장 밝습니다.

## 하루의 흐름

| 시간 | 하는 일 | 스킬 |
|---|---|---|
| 07:00 | 모닝 브리핑: 메일·일정·AI 뉴스 요약 | `inbox` |
| 09:00 | 오늘 계획: 우선순위 3개 정리 | `plan` |
| 14:00 | 지표 확인: 조회수·구독자·팔로워 점검 | `metrics` |
| 19:00 | 마감 정리: 회고 저장, 내일 준비 | `review` |

**부르지 않아도 먼저 실행합니다.** 서버가 30초마다 시계를 보고, 시간이 된 블록을
스스로 돌린 뒤 결과를 `/api/stream` (SSE) 으로 화면에 밀어 줍니다. HUD 는 그 말을
초록으로 강조해서 띄우고 소리로 읽습니다.

두 가지는 일부러 이렇게 했습니다.

- **이미 결과물이 있으면 건너뜁니다.** 서버를 다시 띄웠다고 오늘 브리핑을 두 번
  읽지 않습니다.
- **90분 지난 블록은 넘깁니다.** 놓친 아침 브리핑을 오후 3시에 읽어 주는 건 도움이
  아니라 소음입니다.

외부 크론에 걸고 싶다면 `curl -X POST localhost:8010/api/run/inbox` 를 그대로 쓰면
되고, 자동 실행을 끄려면 `create_app(autorun=False)` 입니다.

## API

| 메서드 | 경로 | 설명 |
|---|---|---|
| `GET` | `/api/vitals` | 시스템 상태 + 볼트 통계 |
| `GET` | `/api/skills` | 로드된 스킬 목록 |
| `GET` | `/api/schedule` | 오늘 흐름과 완료 여부 |
| `POST` | `/api/ask` | `{"text": "오늘 할 일 정리해줘"}` → 라우팅 + 답변 |
| `POST` | `/api/run/{skill}` | 스킬 직접 실행 (크론용) |
| `GET` | `/api/stream` | SSE — 하루 흐름 자동 실행 알림 |
| `GET` | `/api/agents` | 에이전트팀 현황 (사이클·앱·감사·차단) |
| `POST` | `/api/notion/sync` | 노션 정본을 볼트로 다시 끌어오기 |
| `GET` | `/api/conversation` | 오늘 오간 말 |
| `GET` | `/api/vault/notes?q=&kind=&limit=` | 목록 / 검색 |
| `GET` | `/api/vault/notes/{id}` | 본문 + 백링크 |
| `POST` | `/api/vault/notes` | 노트 생성 |
| `GET`·`POST` | `/api/metrics` | 지표 조회 / 스냅샷 기록 |

## 설계상 정해 둔 것

- **지표 비교는 변화율로.** 조회수는 만 단위, 구독자는 십 단위입니다. 절대량으로
  "가장 크게 움직인 지표"를 뽑으면 언제나 조회수가 이깁니다.
- **캡처는 하루 한 파일.** "기억해 …" 는 `raw/capture-YYYY-MM-DD` 에 줄을 덧붙입니다.
- **저장 명령은 앞뒤에서만.** "썸네일 어디 적어놨더라"의 `적어`는 과거형 어미이지
  명령이 아닙니다. 명령형은 문장의 맨 앞이나 맨 뒤에 올 때만 저장으로 봅니다.
- **할 일은 raw 에서만 수집.** wiki 는 정제된 지식이라 체크박스를 세지 않습니다.
- **볼트 전체를 매번 훑습니다.** 개인 볼트(수천 건) 규모에서는 인덱스를 유지하다
  깨뜨리는 쪽이 더 비쌉니다.
- **웨이크워드 없이 들은 말은 흘려 보냅니다.** 상시 대기 중에도 "자비스"가 섞이지
  않은 발화는 서버로 보내지 않습니다.
- **가장 구체적인 트리거 하나가 라우팅을 정합니다.** 짧은 트리거를 여러 개 더하면
  "에이전트팀 상황 보고"에서 곁가지(상황·보고)가 본체(에이전트)를 이겨 버립니다.
- **밀린 화면 하나가 전체를 멈추지 않습니다.** 이벤트 큐가 가득 찬 구독자는 끊고,
  나머지에게는 계속 발행합니다.

## 테스트

```bash
pytest tests/jarvis -q
```
